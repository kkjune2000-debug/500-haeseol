# 작업 로그 — 2026-05-04

`_작업_로그_2026-05-02.md` 이후 진행된 영어 친화·양국어 표준화 세션 기록.

---

## 📑 목차

| Part | 주제 | 커밋 | 비고 |
|------|------|------|------|
| **Part 1** | 영어 친화 구조 개편 | `a9c7f1e` (2026-05-03) | 학습자(외국인) 1차 시야를 영어로 |
| **Part 2** | 부사어 시리즈 일괄 변환 | `a6d8be5` (2026-05-04) | 부사어 10개 + 서술어 일부 |
| **Part 3** | 양국어 문서화 기준 일괄 적용 | `361ec2d` (2026-05-04) | 35개 파일 자동화 + 신규 문서 2건 |
| **Part 4** | 패턴 블록 다듬기 | uncommitted | "Subject + Particle" → "Noun + Particle", 표 너비 미세조정 |
| **Part 5** | 미처리 / 다음 작업 | — | 213~500 일부, 원본 폴더 |

---

# Part 1 — 영어 친화 구조 개편 (`a9c7f1e`, 2026-05-03)

학습 동선이 "한국어 우선 노출 → 영어 보조"였던 것을 **"영어 우선 노출 → 한국어 학습"**으로 뒤집어, 비한국어권 학습자가 페이지를 펼친 즉시 의미를 파악하고 한국어 형태에 집중할 수 있도록 재구성.

## 1-1. exp-table 구조 (3-컬럼 → 4-컬럼 → 영어 우선 정렬)

| 단계 | 컬럼 순서 | 컬럼 너비 |
|------|----------|----------|
| v5 (3-컬럼) | English / 한국어 / 해설 | 16/28/56 |
| v6~v8 (4-컬럼 초안) | English / Role / Korean / Explanation | 20/20/20/40 |
| **v9 (이번)** | **English / Role / Korean / Explanation** | **20/12/18/50** |

**핵심 변경점:**
- Role 컬럼 너비 축소 (20% → 12%) — 짧은 라벨(Subject/Verb)에 과한 공간이 분배되던 문제 해소
- Korean 컬럼 너비 축소 (20% → 18%) — 한국어 형태가 짧으므로 충분
- Explanation 컬럼 확대 (40% → 50%) — 양국어 해설을 위해 여유 확보

## 1-2. 영어 단어 잘림 방지 CSS

긴 영어 단어(particle, interrogative 등)가 공간 부족 시 임의 위치에서 줄바꿈되어 가독성을 해치던 문제 해결:

```css
.exp-table .exp-en {
    color: #000;
    font-weight: 800;
    width: 20%;
    word-break: keep-all;
    overflow-wrap: normal;   /* 추가 */
    hyphens: none;           /* 추가 */
}
.exp-table .exp-role {
    /* 동일하게 overflow-wrap: normal 추가 */
}
```

## 1-3. 영어 소유격 + 신체/소유물 패턴 단순화

영어가 `(소유격) + 신체/소유물`로 분해되어 두 행을 차지하던 케이스(예: His + health → 그의 + 건강)를 한 행으로 통합:

| 영어 | 행 | 변경 전 | 변경 후 |
|------|----|--------|--------|
| His health | Subject | "His" + "health" 두 행 | **"His health"** 한 행 = **그의 건강이** |

소유격 분리는 한국어 토큰(저의/그의)과 1:1 매핑이 어려워 학습자에게 혼란을 주었음. 한 덩어리로 묶는 것이 의미·문법 모두 명확.

## 1-4. 신규 colgroup 강제

각 `<table class="exp-table">` 직후 `<colgroup>` 삽입으로 컬럼 너비 강제:

```html
<table class="exp-table">
    <colgroup><col style="width:20%"><col style="width:12%"><col style="width:18%"><col style="width:50%"></colgroup>
    <thead>
        ...
```

이전에는 `<th style="width:N%">`만으로 너비를 지정했는데, 일부 브라우저(특히 Safari)에서 thead 너비가 tbody에 일관되게 전파되지 않는 문제가 있어 colgroup으로 강제.

---

# Part 2 — 부사어 시리즈 일괄 변환 (`a6d8be5`, 2026-05-04)

87-96 구조 패턴(풍부한 인터랙티브)을 부사어 10개 파일에 일괄 적용.

## 2-1. 적용 파일 (부사어 10개 + 서술어 일부)

| 파일 | 문항 수 | 문법 |
|------|--------:|------|
| 248~252 시간 때 | 5 | ~ㄹ 때 |
| 253~259 기간 동안 | 7 | ~동안 |
| 260~265 전에 | 6 | ~기 전에 |
| 266~270 후에 | 5 | ~ㄴ 후에 |
| 271~276 순서 고 | 6 | ~고 |
| 277~283 순서원인 아어서 | 7 | ~아/어서 (순서·원인) |
| 284~291 동시 동작 으면서 | 8 | ~으면서 |
| 292~295 즉시 자마자 | 4 | ~자마자 |
| 296~299 동작 전환 다가 | 4 | ~다가 |
| 300~306 범위 부터 까지 | 7 | ~부터 ~까지 |
| 307~313 원인 때문에 (2) | 7 | ~때문에 |
| 314~315 원인 덕분에 | 2 | ~덕분에 |

## 2-2. 종합표·테스트 보강

각 파일에 다음 인터랙티브 요소를 추가:
- `gstep` Grammar Overview (문법별 3~5단계)
- 종합 Writing Test (`writing-practice` + 채점 시스템)
- Read Along (`sentence-list`)
- Look & Speak (`flashcard-grid`)

---

# Part 3 — 양국어 문서화 기준 일괄 적용 (`361ec2d`, 2026-05-04)

이 세션의 핵심. 35개 기존 파일에 **양국어(영어 + 한국어 stack) 형식**을 자동화로 일괄 적용.

## 3-1. 새 문서화 기준 7대 원칙

### 원칙 1 — 컬럼 헤더 stack
- **Before:** `<th>English</th><th>Role</th><th>Korean</th><th>Explanation</th>`
- **After:** `<th>English<br>영어</th><th>Role<br>역할</th><th>Korean<br>한국어</th><th>Explanation<br>해설</th>`
- 가운뎃점(`·`) 구분자 제거 — 줄바꿈으로 시각 분리

### 원칙 2 — 양국어 어순 헤더
- **Before:** `한국어 어순 · Korean Structure: Subject + Object + Verb`
- **After:** `Korean structure: Subject + Object + Verb<br>한국어 어순: 주어 + 목적어 + 동사`
- 영어 먼저, 한국어 둘째 (학습 동선 일관성)
- 한국어 어순 부분의 역할명(Subject)도 한국어(주어)로 번역

### 원칙 3 — colgroup 너비 강제 (20/12/18/50)
- 모든 `<table class="exp-table">` 직후 `<colgroup>` 삽입
- 컬럼 너비 최종 확정

### 원칙 4 — 영어 단어 잘림 방지
- `.exp-en`, `.exp-role`에 `overflow-wrap: normal; hyphens: none;` 추가

### 원칙 5 — 영어 소유격 패턴 단순화
- "His" + "health" → "His health" (한 행으로)

### 원칙 6 — '예시' → '예'
- 모든 라벨/헤딩에서 '예시' → '예' (간결)
- 적용 패턴: `예시 (Example)`, `단계별 예시`, `>예시<` 등

### 원칙 7 — explain-tip 일괄 제거
- 해설 박스 안의 `<p class="explain-tip">💡 ...</p>` 모두 삭제
- 이유: 표 안 해설이 충분히 풍부해져서 별도 팁이 중복됨

## 3-2. 35개 적용 파일

| 시리즈 | 파일 수 | 범위 |
|--------|--------:|------|
| 문장구조 | 9 | 1~10, 11~20, 21~30, 31~40, 41~50, 51~60, 61~70, 71~78, 79~86 |
| 서술어 | 12 | 87~96, 97~104, 105~114, 115~122, 123~141, 142~149, 150~156, 157~163, 164~168, 169~180, 181~189, 190~198, 199~203 |
| 부사어 | 11 | 248~252, 253~259, 260~265, 266~270, 271~276, 277~283, 284~291, 292~295, 296~299, 300~306, 307~313, 314~315 |
| 기타 | 3 | 441~444 (ㅡ불규칙), 시제 문법설명, 조사 문법설명 |

## 3-3. 신규 문서화 (2건)

| 파일 | 문법 | 비고 |
|------|------|------|
| `204~208 서술어 문법 변화1.html` | ~아/어지다 (피동·변화) | reference 파일로 활용 |
| `209~212 서술어 문법 변화2.html` | ~게 되다 (변화) | 신규 작성 |

## 3-4. 자동화 스크립트 (3종)

| 스크립트 | 역할 | 라인 수 |
|----------|------|--------:|
| `_bulk_update_documentation.py` | colgroup 삽입 + 헤더 stack + CSS 4종 + explain-tip 제거 일괄 | ~80 |
| `_bilingual_structure_header.py` | "한국어 어순:" 헤더 양국어 변환, 60+ 영어→한국어 치환 규칙 | ~185 |
| `_apply_remaining_standards.py` | gstep-table 헤더 stack + '예시'→'예' 잔여 처리 | ~58 |

추가로 PowerShell 변형 `_bulk_update_documentation.ps1` (Windows 환경 폴백).

### `_bilingual_structure_header.py` 치환 규칙 (발췌)
- 복합어 우선: `Indirect Object` → `간접 목적어`, `Place-from` → `출발 장소`
- 번호 붙은 역할: `Object1` → `목적어1`, `Adj 1` → `형용사1`
- 단일어: `Subject` → `주어`, `Predicate` → `서술어`
- 괄호 보조: `(interrogative)` → `(의문)`, `(want to)` → `(희망)` 등 50종

---

# Part 4 — 패턴 블록 다듬기 (uncommitted)

`1~10 형용사`, `11~20 동사` 두 파일의 **상단 패턴 블록**(`.pattern`) 정합성 보정. (현재 작업 디렉터리 변경분)

## 4-1. Subject + Particle → Noun + Particle

**위치:** Part 1 · 주어부 카드의 `pattern-title`

```diff
-<div class="pattern-title">Subject + Particle</div>
-<div class="pattern-body">주어 + 은/는 <span class="pattern-body-meta">or</span> 이/가</div>
+<div class="pattern-title">Noun + Particle</div>
+<div class="pattern-body">명사 + 은/는 <span class="pattern-body-meta">or</span> 이/가</div>
```

**이유:** "Subject"는 문장 안 역할(role)이고, "은/는·이/가" 결합 직전의 형태는 **명사**. 카드의 좌변(피수식어)을 명사로 표기하는 것이 한국어 조사의 결합 단위(명사)와 일치하고, 학습자가 "주어 자리에 올 수 있는 것 = 명사 + 조사"라는 단계를 분명히 인식.

## 4-2. exp-table 컬럼 너비 미세조정 (1~10·11~20)

```diff
-.exp-table .exp-role { font-weight: 700; color: #6d28d9; width: 12%; ... }
-.exp-table .exp-form { font-weight: 700; color: #1e293b; width: 18%; }
+.exp-table .exp-role { font-weight: 700; color: #6d28d9; width: 18%; padding-right: 18px; ... }
+.exp-table .exp-form { font-weight: 700; color: #1e293b; width: 20%; padding-left: 22px; }
```

헤더 행도 동일하게 인라인 너비 + 좌측 padding 보정.

**이유:** Role(Subject/Predicate) 라벨이 `padding-right` 없이 Korean 셀과 시각적으로 붙어 보이던 문제. Role 18%로 조금 키우고 우측 여백 18px 확보, Korean 형태도 좌측 22px 패딩으로 분리.

> ⚠️ **표준 적용 범위:** 1~10 시범 후 11~20에 동기화 완료(2026-05-04), **21~30·31~40·41~50·51~60·71~78에도 동기화 완료**. 61~70, 79~86, 87~500 등 후속 시리즈로의 확산은 추후 결정. **Part 3의 colgroup 20/12/18/50 기준은 그대로 유지**하되, `.exp-role`/`.exp-form` 인라인 너비만 덮어쓰는 방식.

> 📝 **11~20·21~30·31~40·41~50·51~60·71~78 동기화 시 보강한 항목:** `.exp-en` CSS 클래스 누락분 추가 (v9 표준의 `overflow-wrap: normal; hyphens: none` 포함). 모든 파일에서 그동안 `.exp-en` tbody td만 있었고 CSS 클래스 정의가 빠진 상태였음.

## 4-3. exp-table 가운데 정렬 (1~10·11~20)

```diff
-.exp-table thead th { ... padding: 8px 10px; text-align: left; ... }
-.exp-table tbody td { padding: 12px 10px; ... overflow-wrap: anywhere; }
+.exp-table thead th { ... padding: 8px 10px; text-align: center; ... }
+.exp-table tbody td { padding: 12px 10px; ... overflow-wrap: anywhere; text-align: center; }
```

**적용 범위:** thead 4개 헤더 셀(English/Role/Korean/Explanation) + 모든 tbody 셀 — 총 4컬럼 일괄 가운데 정렬.

**이유:** 이전까지 좌측 정렬이라 짧은 라벨(Subject, Verb)이 셀 좌측에 붙고 우측 여백이 비어 시각 균형이 흐트러짐. 가운데 정렬로 컬럼별 시각 무게 중심이 정렬되어 표가 정돈된 느낌.

> ⚠️ **부수 효과:** 4-2에서 추가한 `.exp-role { padding-right: 18px }`, `.exp-form { padding-left: 22px }`는 좌측 정렬 기준에서 Role과 Korean 사이를 띄우려 도입한 것. 가운데 정렬에서는 비대칭 padding이 텍스트 중심을 좌·우로 살짝 미는 부작용 — 후속 결정 사항(좌우 균등 padding으로 변경 또는 padding 제거).

> ⚠️ **표준 적용 범위:** 1~10 시범 후 11~20에 동기화 완료(2026-05-04), **21~30·31~40·41~50·51~60·71~78에도 동기화 완료**. 61~70, 79~86 및 87~500 시리즈 확산은 추후 결정.

## 4-4. Q7 explain 표 — 영어 어순 분해 패턴 도입 (1~10만)

**대상:** Q7 "There are a lot of people" / "사람들이 많습니다."

영어와 한국어 구조가 비대칭인 문장(영어는 there-be 구문 + 수량사 + 명사, 한국어는 명사 + 형용사)을 학습자가 매핑하기 쉽도록 explain 표를 **영어 어순으로 분해 + 결합 행 추가** 패턴으로 재작성.

### 신규 표 구성 (4행)

| English | Role | Korean | 핵심 |
|---|---|---|---|
| **There are** | Predicate | 있다 | 영어 there-be 구문 ↔ 한국어 존재 동사 |
| **a lot of** | Quantifier | 많다 / 많이 | 형용사 / 부사 양형태 |
| **There are a lot of** | → Predicate | 많이 있다 → 많습니다 | 직역 결합 → 한국어가 형용사 한 단어로 융합 |
| **people** | Subject | 사람들이 | 사람 + 들(복수) + 이(주격) |

### 패턴화한 원칙

1. 표 행은 **영어 원문의 단어 순서**대로 배치 (한국어 어순이 아닌)
2. 영어 구(phrase)의 한국어 대응 개념을 보여주고, 결합 시점에서 자연 한국어로의 변환을 별도 행으로 표시
3. Role 컬럼에 `→ Predicate` 같은 결합/변환 표지 사용 가능
4. Korean 컬럼에서 `많이 있다 → 많습니다` 식 화살표로 직역→자연어 흐름 표시

### explain-note 처리

초기 시도에서 영어/한국어 어순 차이를 정리한 explain-note(① Literal / ② Natural)를 추가했으나 사용자 요청으로 **삭제**. 표 안 4행으로 충분히 자명하다는 판단.

> ⚠️ **표준 적용 범위:** Q7 단발 적용. Subject + Adjective(형용사) 단순 매핑이 안 되는 다른 비대칭 문장(예: 이다 의문문, 의문사 fronting, did + verb 의문, will + verb 미래, have + p.p. 완료 등)에도 같은 분해 패턴이 유용할 수 있음. 다른 시리즈에 시범 적용 후 표준화 여부 판단.

## 4-5. Antonym Matching Game 삽입 (1~10 BONUS 섹션)

**대상:** 1~10 형용사 BONUS 섹션 — Antonym Flashcards 바로 아래, Read Along 위.

기존에 BONUS 섹션은 ① 형용사 반대말 표(`.antonym-table`) + ② Antonym Flashcards 10쌍 두 가지 학습 콘텐츠가 있었음. 외부에서 받은 매칭 게임 HTML(`형용사 반대말 맞추기.html`)을 이 페이지에 인라인 임베드.

### 임베드 시 변경점

| 원본 (standalone) | 임베드 버전 |
|---|---|
| `<html>/<head>/<body>` 풀 페이지 | `.antonym-match` 컨테이너 1개 |
| `:root` CSS 변수(--blue, --indigo 등) | 1~10의 블루 테마 직접 사용 (#3b82f6, #1e40af, #eff6ff) |
| 5×4 grid + hero 배너 + guide | 5×4 grid + 점수 툴바 + 메시지 + 진행바 (콤팩트) |
| `pairs`, `cells`, `selected` 등 글로벌 변수 | 모두 `am*` 프리픽스 (`amPairs`, `amCells`, `amSelected` 등) — 기존 `wt*`/`swt*` 함수와 분리 |
| 한국어 단발 메시지 | 한국어 + 영어 보조 (`<span style="color:#94a3b8;">English hint</span>`) |
| 페이지 진입 시 첫 게임 | 동일 (`amReset()` 즉시 실행) |
| 모바일: 3열 → 2열 | 모바일: 2열 (단순화) |

### 데이터 일관성

10개 쌍은 1~10 BONUS 섹션의 antonym 표 + Antonym Flashcards와 동일:
크다/작다, 많다/적다, 높다/낮다, 길다/짧다, 빠르다/느리다, 좋다/나쁘다, 쉽다/어렵다, 싸다/비싸다, 덥다/춥다, 무겁다/가볍다.

### 기능

- 한 형용사 클릭 → 파란 강조 (`selected`)
- 짝 맞는 반대말 클릭 → 두 셀 페이드아웃(`removing`) 후 제거, 정답 카운트 +1
- 짝 안 맞는 셀 클릭 → 두 셀 빨강 흔들림(`wrong`), 오답 카운트 +1, 선택 해제
- 같은 셀 두 번 클릭 → 선택 취소
- 진행바 = 정답/총 비율
- "🔄 다시 시작" 버튼으로 셔플 + 카운트 초기화
- 10쌍 모두 맞추면 메시지 컨테이너 초록 강조 (`.complete`)

> ⚠️ **표준 적용 범위:** 1~10 단발(형용사 반대말이라는 1~10 고유 콘텐츠 특성상 다른 시리즈로의 직접 확산은 부적합). 동일 패턴(매칭 게임)을 다른 시리즈의 핵심 어휘에 응용한다면 별도 데이터 셋 + 동일 `am-*` 컨테이너 재사용 가능.

### 후속 정리 (4-5 직후)

- 매칭 셀 한국어/영어 양국어 표시 → 사용자 요청으로 **영어 라벨 제거**, 한국어 형용사만 표시. 미사용이 된 `.am-en` CSS 클래스와 `aEn`/`bEn`/`en` 데이터는 영어 복원 가능성을 위해 잔존(데드 코드 양 미미).

## 4-6. 플래시카드·매칭 게임 파스텔 색상 도입 (1~10 BONUS)

기존 단일 블루 테마 (.flashcard-back: 블루 그라데이션 / .am-cell: 슬레이트 무채색)에서 **파스텔 다색 팔레트**로 전환. BONUS 섹션의 시각적 즐거움 + 각 카드 식별성 강화.

### 파스텔 팔레트 (10색 — Tailwind 100/200/300/900 레벨)

| 인덱스 | 색상 | bg (light) | border (medium) | text (dark) |
|------|------|-----------|----------------|-------------|
| 1 | pink | `#fce7f3` | `#f9a8d4` | `#831843` |
| 2 | emerald | `#d1fae5` | `#6ee7b7` | `#064e3b` |
| 3 | amber | `#fef3c7` | `#fcd34d` | `#78350f` |
| 4 | violet | `#ede9fe` | `#c4b5fd` | `#4c1d95` |
| 5 | orange | `#ffedd5` | `#fdba74` | `#7c2d12` |
| 6 | blue | `#dbeafe` | `#93c5fd` | `#1e3a8a` |
| 7 | red | `#fee2e2` | `#fca5a5` | `#7f1d1d` |
| 8 | lime | `#ecfccb` | `#bef264` | `#365314` |
| 9 | cyan | `#cffafe` | `#67e8f9` | `#164e63` |
| 10 | purple | `#f3e8ff` | `#d8b4fe` | `#581c87` |

### 4-6-1. 플래시카드 — 페어별 고정 색상

각 쌍(10개)에 팔레트 인덱스를 1:1 매핑. `.antonym-flashcards .flashcard:nth-child(N) .flashcard-front/back`로 적용:

```css
.antonym-flashcards .flashcard:nth-child(1) .flashcard-front { background:#fce7f3; border-color:#f9a8d4; color:#831843; }
.antonym-flashcards .flashcard:nth-child(1) .flashcard-back  { background:linear-gradient(135deg,#fbcfe8,#f9a8d4); color:#831843; }
... (10쌍 반복)
```

**디자인 결정:**
- 앞면 = 100 레벨 light pastel + 200 레벨 border + 900 레벨 dark text
- 뒷면 = 200→300 레벨 그라데이션 + 동일 900 dark text (앞↔뒤 시각 대비를 그라데이션 강도로 유지)
- 🔊 버튼: `rgba(255,255,255,0.6)` (페어 색에 무관한 흰 반투명, hover 0.9)
- 페어별 식별성 강화 — 학습자가 "분홍 카드 = 크다/작다", "민트 카드 = 많다/적다" 식으로 색 연상 가능

### 4-6-2. 매칭 게임 — 셀별 랜덤 색상 (페어 식별 힌트 차단)

매칭 게임은 **페어를 색으로 노출하면 안 됨** (그러면 게임이 무의미). 따라서 페어와 무관하게 **셀마다 무작위 파스텔** 부여:

**CSS 변수 패턴:**
```css
.am-cell { background: var(--am-bg, #f8fafc); border: 2px solid var(--am-border, #e2e8f0); ... }
.am-ko { color: var(--am-text, #1e40af); }
```

**JS:**
```js
const amPastels = [{bg,border,text}, ... 10개];
function amMakeCells(){
  const cells = ...;
  cells.forEach(c => c.pastel = amPastels[Math.floor(Math.random()*10)]);
  return cells;
}
function amRender(){
  ...
  btn.style.setProperty('--am-bg', cell.pastel.bg);
  btn.style.setProperty('--am-border', cell.pastel.border);
  btn.style.setProperty('--am-text', cell.pastel.text);
}
```

매번 `amReset()` 시 셔플 + 새 랜덤 색 배정.

**상태 처리 충돌 해결:**
- Hover: `filter: brightness(0.95)` — 어떤 파스텔에도 작동 (background 덮지 않음)
- Selected: `box-shadow: 0 0 0 3px #3b82f6, 0 6px 16px rgba(59,130,246,0.25)` — 파란 링이 파스텔 위에 얹힘 (border-color 변경 X → 파스텔 유지)
- Wrong: `background: #fef2f2 !important; border-color: #ef4444 !important` — 파스텔을 강제 덮어 빨강 플래시 (0.45s 후 파스텔 복귀)
- Removing: `opacity: 0; transform: scale(0.5)` — 파스텔 무관

> ⚠️ **표준 적용 범위:** 1~10 단발. 다른 시리즈에 BONUS 콘텐츠 추가 시 같은 팔레트·CSS 변수 패턴 재활용 권장. **CSS 변수 + JS 런타임 색상 주입** 패턴은 향후 다른 인터랙티브 컴포넌트에도 응용 가능.

### 후속 정리 (4-6 직후) — 매칭 셀 색 중복 방지

초기 4-6-2 구현은 10색 팔레트에서 **무작위 추출(replacement)** 방식이라 20셀 중 같은 색이 다수 등장(평균 약 7~8개 중복). 사용자 요청으로 **모든 셀이 서로 다른 색**이 되도록 변경.

**변경 1 — 팔레트 확장 (10 → 20):**

기존 10색 + 추가 10색:
- rose `#ffe4e6` / `#fda4af` / `#881337`
- yellow `#fef9c3` / `#fde047` / `#713f12`
- green `#dcfce7` / `#86efac` / `#14532d`
- teal `#ccfbf1` / `#5eead4` / `#134e4a`
- sky `#e0f2fe` / `#7dd3fc` / `#0c4a6e`
- indigo `#e0e7ff` / `#a5b4fc` / `#312e81`
- fuchsia `#fae8ff` / `#f0abfc` / `#701a75`
- pink-2 (채도 ↑) `#fbcfe8` / `#f472b6` / `#831843`
- green-2 (채도 ↑) `#bbf7d0` / `#4ade80` / `#14532d`
- sky-2 (채도 ↑) `#bae6fd` / `#38bdf8` / `#0c4a6e`

**변경 2 — 샘플링 알고리즘 (with replacement → without replacement):**

```js
// Before (replacement 가능, 중복 발생)
cells.forEach(c => c.pastel = amPastels[Math.floor(Math.random()*amPastels.length)]);

// After (셔플 후 1:1 매핑, 중복 없음)
const palette = amShuffle(amPastels).slice(0, cells.length);
cells.forEach((c, i) => c.pastel = palette[i]);
```

`amShuffle()`은 Fisher-Yates 셔플로 매 라운드마다 새 순서. 20셀 = 20색이므로 슬라이스 후 1:1 부여.

> ⚠️ **확장 시 제약:** 팔레트 크기 ≥ 셀 수가 보장돼야 함. 다른 시리즈에서 매칭 게임 응용 시 셀 수 (= 단어 쌍 × 2)가 20을 초과하면 팔레트도 함께 확장 필요.

## 4-7. 본문 3개 학습 섹션에 파스텔 확장 (Read Along · Writing Test · Look & Speak)

BONUS 섹션(antonym + 매칭 게임)에 도입한 파스텔 패턴을 본문 학습 섹션 3개에 확장. **모든 섹션이 동일한 10색 시퀀스**를 사용해서 항목 N(예: 5번 문장)이 세 섹션 모두에서 같은 색 계열 → 시각적 추적 일관성.

### 공통 10색 시퀀스 (Tailwind 50 → 100 → 200 레벨 사용)

| N | 색상 | 50 (가장 옅음) | 100 | 200 |
|---|------|---------------|-----|-----|
| 1 | pink | `#fdf2f8` | `#fce7f3` | `#fbcfe8` |
| 2 | rose | `#fff1f2` | `#ffe4e6` | `#fecdd3` |
| 3 | orange | `#fff7ed` | `#ffedd5` | `#fed7aa` |
| 4 | amber | `#fffbeb` | `#fef3c7` | `#fde68a` |
| 5 | yellow | `#fefce8` | `#fef9c3` | `#fef08a` |
| 6 | lime | `#f7fee7` | `#ecfccb` | `#d9f99d` |
| 7 | emerald | `#ecfdf5` | `#d1fae5` | `#a7f3d0` |
| 8 | teal | `#f0fdfa` | `#ccfbf1` | `#99f6e4` |
| 9 | sky | `#f0f9ff` | `#e0f2fe` | `#bae6fd` |
| 10 | violet | `#f5f3ff` | `#ede9fe` | `#ddd6fe` |

### 4-7-1. Read Along — 50-level (가장 은은)

```css
.sentence-list .sentence-item:nth-child(N) { background: <50-level>; }
```

`.sentence-item`의 기존 `background: #fff` 덮어쓰기. hover 시 파란 보더·그림자(`.sentence-item:hover`)는 그대로.

### 4-7-2. Writing Test — 50-level (Read Along과 동일 강도)

```css
.writing-practice .writing-item:nth-child(N) { background: <50-level>; }
```

종합 Writing Test 섹션의 10개 입력 카드. 입력 칸(`.writing-input`)은 기존 흰 배경 유지 → 사용자 시선이 파스텔 배경 위 흰 입력칸으로 자연스럽게 모임.

### 4-7-3. Look & Speak — 앞 50 + 뒤 100 그라데이션

플래시카드 구조라 양면 처리:

```css
.flashcard-grid.look-speak .flashcard:nth-child(N) .flashcard-front {
    background: <50-level>;
    border-color: <100-level>;
}
.flashcard-grid.look-speak .flashcard:nth-child(N) .flashcard-back {
    background: linear-gradient(135deg, <100-level>, <200-level>);
    color: <900-level family text>;
}
```

**스코프 변경:** Look & Speak의 `<div class="flashcard-grid">`에 `look-speak` 클래스 추가. BONUS의 antonym 플래시카드(`.antonym-flashcards .flashcard-grid`)와 명확히 분리.

**텍스트 색 변경:** 기존 `.flashcard-back`의 white text를 패밀리 dark(900 레벨 — 예: pink → `#831843`)로 덮어쓰기. 흰 텍스트는 옅은 파스텔 위에서 가독성 X.

**🔊 버튼:** antonym과 동일한 패턴 — `background: rgba(255,255,255,0.6); color: inherit` (10가지 색에 무관하게 깔끔).

### 시각 위계 정리

| 섹션 | 색상 강도 | 의도 |
|------|----------|------|
| Antonym Flashcards (BONUS) | 100 + 200 그라데이션 (페어별 고정) | 게임/암기 — 시각 임팩트 강 |
| Antonym Matching Game (BONUS) | 100/200 셀별 랜덤 | 게임 — 셀별 식별성 |
| Look & Speak | 50 앞 / 100→200 뒤 (순서 고정) | 시각/음성 학습 — 중간 강도 |
| Read Along · Writing Test | 50 (순서 고정) | 텍스트 읽기/입력 — 가장 은은 |

> ⚠️ **표준 적용 범위:** 1~10 단발. 다른 시리즈에 확산하려면 각 섹션의 항목 수가 시리즈마다 다름(11~20: 10개, 71~78: 8개)을 고려해 nth-child 범위 조정 필요. 11색 이상이 필요하면 4-6의 20색 팔레트 확장본 사용.

## 4-8. Antonym Table에 파스텔 확장 (BONUS, 페어 색상 일관성 확보)

BONUS 섹션의 antonym 표(`.antonym-table`)에 50-level 파스텔 행 배경 적용. **antonym 플래시카드와 같은 페어 색상 매핑**을 사용해 BONUS 섹션 내부에서 같은 쌍(예: 크다/작다)이 표·플래시카드 양쪽에서 동일한 색 계열로 식별되도록.

### 두 가지 일관성 원칙의 충돌과 절충

| 원칙 | 적용 대상 | 충돌 |
|------|----------|------|
| **페어 색상 일관성** (BONUS 내부) | 같은 쌍 = 같은 색 계열 (표 ↔ 플래시카드) | Read Along/Writing/Look&Speak의 무지개 시퀀스와 다름 |
| **무지개 시퀀스** (본문 4-7) | 항목 N = N번째 색 (pink→rose→orange→...) | 페어 식별성 X |

**절충:** 표는 페어 매핑 우선 (플래시카드와 시각 연결이 더 중요), 본문 3섹션은 무지개 시퀀스 유지. 결과적으로 BONUS는 자체 색 체계, 본문은 자체 색 체계로 분리됨.

### 페어 매핑 (antonym 플래시카드 4-6-1과 동일 순서)

| 쌍 | 색 패밀리 | 표 bg (50) | 플래시카드 앞 bg (100) | 플래시카드 뒤 bg (200) |
|---|------|-----------|----------------------|----------------------|
| 1 크다/작다 | pink | `#fdf2f8` | `#fce7f3` | `#fbcfe8` |
| 2 많다/적다 | emerald | `#ecfdf5` | `#d1fae5` | `#a7f3d0` |
| 3 높다/낮다 | amber | `#fffbeb` | `#fef3c7` | `#fde68a` |
| 4 길다/짧다 | violet | `#f5f3ff` | `#ede9fe` | `#ddd6fe` |
| 5 빠르다/느리다 | orange | `#fff7ed` | `#ffedd5` | `#fed7aa` |
| 6 좋다/나쁘다 | blue | `#eff6ff` | `#dbeafe` | `#bfdbfe` |
| 7 쉽다/어렵다 | red | `#fef2f2` | `#fee2e2` | `#fecaca` |
| 8 싸다/비싸다 | lime | `#f7fee7` | `#ecfccb` | `#d9f99d` |
| 9 덥다/춥다 | cyan | `#ecfeff` | `#cffafe` | `#a5f3fc` |
| 10 무겁다/가볍다 | purple | `#faf5ff` | `#f3e8ff` | `#e9d5ff` |

### 구현 — nth-child 인덱스 처리

표 첫 행은 "한국어 어순: Subject + Predicate" 라벨 행(인라인 보라 `#ede9fe`)이라 **nth-child(1)은 건너뜀**. 쌍 1~10 = `nth-child(2~11)`.

```css
.antonym-table tbody tr:nth-child(2)  td { background: #fdf2f8; }
.antonym-table tbody tr:nth-child(3)  td { background: #ecfdf5; }
... (총 10개 규칙, nth-child 2~11)
```

### 기존 zebra striping 처리

기존 `.antonym-table tbody tr:nth-child(even) td { background: #f8fafc; }`는 짝수 행에 회색 스트라이프를 부여. 새 nth-child(N) 규칙들이 **CSS 뒤에 위치**하므로 동일 specificity에서 후순위 규칙이 승리 → zebra 자동 덮어쓰기. 별도 `!important`나 specificity boost 불필요.

> ⚠️ **표준 적용 범위:** 1~10 단발. antonym 표는 1~10 BONUS 고유 콘텐츠라 직접 확산 부적합. 다른 시리즈에 유사 페어 표(예: 동의어·유의어 표)를 추가한다면 같은 페어-매핑 + 50-level 패턴 재활용 가능.

## 4-9. BONUS 파스텔 톤 다운 (매칭 게임 + antonym 플래시카드)

4-6/4-7/4-8 적용 후 매칭 게임과 antonym 플래시카드의 색이 본문 50-level 파스텔(Read Along/Writing/Look&Speak)에 비해 진해 시각 위계 역전 발생. 사용자 요청으로 한 단계씩 톤 다운하여 **BONUS 전체를 본문과 같은 은은한 톤으로 통일**.

### 변경 매트릭스

| 컴포넌트 | 이전 (4-6/4-7) | 신규 (4-9) |
|---------|---------------|-----------|
| 매칭 셀 bg | 100 | **50** |
| 매칭 셀 border | 300 | **200** |
| 매칭 셀 text | 900 | **800** |
| 플래시카드 앞면 bg | 100 | **50** |
| 플래시카드 앞면 border | 200 | **100** |
| 플래시카드 뒷면 그라데이션 | 200→300 | **100→200** |
| 플래시카드 텍스트 | 900 | **800** |

### 매칭 게임 — 새 20색 팔레트

17가지는 균일하게 50/200/800. **추가 3종(pink-2 / green-2 / sky-2)은 100/300으로 살짝 진하게** 유지 — 20셀이 모두 너무 옅으면 시각 변화감이 사라지므로 미묘한 강조 변화로 차이 유지.

```js
const amPastels = [
  { bg:'#fdf2f8', border:'#fbcfe8', text:'#9d174d' },  // pink (50/200/800)
  { bg:'#fff1f2', border:'#fecdd3', text:'#9f1239' },  // rose
  ... 17개 ...
  { bg:'#fce7f3', border:'#f9a8d4', text:'#9d174d' },  // pink-2 (100/300, 살짝 진함)
  { bg:'#dcfce7', border:'#86efac', text:'#166534' },  // green-2
  { bg:'#e0f2fe', border:'#7dd3fc', text:'#075985' }   // sky-2
];
```

### 시각 위계 (4-9 적용 후)

| 섹션 | 강도 | 텍스트 |
|------|-----|-------|
| Read Along · Writing Test · Look & Speak 앞면 | 50 (가장 옅음) | 기존 #1e293b |
| Antonym Table | 50 | 기존 #1e40af |
| **Antonym 플래시카드 앞면 (변경)** | **50** | **800 (소프트)** |
| **매칭 게임 셀 (변경, 17/20)** | **50** | **800 (소프트)** |
| **Antonym 플래시카드 뒷면 (변경)** | **100→200 그라데이션** | **800** |
| 매칭 게임 셀 (변형 3/20) | 100 | 800 |
| Look & Speak 뒷면 | 100→200 그라데이션 | 800 |

→ BONUS와 본문 모두 **50-level 기준선**으로 통일. 페어 식별성/셀 구분은 hue로만 보장(채도·명도 차이 최소화).

> ⚠️ **회귀 검토 포인트:** 너무 옅으면 흰 페이지 배경에서 셀 식별이 어려워질 수 있음. 200-level border가 보더 역할로 셀 경계를 분명히 해줌. 추후 사용자 피드백에 따라 200→300 boundary 강화 옵션 검토.

## 4-10. The Basic Pattern 블록에 파스텔 (PART 1 / PART 2 색상 분리)

페이지 상단의 The Basic Pattern 시각화 블록 2개(`.pattern .pattern-block`)에 은은한 50-level 파스텔 적용. 두 블록을 **인접 색상(blue / violet)** 으로 구분해 주어부 ↔ 서술부 시각 분리.

### 색상 매핑 — 의도

| 블록 | 색 | 의도 |
|------|---|------|
| PART 1 · 주어부 (Noun + Particle) | blue (50/200) | 페이지 전체 블루 테마와 일치, 주어 = 기준점 |
| PART 2 · 서술부 (Adjective) | violet (50/200) | blue와 인접하여 조화, 서술어 = 기준에서 한 발 나아간 위치 |

### 구현

```css
.pattern .pattern-block:first-child { background:#eff6ff; border-color:#bfdbfe; }
.pattern .pattern-block:first-child::before { background:linear-gradient(90deg,#3b82f6,#60a5fa); }
.pattern .pattern-block:last-child { background:#f5f3ff; border-color:#ddd6fe; }
.pattern .pattern-block:last-child::before { background:linear-gradient(90deg,#7c3aed,#a78bfa); }
.pattern .pattern-block:last-child .pattern-eyebrow { color:#7c3aed; }
.pattern .pattern-plus { color:#6366f1; }
```

### 세부 결정

- **PART 1 ::before 그라데이션**: 기존 blue 유지 (재선언이지만 의도 명확화)
- **PART 2 ::before 그라데이션**: violet로 변경 (블록 색과 일치)
- **PART 2 .pattern-eyebrow 색**: 기존 blue → violet `#7c3aed`로 갈음 (라벨이 블록 색과 조화)
- **PART 1 .pattern-eyebrow 색**: 기존 blue 유지
- **`+` 구분자 (.pattern-plus)**: 양측 색의 중간점 indigo `#6366f1`로 변경 (blue→indigo→violet 자연 그라데이션 시각 효과)

### nth-child 셀렉터 선택

`.pattern` 컨테이너는 `<div class="pattern-block">` + `<div class="pattern-plus">+</div>` + `<div class="pattern-block">` 3개 자식 구조. `:first-child`/`:last-child`로 두 블록을 명확히 지정 (`:nth-child(1)`/`:nth-child(3)`도 동등).

> ⚠️ **표준 적용 범위:** 1~10 단발. 다른 시리즈 중 `.pattern` 블록을 가진 파일에 동일 패턴 확산 가능 — 단, 11~20도 `.pattern` 구조를 가지므로 4-1과 함께 다음 작업에서 동기화 검토.

## 4-11. 패턴 블록 eyebrow 라벨 간소화 (1~10·11~20)

```diff
-<div class="pattern-eyebrow">Part 1 · 주어부</div>
+<div class="pattern-eyebrow">주어</div>

-<div class="pattern-eyebrow">Part 2 · 서술부</div>
+<div class="pattern-eyebrow">서술어</div>
```

**이유:**
- `Part 1`/`Part 2` 번호 라벨은 `+` 구분자만으로 충분히 순서 표현 가능 — 중복 정보 제거
- `주어부`/`서술부`는 한국어 학교 문법 용어인데 외국인 학습자에게는 어색. **`주어`/`서술어`** 가 본문에서 이미 사용하는 핵심 용어와 일치 (예: 한국어 어순 헤더 `Subject + Predicate / 주어 + 서술어`)
- 결과: 라벨이 실제 문법 역할명만 짧게 표시 → 인지 부하 감소

**적용 범위:** 1~10·11~20 동기화 완료(Part 4-10에서 패턴 블록을 가진 두 파일 한정으로 정리한 것과 짝). 21~86 등 후속 시리즈에는 `.pattern` 블록 자체가 없어 해당 없음.

## 4-12. 패턴 블록 가운데 정렬 (1~10·11~20)

```diff
-.pattern-block { ... display: flex; flex-direction: column; gap: 6px; ... }
+.pattern-block { ... display: flex; flex-direction: column; gap: 6px; ... text-align: center; }
```

**이유:** eyebrow(주어/서술어)·title(Noun + Particle/Adjective)·body(명사 + 은/는 or 이/가/형용사)가 좌측 정렬이라 짧은 라벨이 셀 좌측에 붙어 시각 균형 흐트러짐. 가운데 정렬로 카드 중심에 콘텐츠 모임.

**적용 범위:** 1~10·11~20 동기화 완료. 21~86은 패턴 블록 부재로 해당 없음.

## 4-13. 11~20 일괄 동기화 — §4-7 + §4-10 + §4-12 전체 적용

`_작업_로그_2026-05-04.md`의 1~10 단발 항목 중 11~20에 의미 있는 변경분을 일괄 동기화. BONUS 관련(§4-5, §4-6, §4-8, §4-9)은 1~10 형용사 고유 콘텐츠라 11~20에 해당 없음.

### 적용 항목 매트릭스

| 항목 | 1~10 | 11~20 |
|------|------|------|
| §4-1 패턴 블록 "Subject + Particle" → "Noun + Particle" | ✅ | ✅ (이전 동기화) |
| §4-2 exp-table 너비 미세조정 | ✅ | ✅ (이전 동기화) |
| §4-3 exp-table 가운데 정렬 | ✅ | ✅ (이전 동기화) |
| §4-4 Q7 영어 어순 분해 | ✅ Q7 단발 | — (해당 문항 없음) |
| §4-5 Antonym Matching Game | ✅ BONUS | — (BONUS 부재) |
| §4-6 BONUS 파스텔 팔레트 | ✅ | — |
| **§4-7 본문 3섹션 파스텔 (Read/Writing/Look)** | ✅ | **✅ (이번 동기화)** |
| §4-8 Antonym Table 파스텔 | ✅ BONUS | — |
| §4-9 BONUS 파스텔 톤 다운 | ✅ | — |
| **§4-10 패턴 블록 PART 1/2 파스텔 (blue/violet)** | ✅ | **✅ (이번 동기화)** |
| §4-11 패턴 블록 eyebrow 간소화 | ✅ | ✅ (이전 동기화) |
| **§4-12 패턴 블록 가운데 정렬** | ✅ | **✅ (이번 동기화)** |

### 11~20 디자인 결정 — 색상 통일 vs 단원 테마

11~20는 가이드 §6 단원별 색상 테마 정의에 따르면 **green** 테마 (`.pattern-block` border #a7f3d0, `.sentence-listen` 초록 등). 패턴 블록 4-10 파스텔 적용 시 두 가지 선택지:

| 옵션 | 1~10 | 11~20 |
|------|------|------|
| A. 1~10과 동일 (blue/violet) | blue/violet | blue/violet |
| B. 단원 테마 유지 (green/teal로 적응) | blue/violet | emerald/teal |

**선택: A (blue/violet 통일).** 사유:
- 패턴 블록의 PART 1/2는 "주어/서술어"라는 **보편적 문법 역할** 표현 → cross-file 시각 일관성이 단원 테마보다 우선
- 단원 테마는 다른 요소(.sentence-listen 등)에서 유지 → 페이지 전체에서 단원 정체성은 잃지 않음
- 본문 3섹션의 무지개 시퀀스도 동일 (cross-file 일관성)

**향후 다른 단원 확산 시 같은 원칙 적용.**

### 11~20에 변경하지 않은 부분

- `.sentence-listen`, `.flashcard-listen` 등의 초록 단원 테마: **유지**
- 단원별 h1, .pattern-eyebrow의 단원 색: **유지** (단, 패턴 블록의 eyebrow만 4-10에 따라 blue/violet로 변경)

> ⚠️ **확장 시 점검:** 다른 단원(21~86 등)으로 4-7 확산 시 단원별 항목 수가 다름(예: 71~78은 8개) — nth-child 범위를 단원별로 조정 필요. 4-10/4-12는 패턴 블록을 가진 파일에만 한정 (1~10·11~20 외에 후속 단원은 패턴 블록 부재).

## 4-14. 인터랙티브 카드 텍스트 검은색 통일 (1~10·11~20)

§4-9에서 BONUS 톤 다운 후 매칭 셀과 플래시카드의 페어별 dark text(800-level pastel — #9d174d, #065f46 등)가 셀별로 달라 시각적 산만함이 있다는 사용자 판단. **모든 인터랙티브 카드 텍스트를 검은색(#000)으로 통일**.

### 적용 범위 (3종 컴포넌트)

| 컴포넌트 | 1~10 | 11~20 |
|---------|------|------|
| Antonym 플래시카드 (10쌍) | ✅ | — (BONUS 부재) |
| Antonym 매칭 게임 셀 (.am-ko) | ✅ | — (BONUS 부재) |
| Look & Speak 플래시카드 (10장) | ✅ | ✅ |
| 영어-한국어 매칭 게임 (.em-item) | — (해당 없음) | ✅ (4-15에서 신규 추가, 처음부터 검은색) |

### 구현 — specificity 매칭 후순위 우선 패턴

페어별 색상 CSS 규칙(`.antonym-flashcards .flashcard:nth-child(N) .flashcard-front { color: <800-level>; }`)을 직접 수정하지 않고, **같은 specificity의 nth-child(n) 통합 규칙을 뒤에 추가**해서 후순위 승리 패턴으로 덮어쓰기:

```css
/* Antonym 플래시카드 */
.antonym-flashcards .flashcard:nth-child(n) .flashcard-front,
.antonym-flashcards .flashcard:nth-child(n) .flashcard-back { color: #000; }

/* Look & Speak 플래시카드 */
.flashcard-grid.look-speak .flashcard:nth-child(n) .flashcard-front,
.flashcard-grid.look-speak .flashcard:nth-child(n) .flashcard-back { color: #000; }

/* 매칭 셀 (.am-ko 직접 수정 — var 제거) */
.am-ko { ... color: #000; }  /* was: var(--am-text, #1e40af) */
```

**페어별 color 정의 보존:** 페어별 색상 CSS는 그대로 두고 색상값만 후순위 규칙으로 덮어쓰기. 향후 페어 색상 텍스트로 회귀 시 마지막 override 규칙만 제거하면 됨 (역방향 작업 비용 최소화).

**매칭 셀 .am-ko 처리:** `var(--am-text, #1e40af)` → `#000` 직접 변경. JS의 `--am-text` 인라인 setProperty는 잔존(데드 CSS 변수, 출력 영향 없음).

> ⚠️ **확산 시 주의:** 4-15(영어-한국어 매칭 게임)는 처음부터 검은 텍스트로 작성됐으므로 별도 override 불필요.

## 4-15. 영어-한국어 문장 매칭 게임 신규 임베드 (11~20)

외부 standalone HTML(`영어 한국어 문장 맞추기.html`, 그는 + 동사 10문장)을 11~20에 인라인 임베드. **Writing Test 위에 신규 섹션 "3. Sentence Matching"으로 배치**, 기존 Writing Test는 "4. Writing Test"로 번호 이동.

### 임베드 시 변경점

| 원본 (standalone) | 임베드 버전 |
|---|---|
| `<html>/<head>/<body>` 풀 페이지 + hero 배너 | `.em-game` 컨테이너 1개 (제목 박스 + 설명 + 툴바 + 진행바 + 메시지 + 2칼럼 그리드) |
| `:root --green/--blue/...` CSS 변수 | 11~20 페이지 톤(blue 계열) 직접 사용 |
| 단일 green 테마 (모든 .item 같은 색) | **셀별 50-level 랜덤 파스텔** (4-9 매칭 게임 패턴 재활용, 페어 식별 차단) |
| 글로벌 변수 (englishItems, sentencePairs 등) | 모두 `em*` 프리픽스 (`emPairs`, `emEnItems`, `emKoItems` 등) — `am*` (1~10 antonym 매칭)와 충돌 방지 |
| 단발 한국어 메시지 | 한국어 + English hint (`<span style="color:#94a3b8;">...</span>`) |
| 페이지 진입 시 첫 게임 | 동일 (`emReset()` 즉시 실행) |
| 모바일: 1칼럼 강제 | 모바일: 1칼럼 자동 전환 (`@media max-width: 600px`) |

### 적용된 작업 로그 표준

| 표준 | 적용 |
|------|------|
| §4-6 / §4-9 매칭 게임 패턴 | 50-level 셀 + 20색 셔플 후 1:1 배정 (replacement 없음) |
| §4-9 BONUS 톤 다운 (50/200) | bg=50, border=200 |
| §4-14 검은 텍스트 | `.em-item`/`.em-text` `color: #000` (처음부터) |
| §4-7 무지개 시퀀스 | 동일 20색 팔레트 |
| Bilingual UI (양국어) | "정답/오답" 한국어 + English hint |
| `em-` 네임스페이스 | `am-`, `wt-`, `swt-` 와 충돌 차단 |

### 데이터

10쌍 — 11~20 단원의 동사 문장과 일치(그는 + 잤다/일어났다/걸었다/뛰었다/일했다/쉬었다/생각했다/말했다/들었다/갔다). 학습 동선 일관성: 학습자가 같은 페이지의 Read Along·Look & Speak·매칭 게임·Writing Test에서 동일한 문장 세트를 다양한 활동으로 반복 학습.

### 인터랙션

- 좌측 English 1개 + 우측 Korean 1개 선택 → 자동 검사
- 정답: 두 셀 페이드아웃 (`removing` 애니메이션) + 정답 카운트 +1
- 오답: 두 셀 빨강 흔들림 (`emShake` 0.45s) + 오답 카운트 +1 + 자동 선택 해제
- 같은 셀 재클릭: 선택 취소
- 진행바: 정답/총 비율
- 🔄 다시 시작: 셔플 + 새 팔레트 + 카운트 초기화
- 10쌍 모두 맞추면 메시지 컨테이너 초록 강조 (`.complete`)

### 4-5(antonym 매칭)와의 차이점

| 항목 | 4-5 antonym (1~10) | 4-15 sentence (11~20) |
|------|-------------------|---------------------|
| 데이터 단위 | 단어 쌍 (반대말) | 문장 쌍 (영어-한국어) |
| 그리드 | 단일 5×4 셔플 | **2칼럼 (English / Korean)** |
| 매칭 트리거 | 두 단어 클릭 | 좌측 1개 + 우측 1개 클릭 |
| 셀 셔플 | 20셀 통합 셔플 | 좌·우 칼럼 독립 셔플 |
| 네임스페이스 | `am-` | `em-` |

> ⚠️ **표준 적용 범위:** 11~20 단발. 21~86 등 다른 단원에 응용하려면 단원별 핵심 문장 10개씩 데이터 셋 작성 필요 + 같은 `em-` 컨테이너 + JS 패턴 재활용 가능.

## 4-16. 자동화 스크립트로 33개 파일 일괄 적용 (BONUS 패턴 + 매칭 게임 확산)

§4-15에서 11~20에만 매뉴얼로 임베드한 영어-한국어 매칭 게임을 **모든 적격 단원에 자동화 스크립트로 일괄 확산**. 동시에 §4-7(본문 3섹션 파스텔) + §4-14(검은 텍스트)도 같은 스크립트로 함께 적용.

### 자동화 스크립트 — `_apply_v9_bonus_pattern.py`

**위치:** `d:\놀라운 한국어 2026\_apply_v9_bonus_pattern.py` (프로젝트 루트)

**처리 흐름:**
1. 폴더 내 모든 `*.html` 파일 스캔
2. **Skip 조건** 체크 (다음 중 하나라도 해당하면 건너뜀):
   - 파일명에 `_작업_로그`/`_가이드`/`_정리` 포함
   - 파일명에 `문법설명`/`퀴즈 앱` 포함
   - **수동 커스터마이징된 파일** (1~10·11~20)
   - 이미 `em-game` 적용됨 (idempotency)
   - Read Along (`sentence-list`)·Look & Speak (`flashcard-grid`)·Writing Test (`writing-practice`) 중 하나라도 부재
3. **변환 단계** (적격 파일):
   - Read Along에서 `<div class="sentence-en">...</div><div class="sentence-kr">...</div>` 추출 → 매칭 게임 데이터 셋 자동 생성
   - 각 섹션 항목 수 자동 카운트
   - `flashcard-grid` → `flashcard-grid look-speak` 클래스 추가
   - CSS 일괄 추가 (`</style>` 직전): nth-child 범위 = 실제 항목 수
   - HTML 신규 섹션 삽입: `<h2>3. Sentence Matching</h2>` + em-game 컨테이너
   - 기존 `<h2>3. Writing Test</h2>` → `<h2>4. Writing Test</h2>` 번호 이동
   - JS 추가 (마지막 `</script>` 직전): `emPairs` 데이터 + emShuffle/emRender/emClick/... 함수

**Idempotency:** 같은 파일에 두 번 실행해도 `em-game` 검출로 두 번째는 자동 건너뜀.

**UTF-8 stdout 강제:** Windows cp949 환경에서 `↔` 등 유니코드 출력 시 UnicodeEncodeError 방지를 위해 `sys.stdout.reconfigure(encoding='utf-8')` 추가.

### 적용 결과 — 33개 파일

| 시리즈 | 파일 수 | 항목 수 분포 |
|--------|--------:|------------|
| 문장구조 (21~30 ~ 79~86) | 7 | 8~10 |
| 서술어 (87~96 ~ 209~212) | 14 | 4~19 |
| 부사어 (248~252 ~ 314~315) | 12 | 2~8 |
| **합계** | **33** | **2~19** |

### 41개 파일 건너뜀 — 사유 분포

| 사유 | 파일 수 | 예 |
|------|--------:|----|
| 수동 커스터마이징 (이전 작업) | 2 | 1~10, 11~20 |
| Read Along 부재 (BONUS-only 또는 reference) | ~28 | 213~247 서술어 기타, 316~500 기타 |
| 문법설명 (Practice 섹션 부재) | 2 | 시제 문법설명, 조사 문법설명 |
| 퀴즈 앱 (별도 앱) | 2 | 시제 퀴즈 앱, 조사 퀴즈 앱 |
| 로그·가이드 markdown | 3 | _작업_로그_*, _가이드_* |

### 항목 수 적응 — nth-child 범위 자동 조정

가장 작은 케이스: **314~315 부사어 원인 덕분에 (2 항목)** — read=2 write=2 look=2, 매칭 게임 2쌍.
가장 큰 케이스: **123~141 서술어 부정 (19 항목)** — read=19 write=19 look=19, 매칭 게임 19쌍.

10색 팔레트는 `i % 10` 모듈로로 순환 재사용 (예: 19개 항목 = 10색 1회전 + 9색 2회전).

### 매칭 게임 데이터 셋 — Read Along 문장 자동 추출

각 단원의 Read Along 문장이 그대로 매칭 게임 데이터로 사용됨. 학습 동선 일관성:
- Read Along에서 듣고 따라 읽기
- Look & Speak에서 카드 보고 말하기
- **Sentence Matching에서 짝 맞추기 (신규)**
- Writing Test에서 직접 입력

같은 문장 세트로 4가지 활동을 반복 → 자연스러운 점진적 숙달.

### 자동 생성 매칭 게임의 데이터 품질

- **장점:** Read Along 문장은 단원 핵심 표현이므로 매칭 데이터로 적합. 정확한 영어/한국어 1:1 페어가 마크업에 이미 존재.
- **한계:** Read Along 자체가 잘못된 번역을 포함하면 매칭 게임에도 그대로 전파. 매칭 게임은 Read Along의 신뢰성을 그대로 상속.

> ⚠️ **확장 방향:**
> - **213~247 서술어 시리즈**: Read Along 부재 → 데이터 셋 수동 작성 필요. 또는 Practice의 Q-data-ans에서 추출하는 다른 스크립트 작성 가능.
> - **316~500 기타 시리즈**: 동일. v9 표준(양국어 헤더, exp-table) 적용도 함께 필요.
> - **자동화 스크립트 재실행**: 새 파일에 4-7/4-14/매칭 게임 적용 시 같은 스크립트 재실행하면 idempotent하게 처리됨.

## 4-17. 인터랙티브 카드 텍스트 톤 다운 (#000 → #334155)

§4-14에서 모든 플래시카드·매칭 셀 텍스트를 `#000`으로 통일했으나, 50-level 옅은 파스텔 배경 위에서 너무 진해 시각 균형이 깨짐. 사용자 요청으로 **slate-700 (`#334155`)** 다크 그레이로 톤 다운.

### 색 선택 — 왜 #334155 인가

| 후보 | 헥스 | 평가 |
|------|------|------|
| 검정 | #000 | 옅은 파스텔에서 너무 진함 (이전 결정 회귀) |
| slate-800 | #1e293b | 검정과 거의 차이 없음 |
| **slate-700** | **#334155** | **확실히 부드러우면서 가독성 유지** ✅ |
| slate-600 | #475569 | 너무 흐려서 가독성 저하 |
| 페어별 dark text | #831843 등 (§4-9) | 셀별 색 변화 산만 (§4-14 회귀) |

### 적용 범위 — 36개 파일

`_soften_text_color.py` 스크립트로 5가지 정확한 CSS 패턴 일괄 치환:

| 패턴 | 컴포넌트 | 영향 파일 수 |
|------|---------|--------------|
| `.em-text { ... color: #000; ... }` | 매칭 게임 셀 텍스트 | 34 (11~20 + 33) |
| `.em-item { ... color: #000; }` | 매칭 게임 셀 컨테이너 | 34 |
| `.am-ko { ... color: #000; ... }` | 1~10 antonym 매칭 셀 | 1 |
| `.flashcard-grid.look-speak ... { color: #000; }` | Look & Speak 플래시카드 override | 35 |
| `.antonym-flashcards ... { color: #000; }` | 1~10 antonym 플래시카드 override | 1 |
| **합계** | **36개 파일 (1~10 포함)** | |

### 정확 패턴 매칭 — 부수 효과 차단

`color: #000`은 다른 컴포넌트(`.exp-table .exp-en`, 글로벌 `code`, `.explain-content code`)에도 사용되므로 단순 `s/#000/#334155/g`는 위험. 대신 **각 패턴을 충분히 긴 unique 컨텍스트로 둘러싸서** 정확 매칭:

```python
REPLACEMENTS = [
    ('.em-text { font-size: 0.93rem; font-weight: 700; line-height: 1.4; color: #000; word-break: keep-all; }',
     '.em-text { ... color: #334155; ... }'),
    ('transition: transform 0.15s, filter 0.15s, box-shadow 0.15s, opacity 0.3s; font-family: inherit; color: #000; }',
     '... color: #334155; ...'),
    # ... (5 patterns total)
]
```

스크립트 결과: **36개 파일 × 평균 3 패턴 = 약 100건 치환**. 다른 `color: #000` (exp-en 등)는 그대로 보존.

### 자동화 스크립트 동기화

`_apply_v9_bonus_pattern.py` 내 `color: #000` → `color: #334155` 일괄 갱신. 향후 213~247 등 신규 파일에 4-16 재실행 시 처음부터 부드러운 톤으로 생성.

> ⚠️ **시각 위계 (4-17 적용 후):**
> - 본문 일반 텍스트: #1e293b (slate-800) — 가장 진함
> - 인터랙티브 카드 (플래시카드·매칭 셀): #334155 (slate-700) — 한 단계 부드러움
> - 보조 텍스트 (subtitle, .ko, .pattern-body-meta): #64748b (slate-500) — 가장 흐림

## 4-18. 1~10 매칭 게임 수동 임베드 (SKIP_MANUAL 정책 변경)

§4-16에서 1~10은 SKIP_MANUAL로 제외했었음(이미 antonym 매칭 게임이 BONUS에 있어 중복 우려). 사용자 재확인 후 **두 게임은 다른 컨텐츠**(반대말 단어 매칭 vs 영어-한국어 문장 매칭)이므로 공존 가능 판단 → 1~10에도 sentence matching 게임 추가.

### 자동화 스크립트 한계 — 수동 처리 사유

`_apply_v9_bonus_pattern.py`로 1~10에 적용 시도 시 **`look-speak` 클래스가 이미 존재**해 정규식 매칭 실패. 또한 §4-7 nth-child 파스텔 CSS도 이미 들어있어 자동 추가 시 중복 발생 위험. 결과적으로 1~10은 **수동으로 em-game 컴포넌트만 선별 추가**:
- em-game CSS (§4-15와 동일)
- em-game HTML 섹션 (Look & Speak 다음, Writing Test 직전)
- Writing Test 번호 3 → 4 이동
- em-game JS + emPairs 데이터 (Read Along 10문장 수동 추출)

### 결과 — 모든 적격 단원에 sentence matching 게임 적재 (35개)

| 단원 | em-game | 비고 |
|------|:-----:|------|
| 1~10 형용사 | ✅ (수동) | + antonym 매칭(BONUS) 별도 |
| 11~20 동사 | ✅ (수동, §4-15) | |
| 21~30 ~ 314~315 (33개) | ✅ (자동, §4-16) | |
| 213~247 서술어 | ❌ | Read Along 부재 |
| 316~500 기타 | ❌ | Read Along 부재 |

### SKIP_MANUAL 갱신

`_apply_v9_bonus_pattern.py`에서 1~10 제외 → SKIP_MANUAL = `['11~20 문장구조 주어 동사']`만 유지(11~20도 이미 수동 적용됨, 재실행 방지). 1~10은 idempotent 가드(`em-game in content`)로 보호.

## 4-19. Read Along — 2열 그리드 + 셀 클릭 음성 재생

기존 Read Along은 1열 세로 스택(`flex-direction: column`)이라 화면 위아래로 길게 펼쳐졌음. Look & Speak가 2열 그리드인 점과 일관성을 맞추고 화면 효율을 높이기 위해 **2열 그리드로 변환** + 셀 전체를 클릭 가능하게.

### CSS 변경 (3가지)

```diff
-.sentence-list { display: flex; flex-direction: column; gap: 10px; margin: 0 0 24px; }
+.sentence-list { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; margin: 0 0 24px; }

-.sentence-item { ...; border-radius: 12px; transition: ... }
+.sentence-item { ...; border-radius: 12px; cursor: pointer; transition: ... }

+@media (max-width: 600px) { .sentence-list { grid-template-columns: 1fr; } }
```

- 데스크톱: 2열 그리드 (Look & Speak와 동일 패턴)
- 모바일 (≤600px): 1열로 자동 폴백
- `cursor: pointer` 추가 — 클릭 가능 시각 신호

### HTML 변경 (각 sentence-item)

```diff
-<div class="sentence-item">
+<div class="sentence-item" onclick="speakKorean('한국어 문장.')">
     <div class="sentence-num">01</div>
     <div class="sentence-text">
         <div class="sentence-en">English sentence.</div>
         <div class="sentence-kr">한국어 문장.</div>
     </div>
-    <button class="sentence-listen" onclick="speakKorean('한국어 문장.')" title="Listen">🔊</button>
+    <button class="sentence-listen" onclick="event.stopPropagation(); speakKorean('한국어 문장.')" title="Listen">🔊</button>
 </div>
```

**핵심:**
- `.sentence-item` 자체에 onclick 추가 — 셀 어디든 클릭 시 음성 재생
- 스피커 버튼은 그대로 유지 (시각 단서 + 명시적 클릭 옵션)
- 버튼 onclick에 `event.stopPropagation()` 추가 — 버튼 클릭이 부모로 버블링되어 음성이 두 번 재생되는 것을 차단

### 자동화 스크립트 — `_apply_read_along_grid_click.py`

- 모든 적격 파일 스캔 (Read Along 보유)
- CSS 3가지 변경 + 각 sentence-item HTML 변경
- 정규식으로 button onclick에서 한국어 문장 추출 → 부모 div의 onclick에 동일하게 주입
- Idempotent: `class="sentence-item" onclick="speakKorean` 검출 시 자동 건너뜀

### 결과 — 36개 파일 처리

| 시리즈 | 파일 수 |
|--------|--------:|
| 문장구조 (1~10 ~ 79~86) | 9 |
| 서술어 (87~96 ~ 209~212) | 15 |
| 부사어 (248~252 ~ 314~315) | 12 |
| **합계** | **36** |

## 4-20. 게임 완료 축하 애니메이션 + 컨페티 (36개 파일)

기존 게임 완료 시 메시지 박스 색만 초록으로 변하던 미세한 피드백을 **풀스크린 모달 + 컨페티 + 통계 카드**로 강화. 학습자가 "끝까지 다 맞춤"을 명확히 인지하고 성취감을 느낄 수 있도록.

### 구성 요소

**CSS (`.celebrate-*`):**
- `.celebrate-overlay` — fixed full-screen backdrop, 0.55 opacity 슬레이트 + 6px blur, z-index 1000
- `.celebrate-card` — `linear-gradient(135deg, #fef9c3 0%, #fed7aa 100%)` (yellow→peach), 24px radius, drop shadow
- `.celebrate-emoji` — 🎉 4.5rem, bounce + rotate 1s 무한 루프
- `.celebrate-title` — "완료!" 1.85rem 굵음 amber-900
- `.celebrate-stats` — 정답·오답 카운트 큰 숫자
- `.celebrate-actions` — 🔄 다시 시작 / 닫기 두 버튼 (primary amber, secondary white outline)
- `.confetti` — 90개 색종이 입자 (10색 무작위, 2.4~4.2s 낙하 + 720° 회전)

**키프레임 4종:**

| 이름 | 적용 | 사양 |
|------|------|------|
| `celebrateFadeIn` | 오버레이 등장 | 0.3s ease, opacity 0→1 |
| `celebratePopIn` | 카드 등장 | 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) — 살짝 오버슈트(105%→100%) |
| `celebrateBounce` | 🎉 이모지 무한 | 1s, translateY(-14px) + rotate(±3°) |
| `confettiFall` | 색종이 낙하 | 위→아래 110vh + rotate 720° |

**JS (전역 헬퍼 4개):**
```js
celebrateLaunchConfetti()  // 90개 색종이 동적 생성, 4.5s 후 자동 제거
celebrateShow(correct, wrong, restartFn, subtitleHTML)  // 표시 + 컨페티
celebrateClose()  // 오버레이 숨김
celebrateRestart()  // 닫고 재시작 함수 호출
```

### 게임별 훅

| 게임 | 위치 | 훅 |
|------|------|---|
| am-game (1~10 antonym 반대말 매칭) | `amCorrect === amPairs.length` | `celebrateShow(amCorrect, amWrong, amReset, '모든 반대말을 맞췄습니다!...')` |
| em-game (35 파일 sentence 매칭) | `emCorrect === emPairs.length` | `celebrateShow(emCorrect, emWrong, emReset, '모든 문장을 맞췄습니다!...')` |

기존 `amUpdate('🎉 완료!...', true)` 메시지 박스도 그대로 유지 → 모달을 닫아도 게임 영역에 완료 표시 잔존.

### 자동화 스크립트 — `_apply_celebration.py`

- 모든 `*.html` 파일 스캔
- Skip 조건: 로그/가이드/`em-game` 부재 + idempotency (`celebrate-overlay` 검출)
- 각 파일에 4가지 변경:
  1. CSS 블록 (`</style>` 직전 삽입)
  2. 오버레이 HTML (`</body>` 직전 삽입)
  3. JS 헬퍼 4개 (마지막 `</script>` 직전 삽입)
  4. am-game/em-game 완료 라인 후 `celebrateShow(...)` 호출 추가
- em-game 완료 라인의 두 가지 포맷(여러 줄 vs 한 줄) 모두 매칭

### 처리 결과 — 36개 파일

| 카테고리 | 파일 수 | 적용 훅 |
|---------|--------:|---------|
| 1~10 (am + em 두 게임) | 1 | am-game hook + em-game hook |
| 11~20 + 자동화 33개 = 35 | 35 | em-game hook |
| **합계** | **36** | |

### UX 흐름

1. 마지막 짝 클릭 → 두 셀 페이드아웃 + 정답 카운트 +1
2. `correct === pairs.length` 검출 → 메시지 박스 초록 강조
3. **즉시 풀스크린 오버레이 등장** (0.3s 페이드인)
4. **카드 탄성 등장** (0.5s 오버슈트)
5. **컨페티 90개 화면 위에서 낙하 시작** (각 입자 무작위 시작 시간/지속시간)
6. 🎉 이모지 무한 바운스
7. 닫기 → 게임 영역으로 복귀 (재시작 안 함)
8. 다시 시작 → 자동으로 reset 함수 호출 + 모달 닫힘 + 새 셔플

> ⚠️ **확장 시 주의:** 4-15 패턴(em-game) 신규 적용 단원에는 `_apply_celebration.py` 재실행 필요. Idempotent하므로 기존 36개는 자동 건너뜀.

## 4-21. Writing Test 100% 정답 축하 훅 (36개 파일)

§4-20 축하 시스템을 매칭 게임 외 **Writing Test "✅ 채점" 100% 정답 케이스**에도 적용. 학습자가 단순 매칭 게임뿐 아니라 직접 한국어 문장 작성에도 완벽 점수를 받았을 때 동일한 모달 + 컨페티 보상.

### 훅 삽입 — `wtGrade()` 함수 마지막 줄 직후

```diff
 function wtGrade(){
   const inputs = document.querySelectorAll('.writing-practice .writing-input');
   let c=0, total=inputs.length;
   inputs.forEach(function(inp){ ... if (val === ans && val.length>0){ ... c++; } else { ... } });
   document.getElementById('wtCorrect').textContent = c;
   const pct = total ? Math.round(c*100/total) : 0;
   document.getElementById('wtPct').textContent = '· ' + pct + '%';
   document.getElementById('wtScoreBox').classList.add('is-graded');
+  if(c === total && c > 0){
+    celebrateShow(c, 0, wtReset, '모든 문장을 정확히 작성했습니다!<br><span style="color:#94a3b8;font-size:0.9rem;">All sentences correct!</span>');
+  }
 }
```

### 발동 조건 — 두 조건 AND

| 조건 | 의도 |
|------|------|
| `c === total` | 모든 입력칸이 정답 |
| `c > 0` | 빈 입력칸으로 채점 시 false-positive 발동 방지 (10개 빈 칸 = 0/10 = 100%? 방지) |

### "다시 시작" 동작

축하 모달의 🔄 버튼 → `wtReset()` 호출 → 모든 입력칸 초기화. 사용자가 다시 처음부터 풀이 시도 가능.

### 자동화 스크립트 — `_apply_writing_celebration.py`

- Skip 조건: 축하 시스템(`celebrate-overlay`) 부재 → §4-20 미적용 파일 건너뜀
- Idempotent: `celebrateShow(c, 0, wtReset` 검출 시 자동 건너뜀
- 정확 패턴 매칭: `wtScoreBox`의 `add('is-graded');\n}` (wtGrade 함수 종결부) — `wtScore`의 `remove('is-graded')`와 구별

### 적용된 축하 시스템 — 3가지 트리거

| 게임 | 트리거 | 적용 파일 수 |
|------|------|--------------|
| am-game (1~10 antonym 반대말) | `amCorrect === amPairs.length` | 1 |
| em-game (35개 sentence 매칭) | `emCorrect === emPairs.length` | 35 |
| **wtGrade (Writing Test 100% 정답)** | **`c === total && c > 0`** | **36** |

→ 1~10은 3가지 모두, 11~20·21~30~314~315는 em-game + wtGrade 두 가지.

> ⚠️ **확장 시 주의:** 신규 단원에 §4-20 적용 후 `_apply_writing_celebration.py` 재실행 필요 (순서: celebration → writing celebration).

## 4-22. 토글 버튼 파스텔화 + "New Vocabulary" → "Vocabulary"

기존 3가지 토글 버튼(Vocabulary / Show Answer / Explanation)이 단원 테마의 진한 솔리드 컬러 + 흰 텍스트라 페이지의 50-level 파스텔 배경에서 시각적으로 도드라짐. 사용자 요청으로 **3가지 모두 파스텔화하되 서로 다른 색 패밀리로 구별**.

### 색상 매핑 — 의미적 구분

| 버튼 | 색 패밀리 | 의미 |
|------|---------|-----|
| **Vocabulary** (📚) | emerald (warm green) | 새 어휘 노출 = "성장" 느낌 |
| **Show Answer** | sky (cool blue) | 정답 확인 = "차분한 검토" |
| **Explanation** (📖) | violet (purple) | 해설 = 기존 .explain-content와 컬러 통일 |

### CSS 통일 패턴 (3종 동일 구조)

```css
.vocab-toggle   { background:#d1fae5; color:#065f46; border:1px solid #a7f3d0; ... transition:background 0.2s; }
.vocab-toggle:hover   { background:#a7f3d0; }

.answer-toggle  { background:#e0f2fe; color:#075985; border:1px solid #bae6fd; ... transition:background 0.2s; }
.answer-toggle:hover  { background:#bae6fd; }

.explain-toggle { background:#ede9fe; color:#5b21b6; border:1px solid #ddd6fe; ... transition:background 0.2s; }
.explain-toggle:hover { background:#ddd6fe; }
```

**공통 패턴:**
- bg = Tailwind 100-level pastel (살짝 도드라짐)
- text = 800-level dark family color (가독성 + 색 통일감)
- border = 1px solid 200-level (셀 경계 강조)
- hover = 200-level (한 단계 진하게)
- `transition: background 0.2s` 일관 적용 (이전엔 일부만 있었음)

페이지 일반 배경(50-level 파스텔)보다 한 단계 진해 명확히 클릭 가능 시각 신호.

### 단원 테마 색 → 파스텔 통일

기존 vocab-toggle은 단원별로 다른 색이었음 (예: 1~10 #3b82f6 blue, 21~30 #f59e0b orange, 71~78 #14b8a6 teal). 이제 **모든 단원에서 emerald 통일** — vocab 버튼만 보고 어느 단원인지 구별 X, 대신 **버튼 종류** 구별이 명확해짐 (3가지 색).

### 자동화 — 정규식 매칭

단원별 다른 hex 색을 처리하기 위해 **정규식 기반** `REGEX_REPLACEMENTS`:

```python
(re.compile(r'\.vocab-toggle \{ background:#[0-9a-fA-F]+; color:#fff; border:none;'),
 '.vocab-toggle { background:#d1fae5; color:#065f46; border:1px solid #a7f3d0;'),
(re.compile(r'\.vocab-toggle:hover \{ background:#[0-9a-fA-F]+; \}'),
 '.vocab-toggle:hover { background:#a7f3d0; }'),
# ... (총 7가지 정규식)
```

배경 hex 부분만 와일드카드(`[0-9a-fA-F]+`)로 매칭, 나머지 구조는 정확히 일치 → 다른 #000 같은 우연 매칭 차단.

### "New Vocabulary" → "Vocabulary" 일괄 치환

**사유:** 단어가 단원 내에서 반복될 수 있어 "New"가 부정확. 단순화.

**적용 범위:** 두 종류 위치 모두:
- 토글 버튼 텍스트: `📚 New Vocabulary` → `📚 Vocabulary`
- 박스 제목: `<div class="vocab-title">New Vocabulary</div>` → `<div class="vocab-title">Vocabulary</div>`

### 처리 결과 — 두 패스

| 패스 | 대상 | 처리 |
|------|------|------|
| 1차 — 토글 CSS + 텍스트 (`_apply_pastel_buttons.py`) | 토글 CSS 보유 36개 파일 | CSS + Vocabulary 통일 |
| 2차 — 텍스트만 | 토글 CSS 부재 31개 파일 (213~247, 316~500 BONUS-only) | Vocabulary 통일만 |
| **합계** | **67개 파일** | |

> ⚠️ **단원 테마 정체성:** vocab-toggle 단원별 색을 잃었지만, 다른 요소(.sentence-listen, h1 그라데이션, .pattern-block 보더 등)에서 단원 테마는 유지. 토글은 **기능 식별용**으로만 색 사용.

## 4-23. 21~30 §4-2/§4-3 동기화 — exp-table 너비 미세조정 + 가운데 정렬

§4-16(33개 자동화)에서 본문 3섹션 파스텔·매칭 게임·검은 텍스트 등은 21~30에 자동 적용됐지만, **§4-2(exp-table 너비 미세조정)과 §4-3(exp-table 가운데 정렬)은 1~10 시범 + 11~20만 적용된 상태**라 21~30는 미적용 상태였음. 사용자 요청으로 21~30에도 동기화.

### 적용된 변경

**CSS (5종):**

```diff
-.exp-table thead th { ... padding: 8px 10px; text-align: left; ... }
+.exp-table thead th { ... padding: 8px 10px; text-align: center; ... }

-.exp-table tbody td { padding: 12px 10px; ... overflow-wrap: anywhere; }
+.exp-table tbody td { padding: 12px 10px; ... overflow-wrap: anywhere; text-align: center; }

# .exp-en 신규 (누락 보강)
+.exp-table .exp-en { color: #000; font-weight: 800; width: 20%; word-break: keep-all; overflow-wrap: normal; hyphens: none; }

-.exp-table .exp-role { font-weight: 700; color: #6d28d9; width: 22%; word-break: keep-all; }
+.exp-table .exp-role { font-weight: 700; color: #6d28d9; width: 18%; padding-right: 18px; word-break: keep-all; overflow-wrap: normal; }

-.exp-table .exp-form { font-weight: 700; color: #1e293b; width: 26%; }
+.exp-table .exp-form { font-weight: 700; color: #1e293b; width: 20%; padding-left: 22px; }
```

**인라인 헤더 (10개 일괄, 11~20과 동일):**
```diff
-<th style="width:12%">Role<br>역할</th><th style="width:18%">Korean<br>한국어</th>
+<th style="width:18%">Role<br>역할</th><th style="width:20%; padding-left:22px;">Korean<br>한국어</th>
```

### 21~30 작업 로그 적용 매트릭스 (최종)

| § | 항목 | 21~30 |
|---|------|:-----:|
| 4-2 | exp-table 너비 미세조정 | ✅ (이번 §4-23) |
| 4-3 | exp-table 가운데 정렬 | ✅ (이번 §4-23) |
| 4-7 | 본문 3섹션 파스텔 | ✅ (§4-16 자동) |
| 4-14 | 검은 텍스트 → §4-17 톤 다운 | ✅ |
| 4-15 | em-game 매칭 게임 | ✅ (§4-16 자동) |
| 4-19 | Read Along 2열 그리드 + 셀 클릭 | ✅ |
| 4-20 | 게임 완료 축하 애니메이션 | ✅ |
| 4-21 | Writing Test 100% 정답 축하 | ✅ |
| 4-22 | 토글 버튼 파스텔 + Vocabulary | ✅ |

**해당 없는 항목 (21~30 구조에 부재):**
- §4-1·§4-10·§4-11·§4-12 패턴 블록 (`.pattern` 부재)
- §4-4 Q7 영어 어순 (Q7 specific)
- §4-5·§4-6·§4-8·§4-9·§4-18 BONUS antonym (1~10 형용사 전용)

> ⚠️ **확장 기회:** 같은 §4-2/§4-3 패턴을 31~86 (문장구조 5개), 87~209 (서술어 시리즈), 248~314 (부사어 시리즈)에도 적용 가능 — 21~30 적용 결과 확인 후 사용자 결정.

## 4-24. 명사+하다 동사 — Object+Verb 2행 → Verb 1행 통합 패턴 (21~30 Q22·Q27)

§4-4(Q7 영어 어순 분해)와 같은 종류의 **영어-한국어 비대칭 매핑** 처리. 영어 단일 동사("exercise", "clean")가 한국어에서 명사+하다 구조이고 격식체에서 명사+을/를+하다로 분해되는 경우, 기존 표는 다음과 같이 어색했음:

```
Row 1: I        | Subject | 저는    | ...
Row 2: exercise | Object  | 운동을  | "운동" + object marker
Row 3: —        | Verb    | 합니다  | 하다 → ...
```

문제점:
- "exercise"가 영어로 단일 동사인데 표에서 Object/Verb 두 행으로 쪼개져 학습자 혼란
- Verb 행 영어 셀이 "—" placeholder (영어 원문에 매핑 단어 없음)

### 새 패턴 — 2행 통합 (Subject + Verb)

```
Row 1: I        | Subject | 저는        | ...
Row 2: exercise | Verb    | 운동합니다  | 운동하다 = "noun + 하다" verb. 명사+을/를+하다로 분해 가능.
```

**구조 헤더:** "Subject + Object + Verb / 주어 + 목적어 + 동사" → **"Subject + Verb / 주어 + 동사"** (영어 의미 단위 기준)

**Korean 컬럼:** 격식체 결합형 `운동합니다` / `청소합니다` 표시 (분해형 `운동을 합니다`는 해설에서 언급).

**해설 (영어):**
```
운동하다 is a "noun + 하다" verb. "noun + 하다" verbs can be rewritten as 
"noun + 을/를 + 하다" → 운동하다 = 운동을 하다. Formal: 운동을 합니다.
```

학습 한국어 토큰(운동하다, 운동을 하다, 운동을 합니다, 을/를, 하다)은 그대로 유지 → 학습 대상 보존.

### 적용된 문항 (21~30, 2건)

| Q | 영어 | 분해형 답 | 통합형 표시 |
|---|------|-----------|-------------|
| Q22 | I exercise. | 저는 운동을 합니다. | 운동합니다 |
| Q27 | I clean. | 저는 청소를 합니다. | 청소합니다 |

### 수정 절차 변천 (사용자 반복 피드백)

1. 첫 시도: `운동하다 → 운동을 합니다` (화살표로 두 형태 모두 표시)
2. 사용자 피드백: "운동을 합니다" 삭제 → `운동하다`만
3. 사용자 피드백: `운동하다` → **`운동합니다`** (격식체 결합형으로 최종)
4. 해설 한국어 → **영어로 번역** (학습자 친화)
5. Q27에 동일 패턴 적용

### 패턴화 — 잠재 적용 대상

21~30 외에도 명사+하다 동사가 핵심인 문항이 다른 단원에 다수 존재:
- 공부하다 (study), 운전하다 (drive), 요리하다 (cook), 일하다 (work), 전화하다 (call), 노래하다 (sing) 등

§4-4(Q7) + §4-24(Q22·Q27)는 모두 **영어 단일 의미 ↔ 한국어 다중 토큰 매핑** 케이스를 통합 행으로 처리하는 같은 가족.

> ⚠️ **확장 시 주의:** Korean structure 헤더가 변경되므로(예: S+O+V → S+V) "한국어 어순"의 정확한 표기는 **영어 의미 기준**으로 변경됨. 만약 학습자가 분해형(운동을 합니다)을 보고 "왜 한국어 어순에 Object가 없지?"라고 의문을 가질 수 있으므로 해설에서 분해형 변환을 명시적으로 보여줘야 함.

## 4-25. 압축 CSS 형식 누락분 일괄 보강 (§4-22 follow-up, 71개 파일)

§4-22 토글 파스텔화 자동 적용 후 31~40 감사 중 **`.answer-toggle` CSS가 압축 형식**(공백 없음)이라 정규식 매칭 실패한 사실 발견. 같은 패턴이 다른 파일에도 있는지 일괄 스캔 → **39개 파일에서 동일 누락** 확인.

### 누락 원인 분석

§4-22의 `_apply_pastel_buttons.py` 정규식:
```python
re.compile(r'\.answer-toggle \{ background: #[0-9a-fA-F]+; color: white; border: none;')
```

**전제:** 표준 포맷 `.answer-toggle { background: ...; color: white; border: none;` (공백 포함)
**현실:** 일부 파일은 압축 포맷 `.answer-toggle{background:#XXX;color:white;border:none;` (공백 없음)

압축 포맷은 주로 **초기 자동 생성된 grammar 파일(시제/조사 문법설명, 213~247 서술어, 316~500 기타)**에서 사용. 손으로 다듬은 파일들과 자동 생성 파일들의 양식 차이 → 정규식이 한쪽만 매칭.

### 39개 영향 파일 분포

| 시리즈 | 파일 수 |
|--------|--------:|
| 문장구조 (31~40, 41~50, 51~60, 61~70, 71~78, 79~86) | 6 |
| 서술어 (213~217 ~ 237~247) | 5 |
| 부사어 (316~324 ~ 362~365) | 8 |
| 기타 (366~369 ~ 489~500) | 18 |
| 시제·조사 문법설명 | 2 |
| **합계** | **39** |

### 수정 — 두 번 패스

**1차 패스 (`_fix_compact_answer_toggle.py`):** 메인 색상 + transition 추가
- 압축 포맷 매칭: `\.answer-toggle\{background:#[0-9a-fA-F]+;color:(?:white|#fff);border:none;`
- 치환: `.answer-toggle{background:#e0f2fe;color:#075985;border:1px solid #bae6fd;`
- transition 추가 (margin-top:8px 뒤에 `transition:background 0.2s;` 삽입)
- 38개 파일 처리

**2차 패스 (인라인 Python):** opacity 기반 hover → background hover
- 일부 파일은 hover가 `background` 변경이 아닌 `opacity:0.9` 형식
- 단순 치환: `.answer-toggle:hover{opacity:0.9;}` → `.answer-toggle:hover{background:#bae6fd;}`
- 33개 파일 추가 정리

### 결과 — 모든 파일 답변 토글 통일

`.answer-toggle` 색상 시스템 (모든 파일):
- bg = `#e0f2fe` (sky-100)
- text = `#075985` (sky-800)
- border = `1px solid #bae6fd` (sky-200)
- hover bg = `#bae6fd` (sky-200)
- transition: `background 0.2s`

vocab-toggle (emerald), answer-toggle (sky), explain-toggle (violet) **3색 체계 모든 파일에 일관 적용** — §4-22 본래 의도가 비로소 완전 달성.

### vocab/explain 토글 검증

`grep` 으로 `.vocab-toggle{` 및 `.explain-toggle{` 압축 형식 검색 → **모두 부재 확인**. answer-toggle만 압축 포맷 사용 → 다른 토글은 §4-22 자동화로 정상 처리됨.

### 자동화 회고

| 교훈 | 적용 |
|------|------|
| 정규식은 공백 변형 허용 (`\s*`, `\s+`) | 향후 토글·CSS 자동화에 반영 |
| 적용 후 감사로 누락 검증 필요 | 신규 자동화 시 idempotent + audit 페어 |
| 압축/비압축 양식 혼재 인식 | 파일 양식 다양성 가정해서 패턴 작성 |

> ⚠️ **확장 시 주의:** 새 토글 추가 시 압축/비압축 두 양식 모두 매칭하는 정규식 필요. `_fix_compact_answer_toggle.py` 패턴 참고 가능.

---

# Part 5 — 미처리 / 다음 작업

| 항목 | 상태 | 비고 |
|------|------|------|
| 213~247 서술어 시리즈 (추측·공감·감탄·확인·정중표현) | 미적용 | 양국어 표준 적용 필요 |
| 316~365 부사어 시리즈 (원인·목적·조건·예상밖) | 미적용 | 양국어 표준 적용 필요 |
| 366~500 기타 시리즈 (배경상황·비교급·명사형·부사형·동사화·한정·로/으로·빈도부사·종별사·불규칙·간접화법·관형어·존댓말) | 미적용 | 양국어 표준 적용 필요 |
| 시제·조사 퀴즈 앱 | 미적용 | 별도 앱이라 표준 외 |
| 원본 폴더 (`놀라운 한국어 500 원본/`) | 미변경 | 백업 보존 |
| Part 4-1 패턴 블록 변경 확산 | 1~10·11~20 동기화 완료 | 21~86은 패턴 블록 자체 부재로 해당 없음 |
| Part 4-2 너비 미세조정 확산 | 1~10·11~20 동기화 완료 | 21~86, 87~500 확산 여부 추후 결정 |
| Part 4-3 가운데 정렬 확산 | 1~10·11~20 동기화 완료 | 21~86, 87~500 확산 여부 추후 결정 |
| 4-2 비대칭 padding × 4-3 가운데 정렬 충돌 | 미해결 | padding 제거 또는 좌우 균등으로 통일 검토 |
| Part 4-4 영어 어순 분해 패턴 확산 | 1~10 Q7 단발 | 다른 비대칭 문장(이다 의문문, did/will/have 보조동사 등)에 시범 적용 후 표준화 판단 |
| Part 4-5 Antonym Matching Game | 1~10 단발 | 형용사 반대말 고유 콘텐츠 — 직접 확산 부적합. 동일 매칭 패턴을 다른 어휘에 응용은 가능 |
| Part 4-6 파스텔 팔레트 (10색) | 1~10 BONUS (플래시카드 페어별 고정 + 매칭 셀 랜덤) | 다른 시리즈 BONUS 콘텐츠에 재활용 권장 (CSS 변수 + JS 런타임 색상 주입 패턴) |
| Part 4-7 본문 3섹션 파스텔 확장 | 1~10·11~20 동기화 완료 | 21~500 시리즈 확산 시 항목 수에 맞춘 nth-child 범위 조정 필요 |
| Part 4-8 Antonym Table 파스텔 확장 | 1~10 BONUS 적용 (페어 매핑 = 플래시카드와 동일) | 1~10 고유 콘텐츠라 직접 확산 부적합 |
| Part 4-9 BONUS 파스텔 톤 다운 | 매칭 셀 100→50, antonym 플래시카드 100/200→50/100→200 | BONUS·본문 모두 50-level 기준선으로 통일 |
| Part 4-10 The Basic Pattern 블록 파스텔 | 1~10·11~20 동기화 완료 | 21~86은 패턴 블록 부재로 해당 없음 |
| Part 4-11 패턴 블록 eyebrow 간소화 | 1~10·11~20 동기화 완료 | 21~86은 패턴 블록 부재로 해당 없음 |
| Part 4-12 패턴 블록 가운데 정렬 | 1~10·11~20 동기화 완료 | 21~86은 패턴 블록 부재로 해당 없음 |
| Part 4-13 11~20 일괄 동기화 | 4-7/4-10/4-12를 11~20에 적용 완료 (4-13에서 정리) | — |
| Part 4-14 인터랙티브 카드 텍스트 검은색 통일 | 1~10 antonym 플래시카드·매칭 셀·Look & Speak + 11~20 Look & Speak 적용 완료 | 21~86 등 추후 시리즈 확산 시 동일 override 패턴 적용 |
| Part 4-15 영어-한국어 문장 매칭 게임 (em-) | 11~20에 신규 임베드, Writing Test가 4번으로 번호 이동 | 21~86 등에 응용하려면 단원별 핵심 문장 10개 데이터 셋 작성 필요 |
| Part 4-16 33개 파일 자동화 일괄 적용 | 4-7 + 4-14 + 4-15(매칭 게임) 모두 33개 파일에 자동 적용 (`_apply_v9_bonus_pattern.py`) | Read Along 부재 단원(213~247, 316~500)은 별도 데이터 작성 필요 |
| Part 4-17 인터랙티브 카드 텍스트 톤 다운 | 36개 파일 #000 → #334155 (slate-700) 일괄 갱신 (`_soften_text_color.py`) + 자동화 스크립트 동기화 | — |
| Part 4-18 1~10 매칭 게임 수동 임베드 | 1~10에 sentence matching 게임 수동 추가 (antonym과 공존) + SKIP_MANUAL 정책 갱신 | 모든 적격 단원 35개에 sentence matching 적재 완료 |
| Part 4-19 Read Along 2열 그리드 + 셀 클릭 음성 | 36개 파일 일괄 변환 (`_apply_read_along_grid_click.py`) | event.stopPropagation으로 버튼·셀 중복 발생 차단 |
| Part 4-20 게임 완료 축하 애니메이션 | 36개 파일 (`_apply_celebration.py`) — 풀스크린 모달 + 컨페티 + 🎉 바운스 | 신규 em-game 단원에는 재실행 필요 (idempotent) |
| Part 4-21 Writing Test 100% 정답 축하 훅 | 36개 파일 (`_apply_writing_celebration.py`) — `c===total && c>0` 조건 | 4-20 적용 후 실행 필요 (의존 관계) |
| Part 4-22 토글 버튼 파스텔화 + Vocabulary 통일 | 67개 파일 (`_apply_pastel_buttons.py` + 텍스트 패스) — emerald/sky/violet 3색, "New" 제거 | 단원 테마는 다른 요소에서 유지 |
| Part 4-23 21~60·71~78 §4-2/§4-3 동기화 | exp-table 너비 미세조정 + 가운데 정렬 | 1~10·11~20·21~30·31~40·41~50·51~60·71~78 적용 완료, 61~70·79~86 + 87~314 확산 여부 추후 결정 |
| Part 4-24 명사+하다 동사 통합 행 패턴 | 21~30 Q22(exercise)·Q27(clean) + 41~50 Q46~Q50(전화하다 5문항) Object+Verb 2행 → Verb 1행 | 다른 단원의 공부하다·운전하다·요리하다 등에 동일 패턴 적용 가능 |
| Part 4-25 압축 CSS 형식 누락분 일괄 보강 | §4-22 자동화 누락된 `.answer-toggle` 압축 포맷 39개 파일 일괄 수정 | 모든 파일 vocab/answer/explain 3색 토글 통일 완료 |

---

# 누적 변환 완료 파일 (총 50+개, 2026-05-04 기준)

## 양국어 표준 v9 적용 (35개)
- 문장구조: 9개 (1~10 ~ 79~86)
- 서술어: 13개 (87~96, 97~104, 105~114, 115~122, 123~141, 142~149, 150~156, 157~163, 164~168, 169~180, 181~189, 190~198, 199~203)
- 부사어: 11개 (248~252 ~ 314~315)
- 기타: 3개 (441~444, 시제·조사 문법설명)

## 신규 작성 (2개)
- 204~208 서술어 변화1
- 209~212 서술어 변화2

---

## 부록: 표준 진화 요약 (v5 → v9)

| 버전 | 핵심 변경 | 컬럼 구조 |
|------|----------|----------|
| v5 (2026-05-02) | 3-컬럼 exp-table 도입 | English / 한국어 / 해설 (16/28/56) |
| v6~v7 | 발음 버튼·플래시카드 강화 | (변경 없음) |
| v8 (2026-05-02 후반) | English 열 추가 (4-컬럼) | English / Role / Korean / Explanation (20/20/20/40) |
| **v9 (2026-05-03~04)** | **양국어 stack + colgroup 강제 + 너비 재배분** | **English / Role / Korean / Explanation (20/12/18/50)** |
| v9.1 (2026-05-04, uncommitted) | 패턴 블록 "Subject + Particle" → "Noun + Particle" | 1~10·11~20 한정 |
| v9.2 (2026-05-04, uncommitted) | exp-table 컬럼 너비 미세조정 (Role 18% + padding-right 18px / Korean 20% + padding-left 22px) | 1~10·11~20 |
| v9.3 (2026-05-04, uncommitted) | exp-table 4컬럼 일괄 가운데 정렬 (thead+tbody) | 1~10·11~20 |
| v9.4 (2026-05-04, uncommitted) | Q7 explain 표 — 영어 어순 분해 + 결합 행 패턴 | 1~10 Q7 단발 |
| v9.5 (2026-05-04, uncommitted) | Antonym Matching Game (인터랙티브 매칭 게임) BONUS 섹션 추가 | 1~10 |
| v9.6 (2026-05-04, uncommitted) | 매칭 셀 영어 라벨 제거 — 한국어만 표시 | 1~10 |
| v9.7 (2026-05-04, uncommitted) | 파스텔 팔레트 10색 (플래시카드 페어별 고정 + 매칭 셀 랜덤, CSS 변수 + JS 주입) | 1~10 BONUS |
| v9.8 (2026-05-04, uncommitted) | 매칭 셀 색 중복 방지 — 팔레트 20색 확장 + 셔플 후 1:1 배정 (replacement 제거) | 1~10 매칭 게임 |
| v9.9 (2026-05-04, uncommitted) | Read Along에 50-level 은은한 파스텔 (10색 시퀀스) | 1~10 Read Along |
| v9.10 (2026-05-04, uncommitted) | Writing Test + Look & Speak 파스텔 확장 — 동일 10색 시퀀스로 본문 3섹션 통일 | 1~10 Writing Test · Look & Speak |
| v9.11 (2026-05-04, uncommitted) | Antonym Table 50-level 파스텔 — 페어 매핑(= 플래시카드와 동일)으로 BONUS 내부 시각 일관성 | 1~10 BONUS antonym 표 |
| v9.12 (2026-05-04, uncommitted) | BONUS 파스텔 톤 다운 — 매칭 셀·플래시카드를 50/100→200/text 800 기준으로 통일 (본문과 같은 은은함) | 1~10 BONUS (매칭 게임 + antonym 플래시카드) |
| v9.13 (2026-05-04, uncommitted) | The Basic Pattern 블록 파스텔 — PART 1 blue-50 / PART 2 violet-50, eyebrow·`+` 색상 동기화 | 1~10 .pattern 블록 |
| v9.14 (2026-05-04, uncommitted) | 패턴 블록 eyebrow 간소화 — "Part 1 · 주어부" → "주어", "Part 2 · 서술부" → "서술어" | 1~10·11~20 |
| v9.15 (2026-05-04, uncommitted) | 패턴 블록 가운데 정렬 (text-align: center) | 1~10·11~20 |
| v9.16 (2026-05-04, uncommitted) | 11~20 일괄 동기화 — §4-7 본문 3섹션 파스텔 + §4-10 패턴 블록 PART 1/2 파스텔 + §4-12 가운데 정렬 (look-speak 클래스 추가 포함) | 11~20 |
| v9.17 (2026-05-04, uncommitted) | 인터랙티브 카드 텍스트 #000 통일 — antonym 플래시카드·매칭 셀·Look & Speak 양 파일 | 1~10·11~20 |
| v9.18 (2026-05-04, uncommitted) | 영어-한국어 문장 매칭 게임 (em-) 신규 임베드, Writing Test 번호 3→4 | 11~20 |
| v9.19 (2026-05-04, uncommitted) | **33개 파일 자동화 일괄 적용** — 본문 3섹션 파스텔 + 검은 텍스트 + 매칭 게임 + Writing Test 번호 이동 | 21~30 ~ 314~315 (33개) |
| v9.20 (2026-05-04, uncommitted) | 인터랙티브 카드 텍스트 톤 다운 — #000 → #334155 (slate-700, 36개 파일) | 1~10·11~20·21~30~314~315 |
| v9.21 (2026-05-04, uncommitted) | 1~10에 sentence matching 게임 수동 임베드 — antonym과 공존, SKIP_MANUAL 정책 갱신 | 1~10 |
| v9.22 (2026-05-04, uncommitted) | Read Along 2열 그리드 + 셀 클릭 음성 재생 (36개 파일, event.stopPropagation으로 중복 차단) | 1~10 ~ 314~315 (36개) |
| v9.23 (2026-05-04, uncommitted) | 게임 완료 축하 애니메이션 — 풀스크린 모달 + 90개 컨페티 + 🎉 바운스 + 정답/오답 통계 (36개 파일) | 1~10 (am+em 둘 다) + 35 em-game 파일 |
| v9.24 (2026-05-04, uncommitted) | Writing Test 100% 정답 축하 훅 — wtGrade에 c===total && c>0 조건 (36개 파일) | 1~10·11~20·21~30~314~315 |
| v9.25 (2026-05-04, uncommitted) | 토글 버튼 파스텔화 (emerald/sky/violet) + "New Vocabulary" → "Vocabulary" 일괄 치환 (67개 파일) | 모든 토글·vocab 보유 파일 |
| v9.26 (2026-05-04, uncommitted) | 21~30에 §4-2(exp-table 너비 미세조정) + §4-3(가운데 정렬) 동기화 | 21~30 |
| v9.27 (2026-05-04, uncommitted) | 명사+하다 동사 — Object+Verb 2행을 Verb 1행으로 통합 (Q22 exercise · Q27 clean), 영어 해설 | 21~30 |
| v9.28 (2026-05-04, uncommitted) | 31~40에 §4-2(exp-table 너비 미세조정) + §4-3(가운데 정렬) 동기화 (§4-24는 명사+하다 문장 부재로 N/A) | 31~40 |
| v9.29 (2026-05-04, uncommitted) | 31~40 answer-toggle 파스텔 수동 보강 — 압축 CSS 형식(공백 없음)이라 §4-22 정규식 매칭 실패한 부분 수정 | 31~40 |
| v9.30 (2026-05-04, uncommitted) | 압축 CSS `.answer-toggle` 39개 파일 일괄 보강 (§4-22 follow-up) — 메인 색상 38개 + opacity hover→bg hover 33개 (`_fix_compact_answer_toggle.py` + 인라인 Python) | 39개 (213~247, 316~500, 시제·조사 문법설명, 31~40 외 6개 문장구조) |
| v9.31 (2026-05-04, uncommitted) | 41~50에 §4-2/§4-3 + §4-24 (전화하다 Q46~Q50, 5문항) 동기화 — Object+Verb 2행을 Verb 1행으로 통합, 영어 어순 (S→V→IO→From) 유지 | 41~50 |
| v9.32 (2026-05-04, uncommitted) | 51~60에 §4-2/§4-3 동기화 (§4-24는 의문사 단원 — noun+하다 부재로 N/A) | 51~60 |
| v9.33 (2026-05-04, uncommitted) | 71~78에 §4-2/§4-3 동기화 (§4-24는 좋아하다 단일 동사로 noun+하다 부재 N/A) | 71~78 |

---

> ✍️ 다음 세션 진입 시 우선순위 권장 순서:
> 1. **Part 4-2 너비 미세조정 + 4-3 가운데 정렬** 결과 검토 → 두 변경의 상호작용(비대칭 padding 충돌) 해결 후 확산 여부 판단
> 2. 11~86 문장구조 시리즈에 4-2/4-3 동기화 (확산 결정 시)
> 3. 213~247 서술어 시리즈에 v9 표준 일괄 적용 (`_bulk_update_documentation.py` 재사용)
> 4. 316~365 부사어, 366~500 기타 시리즈에도 동일하게 확산

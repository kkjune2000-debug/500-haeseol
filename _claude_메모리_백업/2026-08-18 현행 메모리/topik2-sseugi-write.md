---
name: topik2-sseugi-write
description: "토픽2 쓰기(51~54) 해설집 완성 — 공식답안 37·41·52만·나머지 생성, 파이프라인·함정"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6ef7c53f-3fb0-4912-a320-2be45f2ec0ff
  modified: 2026-07-27T15:18:04.615Z
---

★토픽2 쓰기 51~54 해설집 완성(2026-07-26, 64 복구 2026-07-28) — **48문항(12회차×4)**★. 결과물 `토픽2 쓰기 해설/토픽2 쓰기 51~54 _ 클로드.html` + `유형53 그림/`(그래프 크롭 12개).

**★64회 함정 — 파일 두 개(반드시 기억)★**: 64 폴더엔 **「토픽2 64회 듣기 문제.pdf」(26쪽·듣기 1~50만)** 와 **「토픽2 64회 듣기 쓰기.pdf」(19쪽·듣기+쓰기, p18=51·52, p19=53·54)** 가 **둘 다** 있다. `LSRC[64]['munje']` 가 **듣기-only 쪽(「듣기 문제」)** 을 가리킨다 → 그걸 열고 "64엔 쓰기 없다·판독이 지어냈다"고 **잘못 판단해 64를 지웠다가**, 사용자가 실제 PDF(「듣기 쓰기」)를 줘서 **판독이 처음부터 맞았음**을 확인하고 되살렸다. 복구는 워크플로 task 출력에서: raw=`wmpwknu8x`, content q51·52·53=`w4agi6e3u`, content q54=`w5gx2jivv` (경로 `<세션>/tasks/*.output` → `result.rounds['64']`). **교훈: 회차 폴더에 "듣기 문제"와 "듣기 쓰기"가 따로 있으면 쓰기는 반드시 「듣기 쓰기」 끝쪽에. LSRC munje가 어느 파일인지 먼저 확인. 판독이 낸 특정 수치(온라인쇼핑 46→92조 등)가 맞으면 "지어냄"으로 속단 말 것.** 64 q54 raw prompt엔 「다음을 주제로…(50점)」 지시문이 앞에 붙어 있어 지문만 남기게 정규화함(다른 회차와 통일). t2w_raw.json/t2w_content.json 은 git 미추적 — 지우기 전 백업.

**★53 그래프 이미지 크롭★**: 그래프는 조각 이미지+그리기라 xref 단일추출 불가. **텍스트 회차(35·36·37·41·47·52·60)는 문제쪽 삽입 이미지 rect 합집합으로 박스**, **이미지 회차(83·91·96·102, 모두 p19)는 렌더 보고 고정 박스(대략 100,136,500,~380 pt)**. **64는 「듣기 쓰기.pdf」 p19에서 박스(116,150,483,382 pt)** — LSRC munje(듣기 문제)가 아니라 듣기쓰기 파일서 크롭. 크롭해 `유형53 그림/H회.png` 저장, 빌드가 `<img>`로 인라인 표시(+「자료를 글로 읽기」 details 로 graphData 텍스트도). 쓰기쪽=텍스트회차 START+1, 이미지회차 p18(51·52)+p19(53·54).

**구조**: 51 실용문 빈칸(㉠㉡, 각10점) · 52 설명문 빈칸(각10점) · 53 그래프 200~300자(30점) · 54 논술 600~700자(50점). 자기 확인식(문제 → 직접 써 보는 칸 → 모범 답안·해설 펼치기, 자동 채점 없음). 각 문제에 모범답안+영어역, 51·52는 ㉠㉡ 답+이유, 53은 구성·표현·변형, 54는 서론본론결론 개요·세부질문 길잡이·표현·변형.

**★공식 모범답안은 37·41·52회에만 있다★**(정답 PDF 텍스트). 나머지 9회차(35·36·47·60·64·83·91·96·102)는 정답 PDF에 객관식 표만 있어 **모범답안을 직접 작성**(채점 기준 맞춤·적대 검토). 사용자 결정: 12회차 전부 + 「이렇게도 쓸 수 있다」 변형.

**파이프라인**(`t2w_*`): `t2w_raw_prep`(쓰기쪽 렌더+공식답안 텍스트 추출) → raw 워크플로(렌더 판독, 실용문·설명문·그래프 자료·논술 주제+공식답안 구조화) → `t2w_raw_assemble` → `t2w_content_prep` → content 워크플로 **둘로 갈라 돌림**(A=51·52·53, B=54 — 논술이 길어 부담 분산) → `t2w_content_assemble`(A+B 병합, 껍데기·길이 검사) → `t2w_build`(문제→써보기칸→모범답안 게이팅) → review 워크플로.

**함정**:
- ★51 실용문은 이미지(문자메시지·이메일·안내문 캡처)라 텍스트층에 없다 — 렌더 판독★. 53 그래프도 이미지 — graphData 로 수치를 말로 옮겨 판독(그래프 이미지 크롭은 미룸, 자료 상자로 대체).
- ★길이 감사 필수: 53은 200~300자, 54는 600~700자. 생성이 초과하기 쉽다(35-53=353·91-53=305·83/96/102-54가 700 초과). 화면 자수 배지 옆에서 눈으로도 확인.★
- ★빈칸 답이 문제에 이미 인쇄된 꼬리말과 겹치지 않게★ — 96-51 '어느 (㉡) 잘 모르겠습니다'에서 답에 '잘 모르겠습니다'를 또 넣어 중복. 91-51 '10시로 (㉠)'에서 답에 '10시로' 또 넣어 중복. **빈칸에 들어갈 부분만** 채운다.
- 공식 회차 모범답안은 「(NNN자)」 자수 표기를 떼되 본문은 글자까지 보존(37·41은 자동 보존, 52는 조립 때 재확인).
- 문체: 해설·이유·요령은 격식체. ★문법 용어 금지 — 사동·피동·부분 부정·시제 등 풀어 쓰기★. 「까닭」 대신 「이유」. 모범답안은 문어체. 영어에 키릴 등 섞지 말 것.

★2026-07-26 세션에 **주간 사용 한도**에 걸림(7/27 오후 6시 리셋) — 검토 verify 4개 미완. 남은 정정(길이·문법용어)은 사용자 없이 내가 판독본 대조로 직접 적용. 워크플로 재실행은 리셋 후.★ [[topik2-deutgi-source]] [[haeseol-munche]] [[haeseol-silent-failure]]

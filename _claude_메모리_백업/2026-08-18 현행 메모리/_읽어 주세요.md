# 이 폴더가 무엇인가 (2026-08-18 갱신)

**이 폴더 하나가 지금 쓰이는 규칙 전부입니다.** 52개 + `MEMORY.md`.

처음에는 「작업 컴퓨터가 바뀌어 새로 시작한 메모리 아홉 개」였는데, 2026-08-18에
**본래 작업 컴퓨터의 메모리 42개를 이 폴더로 합쳤습니다.** 그래서 이제
`2026-08-10 현행 메모리`(38개)는 이 폴더의 **부분집합**이라 따로 읽을 것이 없습니다.

| 폴더 | 개수 | 읽을 값어치 |
|---|---|---|
| `2026-08-18 현행 메모리` (이 폴더) | 52 + 색인 | **여기만 읽으면 됩니다** |
| `2026-08-10 현행 메모리` | 38 | 이 폴더에 다 들어 있습니다 (남겨 둔 사본) |
| (6월판, 백업 폴더 바로 아래) | 55 | ★**부록·숫자표·파일명 규칙은 여기에만 있습니다** |

`MEMORY.md` 뒤쪽의 「다른 컴퓨터에서 온 규칙」 열 줄은 **이 폴더에만** 있는 것입니다
(본래 컴퓨터의 메모리에는 없습니다). 합칠 때 색인에서 빠지지 않게 따로 적어 두었습니다.

## 새 컴퓨터에서

메모리는 그 컴퓨터의 `~/.claude/projects/<프로젝트>/memory/` 에만 있어 **새 컴엔 없습니다.**
그래서 이 사본을 둡니다. 이 폴더의 파일들을 직접 읽히거나, 같은 이름으로 메모리 폴더에
옮겨 놓으십시오.

무엇보다 먼저 **`_작업_인수인계.md`** 를 읽으십시오. 규칙의 전체 그림은 그쪽에 있습니다.

## ★ 이 사본을 갱신할 때

메모리 폴더에서 이 폴더로 덮어쓰면 **`MEMORY.md` 가 갈아 끼워져 위 열 줄이 사라집니다.**
2026-08-18에 실제로 그랬고 git 에서 되찾아 합쳤습니다. 덮어쓴 뒤에는 색인이 폴더 안의
모든 파일을 가리키는지 세어 보십시오.

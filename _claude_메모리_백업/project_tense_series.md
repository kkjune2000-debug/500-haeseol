---
name: project_tense_series
description: 시제(현재·과거·미래) 시리즈 — 67레슨과 별개 템플릿. 원본 3파일 삭제됨(2026-06-12), 기준 파일은 현재형 A. 파생 파일 CSS 누락 버그 주의.
metadata: 
  node_type: memory
  type: project
  originSessionId: 243aa644-b2dd-4ec3-97c9-53b3042d0fe5
---

**시제 시리즈** = 67개 레슨([[project_canonical_folder]]의 `마스터-템플릿-적용본\`)과 **별개의 독립 문서 템플릿**. ⚠️ **원본 단일 파일 3개(시제_1_현재형·시제_2_과거형·시제_3_미래형.html)는 2026-06-12 사용자가 삭제** — 분할 파일(A/B/C/D·불규칙 등)이 완전 대체. 과거 사본만 `정리\` 하위에 남아 있음(참고용, 편집 X). 컴포넌트·CSS 이식이 필요하면 원본 대신 **현재형 A(시제_1_현재형_A_받침없음.html)를 기준 파일**로 쓸 것.

**템플릿 특징**: 파란 테마(`--theme-primary:#3b82f6`), gstep/exp-table 아님. h1/h2/h3 + **Step 1·2·3** 구조. 주요 컴포넌트(클래스): `.stem-extract`(어간 추출 표), `.fan-forms`/`.fan-form`/`.ff-tense`(활용 부채꼴 칩), `.fc-card` 외 `.fc-*`(뒤집기 플래시카드), `.pt-table`/`.pt-*`(타이핑 퀴즈), `.mc-q`(객관식), `.vowel-groups`(밝은/어두운 모음).

**기준 파일 = 시제1 현재형** ("첫 번째 파일과 같게" 지시의 기준). 컨벤션: 영어 비교 금지([[feedback_no_english_comparison]]), 한국어→영어 줄 병기(.ko/.en), 쉬운 용어(양성/음성 대신 **밝은 모음 ☀️/어두운 모음 🌙**).

**⚠️ 알려진 버그**: 파생 파일이 컴포넌트 `<style>` 블록(`.stem-extract`·`.fc-*`·**`.pt-*` 타이핑 퀴즈**)이나 표 `<colgroup>`을 누락 → 한/영이 붙어 보이고("기본형Base Form"), 입력칸·폰트가 작아지고, 레이아웃 깨짐. **시제1을 기준으로 CSS 블록을 비교·이식**해 고침(전역이라 한 번 추가하면 모든 반복 퀴즈에 적용).

**2026-06-06 작업**:
- **시제1**: 영어 비교 제거 — "기본형 = 영어 dictionary form", "영어와 비교해 봅시다" 섹션(to+eat=to eat) → 한국어 자체완결("'-다'는 뜻 없이 기본형 표시", 가다·먹다·공부하다)으로 재구성. 활용 칩에 영어 뜻 추가(큽니다 *is big*·컸습니다 *was big*…). 받침 카드 "자음+모음→끝"에서 "→ 끝" 삭제(받침 있음 카드와 대칭).
- **시제2**: 누락 CSS 복원 — `.stem-extract`·플래시카드 `.fc-*`·**타이핑 `.pt-*`** 블록을 시제1에서 이식(구조 동일 확인; .pt-가 통째로 없어 입력칸·기본형폰트·버튼이 기본스타일로 작게 렌더되던 것). 쓰기 표 8곳에 `<colgroup>`(# 4%·기본형 22%·평서형 37%·의문형 37%) 추가해 현재형과 열 너비 통일. (퀴즈 세트 9개 = 동사 그룹별 다른 내용; 9번째는 6열 종합 퀴즈라 colgroup 이미 있음.)
- **시제3 미래형**: 점검 완료. `.fc-*`(플래시카드)·`.pt-*`(타이핑) CSS가 통째로 누락이라 시제1에서 이식. stem-extract는 미사용. 타이핑은 종합 1개(5열: 기본형/현재/과거/미래, colgroup 이미 있음)+#alltTable 미세조정 style 유지. ⇒ 시제 3파일 모두 누락 CSS 복원 완료.

**2026-06-07 작업**:
- **시제1 현재형 "만들기" 3분할**: 스텝3의 80개 직접연습(`.pr80`)을 규칙별 3개로 분리해 rule A/B/C 카드 바로 아래 각각 배치(A=받침없음+하다 43개 /86, B=받침있음 25개 /50, C=ㄹ받침 12개 /24). 요약 뒤 통합본 삭제. 렌더를 `pr80Build(items,bodyId)`+`data.filter(w[3]==='V'||'H'/'C'/'L')`로, 함수 전부 **DOM-scoped**(`btn.closest('.pr80')`·`nextElementSibling`, id 기반 제거)로 재작성 → 3표 독립 동작. 각 표 1행=예문(prex readonly), `pr80Reset`에 예문 보존 가드.
- **플로우차트 가로 정책 통일**: 시제 템플릿의 모든 조건 플로우 컴포넌트가 `@media (max-width:720px)`에서 1열 세로+화살표 90°회전으로 접히던 것을, **720px 이하=가로 유지(컴팩트: 칩/결과pill/여백 축소)**, **460px 이하=세로 폴백**으로 변경. 적용: 과거형 `.conj-past .cp-row`(🅐·🅑)·`.conj-contract .cc-row`(🅒 줄임 4그룹), 미래형 `.conj-ir .ci-row`(불규칙 ㄷ·ㅂ·ㅅ). 현재형은 `.conj-rule .cr-row`(받침없음/있음/ㄹ받침)도 동일 정책으로 변경. 미래형 메인규칙 🅰🅱🅲는 일반 표라 원래 가로.
- **conj-ir 스타일로 플로우차트 비주얼 통일**(미래형 `.conj-ir`을 기준으로 사용자가 지정): 현재형 `.conj-rule`·과거형 `.conj-past`를 conj-ir 룩으로 재스타일 = ① 헤더를 **공식 필**(둥근 20px·색 채움·흰 글씨, 예 "🅐 받침 없음 → ㅂ니다")+흰 카드+설명(ko 작은 회색/en 이탤릭), ② 기본형을 **단일 흰 칩**(CSS만으로: `cb-word`=흰칩 테두리, `cb-stem`·`cb-da` 배경/취소선 제거→inherit), ③ 결과 `res`를 세로 스택 필(statement 채움/question 외곽선), ④ 칩 radius 7px·행 흰 카드. **셀 규격은 conj-ir과 정확히 일치**(사용자 "폰트 크기·셀 높이 동일" 지시): 기본형 칩 0.96rem·padding 5px 10px·min-width 88px(균일 넓이), 결과 칩 0.96rem·padding 5px 10px·min-width 148px, 더하기 칩 0.92rem, 행 padding 10px 12px. (과거형 8열이지만 넓은 컨테이너에서 fit; 좁으면 720px 컴팩트 쿼리가 처리.) **컴팩트(≤720px) 규격도 3파일 통일**(과거형만 8열이라 0.8/86으로 가장 작게 줄던 문제 수정): 결과 칩 0.92rem/min-width 124px, 기본형 0.9rem/80px 공통. 과거형은 124px 확보 위해 중간 칸(어간·모음·더하기) 더 축소 + **세로 전환 브레이크포인트를 460→560px로 올림**(좁으면 세로 스택+풀사이즈 0.96/148). 현재형·미래형은 5열이라 460px 유지. **색은 규칙별 유지**(현재형 r-a 파랑#3d5a85·r-b 초록#3f6149·r-c 로즈#874949 / 과거형 밝은#8a6a26·어두운#3d5a85). 과거형 `.conj-contract`(cc-)는 이미 공식필+단일칩+결과필 구조(=conj-ir 원형)라 무변경. ⚠️ 헤더 HTML은 카드별 수동 편집(현재형 3개 cr-head, 과거형 2개 cp-head); cc-head는 cc-rule-chip이 이미 공식필.

**2026-06-09 작업 — 현재형 파일 분할(사용자 "학습 단계별 배포")**: `시제_1_현재형.html`(원본 보존)을 **입문 1 + 규칙별 린파일 3 = 4개**로 분리.
- ① `시제_0_시제란무엇인가.html` = 공통 입문(소제목 번호: 1.시제란 무엇입니까? 2.한국어의 세 가지 스타일 3.기본형이란?, 과거·미래형과 공유 가능). 만들기/퀴즈 제거. "세 가지 시제" 카드그리드 위 라벨 추가. 영어비교 카드(English vs Korean) 삭제([[feedback_no_english_comparison]]), 활용 정의 유지. 격식체 설명 자연화("규칙 가장 간단" 주장 삭제).
- ②③④ `시제_1_현재형_A_받침없음`·`_B_받침있음`·`_C_ㄹ받침.html` = 각 규칙 **린(lean)**(무거운 입문 제거 → 상단 입문 링크 배너, Step1·2 + 해당 conj-rule 카드 + 만들기 + 요약(해당 행만) + 퀴즈).
- **단어 리스트 단일화**: 만들기(pr80)·플래시(fc80)·쓰기(ptWords)가 서로 다른 배열을 쓰던 버그 → **`window.TWORDS` 전역 1벌**(pr80 IIFE가 정의·노출, fc80·ptWords가 참조) + **`ruleOf(기본형)`** 필터로 각 파일이 V·H / C / L 만 표시. ruleOf: 하다→H, `(charCode-0xAC00)%28`==0→V·==8(ㄹ)→L·그외→C. C파일은 ㄹ동사 4→12개 보충(놀·울·열·걸·길·멀·들·달).
- **린 파일 제작법**: 원본 복사 → 입문 HTML만 sed 삭제(⚠️만들기용 `<style>` 블록 76~445의 conj-rule CSS는 보존!) → conj-rule 카드·요약행을 해당 규칙으로 교체 → 3필터 변경 → 제목/점수(/86·/52·/24) 변경.
- **현재형 A는 기본단계라 Step1·2(다지우기·어간확인) 유지, B·C는 그림+Step1·2 삭제(sed)**. A 유지 이유: 첫 단계가 기본기능 배우는 자리.

**2026-06-09 과거형 3분할** (사용자 지시: 규칙/줄임/불규칙): `시제_2_과거형.html`(원본 보존) → 3파일.
- ① `시제_2_과거형_규칙.html` = 🅐밝은(았)+🅑어두운(었)+Step1·2+만들기+규칙퀴즈. 원본 1~835 보존, **836~3139 삭제**(footer 보존).
- ② `시제_2_과거형_줄임.html` = 🅒 contraction(conj-contract 4그룹)+줄임 퀴즈. sed `21,86d;216,835d;1220,3139d`(인트로·규칙·하다불규칙종합 삭제, **공유퀴즈CSS 87~215 보존**).
- ③ `시제_2_과거형_불규칙.html` = 🅳하다(했)+불규칙 ㅡ·ㄷ·ㅂ·르·ㅅ+전체요약+종합퀴즈. sed `21,86d;216,1219d`. (사용자: 하다·종합 모두 불규칙 파일로.)
- **핵심 구조 지식**: 과거형은 거대(237K토큰)·섹션마다 자체 퀴즈. **퀴즈 컴포넌트 CSS: 객관식 mc=head(공유), 플래시fc·쓰기pt=본문 인라인 블록 87~215(공유)**. conj-past CSS 448~517(규칙), conj-contract 840~883(줄임), conj-ir 1460~1542(불규칙). **각 퀴즈는 고유접두 자체함수**(mcT3/T6/PEU…, pcot* 등)라 섹션 삭제해도 자립. 제목 "시제 2 · 과거형 — ①규칙/②줄임/③불규칙".

**2026-06-09 추가**: 과거형 하다 별도 분리(④→하다는 ③, 순서 규칙①·줄임②·하다③·불규칙④). 퀴즈 재번호(객관식 숨김 `.quiz-box{display:none}`이라 플래시→퀴즈1·쓰기→퀴즈2, 전 시제파일 sed 일괄). conj-hada(하다)·conj-ir(과거 불규칙)도 가로정책(720↓컴팩트/460↓세로) 적용. 규칙헤더 영어 병기(rule-h에 `<span style=display:block;opacity:0.85>`). **직접 만들기(pr80)를 각 섹션 플래시 위로**: 불규칙 5개는 기존 만들기(섹션끝)를 conj-ir 카드 뒤로 이동(cut+paste, 앵커=fc-grid id fcGridPXX), 줄임·하다는 신규(pr80 CSS+DOM-scoped 엔진+데이터: 줄임=모음끝 14개 갔/왔/봤·줄임설명, 하다=하다 12개 했·하다설명, prCO_body·prHD_body). 요약을 직접만들기 위로 이동(현재형 A·B·C). 현재형 B·C는 Step3 헤더·발음주의 caution 삭제(A는 Step1·2 유지).

**2026-06-09 과거형 추가 작업**: (사용자) ① **하다를 별도 파일로 분리**(`시제_2_과거형_하다.html`) — 불규칙 파일에서 하다 섹션(원본 1220~) 추출. ② **순서 재정렬**: 규칙①·줄임②·하다③·불규칙④ (제목 번호 변경). ③ **불규칙 파일 각 불규칙(ㅡ·ㄷ·ㅂ·르·ㅅ)에 "직접 만들기"(pr80) 추가**: footer 앞에 pr80 CSS+DOM-scoped 엔진+5개 데이터(pr80Exp에 EU/DI/BI/RI/SI 설명)+렌더 블록, 각 불규칙 섹션 끝(다음 헤더 앞)에 `tbody id=prIR_XX` 컨테이너 삽입. 데이터 직접 작성(ㄷ·르·ㅅ는 규칙 pr80에 없어 신규): EU 10·DI 5·BI 10·RI 10·SI 5개, 정확 활용(걷→걸었, 덥→더웠/돕→도왔, 모르→몰랐, 짓→지었 등). 예시 배지(preg) 미사용.

**2026-06-11 미래형 분할 + 종합 시험**: 방법론을 `마스터-템플릿-적용본\_시제_분할_적용_가이드.md`로 **문서화**(분할 원칙=어미 결정 요인: 받침이면 A/B/C, 모음이면 통합 / 파일 템플릿 / 스타일 규칙 / PowerShell 줄-슬라이스 절차 / ruleOf). **미래형을 4파일로 분할**(현재형식 받침 분리): `시제_3_미래형_A_받침없음`(🅰 ㄹ것, V+H 43개 만들기)·`_B_받침있음`(🅱 을것, C 23개)·`_C_ㄹ받침`(🅲 그대로+것, **현재형 ㄹ탈락과 달리 무변화** 강조, L 12개)·`_불규칙`(🅳ㄷ·🅴ㅂ·🅵ㅅ conj-ir 가로, ㄷ·ㅂ·ㅅ 17개 만들기, pr80Exp에 DI·SI 분기 신규·태그는 문자열). 원본 `시제_3_미래형.html` 보존. 각 파일=규칙+요약+만들기(pr80 받침별)만, **플래시·쓰기 없음**(원본 플래시·쓰기가 3시제 종합형 /474이라 받침분할 부적합). **종합 시험 통합**: 구 `시제_현재_과거형_시험.html` → **`시제_종합_시험.html`**으로 교체(미래형의 3시제 플래시 fc80GridFuture + wqg/allt 쓰기 /474 이식, 구 과거전용 제거, 구파일 삭제). 조립법=현재시험 head/CSS(1-148) + 미래형 플래시(474-600)·쓰기(603-854)·꼬리말. **빌드 패턴**: 미래형 원본은 Read 도구 토큰초과 → PowerShell `Get-Content`로 슬라이스, 한글(제목·데이터)은 자리표시자 후 Edit. [[feedback_conjugation_table_style]] [[feedback_no_english_comparison]]

**2026-06-11 하다 분리 + 라벨 시프트**: 현재형·미래형에서 **하다를 Ⓑ로 분리**(사용자 결정: 순서=받침없음 다음). 라벨 전역 시프트: 현재·미래 공통 **A 받침없음 / B 하다(신규) / C 받침있음(구 B 개명) / D ㄹ받침(구 C 개명)**, 미래 불규칙 배지 🅳🅴🅵→🅴🅵🅶. 신규 파일: `시제_1_현재형_B_하다.html`(C받침있음 복사 후 규칙카드·요약·필터 H 교체, 만들기/36·쓰기/36), `시제_3_미래형_B_하다.html`(C받침있음 복사, 🅱 ~하다→할것 표 + H 18개 데이터 /36). A에서 하다 제외: 현재형 A 필터 `V||H`→`V`(만들기·플래시·쓰기 3곳)·/50, 미래형 A 데이터에서 H 18줄 삭제·/50. 현재형 C 쓰기 정적표기 /52, D /24로 보정. **framing 주의**: 현재·미래의 하다는 불규칙이 아니라 "받침없음 규칙이 적용되는 최대 어휘군(명사+하다)" — 과거형 하다(하→했 특수)와 다름.

**2026-06-12 미래형 A~D 재디자인+퀴즈, 시험 파일 3종**: ① 미래형 A~D의 평면 규칙표+요약표를 현재형 컴포넌트로 통합 재디자인(conj-rule 흐름카드 + rule-summary 4행 카드 🅰🅱🅲🅳, rule-h 삭제, CSS는 현재형 A 287-362·769-819 이식+오버라이드). 요약 예시 정렬: rs-base 4.3em 우정렬, rs-ex-pair stretch. ② 미래형 A~D에 **퀴즈1 플래시+퀴즈2 쓰기 추가**(현재형 A 블록 이식, 라벨 미래형 치환, 필터 제거, `window.TWORDS=data.map(의문형 까? 파생)` — "만들기만" 결정 폐기). ③ **시험 3종 체계**: `시제_1_현재형_전체_시험.html`(현재만 80단어 /160, 현재형 A에서 시험요소 추출), `시제_2_과거형_전체_시험.html`(기본형당 현재+과거 4형태 /316, 종합시험 복사 후 미래 제거), `시제_종합_시험.html`(3시제 /474). 누적 구조: 현재→현재+과거→3시제. conj-rule 가운데 칸은 "어간+어미 칩"(cs-plus) 형식 — 현재형 A·B·C·D 모두 적용(D는 ㄹ삭제 칩+어미). 인트로 문구 "가장 큰 단어 가족"→"가장 많이 사용되는 형태 중 하나". ④ **과거형도 A~D 라벨 부여**: 파일명 `시제_2_과거형_A_규칙/_B_줄임/_C_하다/_D_불규칙.html` + 제목 ①②③④→Ⓐ~Ⓓ (내부 규칙 배지 🅐🅑🅲🅳🅴~🅸은 전역 일련번호 그대로). 만들기·쓰기 기본형 칸 영어는 항상 아랫줄(.prb small·.pt-e display:block, 18파일 일괄). ⑧ **수의적 줄임 채점 정규화(2026-06-12)**: 보다·주다·되다는 보았/봤·주었/줬·되었/됐 둘 다 표준 → 채점 normalize 함수에 `.replace(/봤/g,'보았')…` 캐노니컬라이즈 추가(B줄임 pcot·pr80·wqNorm 3곳, 과거형 전체 시험·종합 시험 alltNormalize·wqNorm 각 2곳). **B 줄임의 표시 데이터는 줄임형(봤/줬/됐)으로 교체**(레슨 카드와 일치 — 기존엔 본문은 봤습니다 가르치며 쓰기 정답은 보았습니다였던 자기모순). 시험 데이터는 비줄임 유지(중립). '살다'는 에/에서 둘 다 가능 → 조사 G 15번 문항을 '회사에 다닙니다'로 교체 + 힌트에 살다 병행 안내. ⑦ **미래형 불규칙은 분할하지 않음**(사용자 확인 2026-06-12): 3종(ㄷ·ㅂ·ㅅ)이 "모음 어미 앞 받침 변화" 한 원리 + 각 섹션이 가벼움 + 과거형 복습 성격. 대신 플래시 17장 + 쓰기 /34 보강(미래형 A 블록 이식, window.TWORDS 파생) — 이제 미래형 5파일 모두 동일 구성. ⑥ **과거형 불규칙 5분할(2026-06-12)**: 합본 `_D_불규칙.html` → `_D_ㅡ불규칙/_E_르불규칙/_F_ㄷ불규칙/_G_ㅂ불규칙/_H_ㅅ불규칙` 5파일, **순서 ㅡ·르·ㄷ·ㅂ·ㅅ(사용자 결정)** — 배지 재번호 🅴ㅡ·🅵르·🅶ㄷ·🅷ㅂ·🅸ㅅ (rule-h + H의 sp-grid 카드 순서·배지 모두). H에만 전체 요약(9칸 sp-card 그리드). 합본 원본 삭제. 공유 pr80 엔진은 각 파일에 통째 복제(pr80Build가 null-safe라 타 불규칙 tbody 없어도 무해). conj-ir CSS는 ㅡ섹션에 내장돼 있어 E~H엔 별도 이식. ⑤ **요약 = 누적식**(사용자 결정): A파일=Ⓐ만, B=Ⓐ+Ⓑ, C=Ⓐ~Ⓒ, 마지막=전체. 부분 제목 "요약 · Summary"/전체만 "전체 요약 · Summary". 현재·과거·미래 모두 적용(만들기 위에 배치). 과거형 A~C는 rule-summary 신설(Ⓐ았/었 ㅏㅗ·그외 태그, Ⓑ줄임, Ⓒ하다), 과거형 D는 9칸 sp-card 그리드=전체 요약(유지). 쓰기 퀴즈 예시행은 전 파일 삭제(총점 +1단어분: 과거전체 /320·종합 /480), 예시 셀 스타일(.wqg-eg·.pt-eg 파랑 셀)은 통일됨. 중복 섹션제목("○○형 만들기/How to Make" h1 section-break)은 분할 파일에서 삭제(원본·정리는 유지). rs-head 요약표 헤더는 한국어 위/영어 아래 2줄 스택(.rs-head>div flex-column + small).

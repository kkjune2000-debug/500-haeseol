---
name: haeseol-content-enrich
description: 토픽2 해설 콘텐츠 확장 — 어휘 전량·문법 박스·문제/문항 영어·왜아닌가 삭제(듣기20+읽기18)
metadata: 
  node_type: memory
  type: project
  originSessionId: 6ef7c53f-3fb0-4912-a320-2be45f2ec0ff
  modified: 2026-08-06T22:34:45.190Z
---

**★토픽2 콘텐츠 확장(2026-07-30, 사용자 요청)★** — 학습자가 사전 없이 이해하도록 해설에 네 가지를 더함. **듣기 20유형(600문항) + 읽기 18유형(594문항) 전체 완료·전수 검증.** 상세: `_스크립트/README 콘텐츠 확장.md`.

네 가지:
1. **어휘 전량** — 대본/지문+문제+선택지의 **모든 내용어(명·동·형·부)·유용한 구**를 기본 낱말만 빼고 전부(사전형·형용사 "to be X"). ★칩 형식(2026-08-01 사용자 요청): **한국어 낱말(위) / 영어 뜻(아랫줄)** — `.vocab em{display:block}`, `.vocab{align-items:flex-start}`. 순수 CSS(완성 HTML 38+템플릿 24). 비슷한 말 박스와 같은 세로 형식.★ 예전엔 4~5개뿐이라 사용자가 「너무 간단」 지적. [[haeseol-vocab-jimun]]의 「지문 단어 모두」를 전 유형으로 일반화.
2. **문법 박스**(`문법 · Grammar notes`) — 대본/지문 문법을 간단 용법으로(격식체·용어 금지 [[haeseol-munche]]), `[형태,한국어,영어]`. ★읽기 01·02는 이미 정교한 문법 박스(500링크) 있어 제외(gram=[]).★ ★**한국어 설명(x[1]) 렌더 삭제(2026-08-04 사용자 「외국인은 영어 해석만 봐도 충분」)** — gramxHtml에서 `${esc(x[1])}` 제거, **형태+영어 해석+500링크+예문만**. 데이터 g[1]은 보존(미렌더). t2_gram_desc_remove.py(템플릿24+06). ★적용은 gramxHtml(듣기06~20·읽기04~18)뿐 — 읽기01의 gramHtml(G dict body/bodyEn·「-고」 steps)은 별도 렌더라 미적용, 요청 시.★★
3. **문제/문항 영어(askEn)** — 문항별 스템에 영어. 듣기 짝06~20, 읽기 07·08·09·15·16·17·18. 선택지 영어는 대개 이미 있음(읽기12만 optsEn 추가).
4. **「왜 아닌가」 삭제** — 그 배지 문구만. ★「왜 주제가 아닌가」·「고치면 맞음」·「지문에 없음」 같은 정보성 배지는 유지.★

파이프라인: `extract`(완성 HTML서 입력 번들) → 유형별 에이전트 워크플로(콘텐츠 생성) → `enrichpatch`(빌드에 `const ENRICH`+`ge`+`gramxHtml`+CSS 주입, 렌더 교체) → 재빌드 → `enrich_check`(데이터)+`enrichdomcheck`(헤드리스 렌더). 듣기 `t2l_enrich*`, 읽기 `t2r_enrich*`.

★함정★:
- **지문 공유 유형(읽기 07·08·09·15·16·17·18)**: 어휘/문법이 「분석 문항」 하나에만 렌더(07→20,08→22,09→24,15→43,16→45,17→47,18→50). 각 문항 생성 어휘가 지문 전체를 덜 담아, **공유 문항들 union 병합**(`t2r_enrich_merge_shared.py`) 후 렌더 문항에 몰아야 함. DOM 검사가 데이터의 절반만 잡히면 이 문제.
- **유형06(듣기) 재빌드 불가** → `t2l_enrichpatch06_html.py` 직접(gp가 `gp(h2,no)`). 읽기 09는 옛 형식이나 재빌드 가능(균일 앵커로 처리).
- `<em>`는 어휘 전용 → DOM 어휘 수 셀 때 `<script>` 소스만 빼면 정확(gramxHtml 리터럴이 `class="gx"` +1).
- 워크플로 `args`가 문자열로 깨짐 → 목록은 스크립트에 임베드 [[workflow-args-cr-gotcha]].

**★문법 박스 → 500 수업 링크(하이브리드, 2026-07-30 사용자 요청)★** — 문법 박스에 간단 요약은 그대로 두고 **놀라운 한국어 500 해설집에 수업이 있으면 링크**(`📘 500문장 보기`). 유형01·02의 `LESSON_BASE` 재사용. ★링크 유무가 신호 — 있으면 「500에서 배운 기초」, 없으면 「토픽Ⅱ 새 문법」이라 **다 안 건다**(고급 문법은 요약만).★ 매핑 `t2_gramlink.json={형태:[lfile,ref]}`, 하위 에이전트가 **수업 파일 열어 확인**(뜻으로 묶인 수업이라 파일명만 보면 틀림). 106형태 링크(문법 등장의 32%). 렌더 `t2_gramlink_patch.py`(24빌드 `const GRAMLINK`/`LESSON_BASE`+`gramxHtml` 링크판+`.glnk`)+`t2_gramlink_patch06_html.py`(06 리터럴). 검증 `t2_final_domcheck.py`(.glnk==GRAMLINK 형태수). 새 형태 추가는 t2_gramlink.json에 넣고 전량 재빌드(06 리터럴 별도).

**★연결어미 뒤 쉼표 전량 정리(2026-08)★** — AI 생성 프로즈가 [[haeseol-munche]] 「연결어미 뒤 쉼표 금지」를 안 지켜 남은 걸 정리. 정규식 불가(「냉장고,」 명사 vs 「있고,」 서술어 못 가름) → 하위에이전트 4개가 **문장 판단**으로 연결어미 쉼표만 삭제·나열/덧붙임 유지. 파이프라인: `t2_comma_extract.py`(쌍 ko+content why+enrich gram/distinct에서 쉼표 든 것 수집) → 청크→에이전트({orig:cleaned}, 쉼표외 글자 불변 자가검증) → `t2_comma_apply.py`(★norm(쉼표·공백 제거) 대조로 쉼표외 변경 항목 거부★, 소스 반영+06 HTML은 json.dumps 몸통으로 직접치환) → 재빌드. **658문자열·660곳 정리·거부 0**. 기출(대본/지문/선택지)은 안 건드림.

**★듣기 유형01 레이아웃(2026-08 사용자 요청)★** — 순서 **대본→어휘→문법→그림 설명→번역 연습**(★해설·결정적 단서 삭제, 어휘가 문법보다 먼저 — 다른 유형은 문법→어휘라 불일치★). 「그림을 하나씩 봅시다」→「그림 설명」. 번역 연습은 `wpairs(koLines,lineEn)`(한 덩이 폴백) 대신 **`SCRIPTPAIRS[id].map([ko,en])`로 대본 문장별**. ★듣기 앞구간 **01~05 전부** 적용(02는 SCRIPTPAIRS 뒤 정답 대답 `[q.opts[q.ans-1],q.optsEn[q.ans-1]]` concat). 짝06~20은 번역 연습 없음. 읽기는 이미 문장별(07~18 `it.pairs`, 01~06 sent+예문).★ ★그림/그래프 설명은 **왜 정답/오답(w.why/whyEn) 제거, 묘사(w.d/dEn)만** — 각 도표를 묘사하는 법을 배워 **토픽 쓰기 53(도표 묘사) 대비**(사용자 의도). 「← 정답」 배지(CSS `.dsc::after`)는 유지. 설명(d) 필드 쉼표도 같은 규칙으로 정리(t2_comma_d_*, 113중 101). pics의 'd'는 첫 why 쉼표정리 때 빠졌던 필드.★ ★★교훈: 그림 설명이 **실제 이미지를 안 보고**(대본·맥락 추측) 생성돼 **144장 중 ~22장(15%) 오묘사**(동작·위치·물건·차트유형: 바라봄→이야기, 앞→안, 누움→앉음, 지갑→서류, 대패→톱, 도넛→원 등). 하위에이전트 6개가 이미지 직접 보고 점검·수정(t2_pic_audit_*). **이미지 기반 콘텐츠는 반드시 이미지를 봐서 검증할 것.**★★ ★그림 설명 문체: 「~하는 장면입니다」(장면 서술)보다 **「~가 ~하고 있습니다/~있습니다」(동작·상태 직접 서술)**가 낫다(쓰기 53 묘사 연습에 맞음). 유형01 40개 변환(t2_jangmyeon_*).★ ★그래프 설명은 **PPT로 설명하듯**: 「제목」 원그래프로 X → **제목은/는 X**(「」·차트유형[원/도넛/막대/꺾은선그래프·순위표] 삭제, 제목+은/는+데이터 직접 서술). 한/영 둘 다. 숫자 데이터·순위·추세는 보존. 유형01 48개(t2_graphstyle_*).★ ★도표·조사 결과 서술의 핵심 구조 **「~을/를 대상으로 → ~을/를 조사한 결과 → ~(으)로 나타났다·조사되었다」**를 「도표 설명 틀 · 쓰기 53」 문법 노트로 유형01 그래프 문항 8개(3번) 맨 앞에 추가(쓰기 53 대비). 개별 형태(대상으로/ㄴ 결과/나타나다)는 대개 이미 있었음.★ ★읽기 유형04(그래프 12문항, 각 회차 10번)에도 적용: 그래프 데이터는 `it.alt`(그림 속 숫자=조사제목+수치)에 있음 → PPT식 **모델 도표 묘사**(graphdesc[ko,en], 조사 구조 사용) 생성해 해설 맨 앞 `.gdesc` 섹션(t2r_g4_*) + 도표 설명 틀 문법. build4 solInner에 `it.kind==='graph'&&ge(it,'graphdesc')` 분기.★

**★듣기 유형02~05(이어질 대답/행동 고르기) 해설 간소화(2026-08-01 사용자 요청)★** — 사용자: *"정답이 아닌 이유를 설명할 필요는 없을 것 같다. 토픽 수준이면 그 정도는 설명 없이 이해한다."* → ①별도 「해설」 문단(q.sol) 삭제 ②보기별 설명(ow-why=optWhy) 삭제 ③「대답을 하나씩 봅시다」 제목 → **「해설」**로, 그 아래 **1·2·3·4를 문항+영어 해석만**, **정답만 ✓ 알맞은 대답** 표시(설명 0). **대본(전문 이중언어)이 곧 해설 역할.** 최종 구성: 대본→해설(보기 4개, 정답 ✓)→어휘→문법→번역 연습. t2l_reply_trim.py(build02~05, 치환 3종 각 1회 검증). [[haeseol-odap-capsule]]의 「왜 아닌가 삭제」 방향을 이 유형에선 오답 설명 전면 삭제까지 확장. (읽기 오답 설명은 지문이 길고 오답이 미묘해 유지 — 사용자와 범위를 듣기2~05로 한정.) ★**단, 유형04(내용 일치, 13~16)는 예외 — 짧은 오답 교정 추가(2026-08-01 사용자 재요청)**★: "들었나"가 아니라 "어느 진술이 맞나"라, 오답마다 **✗ → 바른 사실**(한국어+영어 짧게)을 붙이고 정답은 **✓ 맞는 내용** 배지. 예 35회13: ①세일 안 함→세일을 한다 ②더 비쌈→가격이 더 싸다 ③복잡함→복잡하지 않다. 파이프라인: 기존 verbose why를 하위에이전트 3개가 **짧은 바른 사실(fix/fixEn)**로 압축(t2l_g4_fix_extract→out→검증: 오답144=144, 정답엔 fix 없음) → **별도 t2l_g4_fix.json을 build04가 로드**(content04.py 불변, PAIRS/ENRICH와 동일 방식) → build04 ow에 `.ow-fix`/`const FIX`/`__FIX__` 주입(t2l_g4_render.py 5종). ★**유형05(중심 생각, 17~20)도 짧은 오답 이유 추가(2026-08-01)**★ — 단 성격이 달라 「바른 사실」이 아니라 **「왜 중심 생각이 아닌가」 짧은 이유**(반대/다른 사람 생각·대화에 없는 내용·부분 내용). → [이유] · [영어], 정답 배지 **✓ 중심 생각**. 같은 파이프라인(t2l_g5_fix_* + t2l_g5_render.py, ✗ 없이 「→」만, 144=144 검증). 예 35회17: ②여자의 생각 남자는 반대 ③④대화에 없는 내용. **유형02/03(이어질 대답·행동)만 오답 문구 없음 유지**(대본이 곧 해설). 읽기 내용일치는 이미 캡슐. ★교훈: 유형별 오답 성격에 맞춰 문구 종류를 고른다 — 내용일치=바른 사실, 중심생각=왜 요지 아닌가, 이어질 대답=없음.★
★**유형06(한 담화 두 물음 21·22)도 적용(2026-08-01, 사용자 「유형6만 먼저」)**★ — 한 담화에 **21=중심 생각·22=내용 일치**가 같이 있어 물음별로: `q.no%2`로 홀수(21)→유형05식 「→ 왜 요지 아닌가」·배지 ✓ 중심 생각, 짝수(22)→유형04식 「→ 바른 사실」·배지 ✓ 맞는 내용. ★유형06은 **재빌드 불가**(build06 재실행 시 enrich·gramlink·대본 패치 소실)라 **완성 HTML 직접 패치**(t2l_g6_render_patch.py: CSS·const FIX 인라인 주입·ow 템플릿·해설 조립 4곳, 각 1회 검증). FIX 72개(t2l_g6_fix_*, raw06.json+content06.json). 검증: 정적 마커 전부 + JS 백틱 짝수(52). ★함정: 이 환경 헤드리스(msedge --dump-dom)는 정상 파일도 페이지 JS를 안 돌려 렌더 프로브가 무효 — 문법은 백틱 균형/구조 대조로 검증.★ ★**짝문항 07~20도 완료(2026-08-01, 사용자 「진행하라」)**★ — 이건 **공용 빌드 t2l_pair_build.py**(재빌드 안전: enrich·gramlink·pairs 다 로드, `tp` 인자)라 **템플릿을 한 번만** 패치(t2l_pair_g_render.py 6종). 물음 유형은 **`PL.badge(rq['ask'])`가 자동 분류**(내용 일치·하는 일·의도·화자·주제·중심 생각·태도·말하는 방식·앞 내용·설명·이유). 정답 배지 = `'✓ '+q.badge`(유형마다 다름), 오답 = 「→ fix」. FIX는 유형별 `t2l_pairfix%02d.json`을 pair_build가 로드(구조 `RAW[h]['questions'][no]`). 문구 종류는 **에이전트가 why 보고 선택** — 사실 반박형(내용일치·설명·이유·하는 일·화자)=바른 사실, 요지/태도/의도/주제/방식/앞내용=왜 아닌가. 하위에이전트 14개(유형별 72개, 총 **1,008개**), t2l_pairfix_finalize.py로 유형별 오답 id 일치 검증(14/14) → 14개 재빌드 → 렌더+백틱 검증 전부 OK. ★듣기 오답 간소화 총정리: 02/03=문구 없음, 04=바른 사실, 05=왜 요지 아닌가, 06=직접패치(21중심생각/22내용일치), 07~20=pair_build 공용(badge별). 읽기 내용일치는 기존 캡슐.★

**★짝문항(06~20) 해설 공개 게이팅(2026-08-01 사용자 요청)★** — 한 담화에 두 물음이라 공유 자료(대본·어휘·문법)를 **둘 다 풀어야** 보게 함: **첫 물음(홀수)=그 문항 해설만 / 둘째 물음(짝수)=해설+대본+어휘+문법**. 구현: `q.no%2===0`(짝수=둘째) 분기 — sol에서 짝수일 때만 문법+어휘, `reveal()`은 짝수일 때만 `disc.show`, resetOne은 둘째 물음 revealed 여부로 대본 숨김. 어휘는 21·22 **합집합**(mvUnion, 문법은 원래 동일). 06=직접패치(t2l_g6_reveal_patch.py), 07~20=pair_build 템플릿(t2l_pair_g_reveal.py)+재빌드. 안내 문구도 「둘째 물음 해설 보기로 나옴」으로 수정. ★주의: 해설 보기 버튼은 그 문항을 맞혀야 뜨므로 대본을 보려면 둘째 물음을 맞혀야 함(의도된 동작).★
· **읽기 「한 지문 여러 물음」(07·08·15·16·17·18)엔 이 게이팅이 이미 적용돼 있음**(2026-08-01 확인) — 지문은 늘 보여 숨길 게 없고, 어휘·문법은 **merge_shared로 마지막 물음에만** 몰려 있어(예: 읽기18은 48·49=0, 50=4) 앞 물음=해설만·마지막=해설+어휘+문법이 그대로다. 변경 불필요.

**★짝문항(06~20) 번역 연습 추가(2026-08-01 사용자 「유형5 포함 이후 파일」)★** — 01~05만 있던 번역 연습(대본을 문장별 한→영)을 짝문항에도. build05의 CSS·makeWriteQuiz·PT-MINE(엔터 고정)을 **소스째 추출해** 공용 빌드에 이식(t2l_pair_translation.py) + 06 직접패치(t2l_g6_translation.py). ★담화 단위: `makeWriteQuiz('pt_'+hoe, SCRIPTPAIRS[hoe]→[ko,en])`, ptslot은 **둘째 물음 sol**(게이팅 자료와 함께). 힌트 「담화」로, 정답삽입 문구 삭제.★ 06 직접패치 + 07~20 재빌드, 전량 마커·백틱 검증. (05는 이미 있음, 읽기도 이미 있음.) 헤드리스가 이 환경선 JS 미실행이라 렌더 확인은 파일 직접 열기.

**★비슷한 말 구별 박스(선택·문항별)★** — 헷갈리는 낱말(지구/세계/세상 등) 차이를 어휘 아래 앰버 박스로. 전면 아님·필요한 문항에만. ★**낱말별 4필드 형식(2026-08-01 사용자 요청)**: `distinct:[[낱말, 영어낱말, 설명(한국어), 해석(영어)]]` — 낱말(굵게·block)→영어(.en 아랫줄)→한국어 설명→영어 해석 순 stack. (옛 3필드 「머리말+합친 한 문단」에서 바꿈.)★ 렌더 `distinctHtml`(build3, x[0]~x[3]). 현재 유형03 35회5번에만(t2r_distinct_split.py). 낱말은 자동번역 보호됨(가드 `.dx b`→translate=no). ★**「어휘 확장」 박스(2026-08-04 사용자)**: 같은 4필드·앰버(`.distinct/.dx`) 구조를 **제목만 「어휘 확장」**(Building your vocabulary — related expressions)으로 재사용 — **연어 묶음**(같은 낱말+다른 동사: 스트레스를 주다/받다/풀다)용. 유형06에 `expand` 필드 + `expandHtml`(t2_expand6.py), 35회16 첫 적용, 순서 어휘→어휘확장→번역연습. ★**묶음 분리(t2_expand6_split.py)**: expand 데이터가 **그룹(리스트의 리스트)** — 그룹마다 `.distinct.exgrp` 블록(사이 간격). 35회16 = ①스트레스 연어(주다/받다/풀다) ②결혼 낱말(미혼→청혼→약혼→결혼→기혼). 한 어휘확장 안에 여러 묶음.★ ★**묶음별 배경색(t2_expand6_color.py)**: 그룹 인덱스 `gi%3`로 `.exgrp0/1/2` 클래스 — 0앰버(기본)·1로즈(#fff1f2)·2초록(#f0fdf4). 묶음 추가하면 색 자동 순환.★ ★구분: **「비슷한 말 구별」=동의어/헷갈리는 말, 「어휘 확장」=연어 묶음.** 다른 문항엔 enrich `expand` 데이터만 넣으면 켜짐.★ ★**설명(한국어)은 문장이 아니라 명사구로(2026-08-02 사용자 요청)** — 영어 해석("the planet we live on")처럼 「~를 가리킵니다/뜻합니다」 서술어를 빼고 **명사구**로("우리가 사는 행성"). 사용자: *"어휘 설명이 반드시 문장일 필요는 없다."* 정의부만 구로 바꾸고, 뒤에 붙는 정답 근거 문장(예: 세상 항목의 「이 글의 …답이 신문입니다」)은 문장 유지. 앞으로 만드는 distinct 설명·어휘 뜻풀이는 처음부터 명사구로.★

**★자동 번역 보호(translate 속성, 2026-08-01 사용자 지적)★** — 어휘 박스가 「한국어+영어」 한 칸이라, 페이지를 **다른 언어로 일괄 자동번역(구글 번역 등)** 하면 배워야 할 **한국어 낱말까지 번역돼 사라진다**(어휘만이 아니라 지문·대본·선택지·예문·문법형태·번역연습 한국어 전부 같은 문제). 해결: 각 페이지에 **로드 뒤 실행되는 작은 스크립트**(`</body>` 직전, 렌더가 인라인이라 그 뒤 실행)로 **학습 한국어 요소엔 `translate="no"`**(`.psg,.lnko,.ln,.otxt,.ow-t,.pt-ko,.ask,.setask,.qq,.fill,.vocab b,.gx b`), **영어 글로스엔 `translate="yes"`**(`.en,.oen,.lnen`, 부모 no를 덮어씀). → 다른 언어로 번역해도 한국어 학습 내용은 그대로, 해설·뜻풀이·문법설명만 그 언어로. t2_i18n_guard.py(완성 HTML 38 + 빌드 템플릿 24, 마커 `<!--i18n-guard-->`로 중복 방지, 재빌드해도 유지). ★한계: 이 환경 헤드리스가 JS 미실행이라 실제 번역은 브라우저에서 확인 필요. translate 속성은 크롬·구글번역·파폭이 존중하는 표준. (유형03 「비슷한 말 구별」 낱말도 `.dx b`로 보호 추가 완료.)★

**★전 문항 어휘 누락 감사(2026-08-02 사용자 요청, 「애를 쓰다」가 목록에 빠졌다는 지적에서)★** — 계기: 사용자가 듣기35회 49·50 「애를 쓰다」 누락 지적. ★실제로는 안 빠짐 — enrich에 있고 게이팅상 50번(둘째 물음)에만 보임(49=해설만). 사용자 전제를 정정하되 전수 감사는 진행.★ **결과: 어휘는 이미 near-exhaustive**(감사자들 공통 평가 — 대부분 유형이 지문 전체 기준으로 만들어져 선택지-only content의 상위집합, 관용구도 대체로 이미 있음). 파이프라인(재사용 가능): `t2_vocabaudit_extract.py`(빌드 HTML서 (유형·회차)별 [대본/지문+선택지]+현재 vocab union, 456단위·12청크) → 감사 에이전트 12개(내용엔 있으나 vocab에 없는 낱말·**특히 연어/관용구**, 사전형+영어뜻) → `t2_vaudit_apply.py`(★문항 귀속=빌드 HTML의 문항별 내용으로 판정, norm 중복 제외, 06은 인라인 const ENRICH 직접 패치, 그 외 enrich JSON+재빌드★) → **적대 검토** `t2_vaudit_review_prep.py`+검토자 4개(내용실재·뜻정확·중복·형용사 to be·과도기초) → `t2_vaudit_fix.py`(플래그 반영, ★우리가 추가한 항목만: 단어+추가한 뜻 동시일치로 골라 제거/교정★) → `t2_vaudit_verify.py`(안착+회귀). ★수치: 후보 163 → 적용 162(중복1) → 검토 6플래그(제거5: 나이대=연령층중복·힘들다/수업/이메일/반지=A1기초, 교정1: 그립다 형용사 to be) → **순증 157**(대부분 연어/관용구: 태극 마크를 달다·수단과 방법을 가리지 않다·긴장을 늦추다·위험을 감수하다 등) + 빈 vocab 두 문항(읽기09_102·15_96·15_102 감정형용사 보기) 채움. 27개 파일 재빌드, i18n가드+vocab스택 회귀 0.★ ★교훈: A1 기초어 판정 기준을 감사자마다 흔들리게 두지 말 것(검토가 이걸 잡음). 형용사는 "to be/feel"로 시작해야(형용사인데 "to X"면 교정).★

**★시험지 모드 — 정답 전 영어(해석) 숨김(2026-08-02 사용자 요청)★** — 사용자: *"정답을 맞추기 전까지 모든 영어해석 금지(토픽 시험지 환경), 맞춘 후엔 모든 문장에 해석."* **듣기+읽기 전체**. 구현: 템플릿 안 건드리는 **범용 블록**(t2_exammode.py, 마커 `<!--exam-mode-->`, 완성 HTML 38+템플릿 24, i18n 가드와 같은 방식) — CSS `.en,.oen,.lnen{display:none!important}` 기본 숨김, `.q.solved/.q.done .en/.oen/.lnen`(그 문항 맞히면)·`.disc .en/.lnen`(대본 열릴 때)·`body.allsolved …`(페이지 안내·팁·제목: 전 문항 맞히면)일 때 표시. ★클래스 함정: 대부분 `.solved`인데 **읽기15~18은 `.done`** — 둘 다 셀렉터·감시에 넣음.★ `body.allsolved`는 템플릿 훅 대신 **범용 감시 스크립트**(모든 `.q`가 solved/done이면 body에 클래스, MutationObserver+click 폴백)라 함수명(updateProg/updateBar) 차이 무관. ★한계: 이 환경 헤드리스가 JS 미실행이라 실제 게이팅은 브라우저 확인 필요(정적 마커·CSS·클래스 셀렉터로 검증). 기능 버튼 라벨(「해설 보기 · Show explanation」 등 인라인)은 콘텐츠 해석이 아니라 그대로 둠.★ ★**보완(2026-08-03)**: 영어를 담는데 `.en`이 아니던 클래스 **`.galten`(읽기 유형04 도표 「그림 속 숫자」 영어 해석)**가 정답 전에도 보였음 → 시험지모드 셀렉터에 `.galten` 추가(t2_exammode_galten.py, 완성38+템플릿24 전파, 소스 t2_exammode.py도 갱신). 교훈: **영어 클래스가 `.en/.oen/.lnen`만이 아님 — 새 영어 렌더 추가 시 시험지모드·가드 셀렉터에 넣을 것.** 전수 확인법: 템플릿에서 `class="[a-z]*en"` 토큰 grep(현재 en·oen·galten뿐).★ ★**「풀어 보십시오 · Try it」 상태 배지 삭제(2026-08-03 사용자)**: `.q-state`를 exam-mode 블록에 `.q-state{display:none!important;}`로 숨김(t2_qstate_hide.py, 전 파일). ★요소는 남겨 둠 — JS가 `.q-state`.textContent를 갱신하므로 DOM 제거 시 null 오류. CSS 숨김이 안전.★ 정답 확인은 초록 하이라이트+`.feedback` 메시지로 충분(배지 중복).★ ★읽기 유형06(빈칸, 16~18) 선택지에 영어 `.oen` 추가(2026-08-03, 정답 후 시험지모드로 표시) — build6 opts 렌더에 `<span class="oen">optsEn</span>`(build4식). 지문 문장별 번역 연습(ptslot+it.pairs=sents_with_answer 문장별, 빈칸=정답 채움, 142칸)은 **이미 있었고 「해설 보기」 안에 두기로 확정**(사용자).★ ★**해설 재구성(2026-08-04 사용자)**: 유형06 solInner에서 **프로즈 해설(sol)+「빈칸을 푸는 실마리」(keys) 삭제**. **최종 순서(2026-08-04 재배치) = ①어휘 ②어휘 확장 ③문법 ④해설(지문을 한 문장씩: it.pairs로 한국어 문장+아래 영어, 빈칸=정답 채움, 읽고 대조용) ⑤번역 연습(ptslot, 맨 뒤 — 같은 문장을 직접 써 보는 quiz)** — 낱말→문법→지문읽기→직접번역 흐름. 해설 한국어는 `.trko`로 번역 보호(가드 추가). ①은 정적 대조, ④는 인터랙티브 — 둘 다 it.pairs. sol/keys 데이터는 미렌더, answerHtml/keysHtml 미사용.★ ★단 「↻ 다시 풀기」(.qreset)는 유지(사용자 「다시 풀기는 넣어야」) — q-state가 갖고 있던 `margin-left:auto`가 사라져 왼쪽에 붙던 것을 `.qreset{margin-left:auto!important;}`로 오른쪽 정렬(t2_qreset_right.py, 전 파일).★

**★「빈칸을 채우면」 박스 삭제(2026-08-02 사용자 요청)★** — 읽기 06·07·08의 빈칸 정답 시 뜨던 「채운 지문」 박스(answerHtml/answerHtml19/answerHtml21). 지문 위 원문에 정답이 초록으로 채워져 중복이라 삭제 — 세 build의 함수가 `return ''`. (재빌드로 반영.)

**★문법 박스 예문 추가(2026-08-02 사용자 요청)★** — 사용자: *"문법 설명에 아주 간단한 예를 한 두개."* 형식 **한국어+영어 1~2개**(쉬운 500 낱말). gram 항목 `[형태,설명,영어]`→**`[형태,설명,영어,[[예ko,예en],…]]`**(4번째). ★고유 형태 1,482개당 예문 생성 후 전체 4,809 gram(enrich 4,693+06 116)에 형태로 공유★(t2_gramex_prep→생성 에이전트 12→t2_gramex_apply g[3] 세팅→**적대검증 에이전트 12**(형태 실사용·문법·쉬운낱말·영어)→t2_gramex_fix). 검증: ~2,000예문 중 12건만 교정(형태미사용3·영어8·문법1, 오류율 ~0.6%). 렌더 gramxHtml에 `.gxex/.gxe/.gxe-ko` 추가(t2_gramex_render, 전 템플릿 gramxHtml 동일 md5라 1회 치환+06 인라인), **예문 한국어 `.gxe-ko`는 가드에 넣어 번역보호**, 예문 영어 `.en`은 시험지모드로 정답 후 표시. ★함정: 멱등검사는 신규 전용 표식(`x[3]&&x[3].length`)으로 — 앵커 접두부가 원본에도 있어 오판했음.★

**★읽기 유형04(내용과 같은 것) 해설 재설계 — 근거+해설 통합(2026-08-03 사용자 요청)★** — 사용자: *"근거와 해설을 통합, 이전 프로즈 해설 삭제, 문항 1~4를 원문 그대로 쓰고 틀린 부분을 오른쪽 화살표로 교정, 아래 영어."* 기존 solInner의 **프로즈 해설(sol) + 근거(keys 지문대목→판정) 두 섹션을 삭제**하고, **듣기 유형04와 같은 선택지 카드**(`.ow`)로 통합: 선택지 **원문 그대로**(`it.opts[i]`, `.ow-t`+optsEn) → **정답=`✓ 맞는 내용`+`→ 근거`**(왜 맞나, 지문 근거를 여기에 통합) · **오답=`✗ → 바른 사실`**, 각 아래 영어(`.en`, 시험지모드로 정답 후 표시). 파이프라인: t2r_g4cap_extract(48문항 opts·optsEn·ans·keys·sol) → 생성 에이전트 4(선택지별 {fix,fixEn}, keys+sol 근거로만, 쉬운 낱말) → t2r_g4cap_apply(→t2r_g4cap_fix.json, key `회차_번호_i`) → t2r_g4cap_render(build4에 .ow CSS·const CAP·capHtml·__CAP__ 6종 치환, `keysHtml`/`it.sol` 렌더 제거) → 재빌드. **적대검증 2에이전트=192사실 전부 통과(0교정, 숫자·순서 포함)**. ★기존 graphdesc 파이프라인 t2r_g4_*와 이름 충돌 피해 **t2r_g4cap_***. keys 자체(데이터)는 남겨 둠(렌더만 제거).★ ★오답 줄의 `✗ →`에서 **✗ 제거(2026-08-03 사용자)** → `→`만(색·테두리·정답배지로 구분). 선택지 오답 클릭 시 `.mk` ✗(기능 피드백)는 유지 — 다른 것.★ ※같은 「내용일치」인 유형12·07·08 확장은 미적용(07·08은 이미 3분류 캡슐) — 요청 시.
· ★**유형04 추가 손질(2026-08-03)**★: ①「도표 묘사」→**「도표 설명」** 명칭 변경(build4 h4). ②graphdesc 모델문을 **격식체→문어체**(토픽2 쓰기는 문어체 필수) — 「나타났습니다→나타났다·늘었습니다→늘었다·줄었습니다→줄었다」(t2r_enrich04.json graphdesc[0]의 문장-끝 어미만, 12개 전량; `.replace('습니다','다')`로 안전, 중간은 이미 -고/-며 연결어미). ③상단 **「이 유형은 이렇게 풉니다」(풀이 요령) 카드 전체 삭제**(build4의 `<div class="card">`+howto+tip 블록). ④**도표 아래 「그림 속 숫자」 텍스트 블록(`.galt`) 삭제**(2026-08-03, 그림에 숫자 있어 중복 — sentHtml graph 분기에서 `.galt` 제거, `<img>`만; it.alt/altEn 데이터는 번역연습·이미지 alt용으로 유지). ★이 「풀이 요령」 카드는 다른 유형에도 있었음.★
· ★**「이 유형은 이렇게 풉니다」(풀이 요령) 카드 전 유형 삭제(2026-08-03 사용자 「다른 유형에서도 삭제」)**★ — 상단 `<div class="card">`(그 문구 담은 것). t2_howto_del.py: 앵커 「이렇게 풉니다」의 **enclosing `<div class="card">`를 중첩 div 균형 매칭**으로 제거(정규식 아님). 완성 HTML 38 + 템플릿 24 = 60삭제(build4 2개는 이미 제거). ★검증 함정: 정적 본문 div가 net +1인데 이건 **전체를 감싸는 `.wrap`이 `<script>` 뒤에서 닫혀서**(원래 그럼, 다른 방법으로 뺀 유형04도 동일) — 삭제가 깬 게 아님. `<script>` 앞으로 잘라 세면 오판.★
· ★**읽기 유형01 「-고」 문법 카드 재구성(2026-08-03)**★ — 공용 문법사전 `t2r_gram.py`의 특별 항목. ①제목 「-고 — 쓰임 넷」→**「'~고'의 용법」** ②「나란히 늘어놓기」→**「나열」** ③네 쓰임 ①②③④를 **각 줄에, 예문을 바로 아래에**(전엔 body 한 단락 + exs 블록 따로). 구현: `g()`에 `steps` 파라미터 추가, 「-고」에 `steps=[(라벨,설명,영어,예ko,예en)×4]`(ex는 번역연습용으로 유지). t2r_build.py: cards에 `steps=e.get('steps')` 배선 + gramHtml에 `g.steps`면 `.gsteps`(각 `.gstep`=`.gstep-u` 쓰임줄 + `.ex` 예문) 분기 + CSS. 다른 항목은 steps=None이라 기존 경로.

**★문법 모음 링크 전수 수리(2026-08-06, 커밋 8bc6f54)★** — 유형02 「문법 모음 보기」 98링크 깨짐: 모음 빌더가 t2r_gram.py(G1)만 합치는데 build2 화면은 **t2r_gram2.G(=G1+G2 덧붙임)** 를 씀. 수리: 빌더에 G2 합류 — 새 34형태 항목 + 「~/-」 표기만 다른 12형태는 본짝 항목에 **별칭 앵커**(빈 span id, `.galias` scroll-margin + `:has(.galias:target)` 강조 — 중복 항목 방지). 871→905형태. **감사기 `t2r_gramlink_audit.py`**(읽기 18 전수: ENRICH gram x[0] + ITEMS gram title → `g_`+공백→`_` 앵커 대조, 깨짐 110→0) — gram/gram2/enrich gram 형태를 고치면 grammod_build 재실행 후 감사기도 돌릴 것. 함정①: 유형04 「도표 설명 틀」은 렌더러(_gramRest 필터)가 걸러 **링크 자체가 화면에 없음** — 데이터만 보고 깨졌다고 오판 말 것. ★함정②(OneDrive): 다른 기기/세션 산출물이 몇 분 늦게 도착해 **파일이 작업 도중 발밑에서 바뀜** — 커밋 직전 status 재확인, 병렬 세션 여부 mtime으로 확인, 커밋은 내 몫 파일만 골라서.★

**★글쓰기 표현 체계(2026-08-06 사용자 제안, 커밋 7f63ed6·898c59b·8fc458a)★** — ①문법 모음 주제 2호 「글쓰기 표현」 22행 8묶음(쓰기 53·54 대비, 행마다 모범 문장 .thex + 행 앵커 th_writing_N) ②쓰기 51~54 페이지에 진입 링크 ③★읽기 18유형 해설(sol) 맨 위에 스니펫(wexpr-tts, HTML+빌더 양쪽·교체형 멱등): 지문에 나온 표현 칩(활용 변형·ㅁ받침 판별 감지, 최대 5) + 🔊지문 듣기 TTS(영어 글로스 제거한 한국어만) — 해설이 정답 후에만 열려 게이팅 자동★. 함정: ★렌더된 DOM에선 .psg가 .q 안(정적 템플릿 검사로는 밖으로 보임) — 지문 연결은 「문항 안 psg 우선, 없으면 다른 문항 소속 아닌 앞쪽 psg」★. 검증은 Playwright 전수 스윕(18파일 오류 0·바 450·칩 249). 유형03·05·10(그림·조각·제목)과 유형15 비공개 4문항은 psg 구조가 아니라 범위 밖. ★음성 승격(988e7b7→c7f437f): ①브라우저 TTS는 음성 자동 선택(Natural>Google>온라인>로컬)+문장 단위 끊어 읽기 ②**edge-tts(ko-KR-SunHiNeural, 무료)로 지문 477개(87k자) MP3 사전 생성 — 읽기 18/18 전 유형**(62d82b4: psgOf가 .psg 외에 .adbox(유형03 광고)·.headline(유형10 제목)·유형05는 해설의 바른 차례 ordline 묶음까지, koText가 (가)~(라)·번호 표지를 걷어냄) — 「토픽2 읽기 지문 소리/wx_<djb2해시>.mp3」, 열쇠=koText의 djb2(파이썬·JS 동일 공식), 버튼은 MP3 우선·TTS 폴백. 지문 개정 시 t2_tts_extract→t2_tts_gen 재실행(있는 파일 건너뜀). 70MB, 저장소 커밋(음원 커밋이 이 저장소 정책 — git ls-files grep은 따옴표 함정 주의). 제외는 유형15 저작권 비공개 4문항뿐. **문장별 듣기(f971322, 사용자: 번역 연습 말고 해설 지문에)**: 유형06·11 「해설(지문을 한 문장씩)」의 각 trko 문장 옆 인라인 🔊(.wxs2) — 문장 단위 MP3 349개 추가(총 826·102k자). ★함정: koText 정리 규칙을 나중에 바꾸면 옛 해시 MP3가 고아가 된다(유형05 36개 겪음) — 규칙 변경 시 재추출→재생성→매니페스트 밖 파일 삭제까지★

**★주제별 어휘(2026-08-06 사용자 제안, 41773ce)★** — 「토픽2 주제별 어휘 _ 클로드.html」: 세밀 분류 사전(주제→갈래(구별 기준)→보기 단어→연관어 — 미생물: 바이러스/세균/진균→치료제, 천체: 항성/행성/위성). 21주제·67분류·301단어, 데이터 t2r_theme_vocab.json+빌더 t2r_themevocab_build.py(주제 추가=JSON+재실행). 감정 주제는 읽기 23~24 심정 유형 대비.

[[topik2-ilkgi-copyright]] [[topik2-munche-vocab]] [[haeseol-adj-english-gloss]] [[haeseol-gram-relevance]]

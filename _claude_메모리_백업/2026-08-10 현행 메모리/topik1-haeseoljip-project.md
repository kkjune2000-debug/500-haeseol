---
name: topik1-haeseoljip-project
description: TOPIK1 해설집 제작 — 구조·학습방식·언어 등 핵심 설계 결정
metadata: 
  node_type: memory
  type: project
  originSessionId: 4d9a6186-e5c5-4657-88ba-673917915819
---

작업 폴더: `D:\OneDrive\놀라운 한국어 2026\놀라운 한국어 토픽1 해설 _ 클로드` (읽기/듣기 해설 하위폴더). 출력은 자기완결형 인터랙티브 HTML.

**핵심 구조 — 회차별이 아니라 "문제 유형별" 해설집.** 예: "화제 고르기" 유형 = 여러 회차(35·36·37·41·47·52·60·64·83·91·96·102)의 31~33번을 한데 모음. 읽기·듣기 모두. 목적: 저급 학습자가 한 유형씩 step-by-step 정복.

**필수 규칙:**
- 지문·선택지는 기출 **원문 그대로** (변형 금지). ⚠️ **지시문도 회차마다 다르면 회차별로 저장**할 것 — 57~60번은 구형(35~64회)/신형(83~102회)이 갈린다. 상세 [[topik1-pdf-source-status]].
- 학습 방식: 학생이 답을 고른다 → 틀리면 **맞을 때까지 재시도** → 맞으면 해설 공개 (배치 채점/타이머 아님).
- ★**해설은 「📖 해설 보기」 버튼을 눌러야 열린다 — 정답만으로는 안 열린다**★ (2026-07-17 사용자 지적). `.solved`(정답: 배지·초록·지문영어)와 `.revealed`(해설 블록)를 **반드시 분리**하고, `resetOne` 은 둘 다 지운다. **왜:** 유형06·08처럼 **한 지문 두 문제**면 앞 문항 해설이 지문 흐름·어휘 영어를 다 풀어 놓아 뒤 문항이 거저 풀려 **평가가 안 된다**. ⚠️ 내가 유형07에서 이 관례를 깼다가(`.q.solved .sol{display:block}`) 지적받아 고침 — 유형01~06은 원래부터 버튼 방식. **2026-07-17 기준 8파일 전부 통일.** 새 유형 만들 때 `.q.solved .sol{display:block` 이 있으면 위반이다.
- 해설 언어: **한국어 + 영어 병기** (2026-06-25 골든샘플에서 확정). 지문·해설·오답분석·어휘·문법 모두 한국어 줄 아래에 영어 해석(`.en` / `solEn`·`whyEn`·`passageEn` 등). 쉬운 한국어로 풀이하되 영어 해석을 함께 단다. ※ 이전 "한국어만" 규칙은 폐기됨.

**완성된 골든샘플 (전부 `토픽1 읽기 해설\` 폴더, CSS/JS 동일 템플릿):**
- 유형01 `…유형01 화제 고르기 (31~33) _ 클로드.html` — 12회차 36문항.
- 유형02 `…유형02 빈칸 채우기 (34~39) _ 클로드.html` — 12회차 **72문항**(6문항×12), 2026-06-26 완성. 배점 34~36=2점·37~38=3점·39=2점. passage에 `{}` 빈칸 마커 → 정답 맞히면 초록색으로 채워짐.
- 유형03 `…유형03 실용문 불일치 (40~42) _ 클로드.html` — 12회차 **36문항**(3문항×12), 2026-06-27 완성, 전부 3점. "다음을 읽고 맞지 **않는** 것을 고르십시오"형. 빈칸 로직 제거, 대신 `doc` 속성에 실용문 그래픽을 CSS로 재현(`.sy`/`.sy-phone` 휴대폰메시지·`.sy-chat` 다중말풍선·`.sytbl` 표(달력/편성표/건물)·`.sy-wx` 날씨·`.sy-park` 공원지도·`.sy-note` 광고). 선택지는 한 줄 그리드, optsEn(문장영어)는 reveal 시 표시. why는 4개 모두 ✓일치/✗불일치 근거, 정답(불일치) 항목에 `.ok` + CSS `::after`로 "← 맞지 않는 문장(정답)" 자동 표기. **(2026-07-06 오답분석 개선)** why 4개 나열(맞습니다/다릅니다) 대신, 정답(불일치) 문장의 **틀린 어휘에 빨간 취소선(`.mis-x`) + 맞는 어휘(`.mis-ok`) 교정 문장(→)** 제시. 데이터=`const MIS`(36개, `{x:틀린어휘, fix:맞는어휘|null, note, noteEn}`) + `buildMis(id,q)` 렌더(`q.opts[ans-1]`에서 x를 fix로 치환해 교정문장 생성; x는 정답문장의 실제 부분문자열이어야 함). fix=null(지문에 없는 내용형 4개: 36_41·37_42·64_42·83_42)은 "지문에 없는 내용입니다". 맞은 3문장은 `①②④ 는 지문과 같습니다 ✓` 한 줄만(영어는 보기 optsEn에 이미 있어 중복 안 함). 템플릿 `<ul class="why">`→`${misHtml}`, h4 "해설"→"왜 맞지 않을까요?". why/whyEn 데이터는 남겨둠(MIS 없을 때 `buildMis` 폴백). CSS `.mis-*`. 화살표는 `→`(‑>아님). 헤드리스 Edge 스크린샷 검증. **(2026-07-06 최종 검토 완료)** 36개 교정을 지문과 전수 대조: 35개 정확·1개 오류 수정. 오류=**64_42**(초대 메시지) — 종전 `fix:"미영 씨를 초대합니다"`가 주어(마이클)에 붙어 "마이클이 미영을 초대"로 역전됨 → **`fix=null`로 통일**(실제는 미영이 마이클을 초대, 함께 있지 않음; 83_42와 동일 처리). 노트도 "미영 씨가 마이클 씨를 초대하는 메시지입니다. 아직 함께 있지 않습니다."로 교정. **q.sol 산문 문단(mis 블록 위)은 삭제**(mis 블록이 자기완결적이라 중복; 사용자 결정) — 템플릿에서 `${q.sol?...}` 렌더 줄만 제거하고 `sol`/`solEn` 데이터는 SETS에 보존(폴백용). 잔여 경미사항(미적용, 사용자에게 선택사항으로 보고): 37_42(점심=표에 빈칸이라 '없는 내용'보다 '점심엔 안 먹음'이 정밀)·96_42(교정 '아직 안 샀습니다' vs 지문 '못 샀어요'). 헤드리스 Edge로 64_42=nostate+새 노트·전 문항 산문<p> 제거 검증. 처리 `scratchpad/patch_mis_final.py`, 백업 `scratchpad/bak_유형03_premis.html`.
- 유형04 `…유형04 내용 일치 (43~45) _ 클로드.html` — 12회차 **36문항**(3문항×12), 2026-06-28 완성. "다음의 내용과 같은 것을 고르십시오"형(맞는 것 1개 고르기, 유형03의 반전). 배점 43·45=3점, 44=2점(`PT` 상수). `doc` 대신 텍스트 지문 `passage`/`passageEn`(CSS `.psg` 주황박스). `.why li.ok::after`="← 내용과 같은 문장 (정답)". 정답: 35=①②② 36=③②④ 37=①②③ 41=①③③ 47=④③② 52=④①④ 60=①④④ 64=②①② 83=③②③ 91=①②③ 96=②①② 102=④①③ (전수 교차검증). GRAM 22종(순서고·아어서·빈도부사·희망·계획·부정·가능·최상급·으러가다·으려고·에게/께·에서·로/으로·와과·지만(링크없음)·는데·명사형·현재진행·존댓말·종별사·마다/때·동안), EXPAND 11종(시간때·때시점·요일·장소·음식·가족·취미·교통수단·날씨·언어·감정). ⚠️ 60회는 스캔 최저화질이라 1차 판독서 43①(재미있↔재미없)·44①('오늘' 누락) 오독→네이티브 임베디드(793×1122) 크롭·PIL 선명화로 정정. **(2026-07-07 오답분석 mis 개편, 사용자 지시)** 유형03과 동일한 **틀린 부분 빨간 취소선(`.mis-x`)+교정 문장(`.mis-ok`, →)** 방식을 유형04에 적용(유형03의 반전: 03=오답 1개 표시, 04=**정답 1개 `.mis.ok4` 초록+✓ "내용과 같은 문장 (정답)" + 오답 3개 각각 교정**). 데이터 `const MIS4={id:{"2":{x,fix,note,noteEn},"3":..,"4":..}}`(정답 제외 오답 번호만; 108개, x는 오답 문장 실제 부분문자열—사전검증 통과; fix=null 1개=35_44①"수학을 좋아합니다"). 순서/주어/시제 오류는 술어 통째 교체(예 "아침을 먹고 학교에 갑니다"→"학교에 가서 아침을 먹습니다"), 단어 오류는 단어만(학생 식당→커피숍). `buildMis4(id,q)`가 4개 옵션 순회(정답=ok4행, 오답=x취소선+fix교정+note). 템플릿 `<ul class="why">`→`${misHtml}`, h4 "해설"→"왜 맞고, 왜 틀릴까요?", q.sol(정답 근거)은 **유지**. CSS `.mis-*`는 유형03에서 이식+`.mis.ok4`/`.mis-ansmark` 추가. **⚠️ ul.why 사라져 문법 재배치 IIFE 폴백(유형03과 동일: 어휘 h4 앞) 적용**(20문법문항 순서 검증). why/whyEn 데이터는 남겨둠(MIS4 없을 때 buildMis4 폴백). 헤드리스 Edge 검증: mis 144·정답 36·취소선 108·문법위치 20/20·JS오류 0. **유형05(중심 생각)는 미적용**(오답이 세부사실·없는내용형이라 "틀린 부분" 취소선이 부적합; 사용자 결정). 처리 `scratchpad/build_mis4.py`(작성·검증)·`apply_mis4.py`.
- 유형05 `…유형05 중심 생각 (46~48) _ 클로드.html` — 12회차 **36문항**, 2026-06-28 완성. "다음을 읽고 중심 생각을 고르십시오"형(글쓴이가 가장 하고 싶은 말 1개 고르기). 배점 46·47=3점, 48=2점. 유형04와 동일 템플릿(`.psg`, 정답 1개 고르기)이나 라벨만 "중심 생각"으로: `.why li.ok::after`="← 중심 생각 (정답)", 정답해설="중심 생각입니다"/오답="중심 생각이 아닙니다"(세부사실·없는내용·반대). 정답: 35=④③③ 36=②①① 37=④③④ 41=②④② 47=②③① 52=②③③ 60=③①② 64=③④③ 83=④④③ 91=①①② 96=②①③ 102=②④③. 46~48은 PDF page idx 6. 신규 GRAM: 소망 '-(으)면 좋겠다'(3 150~156), 수혜 '-아/어 주다'(링크없음), 원인 '때문에'(3 307~313 …(2)), 부사형 '-게'(3 388~396). EXPAND 10종(취미·감정·때시점·장소·성격·직업·의복·가족·계절·음식).
- 유형06 `…유형06 한 지문 두 문제 (49~56) _ 클로드.html` — **✅ 완성(2026-07-06) — 12회차(35·36·37·41·47·52·60·64·83·91·96·102) 48지문 96문항.** 프로토타입(35·37회 16문항)에서 10회차 확장. **새 구조=한 지문 공유 + 2문항**: 데이터 `SETS[].blocks[]`(각 `{n1,n2,q2type,passage,passageEn,q1,q2}`), q1=빈칸(㉠ `{}` 마커, 정답 시 초록 채움+영어 `( ⋯ )` 동시 채움 `fillBlank`/`data-fill`), q2=`match`(내용 같은 것)·`topic`(무엇에 대한 이야기) 분기(ask·`::after`·`FB`·`ansTagMap`). **정답 전 12회차 공식 정답표 교차검증**(텍스트 회차 36·41·52는 get_text, 47·이미지 회차는 크롭 렌더/OCR 판독). **확장 방법:** 텍스트 추출 회차(36·41·47·52) 4개 먼저(fitz get_text), 이미지 스캔 6회차(60·64·83·91·96·102)는 **회차당 서브에이전트 1명**(general-purpose, 빈 이미지예산)에게 렌더+시각판독으로 원문 verbatim 추출 위임(OCR은 초안용, 육안 확정). 영어 지문·해설·오답분석·어휘는 내가 규칙 준수해 작성. 삽입은 python(`scratchpad/insert_36.py`·`insert_sets.py`·`img_A~D`). **신규 GRAM 11종 추가**(jogeon_myeon·geona·ttae·bigyo_boda·gosipda·hue·jeon_e·ttaemune·go_yeon·hyeonjaejinhaeng·myeonseo — 전부 놀라운500 레슨 링크 실재 확인), **신규 EXPAND**(교통수단·음식·신체·감정·운동·동물·자연·취미). 문법 문항 34개는 대부분 빈칸이 연결어미·문형 고르기라 문법 관계 있음. **전 96문항 headless Edge 검증**(정답 96/96·빈칸채움 48·문법위치 34·번역카드 48·게이팅).
- 유형07 `…유형07 순서 배열 (57~58) _ 클로드.html` — **✅ 완성(2026-07-17) — 12회차 24문항**(57=3점·58=2점, 단 35회만 57=2점·58=3점). "다음을 순서대로 맞게 나열한 것을 고르십시오". 뼈대는 복제 않고 **새로 씀**(사용자 지시). **설계 3가지 ★유형08·09도 따를 것★:** ① **지문을 저장하지 않음** — `passageKo(q)=opts[ans-1]` 순서로 `parts`를 이어 만듦(`orderOf`/`passageKo`/`passageEn`) → parts·ans를 고쳐도 어긋날 수 없음. 번역 연습도 이 정답순서 문단을 씀(조각 1개=1문장이라 자연히 4줄). ② **`steps` 소거 표** — `{clue,clueEn,fix,test}`, `test:o=>o.indexOf('라')<o.indexOf('다')` 식. 렌더러 `stepTable()`이 test를 선택지에 **실제로 적용해 남는 것을 계산**(`.keep`/`.gone`) → **남는 선택지를 손으로 적지 말 것**. 정답 하나로 안 좁혀지면 빨간 `.stepbad` 경고가 화면에 뜸 = 해설 결함 자동 검출. ③ **오답분석(`why[]`) 없음** — 선택지가 독립 주장이 아니라 같은 조각의 **순열**이라 단서가 정답을 확정하면 나머지는 자동 탈락. **⚠️ 「첫 글자 고정」 함정:** 24문항 중 다수가 선택지 4개 모두 같은 조각으로 시작 → "첫 문장 찾기" 단서는 **0개를 소거**함. 진짜 승부는 나머지 3개 순서. **접속사 24문항 전수조사:** 지시어 19회(1위) > 그래서 7 > 그런데 6 > 또·그리고 4 > 하지만·그렇지만 3 > 왜냐하면 1. **그러므로·따라서·그러나 = 0회**(가르치지 말 것). 빌드는 검증본 `data57.py`에서 parts/opts/ans를 뽑아 JS 생성(원문 재입력 없음). 상세=프로젝트 폴더 `_인수인계 (HANDOVER) 2026-07-17.md` §5.
- 유형08 `…유형08 문장 넣기 (59~60)…` — **✅ 완성(2026-07-17) · 12회차 24문항**(59=문장 넣기 2점, 60=내용 일치 3점). **유형06 ∩ 유형07**: 유형06에서 `blocks{q1,q2}`·`.block.done` 지문영어 게이팅·MIS·문항별 어휘를, 유형07에서 `steps` 소거 표·지문 조립·번역연습을 가져옴. 새로 만든 건 **슬롯 UI** 뿐. 지문은 `segs`(빈칸 기준 조각 4)+`q1.ans` 자리에 `sent`를 끼워 조립(완성형 저장 안 함). ⚠️**빈칸은 문장마다 있는 게 아니다** — 조각 하나에 문장 둘일 수 있음(96·102회). `steps`의 `test` 인자는 빈칸 위치 0~3(`AFTER(k)`=`p=>p>=k`, `BEFORE(k)`=`p=>p<k`) — 앞·뒤 양쪽에서 조여져 유형07보다 깔끔. 59 정답 시 문장이 초록으로 채워짐(`.block.ins-done`). 문법 박스 9회차만(36·52·102는 단서가 어휘 연결이라 없음), 📘 링크는 `때문에` 1개뿐이 정상.
- 유형09 `…유형09 빈칸 채우기 (61~62)…` — **✅ 완성(2026-07-17) · 12회차 24문항**(61=㉠ 빈칸, 62=내용 일치, **둘 다 2점** — 배점이 세트 헤더에 「각 2점」). ★**유형07/08이 아니라 유형06을 뼈대로**★ — 61번 선택지가 **서로 독립된 낱말**(어휘·어미·접속사)이라 소거 표가 안 맞음. `why`(선택지별 판정 4개)+MIS+**`fillBlank`**. 빈칸은 **한국어·영어 지문 양쪽에 `{}` 마커**를 두고 함께 채움(`.blank`/`.benb`); **영어의 `{}` 위치는 한국어와 달라도 됨**(어순). 문법 박스 8회차만(41·64·83·102는 네 선택지의 문법이 모두 같은 **어휘 문항**이라 없음). ⚠️91회는 빈칸이 지문 맨 끝(post가 마침표뿐).
- ★**선택지의 성격이 방식을 정한다**★: 선택지가 **순열·위치**(조각 배열·㉠~㉣ 자리)면 **`steps` 소거 표**(07·08), **서로 독립된 낱말**이면 **`why` 판정+`fillBlank`**(06·09). 새 유형은 이것부터 판단할 것.
- **다음=유형10(63~64, 목적+내용일치 24문항)**, 이후 11(65~66)·12(67~68)·13(69~70) 각 24문항, 그 다음 듣기. 63~64는 유형09를 뼈대로 하되 q1이 빈칸이 아니라 **글의 목적**이라 `fillBlank` 불필요 — **먼저 원본(PDF idx 14 추정)으로 지시문·배점·구조 확인할 것.**
- 유형02 어휘 박스 보강(2026-06-30): **품사별 종합 참고 박스 체계** — 형용사 반대말(중복 3박스→1통합, 22쌍, 뜻 비슷한 것끼리)·동사 반대말(8쌍)·부사(예문 .cmp표)·기본 명사(16)·하다 형용사(16). 신규 지내다·서로·안경(한자 없이 안=눈/경=유리)·가다오다 GRAM. anida 링크 이다→부정법(`4 부록 2 부정법.html`). **형용사 반대말 매칭 퀴즈**(인터랙티브, 35회 37번 해설 안 박스 아래·정답 후에만 보임): `EXPAND["형용사 반대말"].words` `" ↔ "` 파싱→무작위 6쌍 클릭 매칭, CSS `.quizcard`/`.qz-*`+JS `qzNew`/`qzPick`+렌더 슬롯 `adjquiz-slot`.
**실용문(40~42) 본문은 그래픽이라 텍스트레이어 회차(35·36·37·41·47·52)도 `get_text()`로 선택지만 추출되고 지문은 PNG 판독 필수.** 40번=페이지 idx3, 41·42번=idx4. 검증된 40·41·42 정답: 35=③②④ 36=①②② 37=②③① 41=②①③ 47=④④③ 52=①④④ 60=①①② 64=③②② 83=④①④ 91=④①④ 96=②②③ 102=②③②.

**정답은 전부 12회차 공식 정답표(NIIED) 교차검증함.** ⚠️ 정답표 **텍스트 추출(`get_text()`) 회차 35·36·37·41·52는 반드시 직접 떠서 확인할 것** — 서브에이전트가 이미지로 렌더해 읽으면 표 행이 어긋나 37·41회를 오독했음(텍스트 추출은 정확). 이미지스캔 정답표(47·60·64·83·91·96·102)는 크롭 렌더 판독 OK. **이미지 판독 주의:** 한 대화에 PNG를 ~30장 넘게 Read하면 이후 모든 이미지가 "exceeds 2000px"라며 거부됨(실제론 누적 이미지 예산 초과) → 추가 판독은 **새 서브에이전트**(빈 컨텍스트)에 맡기고, 이미지는 1장씩·1500px 이하로.

**설계 결정 (유형02에서):** 회차별 `<보기>` 박스는 **넣지 않음**(형식 예시일 뿐이고 회차마다 비슷해 OCR 혼동·지면 낭비). 대신 상단 "이렇게 풀어요" 카드에 대표 예시 1개만. 빈칸 유형은 조사(이/가·을/를·에·도·의·만·와과·에게·에서) 1문항 + 어휘(명사·동사·형용사·부사) 5문항 구성이 보통.

**반복 해설 접기(2026-06-26):** 같은 어휘 확장·문법이 반복되면 **첫 등장만 전체, 이후는 `<details class="dup">`로 접어** 표시(클릭하면 그 자리에서 펼침, 페이지 이동 없음). 렌더 상태 `seenExp`/`seenGram`로 추적, 접힌 요약에 "↑ N회 M번에서 공부했습니다 · 펼치기"(격식체; 2026-07-06 "봤습니다"→"공부했습니다" 학습지향 문구로 변경). 두 파일 합쳐 124개 블록 절약. expandBlock은 `(key, ctxLabel)` 시그니처. CSS는 `details.dup`.

**어휘 확장 패턴:** ① 분류형(음식·가족·장소…), ② 가족은 친가/외가 색구분 **가계도**(`.ftree`, FAMILY_TREE 상수), ③ **형태소 쪼개기**(예: 주말=주+말, 같은 ‘말’이 월말·연말). 표시 제목은 `ex.title`로 덮어쓰기 가능(예: 값→"값(=가격)"). ④ **반대말(↔) 전용 카드 디자인(2026-07-04)** — `words`가 전부 `" ↔ "` 쌍인 블록(형용사 반대말·동사 반대말)은 flat 칩 대신 **카드 그리드**로 렌더: 각 카드 = `한국어(굵게 위) / 영어(회색 이탤릭 아래)` 두 칸 + 가운데 **주황 ↔ 배지**(`.antcard`/`.antside`/`.antx`, 컨테이너 `.ewords.antgrid`=**3열 고정** `repeat(3,1fr)`, 셀 여유롭게 padding 13/14·글씨 k 1rem, `.antcard{min-width:0}`로 그리드 오버플로 방지, 640px↓2열·420px↓1열). `expandBlock`이 `ex.words.every(w=>w[0] includes " ↔ ")`로 자동 감지(비반대말 블록은 기존 칩 유지). 반대말 블록 있는 파일=유형01·02만 적용, 새 유형이 반대말 EXPAND 추가하면 자동 적용됨. 헤드리스 Edge로 22쌍·8쌍 렌더 검증. 형태소 쪼개기 주의 — **한자는 절대 넣지 않는다**(다국어 번역 대상이라 비CJK 학습자에게 장벽), **모든 단어가 아니라 꼭 필요하고 자주 쓰는 핵심 단어에만 선별** 적용. 해설은 [[haeseol-gyeoksikche]] 격식체 규칙 준수.

**공통 UI 규칙 2건(2026-07-03, 유형01~06 전 파일 적용·새 유형도 준수):** ① **문항 라벨** `.q-no`는 `회차회 번호`(예 `47회 40`) — 종전 `47-40` 하이픈형에서 사용자 지시로 변경. 렌더는 `${set.hoe}회 ${q.no}`(유형06은 `${hoe}`). **내부 DOM id `hoe+'_'+q.no`(언더스코어)는 불변**(pick/reveal 로직). ② **접기 요약(dup fold) 영어 병기** — `seenExp`/`seenGram`을 `{ko,en}` 객체로 저장(`ctxLabelEn=\`Round ${hoe} Q${no}\``), 접기 summary에 `.en` 줄(어휘=More related words · studied earlier at Round N QM · click to expand, 문법=Key grammar · …). expandBlock 시그니처 `(key,ctxLabel,ctxLabelEn)`.

**UI 문자열도 전부 한·영 병기(2026-07-04, 유형01~06 적용·새 유형 준수):** 정적 HTML(`.en` 줄)뿐 아니라 **JS로 찍는 동적 상태 메시지·버튼**도 한국어 아래 영어 병기. 대상=① 문항 피드백(`fb` `✅ 정답입니다…`/`❌ 다시…`, 유형06은 `FB{blank/match/topic}.ok·no`), ② 플래시 퀴즈 상태(`🎉 모두 맞혔습니다`/`✅ 짝을 찾았습니다`/`✅ 반대말 짝`/`❌ 짝이 아닙니다`/`❌ 반대말이 아닙니다`), ③ 문항별 `↻ 다시 풀기` 버튼. **핵심 기법: `el.textContent='…'`로 찍던 걸 `el.innerHTML='…<span class="en">…</span>'`로 바꿔야** 영어가 아래줄로 렌더됨(`.en`은 display:block). 상태색(`.qzmsg.ok/.bad`, `.feedback.ok/.try`)은 부모에 걸려 유지되고 `.en`은 자체 회색 이탤릭. 이미 병기된 것=상단 `전체 다시 풀기/Reset all`, `풀어 보십시오 · Try it`, `새로 섞기 · New set`, 유형02 피드백 2줄. **새 유형은 처음부터 innerHTML+span으로 작성.** 처리 `scratchpad/sweep_ui_en.py`·`sweep_feedback_en.py`.

**유형03 전면 감사(2026-07-03, 결함 0):** 결정적(정답 12회차 공식표 일치·36문항·한자 0·해요체 정당예외·링크 18실재) + 의미 감사(멀티에이전트 워크플로 12회차 병렬+적대적 검증, opus-4-8: 정답논리·해설·번역·격식체·문법박스 관계) 모두 통과. 유형03은 위 UI 2건 외 내용 변경 없음.

**재사용 플래시 매칭 카드 팩토리(2026-07-03, 유형02):** 어휘 암기 자가테스트용 **한국어↔영어 뜻 매칭 카드**. 기존 형용사(35_37)·명사(36_34) 퀴즈는 전역 id/함수라 1개뿐 → **`makeFlashQuiz(slotId,title,titleEn,pairs,round)`** 팩토리로 다중 인스턴스화(클로저 상태·이벤트위임·기존 `.quizcard/.qz-*/.nq-en` CSS 재사용). 47회 38번에 어휘 카드(q.vocab+선택지 T[], 8쌍)·어휘확장 카드(EXPAND[exMore].words, `" ↔ "` 반대말형 제외)를 **둘 다 ‘어휘 확장’ 박스 밑에 나란히** 붙임(사용자 지시; 렌더 슬롯 `vocabquiz-47_38`·`expandquiz-47_38`가 `${expandHtml}` 뒤에, .sol 안이라 해설 열어야 보임). 셀 넘침 방지: `.qz-grid` minmax 86→116px + `.qz-card`에 `overflow-wrap:anywhere;min-width:0`(전 퀴즈 공통). **다른 문항엔 slot div + `makeFlashQuiz` 한 줄로 추가 가능.** 레이아웃(2026-07-03 개정): makeFlashQuiz는 단일 그리드가 아니라 **좌(한국어)·우(영어) 2열**(`.qz-match`/`.qz-col`, 열마다 독립 셔플, 헤더 한국어/English), **파스텔 배경**(`.qz-card.pk-ko`=연파랑 #eaf3ff, `.pk-en`=연분홍 #fdeef4 — sel/done/bad 상태색보다 CSS 앞에 둬 상태색이 덮음). **반대말/명사 매칭 퀴즈(단일 `.qz-grid`) 셀 파스텔(2026-07-04):** 흰 셀이 밋밋해 3색 파스텔(`.pc0`연파랑·`.pc1`연분홍·`.pc2`연보라)을 **위치 `i%3`으로 회전** 적용(qzNew/vqNew/nqNew 렌더에 `pc${i%3}`). ⚠️ **색은 반드시 위치 기준·3색 이하**로 — 색 수가 열 수와 같아지면 색=열이 되어 같은 열의 정답 쌍이 같은 색으로 드러남(초기 i%5 실패). 3색이면 색 그룹이 4장 이상이라 쌍이 노출 안 됨. 상태색(sel/done/bad)이 덮도록 pc는 그 앞에 정의. pick 로직: 같은 열(같은 lang) 카드 클릭 시 오답 아니라 선택만 이동. 형용사(35_37)·**동사 반대말(35_36)** 퀴즈는 여전히 단일 `.qz-grid` 전용 슬롯 방식(반대말=한국어↔한국어라 2열 분리 불가). **⚠️ 명사(36_34)는 단일그리드→2열로 전환됨(2026-07-05):** 사용자가 "기본 명사 매칭 카드에서 한국어와 영어를 분리하라" 지시 → 명사 전용 `nqNew/nqPick` 단일그리드 IIFE를 **`makeFlashQuiz('nounquiz-slot','기본 명사 매칭 카드','Word ↔ meaning',EXPAND["기본 명사"].words,8)` 호출로 교체**(2열 분리). 명사는 한국어↔영어라 makeFlashQuiz로 대체 가능(반대말과 다름). **중복 주의:** 36_34는 `exMore:["기본 명사"]`라 일반 와이어링이 자동 ‘어휘 확장 매칭 카드 — 기본 명사’(2열)를 이미 만들어 **같은 16단어가 2장 겹쳤음** → 일반 와이어링 `var seenExpCard={"기본 명사":1}`로 시드해 자동카드 억제(chips 박스는 템플릿 렌더라 유지). 결과 36_34=어휘 매칭 + 기본 명사 매칭(2열) 1장씩. `nqNew/nqPick` 완전 제거. 백업 `scratchpad/bak_noun2col/`. ⚠️ **반대말형 어휘확장(형용사·동사 반대말)은 일반 와이어링이 `" ↔ "` 항목을 스킵**하므로 자동 ‘어휘 확장 매칭 카드’가 안 생김 → 그 문항엔 **전용 반대말 매칭 퀴즈**(`${id==='XX_YY'?'<div id="...-slot">':''}` 렌더 슬롯 + 전용 IIFE, `EXPAND[키].words`의 `" ↔ "` split)를 붙여야 함. 동사=`vqNew/vqPick`·`#verbquiz-slot`. **전용 슬롯 퀴즈 3개(35_36 동사·35_37 형용사·36_34 명사) 모두 사용자 요청으로 `어휘 매칭 카드 → 전용 퀴즈` 순서**: 일반 와이어링이 vocab `__vq`를 핵심문법 앞(없으면 맨끝)에 넣은 뒤, **마지막 IIFE가 `['verbquiz-slot','adjquiz-slot','nounquiz-slot']`을 다시 핵심문법 앵커 앞(없으면 appendChild=맨끝)으로 옮겨** vocab 뒤에 오게 함. 36_34는 핵심문법 없음(GRAMMAP 미등록)이라 appendChild 폴백으로 처리됨. 헤드리스 Edge `--dump-dom`으로 3문항 순서 검증함(`msedge --headless=new --dump-dom`). **전 파일·전 문항 확대(2026-07-03):** 어휘·어휘 확장 매칭 카드를 6개 파일 모든 문항에 부착. 후처리(DOM) 방식(렌더 미변경): `</script>` 앞에 팩토리+CSS 이식, 와이어링 IIFE가 `SETS`를 돌며 `getElementById('q_'+id).querySelector('.sol')`에 삽입(`gramAnchor`로 핵심문법 앞=어휘확장 밑). 어휘=`q.vocab`(유형02만 선택지 T[] 추가), 확장=`EXPAND[exMore].words` 첫 등장만(중복방지, 반대말·note형 스킵). **유형01만** 확장키에 정답 주제어(`q.opts[q.ans-1]`) 포함. 06은 blocks(q1·q2). 유형02 옛 47_38 전용 슬롯·IIFE는 제거·통합. 백업 스크래치패드 `bak_cards/`. 새 유형엔 items/blocks 노멀라이즈만 맞춰 와이어링 붙이면 됨.

**핵심 문법 위치 = 해설(why) 바로 뒤 (2026-07-06, 유형01~06 전 파일):** 사용자 결정으로 해설 카드 순서를 **해설(정답근거·오답) → 핵심 문법 → 어휘 → 어휘 확장 → 매칭·쓰기 카드**로 변경(문법 박스가 있으면 곧 문법 문제이므로 정답근거 옆에 둠; 어휘형 문항은 문법박스 없어 무관). **⚠️ 구현: 렌더 순서 ≠ 템플릿 순서.** 템플릿의 `${gramHtml}`는 그대로 **맨끝**에 둠 — 어휘·어휘확장·쓰기·전용퀴즈 와이어링 IIFE 3개가 '핵심 문법'을 **맨끝 경계 앵커**로 삼아 그 앞에 카드를 넣기 때문(앵커 보존해야 카드 배치 안 깨짐). 대신 **스크립트 맨끝(`</script>` 직전)에 새 IIFE 추가**: 모든 카드 배치가 끝난 뒤 각 `.sol`에서 핵심 문법 블록(h4 + `.gram` div, 또는 dup `details` — 카드 삽입 후 .sol의 꼬리)을 통째로 `ul.why` 바로 뒤로 옮김(gi=문법시작 인덱스, grp=gi~끝 스냅샷, `insertBefore(g, why.nextSibling)`). 문법 없으면(gi<0) skip. **⚠️ 유형03 예외 수정(2026-07-06):** 유형03은 mis 개편으로 `ul.why`가 없어(→`${misHtml}`) 이 IIFE가 `if(!why) return`으로 빠져 **핵심 문법이 맨 아래(카드 뒤)에 방치**돼 있었음(mis 개편이 문법이동 검증 뒤에 와서 회귀). → IIFE에 폴백 추가: `ul.why` 없으면 **어휘 h4(첫 '어휘' 포함 h4) 바로 앞**을 앵커로 삼아 문법을 옮김(= 해설 뒤). 36/36 유형01과 동일 순서 확인(headless Edge). 새 유형이 why 대신 다른 해설 블록을 쓰면 이 폴백 앵커 방식을 따를 것. headless Edge `--dump-dom`(`msedge --headless=new --disable-gpu --dump-dom file:///…`)으로 213문법+19비문법 문항 순서 검증(오류 0). **템플릿에서 gramHtml을 위로 옮기지 말 것** — 카드 와이어링 앵커가 깨져 연습카드가 해설 바로 뒤로 잘못 들어감. 새 유형도 이 IIFE 유지.

**지문·문제 영어는 정답 전 숨김, 정답 후 표시 (2026-07-06, 유형01~06 전 파일):** 학생이 스스로 한국어 실력을 평가하도록, 정답 맞히기 전엔 지문·선택지에 영어 해석이 보이면 안 됨. 트리거는 **정답 즉시(`.solved`)**로 6유형 통일(사용자 결정). 선택지 영어(`.oen`)는 이미 `.q.revealed`(해설 보기) 게이팅이라 정답 전 노출 없음. **지문 영어 게이팅:** 유형01·02 = base `.pen{display:none}` + `.q.solved .passage .pen{display:block}`(종전 `.revealed`→`.solved` 변경); 유형03 = `.q:not(.solved) .docwrap .en{display:none}`(실용문 그래픽 `.sy*` 안 `.en`); 유형04·05 = `.q:not(.solved) .psg .en{display:none}`(지문이 .q 안). **유형06은 한 지문 2문항 공유**라 `.psg`가 `.block` 레벨(.q 밖) → CSS `.block:not(.done) .psg .en{display:none}` + JS: `pick()`에서 블록의 두 `.q` 모두 solved면 `.block`에 `.done` 부여, `resetOne`/`resetAll`에서 `.done` 해제. **한 문항만 풀면 공유 지문 영어가 나머지 문항 답을 노출하므로 반드시 두 문항 모두 정답 시에만 표시.** headless Edge computed-style(`getComputedStyle(...).display`)로 6유형 정답전 노출 0·정답후 표시·06 reset까지 검증. 새 유형도 이 규칙 준수(지문 영어는 정답 전 숨김).

**번역 연습 중심 개편 & 도움말 박스(2026-07-07, 사용자 지시):** ① **번역 카드 행별 정답 보기**(`.pt-rev1`)·비문장 실용문 포함 → [[topik1-writing-practice]] 참고. ② **매칭 카드 삭제(유형03~06):** 번역 연습이 주가 되므로 어휘·어휘확장 매칭 카드를 03~06에서 제거(와이어링의 `addCard(...)` 2줄 주석화; vp/egs 미사용). **유형01·02(어휘형)는 매칭 카드 유지.** 어휘 확장 단어는 칩 박스(`${expandHtml}`)로 여전히 표시. 처리 `scratchpad/item3_delcards.py`. ③ **💡 도움말 콜아웃 박스 신설(6파일 전부):** 놀라운500 레슨에 없는 문법 심화·어휘 뉘앙스·문화 설명용. 데이터 `const NOTES={"회차_번호":{ko,en}}`(빈칸이 기본, 필요한 문항에만 추가; ko/en 안 `<b>` 강조 가능), CSS `.tipnote*`(앰버/honey, 문법 박스 파랑과 구분), 렌더 IIFE가 **핵심 문법 뒤·어휘 h4 앞**에 삽입(문법 재배치 IIFE 뒤 실행, `NUM` 상수 뒤에 NOTES 정의). 데모: 유형03 `36_42`(‘드리다’=주다의 높임말). 나머지는 빈 `{}`(사용자가 채움). 처리 `scratchpad/item4_tipnote.py`. 헤드리스 Edge 검증: 03~06 매칭 0·번역 유지·칩 유지, 01·02 매칭 유지, 36_42 도움말 순서 `문법→도움말→어휘`·JS오류 0.

읽기 유형 배치(35→102회 안정): 31~33 화제 / 34~39 빈칸 / 40~42 실용문 불일치 / 43~45 내용일치 / 46~48 중심생각 / 49~ 지문형 / 57~58 순서나열 / 59~70 지문형.

기술 세부는 [[topik1-pdf-source-status]] 참고. 초기 파일럿(`_파일럿_백업…(31-39).html`)은 35회 읽기만 다룬 구버전.

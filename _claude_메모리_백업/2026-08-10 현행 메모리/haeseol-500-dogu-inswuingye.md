---
name: haeseol-500-dogu-inswuingye
description: 500문장 해설집의 검사·음원 도구와 인수인계 문서가 저장소 어디에 있는가 (2026-08-10 정비)
metadata: 
  node_type: memory
  type: project
  originSessionId: 8e3c6a78-dee4-40c4-acc4-1da5aa48e492
  modified: 2026-08-09T19:54:48.805Z
---

500문장 해설집의 도구는 이제 **저장소 안**에 있습니다. 임시 폴더에 두지 마십시오 — 세션이 끝나면 사라집니다.

- **`마스터-템플릿-적용본\_도구\`** — `check_all.py`(검사 4종 한 번에) · `check_structure/script_order/style/spec.py` · `audio_build.py`(list→make→wire) · `show_text.py` · `README.md`
  - 경로는 `_paths.py` 가 자기 위치에서 거슬러 올라가 찾습니다. **경로를 글자로 박지 마십시오** (컴마다 드라이브 문자가 다름).
  - 출력은 `_paths.stdout()`. PowerShell cp949 에서 한글이 깨집니다.
- **`_작업_인수인계.md`** (2026-08-10 전면 개정) + **`_인수인계_시작멘트.html`** — 새 컴 준비·규칙·함정 10종·남은 일.
- **`_claude_메모리_백업\2026-08-10 현행 메모리\`** — 이 메모리의 사본. 6월판 55개는 부록·숫자표·파일명 규칙이 거기에만 있어 함께 둡니다.

**무엇을 고치든 반영한 뒤 `python check_all.py`.** 모두 0건이어야 정상입니다.

★★ **2026-08-10 바로잡음 — 이 메모리에 적혀 있던 커밋이 실제로는 없었다** ★★
`6710ea0`(어휘)·`883b261`(표 재배열)·`01f6f3f`(음원)은 **이 저장소에 존재하지 않는다**(`git log --all`·`reflog` 확인). 08-06 첫 커밋 하나뿐이었고 사흘치 작업 114파일이 통째로 미커밋이었다. → `692d68b` 로 정본화했다. **커밋했다고 메모리에 적기 전에 `git log` 로 확인할 것.**

- ★`check_structure.py` 의 정답 대조는 **HEAD 기준**이라 커밋을 미루면 그물이 무뎌진다. 그때 23파일이 「정답 바뀜」으로 걸려 있었는데 값을 하나하나 대조하니 전부 의도한 변경이었다(시제 17=지운 「퀴즈 2 쓰기」가 만들던 JS 조각, 문법 5=띄어쓰기·대명사 교정, 부록 3=1개). **일 끝나면 커밋.**
- ★`마스터-템플릿-적용본\.gitignore` 의 `*.py` 에 걸려 **`_도구` 스크립트 8개와 언어판 빌더 2개가 git 에 하나도 안 들어가 있었다.** 「도구는 저장소 안에」가 반만 지켜지던 것. 두 폴더만 `!_도구/*.py`·`!_언어판 시험/*.py` 로 되살렸다. 새 도구를 넣으면 **`git status` 에 보이는지 확인할 것.**
- ★`check_spec.py` 는 **`.container` 라는 이름만** 알아서 부록 2(`.page-container` 210mm A4 낱장)·부록 8(`.sheet` 1380px)·`_음성 확인`(`.wrap` 860px)을 「폭=- · 화면 가득 퍼집니다」로 알렸고 **인수인계 문서가 그 오보를 옮겨 적었다**. 셋 다 `margin:0 auto` 로 멀쩡하다. 예외를 **폭까지 함께** `EXCEPT` 에 적어 값이 바뀌면 잡히게 고쳤다. 폭은 **맨 처음 선언**에서 읽는다(`@media print` 안에서 `width:100%` 로 다시 선언된다). **검사기가 「못 찾은 것」을 「없는 것」으로 말하게 두지 말 것.**
- 6월 1일자 OneDrive 중복 사본 `3 307~313 … 때문에 (2).html`(어휘 0·번역 슬롯 0·소리 없음)을 지웠다. 본문 폴더 **106개**(시제 21·조사 9·본문 67·부록 8·`_음성 확인`).

★ 문서에 숫자를 적을 때는 **파일에서 세십시오.** 이번에도 기억으로 적을 뻔한 둘이 틀렸습니다 — `translate="yes"` 21,916(→실제 23,867), 「`<title>` 84개 미잠금」(→제목은 전부 translate="yes" 이고 그 안의 한국어가 안 감싸인 것이 80파일). [[haeseol-hwagin-an-han-geot]]

관련: [[haeseol-500-eumseong]] [[haeseol-500-i18n]] [[haeseol-silent-failure]]

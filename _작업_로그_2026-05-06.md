# 놀라운 한국어 500 해설집 — 작업 로그 (2026-05-06)

> 진입점: 2026-05-05 세션(`_작업_로그_2026-05-05.md`)에서 v9.85 표준(exp-table Explanation 영어 단일화)까지 정형화 완료. 사용자 명시 지시: "190-198 문서화를 적용하라." → Q229·Q231에서 정형화한 룰들(영어 어순 헤더, 영어 단어만 사용, "~" placeholder 제거)을 190~198(서술어 문법 12: 경험)에 일괄 적용.

## 📑 목차

- [Part 1 — 190~198 (경험 단원) v9.85 표준 적용 — 헤더 영어화 + "~" placeholder 제거 + 주어 생략 표기 명료화](#part-1--190198-경험-단원-v985-표준-적용--헤더-영어화---placeholder-제거--주어-생략-표기-명료화)
- [Part 2 — 형성 공식 한국어 형태소 굵게 룰 + 주어 생략 표기 형식 정형화 (190~198 9개 row 일괄 적용)](#part-2--형성-공식-한국어-형태소-굵게-룰--주어-생략-표기-형식-정형화-190198-9개-row-일괄-적용)
- [Part 3 — 문장구조 + 서술어 문법 27개 파일에 v9.86-v9.87 룰 자동화 일괄 적용 (`_apply_v9_87_rules.py`)](#part-3--문장구조--서술어-문법-27개-파일에-v986-v987-룰-자동화-일괄-적용-_apply_v9_87_rulespy)
- [Part 4 — 해설 컬럼 한국어 토큰 일괄 굵게 룰 정형화 + 1~500 67개 파일 자동 적용 (`_bold_korean_in_explanations.py`)](#part-4--해설-컬럼-한국어-토큰-일괄-굵게-룰-정형화--1500-67개-파일-자동-적용-_bold_korean_in_explanationspy)
- [Part 5 — 부사어 문법 카테고리 v9.86-v9.87 헤더 룰 확산 + 자동 한국어 토큰 굵게 후속 (`_apply_v9_87_rules.py` 확장)](#part-5--부사어-문법-카테고리-v986-v987-헤더-룰-확산--자동-한국어-토큰-굵게-후속-_apply_v9_87_rulespy-확장)
- [Part 6 — 모음 어간 동사 과거형 해설 오류 일괄 수정 — 17건 (5개 파일)](#part-6--모음-어간-동사-과거형-해설-오류-일괄-수정--17건-5개-파일)
- [Part 7 — gstep-pattern accent 하얀 배경 chip 제거 (1~500 67개 파일)](#part-7--gstep-pattern-accent-하얀-배경-chip-제거-1500-67개-파일)
- [Part 8 — 한국어 음절 단위 줄바꿈 방지 — `word-break: keep-all` 일괄 적용 (1~500 67개 파일)](#part-8--한국어-음절-단위-줄바꿈-방지--word-break-keep-all-일괄-적용-1500-67개-파일)
- [Part 9 — 232~236 v3 → v9.93 완전체 재작성 — 확인·동의 단원 (~지요/죠)](#part-9--232236-v3--v993-완전체-재작성--확인동의-단원-지요죠)
- [Part 10 — 237~247 v3 → v9.93 완전체 재작성 — 정중표현 단원 (~(으)세요), 11문제 + 11-항목 파스텔 CSS 확장](#part-10--237247-v3--v993-완전체-재작성--정중표현-단원-으세요-11문제--11-항목-파스텔-css-확장)
- [Part 11 — 부사어 문법 카테고리 전체 v9.93 적용 — 8개 파일 일괄 v3 → v9.93 완전체 재작성 (생성기 자동화)](#part-11--부사어-문법-카테고리-전체-v993-적용--8개-파일-일괄-v3--v993-완전체-재작성-생성기-자동화)
- [Part 12 — 기타 문법 카테고리 전체 v9.93 적용 — 18개 파일 v3 → v9.93 (3-stage 파이프라인)](#part-12--기타-문법-카테고리-전체-v993-적용--18개-파일-v3--v993-3-stage-파이프라인)
- [Part 13 — 룰 4 (형성 공식 한국어 형태소 굵게) 자동화 + 1~500 67개 파일 일괄 적용 (`_bold_korean_in_formations.py`)](#part-13--룰-4-형성-공식-한국어-형태소-굵게-자동화--1500-67개-파일-일괄-적용-_bold_korean_in_formationspy)
- [Part 14 — v9.96 Direction marker 에 한정어 정형화 — 가다/다니다 motion verb 컨텍스트 명시 (14개 파일 18개 row 일괄)](#part-14--v996-direction-marker-에-한정어-정형화--가다다니다-motion-verb-컨텍스트-명시-14개-파일-18개-row-일괄)
- [Part 15 — exp-table 헤더 한국어 어순으로 정정 (v9.97) — "English order" → "Korean order" 일괄 교정](#part-15--exp-table-헤더-한국어-어순으로-정정-v997--english-order--korean-order-일괄-교정)

---

# Part 1 — 190~198 (경험 단원) v9.85 표준 적용 — 헤더 영어화 + "~" placeholder 제거 + 주어 생략 표기 명료화

## 1-1. 사용자 지시

> "190-198 문서화를 적용하라."

→ 2026-05-05 세션 후반에 정형화한 v9.85 룰들을 190~198 (Predicate Grammar 12: Experience, ~ㄴ/은 적이 있다 + ~아/어 보다)에 적용. 본 단원은 Part 48에서 5종 보편 표준 적용 + Part 52~54 글로벌 자동화 룰들이 모두 적용된 상태이지만, Q229·Q231에서 새롭게 정형화한 **English order 헤더 + 영어 단어만 사용 + 명료한 화법** 룰은 미적용.

## 1-2. 적용 전 상태 진단 — 9 problems

| Q | English | 현재 상태 | 적용 필요 사항 |
|:-:|---------|-----------|---------------|
| 190 | Have you ever been to Korea? | 헤더 Korean structure + "Have ~ ever been?" (~ placeholder) | 헤더 영어화 + "~" 제거 |
| 191 | I have been to Korea. | 헤더 Korean structure | 헤더 영어화 |
| 192 | I have never been to Korea. | 헤더 Korean structure | 헤더 영어화 |
| 193 | Have you ever tried going to Korea? | 헤더 Korean structure + "Have ~ ever tried going?" | 헤더 영어화 + "~" 제거 |
| 194 | I have tried going to Korea. | 헤더 Korean structure | 헤더 영어화 |
| 195 | I haven't tried going to Korea. | 헤더 Korean structure | 헤더 영어화 |
| 196 | Please try this food. | 헤더 Korean structure | 헤더 영어화 |
| 197 | I have tried making kimchi. | 헤더 Korean structure | 헤더 영어화 |
| 198 | I want to visit Jeju Island someday. | 헤더 Korean structure + "I want to visit" 행에 "Subject I omitted" 모호한 표기 | 헤더 영어화 + 주어 생략 표기 명료화 |

→ 현재 행은 모두 영어 어순으로 나열되어 있음 (S+V+P 또는 S+V+O 등). 단지 헤더가 옛 "Korean structure" 라벨을 그대로 둠. 9개 행 모두 일괄 갱신.

## 1-3. 적용할 변경 — 3종

### 1-3-a. 헤더 영어화 (모든 9개 Q)

각 exp-table의 colspan 헤더를 다음 형식으로 변경:
- Before: `Korean structure: ... (경험)` / `한국어 어순: ... (경험)`
- After: `English order: ... (experience)` / `영어 어순: ... (경험) — Korean: {Korean order example}`

### 1-3-b. "~" placeholder 제거 (Q190, Q193)

| Q | Before | After |
|:-:|--------|-------|
| 190 | `Have ~ ever been?` | `Have ever been` |
| 193 | `Have ~ ever tried going?` | `Have ever tried going` |

→ "~" 는 placeholder 표기로 비표준. 영어 단어만 사용 원칙(Q229 정형화)에 따라 단순화.

### 1-3-c. Q198 주어 생략 표기 명료화

기존: `Subject I omitted.` (모호함 — 무엇이 무엇을 생략?)
변경: `Korean omits subject 저는 — context implies the speaker.` (Korean 측 주어가 생략됨을 영어로 명시)

## 1-4. 적용 결과 — Q별 변경 내역

### 1-4-a. 헤더 영어화 (9개 Q 모두 갱신)

| Q | Before | After |
|:-:|--------|-------|
| 190 | `Korean structure: Subject + Place + Verb (experience)` | `English order: Subject + Verb + Place (experience, question)` — Korean: 당신은 + 한국에 + 간 적이 있습니까? |
| 191 | `Korean structure: Subject + Place + Verb (experience)` | `English order: Subject + Verb + Place (experience)` — Korean: 저는 + 한국에 + 간 적이 있습니다 |
| 192 | `Korean structure: Subject + Place + Verb (no experience)` | `English order: Subject + Verb + Place (no experience)` — Korean: 저는 + 한국에 + 간 적이 없습니다 |
| 193 | `Korean structure: Subject + Place + Verb (try-experience)` | `English order: Subject + Verb + Place (try-experience, question)` — Korean: 당신은 + 한국에 + 가 보았습니까? |
| 194 | `Korean structure: Subject + Place + Verb (try-experience)` | `English order: Subject + Verb + Place (try-experience)` — Korean: 저는 + 한국에 + 가 보았습니다 |
| 195 | `Korean structure: Subject + Place + Verb (negative try)` | `English order: Subject + Verb + Place (negative try)` — Korean: 저는 + 한국에 + 가 보지 못했습니다 |
| 196 | `Korean structure: Object + Verb (try-request)` | `English order: Verb + Object (try-request, imperative)` — Korean: 이 음식을 + 먹어 보세요 |
| 197 | `Korean structure: Subject + Object + Verb (try-experience)` | `English order: Subject + Verb + Object (try-experience)` — Korean: 저는 + 김치를 + 만들어 보았습니다 |
| 198 | `Korean structure: Place + Adverb + Verb (want-try)` | `English order: Verb + Place + Adverb (want-try, subject omitted in Korean)` — Korean: 제주도에 + 한 번 + 가 보고 싶습니다 |

→ 헤더 형식 통일: `English order: ...(상황)\n영어 어순: ...(상황) — Korean: {Korean order example}`

### 1-4-b. "~" placeholder 제거 (Q190, Q193)

| Q | Before | After |
|:-:|--------|-------|
| 190 | `Have ~ ever been?` | `Have ever been` |
| 193 | `Have ~ ever tried going?` | `Have ever tried going` |

→ "~"는 비표준 placeholder. 영어 단어만 사용 원칙에 따라 단순화. 의미는 동일 (주어 "you"는 별도 row에 명시되어 있음).

### 1-4-c. Q198 주어 생략 표기 명료화

| Before (모호) | After (명료) |
|--------------|--------------|
| `Subject I omitted.` | `Korean omits the subject 저는 — context implies the speaker.` |

→ "Subject I omitted"는 모호함 (영어 I 생략? 한국어 주어 생략?). 명료화: 한국어 측 주어 저는 생략임을 명시.

## 1-5. 검증

```bash
$ grep -c "Korean structure:" "190~198 서술어 문법 경험.html"
0  ← 모든 헤더 갱신 완료

$ grep -c "English order:" "190~198 서술어 문법 경험.html"
9  ← 9개 모두 신규 형식

$ grep -c "Have ~ ever\|Subject I omitted" "190~198 서술어 문법 경험.html"
0  ← "~" placeholder + 모호한 표기 모두 제거
```

**v9.85 표준 적합성**: 본 파일은 이미 v9.85 표준 (refined gstep, 5종 보편 표준, 강조 무인용화, exp-table 영어 단일화)을 모두 만족. 본 Part 1는 Q229·Q231에서 새로 정형화한 **English order 헤더 + 영어 단어만 사용 + 명료한 화법** 룰을 추가 적용.

## 1-6. 영향 범위

- 단일 파일(190~198) 한정.
- exp-table 9개 row 헤더 + Q190·Q193 영어 단어 + Q198 explanation 텍스트 변경.
- 본문 콘텐츠 의미 무변경 (행 수·Korean form·정답 모두 보존).
- Korean 측 row order 정보는 헤더의 보조 표시(`Korean: ...`)로 보존.

## 1-7. 후순위

| 후순위 | 비고 |
|--------|------|
| 232~236 (확인 단원) v3 era → v9.85 완전체 재작성 | Part 51 패턴 재사용 |
| 237~247 (정중표현 단원) v3 era → v9.85 완전체 재작성 | 11문제로 분량 최대 |
| 다른 v9 파일들의 헤더도 "English order" 형식으로 일괄 갱신 | 자동화 스크립트 작성 후보 — 단, Q별로 Korean order example이 달라 단순 치환 불가 |

> 📌 **버전 표기:** v9.86 (2026-05-06) — 190~198 (경험 단원) Q229·Q231 정형화 룰 적용. 9개 row 헤더 영어화 + 2건 "~" placeholder 제거 + Q198 주어 생략 명료화. 본문 의미 0건 변경. v9.85 + 새 룰(English order 헤더) 모두 만족.

> 🔁 **확산 후순위:** 다른 서술어 문법 v9 파일들(87~96 ~ 209~212)도 같은 Q229·Q231 룰 적용 가능. Q별 Korean order example 차이로 단순 자동화 어려움 — 각 단원 수작업 후순위.

---

# Part 2 — 형성 공식 한국어 형태소 굵게 룰 + 주어 생략 표기 형식 정형화 (190~198 9개 row 일괄 적용)

## 2-1. 사용자 지시

> "이것을 문서화에 반영하라." (Q194/Q196/Q197/Q198 사례 적용 후)

→ 이전 turns에서 산발적으로 적용한 두 가지 룰을 정형화 + 190~198의 모든 row에 일괄 적용 + 가이드 문서화.

## 2-2. 룰 정형화 — v9.87

### 2-2-a. 룰 1: 형성 공식 한국어 형태소 굵게

| 룰 | 설명 |
|----|------|
| **대상** | exp-table explanation cell의 형성 공식 (`X + Y → Z → W` 형태 verb conjugation chain) 안의 모든 한국어 형태소 |
| **변환** | 각 한국어 형태소를 `<strong>` 처리 (예: `가다 + 아 보다 → 가 보다 → 가 보았습니다` → `**가다** + **아 보다** → **가 보다** → **가 보았습니다**`) |
| **목적** | 학습자가 동사 활용 단계의 각 형태소를 즉시 식별. 영어 prose는 자연스럽게 흐르고, 한국어 형태소가 시각적 anchor 역할 |
| **포함 형태소 종류** | 사전형 (먹다·가다), 보조동사 (어 보다·고 싶다), 결합형 (먹어 보다), 활용형 (먹어 보세요), 어미·조사 (~지 못했다, ㄴ 적이, 습니까?) |
| **제외** | 영어 안에 자연스럽게 등장하는 한국어 (이미 v9.84 무인용 강조로 처리됨), 부설명/괄호 안 한국어 (예: "(experience of going)" 같은 영어 의미 설명) |

### 2-2-b. 룰 2: 주어 생략 표기 형식 통일

| 룰 | 설명 |
|----|------|
| **이전 (모호)** | `Subject I omitted.` 또는 `Subject you omitted.` — 누가 무엇을 생략했는지 불명확 |
| **신규 (명료)** | `Korean omits the subject **{KOREAN}** — context implies the {speaker/listener}.` |
| **{KOREAN}** | 한국어 주어 (저는, 당신은, 제가 등 — 굵은 폰트) |
| **{speaker/listener}** | 1인칭이면 "speaker", 2인칭이면 "listener", 3인칭이면 "subject" 또는 구체적 인물 |

→ 본 표기는 (a) 한국어가 주어를 생략한다는 사실 명시 (b) 영어 학습자에게 어떤 한국어 주어가 생략되었는지 보여줌 (c) 화자/청자 구분으로 의미 맥락 제공.

## 2-3. 적용 결과 — 190~198 explanation 일괄 정비

### 2-3-a. 형성 공식 한국어 형태소 굵게 (이미 적용된 Q194/Q196/Q197/Q198 + 신규 적용 Q190/Q191/Q192/Q193/Q195)

| Q | Before | After |
|:-:|--------|-------|
| 190 | `가다 stem 가 + ㄴ 적이 = 간 적이 ... + 있습니까?` | `**가다** stem **가** + **ㄴ 적이** = **간 적이** ... + **있습니까?**` |
| 191 | `가다 → 간 적이 + 있습니다 — ...` | `**가다** → **간 적이** + **있습니다** — ...` |
| 192 | `가다 → 간 적이 + 없습니다 — ...` | `**가다** → **간 적이** + **없습니다** — ...` |
| 193 | `가다 + 아 보다 → 가 보다 → past 가 보았 + 습니까? ...` | `**가다** + **아 보다** → **가 보다** → past **가 보았** + **습니까?** ...` |
| 194 | `가 보다 → past 가 보았습니다 — ...` (이전 turn) | `**가 보다** → past **가 보았습니다** — ...` |
| 195 | `가 보다 + ~지 못했다 — ...` | `**가 보다** + **~지 못했다** — ...` |
| 196 | `먹다 + 어 보다 → 먹어 보다 → 먹어 보세요 ...` (이전 turn) | `**먹다** + **어 보다** → **먹어 보다** → **먹어 보세요** ...` |
| 197 | `만들다 + 어 보다 → 만들어 보다 → past 만들어 보았습니다.` (이전 turn) | `**만들다** + **어 보다** → **만들어 보다** → past **만들어 보았습니다**.` |
| 198 | `가 보다 + 고 싶다 (Grammar 6) → 가 보고 싶 + 습니다 — ...` (이전 turn) | `**가 보다** + **고 싶다** (Grammar 6) → **가 보고 싶** + **습니다** — ...` |

→ 9개 row 모두 형성 공식 한국어 형태소 굵게 적용 완료.

### 2-3-b. 주어 생략 표기 통일 (Q196, Q198)

| Q | Before | After |
|:-:|--------|-------|
| 196 | `Subject **you** omitted.` | `Korean omits the subject 당신은 — context implies the listener.` |
| 198 | `Subject **I** omitted.` (또는 직접 영문 작성) | `Korean omits the subject **저는** — context implies the speaker.` |

→ 두 행 모두 신규 형식 적용. 1인칭(I) → speaker, 2인칭(you) → listener 매핑 정확.

## 2-4. 자동화 가능성 검토

| 룰 | 자동화 가능성 |
|----|---------------|
| 룰 1 (한국어 형태소 굵게) | **부분 가능** — Korean character sequence를 정규식으로 검출하여 wrap 가능. 단, 다음 케이스 주의: ① 이미 `<strong>` 안의 한국어 (멱등성) ② 영어 의미 설명 괄호 안 한국어 — 무차별 굵게 시 over-bold. 신중한 자동화 필요 |
| 룰 2 (주어 생략 표기) | **부분 가능** — `Subject **X** omitted.` 패턴 검출 + 대응되는 한국어 주어 매핑 필요. 단, 한국어 주어 결정은 row 컨텍스트 분석 요구 (영어 "I" → 저는, "you" → 당신은). 부분 자동화 + 수작업 검토 |

→ Part 2는 수작업으로 진행. 자동화 스크립트 작성은 후순위 (룰의 정확한 적용 사례를 더 모은 뒤 검토).

## 2-5. 검증

```bash
$ grep -c '\\* \\*\\*가다\\*\\* \\|\\*\\*가 보다\\*\\* \\|\\*\\*먹다\\*\\* \\|\\*\\*만들다\\*\\*' "190~198 서술어 문법 경험.html"
# 한국어 형태소 굵게 처리 다수 확인
```

스폿 검증 — 모든 9개 row의 explanation cell에서 형성 공식 한국어 형태소가 일관되게 굵은 폰트로 표시됨.

## 2-6. 영향 범위

- 단일 파일(190~198) 한정.
- 9개 row의 explanation cell text 변경. 의미 무변경 (시각만 강화).
- 본문 콘텐츠(예문·정답·vocabulary) 무영향.

## 2-7. 후순위

| 후순위 | 비고 |
|--------|------|
| 다른 서술어 문법 v9 파일들(87~96 ~ 209~212)에 동일 룰 적용 | 각 단원 수작업 (Q별 형성 공식 차이로 자동화 어려움) |
| 자동화 스크립트 작성 (`_bold_korean_morphemes.py`) | 후순위 — 룰 적용 사례를 더 축적한 뒤 검토 |

> 📌 **버전 표기:** v9.87 (2026-05-06) — 형성 공식 한국어 형태소 굵게 룰 + 주어 생략 표기 형식 통일 룰 정형화. 190~198 9개 row 일괄 적용. 룰 1: explanation cell 안 동사 활용 chain의 모든 한국어 형태소 굵게. 룰 2: `Korean omits the subject **{KOREAN}** — context implies the {speaker/listener}.` 형식 통일. 본문 의미 0건 변경.

> 🔁 **확산 후순위:** 87~96 ~ 209~212 서술어 문법 v9 파일들에 동일 룰 적용. 자동화 스크립트는 후순위.

---

# Part 3 — 문장구조 + 서술어 문법 27개 파일에 v9.86-v9.87 룰 자동화 일괄 적용 (`_apply_v9_87_rules.py`)

## 3-1. 사용자 지시

> "이 문서화를 문장구조와 서술어 문법에 있는 모든 파일에 적용하라."

→ Part 1·2에서 정형화한 v9.86-v9.87 룰을 두 카테고리(문장구조 9개 + 서술어 문법 20개 exp-table 보유 파일 = 29개)에 자동화 일괄 적용.

## 3-2. 자동화 스크립트 — `d:\놀라운 한국어 2026\_apply_v9_87_rules.py`

### 3-2-a. 핵심 설계

| 룰 | 자동화 방식 |
|----|------------|
| **룰 1: 헤더 영어화** | colspan 헤더의 `Korean structure:`/`한국어 어순:` 패턴을 정규식 매치 → 같은 테이블의 row 데이터에서 영어 role 순서 + 한국어 form 순서 추출 → 새 헤더 `English order: ENG_ROLES (situation) — Korean: KO1 + KO2 + ...` 재구축 |
| **룰 2: "~" placeholder 제거** | `<td class="exp-en"><strong>...~...</strong></td>` 패턴에서 `~` 제거 (예: `Have ~ ever been?` → `Have ever been`) |
| **룰 3: 주어 생략 표기 통일** | `Subject <strong>I</strong> omitted.` → `Korean omits the subject <strong>저는</strong> — context implies the speaker.` (그리고 `you` → `당신은` + `listener`) |
| ~~**룰 4: 형성 공식 한국어 형태소 굵게**~~ | (자동화 deferred — 형성 공식별 한국어 토큰 식별이 복잡, false positive 위험. 단원별 수작업 필요) |

### 3-2-b. 헤더 재구축 알고리즘

```python
# 1. Match HEADER + capture trailing '...</tbody>' to access row data
# 2. From row data, extract: each row's Role (English) + Korean form text
# 3. Map English Role → Korean Role using ROLE_KO_MAP
# 4. Detect trailing situation parenthetical (e.g., "(experience)") from old header
# 5. Build new header:
#    - English line: 'English order: ROLE1 + ROLE2 + ... (situation)'
#    - Korean line: '영어 어순: 주어 + ... (상황) — Korean: KO1 + KO2 + ...'
```

→ `Negation (short)`처럼 role 안에 괄호가 있는 경우도 처리 (situation은 trailing 괄호만, 중간 role 괄호는 보존).

### 3-2-c. 멱등성

각 룰의 정규식이 변경 후 패턴과 다른 형태를 매치하므로 재실행 시 0 changes.

## 3-3. 적용 결과

```
$ python _apply_v9_87_rules.py
# 29 target file(s)
... (각 파일별 header×N + tilde×N + subj×N)
# Summary: OK=29 SKIP=0 (after second pass for edge case headers)
```

| 카테고리 | 파일 | 헤더 변경 합계 |
|----------|------|--------------:|
| 문장구조 (9 파일) | 1~10, 11~20, 21~30, 31~40, 41~50, 51~60, 61~70, 71~78, 79~86 | 84 (10+10+10+10+10+10+10+8+8 — 일부는 dual header) |
| 서술어 문법 (20 파일) | 87~96 ~ 237~247 | 168 |
| **합계** | **29 파일** | **~252 헤더 변경** |

→ 모든 exp-table 헤더가 `English order: ... — Korean: KO1 + KO2 + ...` 형식으로 통일.

→ "~" placeholder + 주어 생략 표기는 해당 패턴이 있는 파일에만 적용됨 (대부분의 파일은 이미 v9.86 룰을 만족하므로 0 변경).

## 3-4. 검증

```bash
$ grep -c "Korean structure:" "*문장구조*.html" "*서술어*.html"
0  ← 모든 헤더 갱신 완료

$ grep -c "English order:" "*.html" | wc -l
27  ← 27개 파일에 새 헤더 (232~236, 237~247은 v3-era로 exp-table 부재)
```

스폿 검증 — `1~10 문장구조 주어 형용사.html` line 479:

**Before**:
```html
<th colspan="4" ...>Korean structure: Subject + Predicate (adjective)<br>한국어 어순: 주어 + 서술어 (형용사)</th>
```

**After**:
```html
<th colspan="4" ...>English order: Subject + Predicate (adjective)<br>영어 어순: 주어 + 서술어 (형용사) — Korean: 저는 + 행복합니다</th>
```

→ 영어 헤더로 갱신 + 실제 한국어 form 시퀀스(저는 + 행복합니다)가 reference로 추가되어 학습자가 한국어 자연 어순도 즉시 확인 가능.

## 3-5. 룰 4 (형성 공식 한국어 형태소 굵게) — 자동화 미적용 사유

**복잡성**:
- 형성 공식의 다양성: `X + Y → Z`, `X stem Y + Z = W`, `X → past Y → present Z` 등
- 영어 prose 안에 inline된 한국어 (`(experience of going)` 같은 영어 의미 설명)와 형성 공식 한국어 토큰 구분 어려움
- False positive 시 영어 단어 굵게 처리되거나 모든 한국어가 wrap되어 시각 노이즈 증가

**현실적 해결**:
- 단원별 수작업 (Part 2에서 190~198에 적용한 것처럼)
- 또는 향후 정밀 자동화 스크립트 작성 (`_bold_formation_chain.py`) — 복잡한 패턴 분석 필요

**결정**: 본 Part 3에서 룰 1~3만 자동화. 룰 4는 단원별 후속 작업으로 진행.

## 3-6. 영향 범위

- 29개 HTML 파일 (문장구조 9 + 서술어 문법 20).
- exp-table colspan 헤더 ~252개 변경 + "~" placeholder + 주어 생략 (해당 시).
- 본문 콘텐츠(예문·정답·vocabulary·exp-form·explanation 본체) 무영향.
- 행 순서·역할 라벨 무변경 (헤더 재구축은 기존 row 데이터 사용).

## 3-7. 잔여 작업

| 작업 | 비고 |
|------|------|
| 룰 4 (형성 공식 한국어 형태소 굵게) — 29개 파일 explanation cell 전체 재검토 | 단원별 수작업, ~10-15시간 추정 |
| 232~236 (확인 단원) v3 → v9.87 완전체 재작성 | 5문제, Part 49 패턴 재사용 |
| 237~247 (정중표현 단원) v3 → v9.87 완전체 재작성 | 11문제, 분량 최대 |
| 부사어 문법 + 기타 문법 카테고리에 동일 룰 확산 | 별도 후속 작업 |

> 📌 **버전 표기:** v9.88 (2026-05-06) — 문장구조 + 서술어 문법 29개 파일에 v9.86-v9.87 룰 1~3 자동화 일괄 적용. 자동화 스크립트 `_apply_v9_87_rules.py` (헤더 재구축 + tilde 제거 + 주어 생략 통일, 멱등성 보장). ~252개 헤더 변경. 본문 의미 0건 변경. 룰 4는 단원별 수작업으로 deferred.

> 🔁 **확산 후순위:** ① 룰 4 (형성 공식 굵게) 29개 파일 단원별 적용 ② 부사어·기타 문법 카테고리 확산 ③ 232~236, 237~247 v3 → v9.88 완전체 재작성.

---

# Part 4 — 해설 컬럼 한국어 토큰 일괄 굵게 룰 정형화 + 1~500 67개 파일 자동 적용 (`_bold_korean_in_explanations.py`)

## 4-1. 사용자 지시

> "1) 해설은 영어로 한다. 2) 해설에 사용되는 한국어(음절, 어휘, 형태소...)등은 모두 굵은 폰트로 한다. 3) 한국어 해석은 지운다. — 이것을 문서화 하고 모든 파일에 적용하라."

→ Part 3까지 deferred되었던 "룰 4 (형성 공식 한국어 형태소 굵게)"를 더 일반화한 형태로 정형화 + 자동화 + 1~500 전 67개 파일 일괄 적용.

## 4-2. 룰 정형화 (v9.89) — 해설 영어화 3-룰 통합

| 룰 | 상태 | 정의 |
|----|------|------|
| **A** | 기존 (v9.85) | exp-table 해설 컬럼은 영어 prose. Korean translation 라인(`<br>` + 한국어 번역) 금지 |
| **B** | **신규 (v9.89)** | **해설 영어 prose 안에 등장하는 모든 한국어(음절·자모·어휘·형태소·구) 토큰을 `<strong>` 처리** |
| **C** | 기존 (v9.85, v9.87) | 한국어 해석 별도 라인 작성 금지 (Rule A의 부정형) — 유지 |

### 4-2-a. 룰 B의 한국어 토큰 정의

**대상**:
- 한글 음절 (`가`, `먹`, `다`)
- 한글 자모 (`ㄱ`, `ㄴ`, `ㅂ`)
- 단어 (`먹다`, `예쁘다`, `가다`)
- 형태소 (`~지 않다`, `~ㄴ 적이`, `~겠습니다`)
- 구 (`간 적이`, `가 보다`, `~아/어 주세요`)
- 시제 형태 (`갔습니다`, `왔어요`, `먹는다`)
- 의문문 어미 (`습니까?`, `네요?`)
- 강조 마커 (`~`, `?` 포함)

**제외**:
- 이미 `<strong>` 안에 있는 한국어 (멱등성 — 재처리 안 함)
- HTML 속성·CSS·JS·`<title>` 안의 한국어 (안전 skip — 자동화 스크립트가 처리)

### 4-2-b. 룰 B의 학습 효과

| 효과 | 설명 |
|------|------|
| 시각 anchor | 영어 prose 흐름 안에서 한국어 토큰이 즉시 식별 — 학습자가 영어로 의미를 잡고 한국어 형태를 빠르게 매핑 |
| 학습 핵심 강조 | 해설의 본질은 "어떤 영어가 어떤 한국어로 표현되는가" — 한국어 토큰이 학습 핵심 |
| 일관성 | 형성 공식뿐 아니라 모든 한국어 토큰을 굵게 → 문법 설명 vs 어휘 설명 vs 형태소 설명 등 컨텍스트 무관 일관 적용 |

## 4-3. 자동화 스크립트 — `d:\놀라운 한국어 2026\_bold_korean_in_explanations.py`

### 4-3-a. 핵심 알고리즘 (Placeholder Substitution + Idempotent Regex)

```python
# 1. exp-table row의 4번째 <td> (해설 컬럼) 매치
EXP_ROW_TD_RE = r'(<td class="exp-form">...</td>)<td>(EXPLANATION)</td>'

# 2. 해설 안의 기존 <strong>...</strong> stash (멱등성 보장)
stashed = re.sub(r'<strong>.*?</strong>', stash, text, DOTALL)

# 3. 한국어 토큰 정규식
KOREAN_TOKEN_RE = r'~?[가-힯ㄱ-ㆎ]+(?:\s+[가-힯ㄱ-ㆎ]+)*[~?]?'

# 4. 매치된 한국어 토큰을 <strong> wrap
bolded = KOREAN_TOKEN_RE.sub(lambda m: f"<strong>{m.group(0)}</strong>", stashed)

# 5. Placeholder 복원
restored = re.sub(r'\x00(\d+)\x00', restore, bolded)
```

### 4-3-b. 한국어 토큰 정규식 설계

```regex
~?[가-힯ㄱ-ㆎ]+(?:\s+[가-힯ㄱ-ㆎ]+)*[~?]?
```

| 부분 | 의미 |
|------|------|
| `~?` | 선행 tilde 선택 (`~지`, `~ㄴ`) |
| `[가-힯ㄱ-ㆎ]+` | 한글 syllable + Hangul Jamo 1+개 |
| `(?:\s+[가-힯ㄱ-ㆎ]+)*` | 다중 토큰 phrase 매치 (예: `간 적이`, `가 보다`) — 0+회 반복 |
| `[~?]?` | 후행 tilde/물음표 선택 (`~`, `습니까?`) |

### 4-3-c. 멱등성 검증

- 첫 실행 후 모든 한국어 토큰이 `<strong>` wrap.
- 재실행 시 stash phase에서 `<strong>...</strong>`로 모두 stash됨 → 매치 대상 없음 → 0 changes.

## 4-4. 적용 결과

```
$ python _bold_korean_in_explanations.py
# 67 target file(s)
... (각 파일별 N Korean tokens bolded)
# Summary: OK=38 SKIP=29 | Total: 778 Korean tokens bolded

$ python _bold_korean_in_explanations.py  # 재실행
# Summary: OK=0 SKIP=67 | Total: 0 Korean tokens bolded  ← 멱등성 검증 ✓
```

| 카테고리 | 처리된 파일 | 한국어 토큰 굵게 |
|----------|-----------:|------------------:|
| 문장구조 | 9 (모두 OK) | ~150 |
| 서술어 문법 | 18 (exp-table 보유 + 기 적용 일부 SKIP) | ~500 |
| 부사어 문법 | 11 (일부 SKIP) | ~120 |
| 기타 문법 | 0 (SKIP — 다른 구조) | 0 |
| **합계** | **38 OK + 29 SKIP** | **778** |

## 4-5. 스폿 검증 — 사용자 예시 (181~189 Q "Could you please help")

[181~189 서술어 문법 부탁.html:453](놀라운 한국어 500 해설집/181~189 서술어 문법 부탁.html#L453)

**Before** (사용자 screenshot 상태):
```
도와 (from 돕다 ㅂ-irregular) + 주시겠습니까? — the higher-honorific form. 주시 (honorific) + 겠 (volition) + 습니까? (interrogative).
```

**After** (v9.89 적용):
```html
<strong>도와</strong> (from <strong>돕다 ㅂ</strong>-irregular) + <strong>주시겠습니까?</strong> — the higher-honorific form. <strong>주시</strong> (honorific) + <strong>겠</strong> (volition) + <strong>습니까?</strong> (interrogative).
```

→ 모든 한국어 토큰(`도와`, `돕다`, `ㅂ`, `주시겠습니까?`, `주시`, `겠`, `습니까?`) 굵게 처리. 영어 메타 라벨(`honorific`, `volition`, `interrogative`)은 굵게 처리 안 됨 (한국어가 아니므로). 영어 prose 흐름 보존.

## 4-6. 영향 범위

- 67개 HTML 파일의 exp-table 해설 컬럼 안 한국어 토큰 778개 추가 굵게 처리.
- HTML 속성·CSS·JS·`<title>` 무영향 (스크립트가 td 컬럼만 target).
- 본문 의미 무변경 (시각 강화만).
- 멱등성: 신규 파일 추가 시 동일 명령으로 즉시 적용 가능.

## 4-7. 룰 적용 우선순위 (신규 파일 작성 시)

신규 파일 v9.89 표준 도달 워크플로:
1. 본문 작성 (제목·gstep·exp-table·인터랙티브 섹션 등 v9.79 완전체 구조)
2. `_bold_quoted_text.py` 실행 (인용 텍스트 굵게)
3. `_strip_korean_translation.py` 실행 (해설 한국어 번역 라인 제거 — Rule A)
4. `_apply_v9_87_rules.py` 실행 (헤더 영어화 + tilde 제거 + 주어 생략 통일)
5. **`_bold_korean_in_explanations.py` 실행 (Rule B — v9.89 신규)**
6. (수작업) 형성 공식 한국어 토큰 추가 굵게 (룰 4 잔여, 단원별)

→ 5 단계가 본 Part 4의 자동화 스크립트로 v9.89 룰 B 자동 적용.

## 4-8. 부수 산출물

- **`_bold_korean_in_explanations.py`**: exp-table 해설 컬럼 한국어 토큰 자동 굵게 (placeholder substitution + idempotent regex)

> 📌 **버전 표기:** v9.89 (2026-05-06) — 해설 한국어 토큰 일괄 굵게 룰 (Rule B) 정형화. 자동화 스크립트 `_bold_korean_in_explanations.py` 신규 (placeholder substitution + idempotent regex). 1~500 전 67개 파일 적용 — 38 OK + 29 SKIP, 778개 한국어 토큰 굵게. 멱등성 검증. v9.85 (Rule A) + v9.89 (Rule B)로 해설 룰 통합 완성.

---

# Part 5 — 부사어 문법 카테고리 v9.86-v9.87 헤더 룰 확산 + 자동 한국어 토큰 굵게 후속 (`_apply_v9_87_rules.py` 확장)

## 5-1. 사용자 지시

> "이어서 작업하라."

→ Part 3에서 deferred된 "부사어·기타 문법 카테고리에 동일 룰 확산"을 자율 판단으로 진행. 진단 결과 12개 부사어 파일에 옛 `Korean structure:` 헤더 잔존, 기타 문법 18개 파일은 exp-table 부재로 본 작업 비대상.

## 5-2. 적용 전 진단 — 12개 부사어 파일

```bash
$ grep -l "Korean structure:" *부사어*.html | wc -l
12
```

| Q 범위 | 파일 | "Korean structure:" 잔존 |
|:-----:|------|:-:|
| 248~252 | 시간 때 | 5 |
| 253~259 | 기간 동안 | 7 |
| 260~265 | 전에 | 6 |
| 266~270 | 후에 | 5 |
| 271~276 | 순서 고 | 6 |
| 277~283 | 순서원인 아어서 | 7 |
| 284~291 | 동시 동작 으면서 | 8 |
| 292~295 | 즉시 자마자 | 4 |
| 296~299 | 동작 전환 다가 | 4 |
| 300~306 | 범위 부터 까지 | 7 |
| 307~313 | 원인 때문에 (2) | 7 |
| 314~315 | 원인 덕분에 | 2 |
| **합계** |  | **68** |

→ 다른 8개 부사어 파일(316~365)은 exp-table 부재 → 본 작업 비대상. 기타 문법 18개 파일도 exp-table 0건이라 비대상.

## 5-3. 부사어 카테고리 특수 사항 — 복합 역할 라벨

서술어 문법 카테고리는 단순 역할(Subject, Object, Verb 등)만 사용했으나, 부사어 문법은 시간·원인·순서·동시 동작 등 의미 연결을 표현하기 위해 **복합 역할 라벨**을 사용:

| 패턴 | 예시 | Korean translation |
|------|------|-------------------|
| 단순 | `Subject`, `Object`, `Verb`, `Time`, `Place`, `Cause`, `Duration`, `Means`, `Range` | 주어, 목적어, 동사, 시간, 장소, 원인, 기간, 수단, 범위 |
| 번호 | `Subject 1`, `Object 1`, `Verb 2`, `Noun 1` | 주어 1, 목적어 1, 동사 2, 명사 1 |
| Paren | `Verb (past)`, `Predicate (adjective)`, `Object (idiom)`, `Place (from)`, `Cause (positive)` | 동사 (과거), 서술어 (형용사), 목적어 (관용), 장소 (부터), 원인 (긍정) |
| 번호+Paren | `Verb 1 (~아서)`, `Verb 2 (final, past)`, `Object 1 (idiom)` | 동사 1 (~아서), 동사 2 (최종, 과거), 목적어 1 (관용) |
| 복합어 | `Time clause`, `Inner Object`, `Direct Object`, `Indirect Object`, `DO`, `IO` | 시간 절, 내부 목적어, 직접목적어, 간접목적어 |

→ 단순 ROLE_KO_MAP만으로는 처리 불가. 정규식 기반 스마트 파서 필요 — `translate_role(role)` 함수로 base + number + parenthetical 분리 후 조립.

## 5-4. 자동화 스크립트 확장 — `_apply_v9_87_rules.py` v2

### 5-4-a. `detect_category` 확장

| Before | After |
|--------|-------|
| `if "문장구조" in name: return "sentence"`<br>`if "서술어 문법" in name: return "predicate"`<br>`return None` | + `if "부사어 문법" in name: return "adverbial"` |

### 5-4-b. ROLE_KO_MAP 확장 (12개 → 24개)

추가 항목:
- `Cause` → 원인, `Duration` → 기간, `Means` → 수단, `Range` → 범위, `Noun` → 명사
- `Time clause` → 시간 절, `Inner Object` → 내부 목적어
- `Direct Object` / `Indirect Object` (전체 단어), `DO` / `IO` 약자

### 5-4-c. PAREN_KO_MAP 신규 (parenthetical translation table)

| English | Korean |
|---------|--------|
| past | 과거 |
| present | 현재 |
| future | 미래 |
| noun | 명사 |
| adjective | 형용사 |
| interrogative | 의문 |
| imperative | 명령 |
| prohibition | 금지 |
| final | 최종 |
| final, past | 최종, 과거 |
| from | 부터 |
| to | 까지 |
| with 에 | ~에 |
| idiom | 관용 |
| direction | 방향 |
| positive | 긍정 |
| ~고 / ~다가 / ~면서 / ~으면서 / ~아서 / ~자마자 / ~이면서 / wh | (그대로) |

### 5-4-d. `translate_role(role)` 신규 함수

```python
def translate_role(role: str) -> str:
    role = role.strip()
    # 1. 직접 매칭
    if role in ROLE_KO_MAP:
        return ROLE_KO_MAP[role]
    # 2. "Base [N] [(paren)]" 패턴
    m = re.match(r"^([A-Za-z][A-Za-z\s]*?)\s*(\d+)?\s*(?:\(([^)]+)\))?\s*$", role)
    if m:
        base = m.group(1).strip()
        num = m.group(2)
        paren = m.group(3)
        base_ko = ROLE_KO_MAP.get(base, base)
        result = base_ko
        if num:
            result += f" {num}"
        if paren:
            paren_ko = PAREN_KO_MAP.get(paren, paren)
            result += f" ({paren_ko})"
        return result
    return role  # fallback
```

### 5-4-e. `transform_header` 통합

기존 `ROLE_KO_MAP.get(r, r)` 직접 호출을 `translate_role(r)`로 교체. 기존 서술어/문장구조 파일에 대한 동작은 변경 없음 (멱등성 보존).

## 5-5. 적용 결과

```
$ python _apply_v9_87_rules.py --dry-run
# 49 target file(s) [DRY RUN]
... (12 OK 부사어 + 37 SKIP no-change 기존)
# Summary: OK=12 SKIP=37

$ python _apply_v9_87_rules.py  # 본 적용
# Summary: OK=12 SKIP=37

$ python _apply_v9_87_rules.py  # 멱등성 검증
# Summary: OK=0 SKIP=49
```

| 카테고리 | 파일 수 | 헤더 변경 | 기타 |
|----------|--------:|----------:|------|
| 문장구조 | 9 SKIP | 0 (이미 적용됨) | — |
| 서술어 문법 | 20 SKIP | 0 (이미 적용됨) | — |
| 부사어 문법 | **12 OK** | **68** | + tilde×1 (253~259) |
| **합계** | **49** | **68** | tilde×1 |

부사어 OK 분포:

| 파일 | 헤더 변경 |
|------|----------:|
| 248~252 시간 때 | 5 |
| 253~259 기간 동안 | 7 (+tilde×1) |
| 260~265 전에 | 6 |
| 266~270 후에 | 5 |
| 271~276 순서 고 | 6 |
| 277~283 순서원인 아어서 | 7 |
| 284~291 동시 동작 으면서 | 8 |
| 292~295 즉시 자마자 | 4 |
| 296~299 동작 전환 다가 | 4 |
| 300~306 범위 부터 까지 | 7 |
| 307~313 원인 때문에 (2) | 7 |
| 314~315 원인 덕분에 | 2 |

복합 역할 라벨 변환 검증 — 284~291 (`동시 동작 으면서`):
- Before: `Korean structure: Subject + Object1 + Verb1+~면서 + Object2 + Verb2`
- After: `English order: Subject + Verb 2 (final) + Object 2 (idiom) + Verb 1 (~면서) + Object 1`<br>`영어 어순: 주어 + 동사 2 (최종) + 목적어 2 (관용) + 동사 1 (~면서) + 목적어 1 — Korean: 아버지는 + 합니다 + 식사를 + 보면서 + 뉴스를`

→ `Verb 2 (final)`, `Object 2 (idiom)`, `Verb 1 (~면서)` 모두 정확히 한국어 번역 (스마트 파서 동작 검증).

## 5-5-a. parse_situation 비활성화 — 부사어 카테고리 특수 처리

**버그 발견 및 수정**: 초기 구현 시 `parse_situation`을 부사어에도 그대로 적용한 결과 — 옛 헤더 `Cause+때문에 + Subject + Verb (past)` 의 trailing `(past)` 가 situation으로 잘못 추출되어 새 헤더에 `Subject + Verb (past) + Cause (past)` 식으로 중복 표기됨.

**원인**: 서술어/문장구조의 trailing `(X)` 는 의미적 상황 마커(예: `(experience)`, `(question)`)인 반면, 부사어의 trailing `(past)` 는 row 역할(`Verb (past)`)에 이미 인코딩된 qualifier.

**수정**: `transform_header(content, category)` 시그니처 추가 → 부사어일 때 `old_situation = ""` 으로 우회. 서술어/문장구조 동작 무변경.

## 5-6. 후속 — `_bold_korean_in_explanations.py` 재실행

Part 5 헤더 변환과 별개로, 본 세션 세팅 점검 차원에서 v9.89 Rule B 자동화 재실행:

```
$ python _bold_korean_in_explanations.py  # 첫 재실행
# Summary: OK=2 SKIP=65 | Total: 97 Korean tokens bolded  ← Part 4 누락분 보완

$ python _bold_korean_in_explanations.py  # 멱등성 검증
# Summary: OK=0 SKIP=67 | Total: 0 Korean tokens bolded  ← 멱등성 ✓
```

→ Part 5 헤더 변환은 row data 무변경 (colspan `<th>` 만 갱신) 이므로 row의 4번째 `<td>` 를 처리하는 Rule B 자동화에 영향 없음. OK=2 (97 tokens) 는 Part 4 첫 실행 후 형태 안정화로 발견된 누락분 보완 (예: `</td>` 와 `<td>` 사이 화이트스페이스가 있던 row 등) — 이번 재실행으로 정합성 도달.

## 5-7. 영향 범위

- 12개 부사어 HTML 파일의 colspan 헤더 68개 변경 (`Korean structure:` → `English order:` + Korean form chain 추가).
- 본문 콘텐츠(예문·정답·vocabulary·exp-form·explanation 본체) 무영향.
- 행 순서·역할 라벨 무변경 (헤더 재구축은 기존 row 데이터 사용).

## 5-8. 잔여 작업 — 다음 세션 진입점

| 작업 | 비고 |
|------|------|
| 룰 4 (형성 공식 한국어 형태소 굵게) — 41개 파일 explanation cell 재검토 | 단원별 수작업, ~10-15시간 추정 |
| 232~236 (확인 단원) v3 → v9.88 완전체 재작성 | 5문제, Part 49 (2026-05-05 로그) 패턴 재사용 |
| 237~247 (정중표현 단원) v3 → v9.88 완전체 재작성 | 11문제, 분량 최대 |
| 기타 문법 18개 파일 exp-table 도입 | 별도 후속 작업 (현재 exp-table 0건) |

> 📌 **버전 표기:** v9.90 (2026-05-06) — 부사어 문법 카테고리 v9.86-v9.87 헤더 룰 확산. 자동화 스크립트 `_apply_v9_87_rules.py` 확장 (`detect_category` + `translate_role` 스마트 파서 + `PAREN_KO_MAP` 신규). 12개 파일 68개 헤더 변환. 멱등성 보존. exp-table 보유 카테고리(문장구조 + 서술어 문법 + 부사어 문법) 전체에 v9.86-v9.87 표준 100% 도달.

> 🔁 **표준 갱신:** v9.90부터 부사어 문법 헤더도 `English order: EN_ROLES (상황)\n영어 어순: KO_ROLES (상황) — Korean: KO_FORM_CHAIN` 통일 형식. 복합 역할 라벨(`Verb 1 (~아서)`, `Cause+때문에`, `Time clause` 등)도 자동 번역.

---

# Part 6 — 모음 어간 동사 과거형 해설 오류 일괄 수정 — 17건 (5개 파일)

## 6-1. 사용자 지시

> 스크린샷 — `샀습니다` row의 해설이 `Stem ends in a consonant → formal ending ~습니다.` → "사다 - 사 +았습니다 - 샀습니다"

→ 사용자가 모음 어간 (`사`) 동사를 자음 어간으로 잘못 표기한 해설을 발견. 동일 패턴이 다른 모음 어간 동사 과거형에도 있는지 전수 점검.

## 6-2. 오류 유형 분석

**자음 어간 (정확):**
- 좋다(ㅎ), 높다(ㅍ), 작다(ㄱ), 무겁다(ㅂ), 재미있다(ㅆ), 먹다(ㄱ), 듣다(ㄷ), 입다(ㅂ), 읽다(ㄺ) → "Stem ends in a consonant" ✓
- 현재형: 어간 + ~습니다 (자음 어간 룰)

**모음 어간 + 과거형 (오류):**
- 사다(사+았→샀), 만나다(만나+았→만났), 주다(주+었), 보다(보+았), 가다(가+았→갔)
- 과거 형성: stem + ~았/었 → 축약/유지 + ~습니다
- 잘못된 해설: "Stem ends in a consonant" — 모음 어간임에도 자음 어간 룰로 오해

## 6-3. 전수 점검 결과 — 17건 (5개 파일)

| 파일 | 한국어 form | 동사 | 건수 |
|------|------------|------|----:|
| 31~40 시간 장소 목적어 동사 | 샀습니다 | 사다 | 3 |
| 31~40 시간 장소 목적어 동사 | 만났습니다 | 만나다 | 3 |
| 31~40 시간 장소 목적어 동사 | 보았습니다 | 보다 | 2 |
| 41~50 간접목적어 직접목적어 | 주었습니다 | 주다 | 5 |
| 51~60 의문사 누구 언제 | 만났습니까? | 만나다 | 1 |
| 51~60 의문사 누구 언제 | 만났습니다 | 만나다 | 1 |
| 61~70 의문사 어디 무엇 | 갔습니까? | 가다 | 1 |
| 79~86 접속 과와 이나나 | 샀습니다 | 사다 | 1 |
| **합계** |  |  | **17** |

## 6-4. 수정 형식 — v9.89 호환

기존 (오류): `Stem ends in a consonant → formal ending <strong>~습니다</strong>.`

수정 (v9.89 Rule B 호환 — 모든 한국어 토큰 굵게):

| Form | 수정된 해설 |
|------|------------|
| 샀습니다 | Past form of <strong>사다</strong>. Stem <strong>사</strong> + past <strong>~았</strong> → <strong>샀</strong> + formal ending <strong>~습니다</strong>. |
| 만났습니다 | Past form of <strong>만나다</strong>. Stem <strong>만나</strong> + past <strong>~았</strong> → <strong>만났</strong> + formal ending <strong>~습니다</strong>. |
| 만났습니까? | Past form of <strong>만나다</strong>. Stem <strong>만나</strong> + past <strong>~았</strong> → <strong>만났</strong> + interrogative <strong>~습니까?</strong>. |
| 보았습니다 | Past form of <strong>보다</strong>. Stem <strong>보</strong> + past <strong>~았</strong> → <strong>보았</strong> + formal ending <strong>~습니다</strong>. |
| 주었습니다 | Past form of <strong>주다</strong>. Stem <strong>주</strong> + past <strong>~었</strong> → <strong>주었</strong> + formal ending <strong>~습니다</strong>. |
| 갔습니까? | Past form of <strong>가다</strong>. Stem <strong>가</strong> + past <strong>~았</strong> → <strong>갔</strong> + interrogative <strong>~습니까?</strong>. |

→ 형식 통일: `Past form of <X다>. Stem <stem> + past <~았/었> → <축약> + formal ending <~습니다> | interrogative <~습니까?>`

→ ~았/었 선택은 한국어 모음 조화 룰: 어간 모음이 ㅏ/ㅗ → ~았, 그 외 → ~었.

→ 277~283 부사어 문법(`가서`, `샀습니다`)에 이미 정착된 형식과 일치 — 카테고리 간 일관성 확보.

## 6-5. 멱등성 검증

```
$ python _bold_korean_in_explanations.py
# Summary: OK=0 SKIP=67 | Total: 0 Korean tokens bolded  ← 멱등성 ✓
```

→ Part 6 수정은 직접 `<strong>` 포함 작성이라 Rule B 자동화에 의해 재처리 안 됨 (이미 wrap된 상태).

## 6-6. 영향 범위

- 5개 HTML 파일의 17개 row explanation cell 의미적 정확성 회복.
- 다른 row(자음 어간 정확 표기)와 본문 콘텐츠는 무영향.
- 277~283 (부사어 `가서/샀습니다`) 정형 형식과 카테고리 간 일관성 도달.

## 6-7. 잔여 작업 — 다음 진입점

| 작업 | 비고 |
|------|------|
| 다른 모음 어간 동사 과거형 전수 점검 | 본 작업으로 17건 수정 완료. 향후 신규 파일 작성 시 동일 형식 적용 |
| 자음 어간 동사 + 모음 어간 동사의 현재형/미래형 해설도 같은 형식 통일 | 후순위 (현재 자음 어간 표기는 정확, 단지 형식이 다름) |

> 📌 **버전 표기:** v9.91 (2026-05-06) — 모음 어간 동사 과거형 해설 오류 일괄 수정. 5개 파일 17건. 형식: `Past form of <X다>. Stem <stem> + past <~았/었> → <축약> + formal ending <~습니다>`. 277~283 정형 형식과 일치. 멱등성 검증.

> 🔁 **표준 갱신:** v9.91부터 모든 모음 어간 동사 과거형 해설은 `Past form of <기본형>. Stem <어간> + past <~았/었> → <축약> + formal ending <~습니다>` 형식. 자음 어간 룰("Stem ends in a consonant")을 모음 어간에 적용 금지.

---

# Part 7 — gstep-pattern accent 하얀 배경 chip 제거 (1~500 67개 파일)

## 7-1. 사용자 지시

> "글씨 뒤에 있는 하얀 배경을 없애라. 다른 것에도 적용하라."

→ `.gstep-pattern .accent` chip의 흰색 배경 + box-shadow + padding (chip 효과)을 제거. 1~500 전 파일 일괄 적용.

## 7-2. 적용 전 진단

```bash
$ grep -h "gstep-pattern \.accent" 놀라운\ 한국어\ 500\ 해설집/*.html | sort -u
.gstep-pattern .accent { color: #047857; background: #fff; padding: 2px 9px; border-radius: 5px; box-shadow: 0 1px 3px rgba(6,78,59,0.18); font-family: inherit; }    # adverbial
.gstep-pattern .accent { color: #2563eb; background: #fff; padding: 2px 9px; border-radius: 5px; box-shadow: 0 1px 3px rgba(30,58,138,0.18); font-family: inherit; }  # sentence
.gstep-pattern .accent { color: #6d28d9; background: #fff; padding: 2px 9px; border-radius: 5px; box-shadow: 0 1px 3px rgba(76,29,149,0.18); font-family: inherit; }  # predicate
.gstep-pattern .accent { color: #b45309; background: #fff; padding: 2px 9px; border-radius: 5px; box-shadow: 0 1px 3px rgba(124,45,18,0.18); font-family: inherit; }  # other
.gstep-pattern .accent { color:#2563eb; }  # legacy 248~252, 307~313 (단순 variant — chip 미적용)
```

→ 카테고리별 4 chip variant + legacy 단순 variant 1종 = 5종.

## 7-3. 변경 형식 — chip 효과 제거 + emphasis 보존

| 부분 | Before | After |
|------|--------|-------|
| color | `#2563eb` | `#2563eb` (유지) |
| background | `#fff` | (제거) |
| padding | `2px 9px` | (제거) |
| border-radius | `5px` | (제거) |
| box-shadow | `0 1px 3px rgba(...)` | (제거) |
| font-weight | (없음) | `800` (신규 — chip 없이도 강조) |
| font-family | `inherit` | `inherit` (유지) |

→ 결과: `.gstep-pattern .accent { color: VAR; font-weight: 800; font-family: inherit; }`

→ 시각적 효과: gstep-pattern 그라데이션 배경 위에서 카테고리 색상 + 굵은 글씨로 직접 강조 (chip 형태 제거 → 단순 텍스트 강조).

## 7-4. 자동화 스크립트 — `d:\놀라운 한국어 2026\_strip_accent_chip_bg.py`

핵심 정규식:

```python
ACCENT_CHIP_RE = re.compile(
    r"\.gstep-pattern\s+\.accent\s*\{\s*"
    r"color:\s*(#[0-9a-fA-F]{3,8})\s*;\s*"
    r"background:\s*#fff\s*;\s*"
    r"padding:\s*[^;]+;\s*"
    r"border-radius:\s*[^;]+;\s*"
    r"box-shadow:\s*[^;]+;\s*"
    r"font-family:\s*inherit\s*;?\s*"
    r"\}"
)
```

→ 색상 캡처 후 단순 형태로 재조립. 4종 chip variant 모두 매치.

## 7-5. 적용 결과

```
$ python _strip_accent_chip_bg.py --dry-run
# 67 target file(s) [DRY RUN]
# Summary: OK=65 SKIP=2 | accent rules stripped: 65

$ python _strip_accent_chip_bg.py
# Summary: OK=65 SKIP=2 | accent rules stripped: 65

$ python _strip_accent_chip_bg.py  # 멱등성 검증
# Summary: OK=0 SKIP=67 | accent rules stripped: 0
```

| 카테고리 | OK | 색상 |
|----------|---:|------|
| 문장구조 | 9 | `#2563eb` |
| 서술어 문법 | 21 | `#6d28d9` |
| 부사어 문법 | 16 | `#047857` |
| 기타 문법 | 19 | `#b45309` |
| **합계** | **65** | — |
| SKIP (legacy 단순 variant) | 2 | (수작업 통일) |

SKIP 2개 (legacy `.accent { color:#2563eb; }`):
- 248~252 부사어 문법 시간 때 → `#047857; font-weight: 800; font-family: inherit;` (수작업 통일)
- 307~313 부사어 문법 원인 때문에 (2) → 동일

→ 최종 67개 파일 모두 `color: VAR; font-weight: 800; font-family: inherit;` 통일.

## 7-6. 영향 범위

- 67개 HTML 파일의 `.gstep-pattern .accent` CSS 룰 단순화.
- HTML 콘텐츠(`<span class="accent">...</span>`) 무변경 — CSS 변경만으로 시각 효과 적용.
- 다른 CSS·JS·본문 무영향.

## 7-7. 부수 산출물

- **`_strip_accent_chip_bg.py`**: gstep-pattern accent chip 효과 자동 제거 (정규식 기반, idempotent)

> 📌 **버전 표기:** v9.92 (2026-05-06) — gstep-pattern accent 하얀 배경 chip 제거. 자동화 스크립트 `_strip_accent_chip_bg.py` 신규. 67개 파일 일괄 적용 (65 자동 + 2 legacy 수작업). 멱등성 보존.

> 🔁 **표준 갱신:** v9.92부터 `.gstep-pattern .accent` 는 `color: VAR; font-weight: 800; font-family: inherit;` 형태로 통일. 흰색 배경 chip 효과 금지 — 강조는 카테고리 색상 + 굵은 글씨로 표현.

---

# Part 8 — 한국어 음절 단위 줄바꿈 방지 — `word-break: keep-all` 일괄 적용 (1~500 67개 파일)

## 8-1. 사용자 지시

> "한국어가 잘린 경우 아래로 배치하라."

→ 사용자 스크린샷 — `<td>`에 `Future (미래)` 표시 시 `(미\n래)` 형태로 한국어 "미래" 가 음절 단위로 줄바꿈됨. 가독성 저하 + 학습자가 단어를 한 덩어리로 인식 어려움.

## 8-2. 원인 분석

기본 CSS `word-break: normal` 은 CJK 문자(한글·한자·일본어 등) 사이를 임의 위치에서 줄바꿈 가능 — 영문은 단어 경계에서만 끊지만 CJK는 음절 단위로 끊김.

`.gstep-table tbody td` 의 룰에 word-break 설정이 없어 기본값 적용됨. `.exp-table` 도 일부 셀(`.exp-en`, `.exp-role`)에만 `word-break: keep-all` 적용되어 있고 본 td 전체에 미적용.

## 8-3. 수정 방안 — `word-break: keep-all` + `overflow-wrap: break-word`

| 속성 | 값 | 효과 |
|------|----|------|
| `word-break: keep-all` | CJK 보존 | 한글 음절 사이에서 끊지 않음 (단어 단위로만 끊김) |
| `overflow-wrap: break-word` | 영문 안전망 | 한 단어가 셀 너비보다 길면 강제 끊김 (overflow 방지) |

→ 한국어는 단어 사이에서만 줄바꿈, 영어 long word는 필요 시 끊김 — 양쪽 모두 가독성 보장.

## 8-4. 자동화 스크립트 — `d:\놀라운 한국어 2026\_apply_korean_word_break.py`

핵심 알고리즘:

```python
MARKER = "/* v9.93 Korean word-break — keep CJK syllables together */"

CSS_BLOCK = f"""
        {MARKER}
        .gstep-table td, .gstep-table th {{ word-break: keep-all; overflow-wrap: break-word; }}
        .exp-table td, .exp-table th {{ word-break: keep-all; overflow-wrap: break-word; }}
"""

def transform(content: str) -> tuple[str, int]:
    if MARKER in content:
        return content, 0  # idempotent
    return re.sub(r"(\s*)</style>", f"{CSS_BLOCK}\\1</style>", content, count=1), 1
```

→ `</style>` 직전에 신규 룰 삽입 (least-invasive). 기존 CSS 무변경. 마커 코멘트로 멱등성 보장.

## 8-5. 적용 결과

```
$ python _apply_korean_word_break.py --dry-run
# 67 target file(s) [DRY RUN]
# Summary: OK=67 SKIP=0

$ python _apply_korean_word_break.py
# Summary: OK=67 SKIP=0

$ python _apply_korean_word_break.py  # 멱등성 검증
# Summary: OK=0 SKIP=67
```

## 8-6. 영향 범위

- 67개 HTML 파일의 `<style>` 블록 끝에 v9.93 마커 + 2개 CSS 룰(8개 셀렉터) 추가.
- `.gstep-table` 과 `.exp-table` 의 모든 td/th 한국어 텍스트 음절 단위 줄바꿈 방지.
- 다른 영역(blockquote, gstep-pattern, p, h1~h3 등)은 무변경 — 본 두 테이블에 한해 안전한 적용.
- 영어 콘텐츠 무영향 (`overflow-wrap: break-word` 가 안전망 역할).

## 8-7. 부수 산출물

- **`_apply_korean_word_break.py`**: 마커 기반 idempotent CSS 룰 삽입 자동화 스크립트

> 📌 **버전 표기:** v9.93 (2026-05-06) — 한국어 음절 단위 줄바꿈 방지. `.gstep-table`·`.exp-table` 셀에 `word-break: keep-all; overflow-wrap: break-word;` 일괄 적용. 자동화 스크립트 `_apply_korean_word_break.py` 신규. 67개 파일 일괄. 멱등성 보존.

> 🔁 **표준 갱신:** v9.93부터 모든 .gstep-table·.exp-table 셀의 한국어는 음절 단위가 아닌 단어 단위로만 줄바꿈. 신규 파일 작성 시 동일 CSS 룰 포함.

---

# Part 9 — 232~236 v3 → v9.93 완전체 재작성 — 확인·동의 단원 (~지요/죠)

## 9-1. 사용자 지시

> "232-236 파일에 문서화 적용"

→ 메모리에 명시된 잔여 항목 "v3-era 232~236 (확인 단원, 5문제) — Part 49 (2026-05-05 로그) 패턴 재사용" 진행. 213~217 (Part 49) 템플릿 기반 5-문제 v9.93 완전체 재작성.

## 9-2. 진단 — 변환 전 v3 상태

| 항목 | Before (v3, 185 라인) | After (v9.93, 목표) |
|------|----------------------|---------------------|
| Title block | predicate (이미 적용) | 유지 |
| Grammar Overview | 단일 gstep + pre 블록 (구식) | 4-step gstep (Why → What → Past → Noun) + gstep-pattern + gstep-table |
| Practice 5문제 | h3 + vocab-box + try-box + answer-box (build step-by-step 형식) | h3 + vocab-toggle + single-wt + answer-box + explain-box (with exp-table) |
| Read Along | (없음) | 5-item sentence-list + pastel CSS |
| Look & Speak | (없음) | 5-item flashcard-grid (3D flip) |
| Sentence Matching | (없음) | em-game (5 pairs) + 셀러브레이션 |
| Writing Test | (없음) | wt-toolbar + 5-item writing-practice + grade/reset/reveal + 셀러브레이션 |
| Complete Summary | 표 형식 (마지막) | 삭제 (v9.79 표준 — 중복) |
| 자동화 표준 | v9.93 word-break 만 적용 | 5종 보편 표준 + bold quoted + bold korean + accent chip 제거 모두 적용 |

## 9-3. 단원 학습 포인트 — 확인·동의 (~지요/죠)

### 9-3-a. 핵심 룰

| Pattern | 한국어 | 영어 | 의미 |
|---------|--------|------|------|
| stem + 지요? / 죠? | 좋죠? | nice, right? | 형용사 + 확인 (현재) |
| stem + 지요? / 죠? | 맛있죠? | delicious, right? | 형용사 + 확인 |
| 동사 + ~지요? / 죠? | 하죠? | do, right? | 동사 + 확인 |
| stem + 았/었 + 죠? | 피곤했죠? | were tired, right? | 형용사 + 과거 + 확인 |
| 명사 + (이)죠? | 11시죠? | it's 11, right? | 명사 + 확인 (받침 X = 죠?, 받침 O = 이죠?) |

### 9-3-b. ~지요 vs ~죠

`~지요` 는 정식 형태, `~죠` 는 일상 회화에서 더 흔한 줄임 형태. 양쪽 모두 의미·정중함 동일.

### 9-3-c. 학습 동기 — 한국 문화의 부드러운 화법

한국어는 단정적 표현보다 부드러운 확인·동의를 선호. ~지요/죠 는 화자가 X 가 사실이라고 믿지만 청자에게 "그렇죠?" 식으로 가벼운 동의를 구함. 영어 tag question (`isn't it?` / `right?`) 의 한국어 등가.

## 9-4. 5문제 — 영어/한국어/exp-table 행 설계

| Q | English | Korean | exp-role 시퀀스 (English order) |
|:-:|---------|--------|---------------------------------|
| 232 | The weather is really nice today, isn't it? | 오늘 날씨가 참 좋죠? | Time + Subject + Adverb + Predicate (confirmation, adjective) |
| 233 | This coffee is really good, isn't it? | 이 커피 정말 맛있죠? | Subject + Adverb + Predicate (confirmation, adjective) |
| 234 | The class is at 11 tomorrow, right? | 내일 수업이 11시죠? | Time + Subject + Predicate (confirmation, noun + 이죠) |
| 235 | You were really tired yesterday, weren't you? | 어제 너무 피곤했죠? | Time + Adverb + Predicate (confirmation, adjective past) |
| 236 | This song is really good, isn't it? | 이 노래 정말 좋죠? | Subject + Adverb + Predicate (confirmation, adjective) |

### 9-4-a. 형성 공식 핵심 굵게 (Q별)

- Q232: 좋다 (good) → stem **좋** + **죠?** = **좋죠?** (adjective confirmation, present)
- Q233: 맛있다 (delicious) → stem **맛있** + **죠?** = **맛있죠?**
- Q234: 11시 (eleven o'clock) → noun **11시** + **죠?** (받침 X — 11시의 시는 모음 ㅣ 끝) = **11시죠?**
- Q235: 피곤하다 (tired) → past stem **피곤했** + **죠?** = **피곤했죠?** (adjective past confirmation)
- Q236: 좋다 (good) → stem **좋** + **죠?** = **좋죠?**

## 9-5. 적용 결과

- 185 라인 → ~920 라인 (5배 확장).
- 5 자동화 검증 (5종 보편 표준 + bold quoted + bold korean + accent chip 제거 + word-break) 멱등성 SKIP.
- 본문 의미 0건 변경 — 5문제·정답·어휘 전부 그대로 유지.

## 9-6. 영향 범위

- 단일 파일 `232~236 서술어 문법 확인.html` 변경.
- 학습자 인터랙션: Read Along (5건) + Look & Speak (5건 카드 뒤집기) + Sentence Matching (5쌍 매칭 게임) + Writing Test (5건 채점 + 정답 표시) — 본 단원 진입 시 4가지 학습 모드 제공.
- exp-table v9.86-v9.87 룰 (English order 헤더) + v9.89 Rule A+B (영어 prose + 한국어 토큰 굵게) 모두 적용.

## 9-7. 잔여 작업 — 다음 진입점

| 작업 | 비고 |
|------|------|
| 237~247 (정중표현 단원) v3 → v9.93 완전체 재작성 | 11문제, 11-항목 파스텔 CSS 확장 필요 |
| 룰 4 (형성 공식 한국어 형태소 굵게) — 49개 파일 explanation cell 재검토 | 단원별 수작업 |
| 기타 문법 18개 파일 exp-table 도입 | 별도 후속 작업 |

> 📌 **버전 표기:** v9.93+ (2026-05-06) — 232~236 v3 → v9.93 완전체 재작성. Part 49 (213~217) 템플릿 기반. 5문제 일괄 학습 모드 4종 (Read Along, Look & Speak, em-game, Writing Test) + exp-table v9.86-v9.87 헤더 + v9.89 Rule B 적용. 멱등성 보존.

---

# Part 10 — 237~247 v3 → v9.93 완전체 재작성 — 정중표현 단원 (~(으)세요), 11문제 + 11-항목 파스텔 CSS 확장

## 10-1. 사용자 지시

> "237-247 파일에 문서화를 적용하라."

→ 마지막 v3-era 잔여 항목 진행. 정중표현(`~(으)세요`) 단원, 11문제 — Part 49의 5/9-항목 패턴을 11-항목으로 확장. 일상 정중 표현(인사·요청·작별 등) 11종 일괄.

## 10-2. 진단 — 변환 전 v3 상태

| 항목 | Before (v3, 280 라인) | After (v9.93, 목표) |
|------|----------------------|---------------------|
| Title block | predicate (적용됨) | 유지 |
| Grammar Overview | 단일 gstep + pre | 4-step gstep (Why politeness · Pattern · 받침 X/O · Honorific irregulars) |
| 11문제 본체 | h3 + try-box + answer-box | h3 + vocab-toggle + single-wt + answer-box + explain-box (exp-table) |
| Read Along | (없음) | 11-item sentence-list + 11-항목 파스텔 |
| Look & Speak | (없음) | 11-item flashcard-grid (3D flip) + 11-항목 그라데이션 |
| Sentence Matching | (없음) | em-game (11 pairs) |
| Writing Test | (없음) | wt-toolbar + 11-item writing-practice + 채점·정답·셀러브레이션 |
| Complete Summary | 표 | 삭제 (v9.79) |

## 10-3. 단원 학습 포인트 — 정중표현 (~(으)세요)

### 10-3-a. 핵심 룰

| 어간 종류 | 패턴 | 예시 |
|----------|------|------|
| 받침 X (모음 끝) | stem + **세요** | 오다 → **오세요** (please come) |
| 받침 O (자음 끝) | stem + **으세요** | 앉다 → **앉으세요** (please sit) |
| 하다 동사·형용사 | 하 + 세요 | 안녕하다 → **안녕하세요?** |
| 명령 + 정중 | stem + (으)세요 | 받다 → **받으세요** |

### 10-3-b. 특수 honorific 동사

일부 동사는 별도 honorific 어간:
- 먹다 (eat) → 드시다 → **드세요**
- 자다 (sleep) → 주무시다 → 주무세요
- 있다 (stay) → 계시다 → **계세요**
- 가다/오다 (go/come) — 일반 그대로 → 가세요/오세요 (단, 정중 인사 시 안녕히 가세요/계세요 짝 사용)

### 10-3-c. 일상 관용 표현 4종

| 표현 | 의미 | 비고 |
|------|------|------|
| **맛있게 드세요** | Enjoy your meal | 직역: "deliciously eat" — Korean idiom |
| **좋은 하루 되세요** | Have a nice day | "become a nice day" |
| **새해 복 많이 받으세요** | Happy New Year | "receive lots of New Year fortune" |
| **안녕히 가세요/계세요** | Goodbye | 떠나는 사람 → 가세요 / 남는 사람 → 계세요 |

## 10-4. 11문제 — 영어/한국어/exp-table 행 설계

| Q | English | Korean | exp-role 시퀀스 |
|:-:|---------|--------|-----------------|
| 237 | Hi, how are you? | 안녕하세요? | Predicate (greeting, polite question) |
| 238 | Welcome. | 어서 오세요. | Adverb + Predicate (polite imperative) |
| 239 | Please sit down here. | 여기에 앉으세요. | Place + Predicate (polite imperative) |
| 240 | One bibimbap, please. | 비빔밥 하나 주세요. | Object + Quantity + Predicate (polite request) |
| 241 | Enjoy your meal. | 맛있게 드세요. | Adverb + Predicate (polite imperative, honorific) |
| 242 | Goodbye. (to leaving) | 안녕히 가세요. | Adverb + Predicate (farewell to leaving) |
| 243 | Goodbye. (to staying) | 안녕히 계세요. | Adverb + Predicate (farewell to staying) |
| 244 | Have a nice day. | 좋은 하루 되세요. | Object + Predicate (polite wish) |
| 245 | Always be happy. | 늘 행복하세요. | Adverb + Predicate (polite wish, adjective) |
| 246 | Stay healthy always. | 늘 건강하세요. | Adverb + Predicate (polite wish, adjective) |
| 247 | Happy new year! | 새해 복 많이 받으세요. | Time + Object + Adverb + Predicate (idiomatic, ~으세요) |

## 10-5. 11-항목 파스텔 CSS 확장

기존 9-항목 패스텔 (Part 51 223~231 정형):
1. pink-50 `#fdf2f8` · 2. rose-50 `#fff1f2` · 3. orange-50 `#fff7ed` · 4. amber-50 `#fffbeb` · 5. yellow-50 `#fefce8` · 6. lime-50 `#f7fee7` · 7. emerald-50 `#ecfdf5` · 8. teal-50 `#f0fdfa` · 9. sky-50 `#f0f9ff`

**11-항목 확장 (신규)**:
- 10. blue-50 `#eff6ff` (border `#dbeafe`, back grad `#dbeafe→#bfdbfe`, color `#1e3a8a`)
- 11. indigo-50 `#eef2ff` (border `#e0e7ff`, back grad `#e0e7ff→#c7d2fe`, color `#312e81`)

→ Tailwind 50-level 11-색 무지개 일관 적용. Read Along · Writing Test · Look & Speak 모두 동일 컬러 사용.

## 10-6. 적용 결과 — 자동화 4종 검증

| 자동화 스크립트 | 결과 |
|----------------|------|
| `_apply_v9_87_rules.py --only "237~247"` | SKIP no-change (헤더 처음부터 English order 형식) |
| `_bold_korean_in_explanations.py --only "237~247"` | SKIP no-change (Rule B 처음부터 적용) |
| `_strip_accent_chip_bg.py --only "237~247"` | SKIP no-change (chip 부재) |
| `_apply_korean_word_break.py --only "237~247"` | SKIP no-change (v9.93 마커 적용) |

## 10-7. 영향 범위

- 단일 파일 `237~247 서술어 문법 정중표현.html` 변경.
- 280 → ~1170 라인 (4배 확장).
- 일상 11종 정중표현 → 4가지 학습 모드 (Read · Speak · Match · Write).
- 본문 정답·어휘 0건 변경.

## 10-8. 잔여 작업 — v3-era 완전 종료 ✓

| 작업 | 상태 |
|------|------|
| ~~232~236 (확인 단원)~~ | ✅ Part 9 완료 |
| ~~237~247 (정중표현 단원)~~ | ✅ Part 10 완료 |
| 룰 4 (형성 공식 한국어 형태소 굵게) | 49개 파일 단원별 수작업 (계속) |
| 기타 문법 18개 파일 exp-table 도입 | 별도 후속 작업 (현재 exp-table 0건) |

→ **v3-era 잔여 파일 0건 — exp-table 보유 카테고리(문장구조 9 + 서술어 21 + 부사어 20 = 50) 전체에 v9.93 표준 100% 도달.**

> 📌 **버전 표기:** v9.93+ (2026-05-06) — 237~247 v3 → v9.93 완전체 재작성. 11문제 + 11-항목 파스텔 CSS 확장 (Tailwind 50-level rainbow 11색 일관). Part 49/51 템플릿 기반. v3-era 잔여 0건 — 50개 파일 exp-table 카테고리 v9.93 표준 100%.

> 🔁 **표준 갱신:** v9.93+부터 multi-item 단원(5/9/11) 모두 동일 11-색 Tailwind 50-level rainbow 패스텔 사용. 학습 모드 4종(Read Along, Look & Speak, em-game, Writing Test) 일괄 적용.

---

# Part 11 — 부사어 문법 카테고리 전체 v9.93 적용 — 8개 파일 일괄 v3 → v9.93 완전체 재작성 (생성기 자동화)

## 11-1. 사용자 지시

> "문서화를 부사어 문법 전체에 적용하라."

→ 부사어 문법 카테고리 20개 파일 전수 점검 + 미적용 파일 일괄 v9.93 변환.

## 11-2. 진단 — 부사어 20개 파일 상태

```bash
$ for f in *부사어*.html; do exp=$(grep -c '"exp-table"' "$f"); ...
```

| 분류 | 파일 수 | 상태 | 처리 |
|------|--------:|------|------|
| **A** exp-table 보유 (Part 5에서 v9.86-v9.87 + word-break 적용 완료) | 12 | v9.93 ✓ | 자동화 SKIP 검증 |
| **B** exp-table 부재 (v3 단순 try-box 형식) | 8 | v3 → v9.93 변환 필요 | **생성기 일괄 변환** |
| 합계 | 20 | | |

**A 그룹 (12 파일, 자동화 SKIP 검증)**: 248~252, 253~259, 260~265, 266~270, 271~276, 277~283, 284~291, 292~295, 296~299, 300~306, 307~313, 314~315 — `_apply_v9_87_rules.py`, `_bold_korean_in_explanations.py`, `_strip_accent_chip_bg.py`, `_apply_korean_word_break.py` 4종 모두 SKIP no-change ✓

**B 그룹 (8 파일, 50문제, 변환 대상)**:

| 파일 | Q수 | 단원 | 핵심 룰 |
|------|---:|------|---------|
| 316~324 | 9 | 원인 | ~아/어서 (statement only) |
| 325~331 | 7 | 원인 | ~(으)니까 (commands OK) |
| 332~337 | 6 | 목적 | (noun) 위해서 |
| 338~345 | 8 | 목적 | (verb) ~(으)려고 |
| 346~351 | 6 | 목적 | ~(으)러 가다/오다 |
| 352~355 | 4 | 조건 | ~(으)면 |
| 356~361 | 6 | 필수 조건 | ~(으)려면 |
| 362~365 | 4 | 예상과 다른 결과 | ~아/어도 |
| **합계** | **50** | | |

## 11-3. 변환 전략 — 생성기 자동화

직접 수작업 시 8 파일 × ~1100 라인 = ~8800 라인 HTML 작성 → context-heavy. **Python 생성기** 작성으로 효율화:

### 11-3-a. 생성기 구조

`d:\놀라운 한국어 2026\_generate_adverbial_v9_93.py`:
- **CSS 템플릿** (전체 파일 공통, 1-callable function): adverbial 팔레트 (Emerald `#064e3b`/`#047857`/`#10b981`) + 4-11 항목 파스텔 CSS 동적 확장
- **per-Q 렌더 함수**: vocab-toggle + single-wt + answer-box + explain-box (exp-table 4-row 표준)
- **per-file 렌더**: header(title-block) + gstep multi-step + Practice + Read Along + Look & Speak + em-game + Writing Test + scripts
- **per-file 데이터 (DATA dict in script)**: 8 파일 각각 (제목, 부제, gstep 내용, 50 Q 데이터)

### 11-3-b. 동적 항목 수 처리

n-항목 파스텔 CSS는 Tailwind 50-level rainbow 11색에서 첫 n개 자동 슬라이스:
- 4-항목: pink·rose·orange·amber (4)
- 6-항목: + yellow·lime (6)
- 7-항목: + emerald (7)
- 8-항목: + teal (8)
- 9-항목: + sky (9)

각 파일에 정확히 필요한 항목 수만큼 CSS 룰 생성.

### 11-3-c. exp-table 행 데이터 (per-Q)

본 작업의 explain-box는 **간소화 형태**: 핵심 형성 공식 + 1-2개 row 분석. 232~236/237~247 의 full role-by-role 분석보다 컴팩트 (8 파일 × 50 Q 규모 작업의 효율성 우선).

표준 형식:
- exp-table colspan 헤더: `English order: ENG_ROLES — Korean: KO_FORM_CHAIN`
- 핵심 행 1-3개 (Subject·Verb·Predicate 또는 절·절 분리)
- 형성 공식 explain-note: stem + 어미 결합 룰 (~아/어서 / ~(으)니까 / 위해서 등)

## 11-4. 적용 결과 (예정)

```
$ python _generate_adverbial_v9_93.py
# Generating 8 files (50 Q total)...
... (8 files written)
# Done.

$ python _apply_v9_87_rules.py 2>&1 | grep -E "OK|Summary"
# Summary: OK=0 SKIP=N (모두 SKIP — 처음부터 v9.86-v9.87 표준 충족)

$ python _bold_korean_in_explanations.py 2>&1 | tail -1
# Summary: ... | (Rule B 첫 실행 후 멱등성 ✓)

$ python _apply_korean_word_break.py 2>&1 | tail -1
# Summary: OK=0 SKIP=67 (마커 처음부터 포함)
```

## 11-5. 영향 범위

- 8개 부사어 HTML 파일 v3 → v9.93 완전체 재작성.
- 본문 50문제 의미 0건 변경 (모든 Korean 정답 기존 v3 그대로 보존).
- Grammar Overview 단원별 4-step gstep 구조 신규 작성 (총 32 gsteps).
- 학습 모드 4종 (Read Along · Look & Speak · em-game · Writing Test) 8 파일 모두 신규 추가.
- exp-table 카테고리 100% 도달: 문장구조 9 + 서술어 21 + 부사어 20 = **50 → 58 파일** (8 신규).

## 11-6. 잔여 작업

| 작업 | 상태 |
|------|------|
| ~~232~236, 237~247 v3 → v9.93~~ | ✅ 완료 |
| ~~부사어 문법 8개 파일 v3 → v9.93~~ | ✅ Part 11 완료 |
| 룰 4 (형성 공식 한국어 형태소 굵게) | 단원별 수작업 (deferred) |
| 기타 문법 18개 파일 exp-table 도입 | 별도 후속 작업 |

> 📌 **버전 표기:** v9.94 (2026-05-06) — 부사어 문법 카테고리 전체 v9.93 도달. 8개 파일 v3 → v9.93 완전체 재작성. 자동화 생성기 `_generate_adverbial_v9_93.py` 신규. 50문제 + 32 gsteps + 4 학습 모드 일괄. 멱등성 검증.

> 🔁 **표준 갱신:** exp-table 카테고리(문장구조 + 서술어 + 부사어) 전체 58 파일 v9.93 표준 100% 도달. 다음 진입점: 룰 4 (형성 공식 굵게) 또는 기타 문법 18개 exp-table 도입.

---

# Part 12 — 기타 문법 카테고리 전체 v9.93 적용 — 18개 파일 일괄 v3 → v9.93 완전체 재작성 (3-stage 파이프라인)

## 12-1. 사용자 지시

> "기타문법 전체에 문서화를 적용하라."

→ 기타 문법 카테고리 18개 파일 전수 점검 + 모두 v9.93 완전체 변환.

## 12-2. 진단 — 기타 18개 파일 상태

```powershell
PS> Get-ChildItem -Filter "*기타*.html" | %{ $f=$_.FullName; ... }
```

| 파일 (18개) | exp-table | gstep | title-block | 상태 |
|------------|----------:|------:|------------:|------|
| 366~369 배경/상황 (~는데) | 0 | 1 | 1 | v3 |
| 370~383 비교급/최상급 | 0 | 1 | 1 | v3 |
| 384~387 명사형 | 0 | 1 | 1 | v3 |
| 388~396 부사형 | 0 | 1 | 1 | v3 |
| 397~400 동사화 | 0 | 1 | 1 | v3 |
| 401~407 한정 (만/밖에) | 0 | 1 | 1 | v3 |
| 408~413 ~로/으로 | 0 | 1 | 1 | v3 |
| 414~418 빈도부사 | 0 | 1 | 1 | v3 |
| 419~435 종별사 | 0 | 1 | 1 | v3 |
| 436~440 ㄹ불규칙 | 0 | 1 | 1 | v3 |
| 441~444 ㅡ불규칙 | 0 | 1 | 1 | v3 |
| 445~448 르불규칙 | 0 | 1 | 1 | v3 |
| 449~453 ㅂ불규칙 | 0 | 1 | 1 | v3 |
| 454~459 ㄷ불규칙 | 0 | 1 | 1 | v3 |
| 460~461 ㅅ불규칙 | 0 | 1 | 1 | v3 |
| 462~473 간접화법 | 0 | 1 | 1 | v3 |
| 474~488 관형어 | 0 | 1 | 1 | v3 |
| 489~500 존댓말 | 0 | 1 | 1 | v3 |

**18개 모두 v3-era**: title-block other + 단일 gstep + 기존 v3 Practice (try-box "Build step-by-step" + Complete Summary). exp-table·Read Along·Look & Speak·em-game·Writing Test 모두 부재. 합계 **135문제**.

## 12-3. 3-stage 변환 파이프라인

부사어 단일 생성기 패턴(Part 11) 대비 **추출 → 증강 → 생성** 3단계로 분리하여 데이터 흐름 명확화:

| Stage | 스크립트 | 입력 | 출력 |
|-------|---------|------|------|
| 1 추출 | `_extract_other_v3_data.py` | 18개 v3 HTML | `_other_v9_93_skeleton.json` (135 Q raw — num/en/ko/vocab/note) |
| 2 증강 | `_augment_other_v9_93.py` | skeleton.json + per-file FILE_AUG dict | `_other_v9_93_data.json` (gsteps + exp_header + exp_rows + explain_note 추가) |
| 3 생성 | `_generate_other_v9_93.py` | data.json + Amber 팔레트 CSS 템플릿 | 18개 v9.93 HTML |

### 12-3-a. Stage 1 — 추출 (`_extract_other_v3_data.py`)

정규식으로 v3 파일에서 핵심 데이터 추출:
- **메타**: `<title>` · `.title-main`/`.title-sub`/`.title-ko-main`/`.title-ko-sub`
- **per-Q**: `<h3>NUM) EN</h3>` 경계로 청크 분할 → 각 청크에서 `.ans-kr` 한국어 정답, `.vocab-box` 어휘, `.note-box` 학습 팁 추출

결과: 135 Q × 5필드 (num/en/ko/vocab/note) — 본문 의미 0건 가공, 그대로 보존.

### 12-3-b. Stage 2 — 증강 (`_augment_other_v9_93.py`)

**`FILE_AUG` 딕셔너리** (18 키): 파일별 `gsteps` (2-3 step 문법 정리), `overview_subtitle`, `footer`, 누락된 `title_ko_main/sub` 보강.

**범용 exp-row 생성기** `auto_exp(en, ko)`:
- `split_subject_predicate()` 토큰 단위 분할 — 첫 번째 한국어 토큰이 `(은|는|이|가|께서)`로 끝나면 그 시점에 분할
- Subject row + Predicate row 자동 생성 (단일 row fallback: 주어 생략 시)
- exp_header: `English order: Subject + Predicate<br>영어 어순: 주어 + 서술어 — Korean: <subj> + <pred>`

**explain_note**: 원본 `.note-box` 내용을 그대로 `explain_note`로 승격 (이미 `<strong>` 태그 포함된 학습 팁 재사용).

**룰 4 (형성 공식 굵게)**: gstep `extra` 안의 `gstep-table`/`gstep-pattern`/`explain-note`에서 한국어 형태소·어미 모두 `<strong>` 처리 (수작업).

### 12-3-c. Stage 3 — 생성 (`_generate_other_v9_93.py`)

부사어 생성기(`_generate_adverbial_v9_93.py`) 복제 후 **Amber 팔레트로 일괄 swap**:

| 요소 | Adverbial (Emerald) | Other (Amber) |
|------|---------------------|---------------|
| Title gradient | `#064e3b → #047857 → #10b981` | `#7c2d12 → #b45309 → #f59e0b` |
| h2/h3 텍스트 | `#047857` | `#b45309` |
| h3/vocab-box bg | `#ecfdf5` | `#fef3c7` |
| answer-toggle bg | `#d1fae5` | `#fde68a` |
| flashcard-back gradient | emerald | amber |
| writing-num/em-num | `#10b981` | `#f59e0b` |
| em-progress-bar | `#10b981 → #6ee7b7` | `#f59e0b → #fcd34d` |
| exp-table colspan th | `#d1fae5/#065f46` | `#fde68a/#78350f` |
| em-message | `#ecfdf5/#10b981` | `#fef3c7/#f59e0b` |
| header class | `title-block adverbial` | `title-block other` |
| config 경로 | `_adverbial_v9_93_data.json` | `_other_v9_93_data.json` |

**파스텔 팔레트 확장**: 11색 (1-11항목) → **17색 (1-17항목)**. 종별사 17문제(419~435)와 관형어 15문제(474~488)에 대응. Tailwind 50-level rainbow 17색 (pink·rose·red·orange·amber·yellow·lime·green·emerald·teal·cyan·sky·blue·indigo·violet·purple·fuchsia).

## 12-4. 적용 결과

```
$ python _extract_other_v3_data.py
# 18 files · 135 Q extracted

$ python _augment_other_v9_93.py
OK 366~369 ... : 4 Q, 3 gsteps
OK 370~383 ... : 14 Q, 3 gsteps
... (16 lines omitted) ...
OK 489~500 ... : 12 Q, 3 gsteps
Wrote _other_v9_93_data.json (135 Q across 18 files)

$ python _generate_other_v9_93.py
# 18 file(s)
OK 366~369 ... (4 Q, 836 lines)
OK 370~383 ... (14 Q, 1416 lines)
... (16 lines omitted) ...
OK 489~500 ... (12 Q, 1299 lines)
# Done.
```

| 파일 | Q | 라인 (변환 후) | 변환 전 라인 | 증가율 |
|------|---:|---:|---:|---:|
| 366~369 | 4 | 836 | 230 | ×3.6 |
| 370~383 | 14 | 1416 | 299 | ×4.7 |
| 384~387 | 4 | 835 | 247 | ×3.4 |
| 388~396 | 9 | 1123 | 283 | ×4.0 |
| 397~400 | 4 | 827 | 150 | ×5.5 |
| 401~407 | 7 | 1007 | 193 | ×5.2 |
| 408~413 | 6 | 942 | 163 | ×5.8 |
| 414~418 | 5 | 885 | 146 | ×6.1 |
| 419~435 | 17 | 1588 | 284 | ×5.6 |
| 436~440 | 5 | 885 | 148 | ×6.0 |
| 441~444 | 4 | 827 | 161 | ×5.1 |
| 445~448 | 4 | 827 | 138 | ×6.0 |
| 449~453 | 5 | 894 | 160 | ×5.6 |
| 454~459 | 6 | 952 | 167 | ×5.7 |
| 460~461 | 2 | 711 | 125 | ×5.7 |
| 462~473 | 12 | 1300 | 218 | ×6.0 |
| 474~488 | 15 | 1474 | 249 | ×5.9 |
| 489~500 | 12 | 1299 | 218 | ×6.0 |
| **합계** | **135** | **18628** | **3479** | **×5.4** |

총 **18628 라인 v9.93 HTML 일괄 생성** (이전 v3 대비 5.4배 증가 — exp-table·Read Along·Look & Speak·em-game·Writing Test·셀러브레이션·전체 CSS 일습 추가).

## 12-5. 멱등성 검증 — 7종 자동화 스크립트

| 스크립트 (룰) | 첫 실행 | 두 번째 실행 |
|---------------|---------|---------------|
| `_apply_korean_word_break.py` (v9.93) | OK=0 SKIP=67 | OK=0 SKIP=67 ✓ |
| `_strip_accent_chip_bg.py` (v9.92) | OK=0 SKIP=67 | OK=0 SKIP=67 ✓ |
| `_apply_v9_87_rules.py` (v9.86-v9.87) | OK=0 SKIP=67 | OK=0 SKIP=67 ✓ |
| `_strip_korean_translation.py` (v9.85) | 0 stripped × 67 | 0 stripped × 67 ✓ |
| `_strip_quotes_around_bold.py` (v9.84) | 64 stripped × 67 | 0 stripped × 67 ✓ |
| `_bold_quoted_text.py` (v9.84) | 71 replacements × 67 | 0 replacements × 67 ✓ |
| `_bold_korean_in_explanations.py` (v9.89) | 0 bolded × 67 | 0 bolded × 67 ✓ |

**스크립트 보강**: `_apply_v9_87_rules.py`의 `detect_category` 에 `"기타 문법" → "other"` 추가, `parse_situation` 비활성화 카테고리 목록에 `"other"` 추가 — 부사어와 동일하게 trailing parenthetical은 row qualifier로 처리.

**v9.84 룰 1차 통과 사유**: 증강 단계에서 v3 `note-box` 콘텐츠를 `explain_note` 로 승격할 때 일부 `"<strong>X</strong>"` 패턴 (인용부호로 감싼 굵은 강조)이 노출 → v9.84 무인용 룰로 64건 자동 제거. 이후 신규 노출 없음 (멱등성 ✓). 본문 인용 (간접화법 직접화법 인용 `"날씨가 너무 덥습니다.라고 했습니다"` 등)은 strong 단독 wrap 패턴이 아니라서 보존됨 ✓.

## 12-6. 영향 범위

- 18개 기타 문법 HTML 파일 v3 → v9.93 완전체 재작성.
- **본문 의미 0건 변경** — 추출된 135 Q 한국어 정답은 v3 그대로 보존.
- Grammar Overview 단원별 2-3 step gstep 구조 신규 작성 (총 49 gsteps).
- 학습 모드 4종 (Read Along · Look & Speak · em-game · Writing Test) 18 파일 모두 신규 추가.
- 셀러브레이션 모달 (정답 100% 시 confetti) 18 파일 신규.
- exp-table colspan 헤더 영어 우선 + 한국어 어순 + Korean 토큰 형식 (v9.86-v9.87) 신규.
- 자동 Subject/Predicate 분할 (135 Q 중 ~110 건 2-row, ~25 건 1-row "Subject omitted" fallback).
- 4 카테고리 팔레트 통일: 문장구조(Sapphire) + 서술어(Violet) + 부사어(Emerald) + **기타(Amber 신규 일괄 적용)**.

## 12-7. 산출물 — 신규 자동화 스크립트 3종

- `d:\놀라운 한국어 2026\_extract_other_v3_data.py` — v3 HTML → skeleton.json (idempotent, 단순 정규식 추출)
- `d:\놀라운 한국어 2026\_augment_other_v9_93.py` — skeleton.json + FILE_AUG → data.json (per-file gsteps 하드코드 + 자동 row 생성)
- `d:\놀라운 한국어 2026\_generate_other_v9_93.py` — data.json → 18 HTML (Amber 팔레트, 17-항목 파스텔 확장)

**중간 산출물**:
- `d:\놀라운 한국어 2026\_other_v9_93_skeleton.json` (1126 라인 — 추출 결과, 재실행 시 갱신)
- `d:\놀라운 한국어 2026\_other_v9_93_data.json` (증강 결과, 재실행 시 갱신)

신규 파일 추가 시: skeleton에 entry 추가 → augment 재실행 → generate 재실행 → 자동화 4종 검증 통과 확인.

## 12-8. 다음 진입점

| 작업 | 상태 |
|------|------|
| ~~232~236, 237~247 v3 → v9.93~~ | ✅ Part 9·10 완료 |
| ~~부사어 문법 8개 파일 v3 → v9.93~~ | ✅ Part 11 완료 |
| ~~기타 문법 18개 파일 exp-table 도입~~ | ✅ Part 12 완료 |
| ~~룰 4 (형성 공식 한국어 형태소 굵게)~~ | ✅ Part 13 완료 (자동화 가능 판명) |
| 셀러브레이션 모달 1~86 시리즈 확산 (현재 v9.93 67파일 미보유) | 별도 후속 |

> 📌 **버전 표기:** v9.95 (2026-05-06) — 기타 문법 카테고리 전체 v9.93 도달. 18개 파일 v3 → v9.93 완전체 재작성. 자동화 3-stage 파이프라인 신규 (extract → augment → generate). 135문제 + 49 gsteps + 4 학습 모드 + Amber 팔레트 + 17-항목 파스텔 확장. 멱등성 검증 7종 통과.

> 🔁 **표준 갱신:** exp-table 카테고리(문장구조 9 + 서술어 21 + 부사어 20 + 기타 18) **전체 68 파일 v9.93 표준 100% 도달**. v3-era 잔여 0건. 다음 진입점: 룰 4 (형성 공식 굵게) — 단원별 수작업.

> 🔁 **자동화 보강:** `_apply_v9_87_rules.py` 의 `detect_category` 에 `"기타 문법" → "other"` 추가, `parse_situation` 비활성화 카테고리에 `"other"` 추가. v9.96+ 신규 기타 파일 자동 처리 가능.

> 🔁 **표준 갱신:** v9.89부터 해설 컬럼은 항상 (1) 영어 prose (2) 안의 모든 한국어 토큰 굵게 (3) Korean translation 라인 없음. 신규 파일에 4-7의 워크플로 따라 자동화 5단계로 표준 도달.

---

# Part 13 — 룰 4 (형성 공식 한국어 형태소 굵게) 자동화 + 1~500 67개 파일 일괄 적용 (`_bold_korean_in_formations.py`)

## 13-1. 사용자 지시

> "놀라운 한국어 500 해설집 이어서 작업하라."

→ Part 12 (v9.95) 종료 시점 잔여 진입점 = **룰 4 (형성 공식 한국어 형태소 굵게)**. 메모리 기록상 "단원별 수작업, ~10-15시간 추정 / 자동화 어려움"으로 deferred 됐던 항목.

## 13-2. 자동화 가능성 재검토 — 결정: 자동화 가능

이전(Part 3 §3-5) 자동화 미적용 사유:
- 형성 공식 다양성 (`X + Y → Z`, `X stem Y + Z = W` 등)
- 영어 prose 안 inline 한국어와 형성 공식 한국어 토큰 구분 어려움
- False positive 우려

재검토 결과:
- **v9.89 (`_bold_korean_in_explanations.py`)에서 동일 문제(영어 prose + inline 한국어 혼재)를 placeholder substitution + KOREAN_TOKEN_RE 정규식으로 안전하게 해결한 선례 있음.**
- 룰 4의 형성 공식은 특정 컨테이너(`gstep-pattern`, `gstep blockquote`, `gstep-table tbody td`)에 한정되어 있음 → 컨테이너 단위로 처리 가능.
- 기존 `<strong>` 및 `<span class="accent">` 블록을 stash → 한국어 토큰 wrap → restore 방식으로 멱등성 보장.

## 13-3. 룰 4 적용 범위 정의

**대상 컨테이너 3종** (단계별 적용):

| 컨테이너 | 위치 | 예시 |
|---------|------|------|
| `<div class="gstep-pattern">…</div>` | 형성 공식 박스 (양쪽 stems + accent + small 예시) | `받침 X: stem + <span class="accent">ㄴ 적이 있다</span><small>가다 → 간 적이 있다</small>` |
| `<blockquote>…</blockquote>` | gstep 카드 내 형성 chain demonstration | `가다 → 가 + ㄴ 적이 = <strong>간 적이 있습니다</strong>` |
| `<td>` in `<table class="gstep-table"><tbody>` | Pattern·Meaning·Example 컬럼 | `<td>간 적이 있습니다</td>` (Example col, 미굵게 상태) |

**비대상 (의도적 제외)**:
- `<p class="ko">` — 한국어 hint 문장. 모든 글자가 한국어 → 전체 굵게는 의미 강조 효과 무효화.
- `<p class="en">` — 영어 prose. 한국어 토큰은 이미 strong 처리되어 있음 (gstep 본문 강조 정책).
- `.exp-table` td — v9.89 Rule B 별도 자동화로 이미 처리됨.
- `.note-box`, `.explain-note` — 대부분 이미 strong 수동 적용됨. 후순위.

## 13-4. 자동화 스크립트 — `d:\놀라운 한국어 2026\_bold_korean_in_formations.py`

**전략 (placeholder substitution):**
1. 컨테이너 3종 각각 정규식으로 추출
2. 컨테이너 내부 `<strong>...</strong>` 블록 stash
3. 컨테이너 내부 `<span class="accent">...</span>` 블록 stash
4. 남은 텍스트에서 `KOREAN_TOKEN_RE` (`~?[가-힯ㄱ-ㆎ]+(?:\s+[가-힯ㄱ-ㆎ]+)*[~?]?`) 매칭하여 `<strong>...</strong>` wrap
5. stash된 블록 복원

**멱등성**: 두 번째 실행 시 모든 한국어 토큰이 이미 `<strong>` 안에 있어서 stash → wrap 단계에서 변화 없음 → restore 후 결과 동일.

**안전 skip**:
- `<style>` / `<script>` / `<title>` 안 텍스트는 컨테이너 정규식이 닿지 않음 (3종 컨테이너 모두 body 안 특정 클래스)
- HTML 속성 값 (`class="gstep-pattern"` 등)은 정규식 group capture에서 제외

## 13-5. 적용 결과

**Dry-run (전체 67파일)**: OK=62, SKIP=5, **총 1705 한국어 토큰 wrap 예정**

**실제 적용 (2단계)**:
1. 단일 파일 검증: `190~198` (pattern=9, bq=9, table=2 → 20 tokens) + `51~60` (pattern=53, bq=1 → 54 tokens) 일괄 적용 후 spot check
2. 전체 일괄: `OK=60, SKIP=7, Total: 1518 Korean tokens bolded` (1단계 적용분 제외)

**총 합계: 62 파일 × 평균 27.5 토큰 = 1705 한국어 토큰 굵게**

**카테고리별 분포**:
- 문장구조 (9개): 79~86 35 tokens 등 (기본 형성 표 위주)
- 서술어 문법 (21개): 169~180 133 tokens (최대), 199~203 63, 181~189 46
- 부사어 문법 (20개): 248~252 42, 266~270 41, 271~276 40
- 기타 문법 (18개): 445~448 36, 441~444 34, 366~369 22

**SKIP 5건**: 1~10, 11~20, 61~70, 71~78, 190~198 (이미 v9.89 또는 본 작업 전 단계에서 처리되었거나 formation 컨테이너 미보유)

## 13-6. 검증 — spot check

**51~60 (pattern=53)**: gstep-pattern 다수 박스 — `<strong>주어</strong> + <strong>서술어</strong>(<span class="accent">누구입니까?</span>)` 형태로 한국어 토큰 굵게, accent span 안 토큰 보존 ✓

**190~198 (이미 적용)**:
- gstep-pattern: `<small><strong>가다</strong> → <strong>간 적이 있다</strong> · <strong>먹다</strong> → <strong>먹은 적이 있다</strong></small>` ✓
- blockquote: `<strong>가다</strong> → <strong>가</strong> + <strong>ㄴ 적이</strong> = <strong>간 적이 있습니다</strong>` ✓
- gstep-table tbody td: `<td><strong>간 적이 있습니다</strong></td>` (Example col 한국어 굵게) ✓

**멱등성**: 1차 적용 후 dry-run 재실행 → 67/67 SKIP (Total: 0) ✓

## 13-7. 영향 범위

- 67개 HTML 파일 중 62개에 변경 (5개는 formation 컨테이너 미보유)
- `<strong>` 신규 wrap 1705개. 기존 `<strong>` 및 `<span class="accent">` 보존
- 본문 의미·구조 무변경 (시각 강조만 추가)
- v9.86-v9.95 표준 모두 보존

## 13-8. 부수 산출물

신규 자동화 스크립트:
- **`d:\놀라운 한국어 2026\_bold_korean_in_formations.py`** (v9.96)
  - 3종 컨테이너 (`gstep-pattern` / `blockquote` / `gstep-table tbody td`)에 KOREAN_TOKEN_RE 적용
  - placeholder substitution: 기존 `<strong>...</strong>` + `<span class="accent">...</span>` stash → 한국어 토큰 wrap → restore
  - 멱등성 보장 (두 번째 실행 0 changes)

> 📌 **버전 표기:** v9.96 (2026-05-06) — 룰 4 자동화 가능 판명 + 1~500 67파일 일괄 적용. 1705 한국어 토큰 formation 컨테이너에서 굵게 처리. 메모리 기록상 "10-15시간 수작업 / 자동화 어려움" deferred 항목이 정규식 컨테이너 단위 처리로 분 단위 자동화 완료.

---

# Part 14 — v9.96 Direction marker 에 한정어 정형화 — 가다/다니다 motion verb 컨텍스트 명시 (14개 파일 18개 row 일괄)

## 14-1. 사용자 지시

> 이미지: `한국 + direction marker 에.`
> "문장의 동사(서술어)가 가다, 다니다 인 경우 장소+에 가 온다. 한국에 가다, 한국에 간 적이 있다,"

→ 외국인 학습자가 `direction marker 에`만 보고는 **언제** 이 표지를 쓰는지 알 수 없음. 사용자의 핵심 통찰: 이 표지의 사용 조건은 **서술어가 motion verb (가다/다니다)** 인 경우. 따라서 explanation cell에서 동사 컨텍스트를 명시해야 함.

## 14-2. 적용 전 상태 진단 — 35개 row의 직교성 검사

전 14개 파일 grep `direction marker` 결과 35건. 두 부류로 분리:

**A. 이미 한정어 보유 (17건) — 변경 불필요:**
- `(destination of motion verb 가다)` — 8건 (123~141:1344, 142~149:412, 157~163:397/577, 277~283:399, 51~60:671/711, 61~70:354)
- `(destination of a motion verb)` — 4건 (51~60:671/711, 61~70:311, 79~86:378)
- `(destination of motion verb 가다)` 변형 — 5건 (97~104:427 "the motion verb 가다", 271~276:489, 292~295:474 "도착하다", 316~324:514 "with motion verb 가다", 142~149:458 "(a place name)")
- 209~212:481 (입원하다 takes 에 — 다른 동사 케이스)
- 260~265:533 (Korea + direction marker 에 — 인라인 형식)

**B. 한정어 부재 (18건) — bare `X + direction marker 에.` 형식, 정형화 필요:**

| 파일 | Row | 명사 | 본 row의 동사 |
|------|-----|------|--------------|
| 123~141 | 1388 | 산 | 가지 마세요 (가다) |
| 123~141 | 1432 | 산 | 갈까요? (가다) |
| 123~141 | 1476 | 산 | 가지 말까요? (가다) |
| 123~141 | 1520 | 산 | 갑시다 (가다) |
| 123~141 | 1564 | 산 | 가지 맙시다 (가다) |
| 190~198 | 411 | 한국 | 간 적이 있습니까? (가다) |
| 190~198 | 455 | 한국 | 간 적이 있습니다 (가다) |
| 190~198 | 499 | 한국 | 간 적이 없습니다 (가다) |
| 190~198 | 543 | 한국 | 가 보았습니까? (가다) |
| 190~198 | 587 | 한국 | 가 보았습니다 (가다) |
| 190~198 | 631 | 한국 | 가 보지 못했습니다 (가다) |
| 190~198 | 761 | 제주도 | 가 보고 싶습니다 (가다) |
| 277~283 | 447 | 극장 | 갔습니다 (가다) |
| 277~283 | 491 | 극장 | 가서 (가다) |
| 277~283 | 537 | 백화점 | 가서 (가다) |
| 277~283 | 629 | 체육관 | 가서 (가다) |
| 277~283 | 675 | 은행 | 가서 (가다) |
| 316~324 | 560 | 병원 | 갑니다 (가다) |

→ 18건 모두 동일 동사 (**가다**). `다니다`는 Bare 케이스에 없음 (현재 단원 범위 한계). 사용자의 일반 규칙은 가다/다니다 두 동사 모두 포함이지만, 본 정형화는 **각 row의 실제 동사를 명시**하는 정책 (Part 1 §1-3 v9.86 헤더 정책과 일치 — "row의 실제 데이터를 헤더에 반영").

## 14-3. 적용 룰 — v9.96

**Before:** `<strong>{NOUN}</strong> + direction marker <strong>에</strong>.`
**After:** `<strong>{NOUN}</strong> + direction marker <strong>에</strong> (destination of motion verb <strong>가다</strong>).`

→ 17건 기존 한정어와 형식 통일. `(destination of motion verb 가다)` 가 가장 빈번한 표현(8건)이므로 표준 형식으로 채택.

**다니다 케이스 후속 처리 (예정)**: 향후 `~에 다닙니다` (학교에 다닙니다 등) 패턴 발견 시 동일 룰 적용 (`(destination of motion verb 다니다)`). 현재 단원 범위에서는 0건.

## 14-4. 적용 — 18건 일괄 Edit

(Edit 결과 기록 예정)

## 14-5. 검증 — grep 재실행

(grep `direction marker <strong>에</strong>\.</td>` → 0건 잔여 확인 예정)

## 14-6. 메모리 갱신

`project_500_explanation_set.md` 에 v9.96 추가 — direction marker 한정어 정책 (motion verb 컨텍스트 명시).

## 14-7. 향후 자동화 후보

본 정형화는 18건 수작업으로 충분(범위 작음). 향후 다니다/오다/도착하다 등 다른 motion verb 케이스 확산 시 다음 자동화 가능:
- 패턴: `<td>` exp-form 셀에 `에</strong>` 패턴 + 인접 row의 동사 추출 → 동일 td의 explanation 셀에 한정어 자동 추가
- 우선순위: 본 v9.96 적용 후 다른 단원에 누락 발견 시

---

# Part 15 — exp-table 헤더 한국어 어순으로 정정 (v9.97) — "English order" → "Korean order" 일괄 교정

## 15-1. 사용자 지시

사용자 스크린샷 (199~203 위한 행동 단원 Q에서):
> 헤더 라인: `English order: Subject + Verb + Object + Time (plan)` / `영어 어순: 주어 + 동사 + 목적어 + 시간 (계획) — Korean: 저는 + 보려고 합니다 + 영화를 + 이번 주 일요일에`

> "해설에서 맹 위에 있는 제목은 영어 어순이 아니라 한국어 어순이다. 한국어 어순은 '주어 + 부사어 + 목적어 + 동사'이다. 부사어에는 시간, 장소, 이유, 목적, 가정/조건, 상황/배경, 양보 등이 들어간다. 처음 한국어를 배울 때 많이 사용하는 부사어는 시간과 장소이다. 이 경우 문장 순서는 '주어 + 시간 + 장소 + 목적어 + 서술어'이다. 해설 표 제일 윗줄에는 영어 어순이 필요한 것이 아니다. 이것은 영어를 배우는 사람에게 필요한 것이 아니라 한국어를 배우는 사람에게 이 설명이 필요한 것이다. 따라서 반드시 한국어 문장에 맞는 한국어 어순을 써야 한다. 다만 그 아래 표에 단어나 어휘를 쓸 때는 영어 문장에 나오는 순서대로 써야 한다. 이것을 문서화 하고 모든 파일에 적용하라."

## 15-2. 룰 정형화 — v9.86 표준 정정

**v9.86 (잘못된 표준)**:
- `English order: Subject + Verb + Object + Time` / `영어 어순: 주어 + 동사 + 목적어 + 시간 — Korean: KO_FORMS_IN_ENGLISH_ORDER`
- 표 본문 행: 영어 문장 순서

**v9.97 (정정된 표준)**:
- `Korean order: Subject + Time + Object + Verb` / `한국어 어순: 주어 + 시간 + 목적어 + 서술어 — 예: KO_FORMS_IN_KOREAN_ORDER`
- 표 본문 행: 영어 문장 순서 **유지** (영어 학습 동선 보존)

**근거 (사용자)**: 표는 한국어를 배우는 외국인 학습자를 위한 것이므로 헤더는 한국어 문장 구조를 가르쳐야 한다. 영어 어순을 보여주면 한국어 어순 감각 형성에 방해.

## 15-3. 한국어 어순 그룹 정의

학습 우선순위에 따른 그룹 인덱스:

| 그룹 | 인덱스 | 역할 라벨 (예시) | Korean position |
|------|--------|-----------------|-----------------|
| 주어 | 1 | Subject, Topic | 문장 시작 |
| 부사어 | 2 | Time, Place, Direction, Adverb, Cause, Reason, Duration, Means, Range, Frequency, Manner, Condition, Purpose, Background, Concession, Comparison, Hypothesis, Time clause | 주어 다음, 목적어 앞 |
| 간접목적어 | 3 | IO, Indirect Object, Indirect Obj | 부사어 다음 |
| 직접목적어 | 4 | DO, Direct Object, Object, Inner Object | 간접목적어 다음 |
| 부정 | 5 | Negation | 동사 직전 (안/못) |
| 서술어 | 6 | Verb, Predicate, Adjective, Noun | 문장 끝 |

**번호·괄호 처리**: `Verb 1 (~아서)` → base `Verb` → 그룹 6. 같은 그룹 내에서는 영어 row 순서 보존 (stable sort).

## 15-4. 자동화 스크립트 — `d:\놀라운 한국어 2026\_apply_v9_97_korean_order.py`

**기능**:
1. v9.86 헤더 (`English order: ... <br> 영어 어순: ... — Korean: ...`) 검출
2. v3-era 헤더 (`Korean structure: ... <br> 한국어 어순: ...`) 검출 (잔여 시)
3. row 데이터 추출 → Korean-order group index로 stable sort
4. 새 헤더 생성: `Korean order: REORDERED_EN_ROLES — 한국어 어순: REORDERED_KO_ROLES — 예: REORDERED_KO_FORMS`
5. 표 본문 row 순서 무변경

**멱등성**: v9.97 출력 (`Korean order: ...`)은 v9.86 regex와 v3 regex 모두 매치하지 않음 → 두 번째 실행 SKIP.

**대상**: exp-table 보유 68개 파일 (문장구조 9 + 서술어 21 + 부사어 20 + 기타 18).

## 15-5. 적용 결과

**1차 dry-run (전체 67파일)**: OK=67, **496 헤더 재구축 예정**

**2차 검증 — 199~203 (사용자 IDE 오픈 파일) spot check**:
- 적용 전: `English order: Subject + Verb + Object` / `영어 어순: 주어 + 동사 + 목적어 — Korean: 저는 + 도와 주었습니다 + 제 친구를`
- 적용 후 (1차, 버그): `Korean order: 주어 + 동사 + 목적어` ❌ (영어 라벨 위치에 한국어 출력 + 영어 어순 그대로)
  - 원인: 199~203 row 데이터가 한국어 역할명(주어/동사/목적어) 사용 → 영어 매핑 실패
- 스크립트 수정 (한국어 ↔ 영어 양방향 매핑 + Korean order sub-priority 추가):
  - `ROLE_EN_MAP` 추가 (한국어 역할 → 영어)
  - `KOREAN_ORDER_GROUP` 인덱스 21~24 sub-priority (시간 → 장소 → 원인/이유 → 기타 부사어)
  - `parse_role` 한국어 정규식 매치 추가
  - `HEADER_V97_RE` 추가 (버그 v9.97 출력 재처리)
- 적용 후 (2차, 정상): `Korean order: Subject + Object + Verb` / `한국어 어순: 주어 + 목적어 + 동사 — 예: 저는 + 제 친구를 + 도와 주었습니다` ✓

**3차 검증 — 31~40 시간 장소 목적어 동사 (S+T+P+O+V)**:
- 적용 후: `Korean order: Subject + Time + Place + Object + Verb` / `한국어 어순: 주어 + 시간 + 장소 + 목적어 + 동사 — 예: 저는 + 어제 + 백화점에서 + 선물을 + 샀습니다` ✓ (사용자 명시 어순 정확)

**4차 검증 — 169~180 부정 (Negation)**:
- 사진 행 + 부정(안) + 동사: `Korean order: Object + Negation + Verb (prohibition)` / `한국어 어순: 목적어 + 부정 + 동사 (금지) — 예: 사진을 + 안 + 찍어도 됩니다` ✓
- 안/못 pre-verbal negation 정확한 위치 (목적어 다음, 동사 직전)

**최종 일괄 적용**: `OK=65, SKIP=2 (31~40·199~203 사전 적용분), Total headers rebuilt: 484`
- 합계: **65 파일 × 평균 7.4 헤더 = 484 헤더 + 사전 적용 13 = 497 헤더**

**멱등성 검증**: 일괄 후 dry-run 재실행 → **67/67 SKIP** (Total: 0) ✓

**카테고리별 헤더 분포** (대표):
- 문장구조 (9개): 평균 9.7 헤더 (1~10 10건 등)
- 서술어 문법 (21개): 169~180 12, 237~247 11, 87~96·105~114 10
- 부사어 문법 (20개): 316~324 9, 284~291 8, 338~345 8
- 기타 문법 (18개): 419~435 17, 474~488 15, 370~383 14, 462~473·489~500 12

## 15-6. 영향 범위

- 67개 HTML 파일 중 65 + 2 = 67 모두 v9.97 헤더 표준 도달
- ~497 헤더 재구축 (입력: v9.86 영어 어순 / v3 Korean structure / v9.97 버그 출력)
- 표 본문 row 순서 무변경 (영어 어순 유지) ✓ 사용자 요구 충족
- v9.86 ~ v9.96 다른 표준 모두 보존

## 15-7. 산출물

- **`d:\놀라운 한국어 2026\_apply_v9_97_korean_order.py`** (v9.97)
  - `KOREAN_ORDER_GROUP` 6단계 + 부사어 sub-priority (Time → Place → Cause → 기타)
  - `ROLE_KO_MAP` / `ROLE_EN_MAP` 양방향 매핑 — 영어/한국어 row 데이터 모두 처리
  - `PAREN_KO_MAP` / `PAREN_EN_MAP` 상황 라벨 양방향 (permission/허락, prohibition/금지 등 35종)
  - 3종 헤더 regex (v9.86 / v3 / v9.97) — 모든 입력 형식 매치
  - 멱등성: v9.97 정상 출력은 동일 결과 → SKIP

## 15-8. 다음 진입점

| 작업 | 상태 |
|------|------|
| ~~exp-table 헤더 한국어 어순 정정~~ | ✅ Part 15 완료 (67/67 v9.97) |
| 룰 4 (formation 굵게) — 1~500 67파일 | ✅ Part 13 완료 (1705 토큰) |
| 셀러브레이션 모달 1~86 시리즈 확산 | 별도 후속 |
| `Affirmation`(예/네) 역할 sentence-initial 우선순위 (현재 group 40) | 후순위 — 출현 빈도 낮음 |
| 1차 적용 후 미번역 상황 라벨(`Affirmation` 등 unknown PAREN) 발견 시 추가 매핑 | 후순위 — 1~2건 |

> 📌 **버전 표기:** v9.97 (2026-05-06) — exp-table colspan 헤더를 한국어 어순(주어 + 부사어 + 목적어 + 서술어)으로 정정. 사용자 지적 (199~203 스크린샷): 헤더는 한국어 학습자용이므로 한국어 문장 구조를 가르쳐야 함. 67파일 ~497 헤더 일괄 재구축. 표 본문 row 순서 무변경 (영어 어순 유지).

> 🔁 **표준 정정:** v9.86-v9.87에서 정형화한 "English order: ..." 헤더는 **잘못된 표준**이었음. v9.97부터 항상 "Korean order: ... / 한국어 어순: ... — 예: KO_FORMS_REORDERED" 형식. 신규 파일 작성·자동 생성 시 처음부터 v9.97 형식 적용.

> 🔁 **자동화 보강:** `_apply_v9_97_korean_order.py` 는 한국어/영어 row 역할명 양방향 처리 + 부사어 sub-priority + 3종 입력 헤더 매치(v9.86/v3/v9.97). 멱등성 보장.

---

## 15-9. 사용자 추가 지시 — `— 예: KO_FORMS` 라인 제거 (v9.97.1)

**스크린샷 (87~96 행복하다 단원)**:
> 헤더: `Korean order: Subject + Predicate` / `한국어 어순: 주어 + 서술어 — 예: 저는 + 행복합니다`
> "예: 저는 + 행복합니다. -삭제"

**해석**: 헤더 끝 `— 예: KO_FORMS` 부분 불필요 — 실제 한국어 형태소는 표 본문 KOREAN 컬럼에 이미 명시되어 있으므로 헤더에 중복할 필요 없음. 헤더는 구조 설명(역할 라벨)만으로 충분.

**v9.97.1 변경**: `_apply_v9_97_korean_order.py` 의 `build_v97_header` 에서 `— 예: {ko_forms_str}` 부분 제거.

**최종 헤더 형식 (두 줄 stacked, 구조만)**:
```
Korean order: Subject + Time + Object + Verb
한국어 어순: 주어 + 시간 + 목적어 + 서술어
```

**적용 결과**: 일괄 재실행 → **OK=67, 500 헤더 재구축**. 멱등성 검증: 67/67 SKIP ✓

**감소된 시각 노이즈**: 헤더 글자 수 절반 가까이 축소(예: `한국어 어순: 주어 + 시간 + 장소 + 목적어 + 동사 — 예: 저는 + 어제 + 백화점에서 + 선물을 + 샀습니다` 51자 → `한국어 어순: 주어 + 시간 + 장소 + 목적어 + 동사` 25자).

> 📌 **버전 표기:** v9.97.1 (2026-05-06) — 헤더에서 한국어 모르페임 예시 라인 제거. 두 줄 stacked, 구조 설명만 (`Korean order: ENG / 한국어 어순: KO`). 67파일 500 헤더 일괄.

> 🔁 **표준 추가:** 신규 파일 작성·자동 생성 시 헤더에는 `— 예:` 라인 포함 금지. 한국어 형태소는 표 본문 KOREAN 컬럼에만 명시.

---

## 15-10. 사용자 추가 지시 — 활용 explanation 4단계 chain 형식 (v9.97.2)

**스크린샷 (1~10 좋습니다 row)**:
> 셀 본문: `Stem ends in a consonant → formal ending ~습니다.`
> 사용자 지시: `좋다 - 좋 + 습니다 - 좋습니다`

**해석**: generic 설명(`Stem ends in a consonant → formal ending ~습니다`)은 어떤 단어에든 동일하게 적용되어 학습 가치가 낮음. 사용자가 원하는 것은 **단어별 형성 chain (lemma → stem + ending → final form)**. 학습자는 구체적 활용 과정을 단계별로 보면서 패턴을 내재화함.

**v9.97.2 룰 (4단계 chain)**:
```
<strong>{lemma}</strong> ({part of speech}) → stem <strong>{stem}</strong> ({stem 속성}) + <strong>~{ending}</strong> → <strong>{final form}</strong>.
```

**적용 11건 (3 파일)**:

| 파일 | Row | Generic 설명 → Chain 형식 |
|------|-----|--------------------------|
| 1~10 | 행복합니다 | (already chain, but `<code>` 래퍼) → `<strong>` 단일화 + final form 추가 |
| 1~10 | 좋습니다 | `좋다 (adjective) → stem 좋 (ends in a consonant) + ~습니다 → 좋습니다.` |
| 1~10 | 높습니다 | `높다 (adjective) → stem 높 (ends in a consonant) + ~습니다 → 높습니다.` |
| 1~10 | 작습니다 | `작다 (adjective) → stem 작 (ends in a consonant) + ~습니다 → 작습니다.` |
| 1~10 | 무겁습니다 | `무겁다 (adjective) → stem 무겁 (ends in a consonant) + ~습니다 → 무겁습니다.` |
| 1~10 | 재미있습니다 | `재미있다 (adjective) → stem 재미있 (ends in a consonant) + ~습니다 → 재미있습니다.` |
| 21~30 | 먹습니다 | `먹다 (verb) → stem 먹 (ends in a consonant) + ~습니다 → 먹습니다.` |
| 21~30 | 듣습니다 | `듣다 (verb) → stem 듣 (ends in a consonant) + ~습니다 → 듣습니다.` |
| 21~30 | 입습니다 | `입다 (verb) → stem 입 (ends in a consonant) + ~습니다 → 입습니다.` |
| 21~30 | 읽습니다 | `읽다 (verb) → stem 읽 (ends in a consonant) + ~습니다 → 읽습니다.` |
| 79~86 | 먹습니다 | `먹다 (verb) → stem 먹 (ends in a consonant) + ~습니다 → 먹습니다.` |

**금지 형식**:
- ❌ generic: `Stem ends in a consonant → formal ending ~습니다.`
- ❌ `<code>` 래퍼 (구버전 스타일)

**검증**:
- v9.89 `_bold_korean_in_explanations.py` dry-run on 1~10 → SKIP (모든 한국어 토큰 이미 strong wrap됨)
- 한국어 토큰 정규식 매치 → 0개 신규 (멱등성)

> 📌 **버전 표기:** v9.97.2 (2026-05-06) — 활용 explanation 4단계 chain 형식 정형화. lemma → stem → ending → final form 모두 명시. 11건 일괄 (1~10 6건, 21~30 4건, 79~86 1건). generic "Stem ends in a consonant" 패턴 잔여 0건.

> 🔁 **표준 추가:** 신규 파일 작성 시 모든 활용 explanation에 4단계 chain 형식 적용. `<code>` 래퍼 금지, `<strong>` 단일화. part-of-speech 라벨(`(adjective)`/`(verb)`) + stem 속성(`(ends in a consonant/vowel)`) 명시.

---

## 15-11. 사용자 추가 지시 — 과거형 explanation 4단계 chain 확산 (v9.97.3)

**스크린샷 (11~20 잤습니다 row)**:
> 셀 본문: `Stem's last vowel is ㅏ/ㅗ → use ~았습니다 (contract if needed: 자+았 → 잤).`
> 사용자 지시: `자다 - 자 + 았습니다 - 잤습니다`

**해석**: 형용사 현재형(§15-10)과 동일하게 동사 과거형도 generic 패턴(`Stem's last vowel is ㅏ/ㅗ → use ~았습니다`)을 단어별 4단계 chain으로 변환.

**v9.97.3 적용 — 10건 (2 파일, 3종 과거형)**:

| 파일 | Row | Chain 형식 |
|------|-----|----------|
| 11~20 | 잤습니다 | `자다 (verb) → stem 자 (last vowel ㅏ) + ~았습니다 → 잤습니다 (contract: 자+았 → 잤).` |
| 11~20 | 일어났습니다 | `일어나다 (verb) → stem 일어나 (last vowel ㅏ) + ~았습니다 → 일어났습니다 (contract: 일어나+았 → 일어났).` |
| 11~20 | 갔습니다 | `가다 (verb) → stem 가 (last vowel ㅏ) + ~았습니다 → 갔습니다 (contract: 가+았 → 갔).` |
| 11~20 | 뛰었습니다 | `뛰다 (verb) → stem 뛰 (last vowel ㅟ — not ㅏ/ㅗ) + ~었습니다 → 뛰었습니다.` |
| 11~20 | 쉬었습니다 | `쉬다 (verb) → stem 쉬 (last vowel ㅟ — not ㅏ/ㅗ) + ~었습니다 → 쉬었습니다.` |
| 11~20 | 일했습니다 | `일하다 (verb) → stem 일하 + ~았/었습니다 → 일했습니다 (~하다 contracts: 하+였 → 했).` |
| 11~20 | 생각했습니다 | `생각하다 (verb) → stem 생각하 + ~았/었습니다 → 생각했습니다 (~하다 contracts: 하+였 → 했).` |
| 11~20 | 말했습니다 | `말하다 (verb) → stem 말하 + ~았/었습니다 → 말했습니다 (~하다 contracts: 하+였 → 했).` |
| 31~40 | 샀습니다 | `사다 (verb) → stem 사 (last vowel ㅏ) + ~았습니다 → 샀습니다 (contract: 사+았 → 샀).` |
| 31~40 | 만났습니다 | `만나다 (verb) → stem 만나 (last vowel ㅏ) + ~았습니다 → 만났습니다 (contract: 만나+았 → 만났).` |

**3종 과거형 패턴 정형화**:
- ㅏ/ㅗ stem + ~았: `{lemma} → stem {stem} (last vowel ㅏ/ㅗ) + ~았습니다 → {final} (contract: {stem}+았 → {contracted}).`
- 기타 모음 stem + ~었: `{lemma} → stem {stem} (last vowel {V} — not ㅏ/ㅗ) + ~었습니다 → {final}.`
- ~하다 + ~었: `{lemma} → stem {stem-하} + ~았/었습니다 → {final} (~하다 contracts: 하+였 → 했).`

**검증**:
- 잔여 generic 패턴 0건 (`grep "Stem's last vowel"` → no files; `grep "~하다 → ~했습니다"` → 0)
- v9.89 `_bold_korean_in_explanations.py` dry-run on 11~20 → SKIP (모든 한국어 토큰 strong wrap 됨)

> 📌 **버전 표기:** v9.97.3 (2026-05-06) — 과거형 explanation 4단계 chain 확산 (~았/~었/~했 3종). 10건 일괄 (11~20 8건, 31~40 2건). generic 패턴 잔여 0건.

> 🔁 **표준 확장:** 4단계 chain 형식이 모든 활용에 적용 (현재형 ~습니다 / 과거형 ~았/었습니다 / ~하다 contraction). 신규 파일 작성 시 모든 활용 explanation에 lemma → stem → ending → final 4단계 명시.

---

## 15-12. 사용자 추가 지시 — chain ending 앞 `~` 일괄 제거 (v9.97.4)

**스크린샷 (v9.97.3 잤습니다 row)**:
> 셀 본문: `자다 (verb) → stem 자 (last vowel ㅏ) + ~았습니다 → 잤습니다 (contract: 자+았 → 잤).`
> 사용자 지시: `"았습니다 " 앞에 ~ 삭제`

**해석**: 4단계 chain의 `+ <strong>~{ending}</strong>` 패턴에서 `~`는 시각 노이즈. 어미 자체(`았습니다`)를 그대로 쓰는 것이 가독성 우수. `~`는 placeholder convention이지만 chain에서는 stem과 ending이 명시적으로 분리되어 있으므로 placeholder 표기 불필요.

**v9.97.4 룰**:
- `+ <strong>~{ending}</strong>` → `+ <strong>{ending}</strong>` (전 33파일)
- `~하다`, `~지요`, `~ㄴ 적이 있다` 등 noun 참조의 `~`는 보존 (regex `+ <strong>~`만 매치하므로 안전)

**자동화 — `d:\놀라운 한국어 2026\_strip_tilde_in_chain_endings.py`**:
- 정규식: `r'\+ <strong>~'` → `r'+ <strong>'`
- 멱등성: 두 번째 실행 시 매치 0건

**적용 결과**: OK=31, SKIP=36, **Total: 112 tildes stripped**
- 멱등성 검증: 67/67 SKIP (재실행 0 changes) ✓

**카테고리별 분포**:
- 문장구조 4파일: 1~10 (6) + 11~20 (8) + 21~30 (4) + 31~40 (2) + 79~86 (6) = 26
- 서술어 문법 4파일: 97~104 (6) + 190~198 (1) + 223~231 (1) + 등
- 부사어 문법 16파일: 248~252 (1) + 260~265 (1) + 266~270 (4) + 271~276 (4) + 277~283 (6) + 284~291 (6) + 292~295 (3) + 296~299 (3) + 300~306 (4) + 307~313 (2) + 314~315 (1) + 316~324 (4) + 332~337 (1) + 338~345 (5) + 352~355 (5) + 356~361 (3) + 362~365 (5) + 366~369 (2) = 60
- 기타 문법 6파일: 384~387 (1) + 436~440 (1) + 462~473 (2) + 474~488 (1) + 489~500 (2) = 7

**검증** (사용자 캡처 11~20 잤습니다):
- 적용 후: `자다 (verb) → stem 자 (last vowel ㅏ) + 았습니다 → 잤습니다 (contract: 자+았 → 잤).` ✓ `~` 정확히 제거

> 📌 **버전 표기:** v9.97.4 (2026-05-06) — 4단계 chain의 `+ <strong>~{ending}</strong>` 패턴에서 `~` 일괄 제거. 31파일 112 tildes stripped. `~하다`/`~지요` 등 noun 참조는 보존 (regex 정밀 매치). 멱등성 보장.

> 🔁 **표준 정정:** 4단계 chain 형식에서 `+` 다음 어미 앞 `~` 금지. 사용자 캡처 → 어미 그대로 명시(예: `+ 습니다`, `+ 았습니다`, `+ ㅂ니다`). chain 외 위치(noun 참조 `~하다`)는 무영향.

---

## 15-13. 사용자 추가 지시 — ㄷ-불규칙 chain (v9.97.5)

**스크린샷 미제공**, 사용자 텍스트 지시:
> `걷다 - 걷 + 었습니다 (받침 ㄷ이 모음을 만나면 부드러운 발음인 ㄹ로 바뀐다) - 걸었습니다`

**해석**: 11~20 `걸었습니다` row의 generic 설명(`ㄷ-irregular: ㄷ batchim changes to ㄹ before a vowel ending → ~었습니다`)을 4단계 chain + 불규칙 변화 룰 명시 형식으로 변환.

**v9.97.5 적용 (1건, 11~20)**:
- Before: `ㄷ-irregular: ㄷ batchim changes to ㄹ before a vowel ending → ~었습니다.`
- After: `걷다 (verb) → stem 걷 + 었습니다 (ㄷ-irregular: 받침 ㄷ + vowel → softer ㄹ, so 걷+어 → 걸어) → 걸었습니다.`

**불규칙 활용 chain 정형화 — `+` 다음 ending 안에 변화 룰 명시**:
```
{lemma} (verb) → stem {stem} + {ending} ({X}-irregular: 변화 룰 — {stem}+{vowel} → {contracted}) → {final}.
```

다른 불규칙(ㅂ/ㄹ/ㅅ/르/ㅡ)도 동일 패턴 적용 가능 — 출현 시 단원별 수작업.

**검증**:
- v9.97.4 멱등성: `_strip_tilde_in_chain_endings.py` 11~20 dry-run → SKIP ✓
- v9.89 (Rule B): 새 한국어 토큰 `받침` 자동 wrap 1건 ✓

> 📌 **버전 표기:** v9.97.5 (2026-05-06) — 4단계 chain에 불규칙 활용 룰 통합 형식 정형화. 사용자 명시 ㄷ-불규칙(걷다 → 걸었습니다) 1건 적용. 다른 불규칙 동사 단원별 동일 패턴.

> 🔁 **표준 추가:** 불규칙 활용 explanation은 lemma → stem + ending → final 4단계 chain에 `(X-irregular: 변화 룰)` parenthetical 통합. 받침 변화·모음 결합 명시.

---

## 15-14. 사용자 추가 지시 — `~하다` contraction 단순화 (v9.97.6)

**스크린샷 (11~20 생각했습니다 v9.97.3 형식)**:
> `생각하다 (verb) → stem 생각하 + 았/었습니다 → 생각했습니다 (~하다 contracts: 하+였 → 했).`
> 사용자 지시: `하다 - 했습니다, 생각하다 - 생각했습니다`

**해석**: 이전 v9.97.3 표기는 verbose하고 학습자에게 혼란 유발 — `았/었` 두 옵션 (~하다는 항상 ~었) + `~하다 contracts: 하+였 → 했` 룰 (lemma 형태 vs 음운 변화 혼동). 사용자 명시 형식은 직접 매핑 `하다 → 했습니다`.

**v9.97.6 적용 (3건, 11~20)**:

| Row | Before | After |
|-----|--------|-------|
| 일했습니다 | `일하다 (verb) → stem 일하 + 았/었습니다 → 일했습니다 (~하다 contracts: 하+였 → 했).` | `일하다 (verb) → stem 일하 + 었습니다 → 일했습니다 (하다 → 했습니다).` |
| 생각했습니다 | `생각하다 (verb) → stem 생각하 + 았/었습니다 → 생각했습니다 (~하다 contracts: 하+였 → 했).` | `생각하다 (verb) → stem 생각하 + 었습니다 → 생각했습니다 (하다 → 했습니다).` |
| 말했습니다 | `말하다 (verb) → stem 말하 + 았/었습니다 → 말했습니다 (~하다 contracts: 하+였 → 했).` | `말하다 (verb) → stem 말하 + 었습니다 → 말했습니다 (하다 → 했습니다).` |

**변경 요점**:
1. `았/었습니다` → `었습니다` (~하다는 항상 ~었 결합, ㅏ/ㅗ 모음 룰 적용 안 됨)
2. `(~하다 contracts: 하+였 → 했)` → `(하다 → 했습니다)` — 학습자에 직접 매핑 제시
3. `~` placeholder 제거 (v9.97.4 룰 일관)

> 📌 **버전 표기:** v9.97.6 (2026-05-06) — `~하다` contraction explanation 단순화. verbose 룰(`하+였 → 했`) 제거, 직접 매핑(`하다 → 했습니다`) 사용. 11~20 3건 적용.

> 🔁 **표준 정정:** `~하다` 동사 과거형 chain은 항상 `+ 었습니다` 사용 (`+ 았/었습니다` 금지). contraction parenthetical은 `(하다 → 했습니다)` 단일 매핑.

---

## 15-15. 사용자 추가 지시 — `~하다` 불규칙 처리: stem+ending 분해 제거 (v9.97.7)

**스크린샷 (11~20 말했습니다 v9.97.6 형식)**:
> `말하다 (verb) → stem 말하 + 었습니다 → 말했습니다 (하다 → 했습니다).`
> 사용자 지시: "었습니다 삭제, 왜냐하면 불규칙이기 때문이다. 불규칙은 외워야 한다. '하다'의 과거는 '했습니다'이다."

**해석**: 사용자의 핵심 통찰 — `~하다` 동사는 **불규칙 활용**이므로 정규 chain (`stem + ending → final`)으로 분해할 수 없음. 학습자는 `하다 → 했습니다`를 직접 외워야 함. 따라서 chain 분해 단계(`stem 말하 + 었습니다`) 자체를 제거하고 lemma → final 직접 매핑만 남김.

**v9.97.7 적용 (3건, 11~20)**:

| Row | Before (v9.97.6) | After (v9.97.7) |
|-----|------|--------|
| 일했습니다 | `일하다 (verb) → stem 일하 + 었습니다 → 일했습니다 (하다 → 했습니다).` | `일하다 (verb) → 일했습니다 (하다 → 했습니다, irregular — memorize).` |
| 생각했습니다 | `생각하다 (verb) → stem 생각하 + 었습니다 → 생각했습니다 (하다 → 했습니다).` | `생각하다 (verb) → 생각했습니다 (하다 → 했습니다, irregular — memorize).` |
| 말했습니다 | `말하다 (verb) → stem 말하 + 었습니다 → 말했습니다 (하다 → 했습니다).` | `말하다 (verb) → 말했습니다 (하다 → 했습니다, irregular — memorize).` |

**불규칙 동사 chain 룰 정형화**:
- 정규 동사: `lemma → stem + ending → final` (4단계 chain)
- 불규칙 동사 (~하다 등): `lemma → final (irregular: 변환 룰 — memorize)` (단계 분해 제거)
- 불규칙 ㄷ/ㅂ/ㅅ/르/ㅡ 등: stem이 변형되더라도 기본 룰이 있어 chain 표기 가능 (v9.97.5 ㄷ-불규칙 형식 참조)
- ~하다는 음운 변화가 아닌 **lexical irregularity** (외움 대상)이므로 chain 분해 무용

**검증 — 다른 파일에 verbose `~하다` 형식 잔여**:
- regex `stem <strong>[^<]+하</strong> \+ <strong>었습니다</strong>` → 0 matches (11~20 외 없음, 11~20 3건도 변환 후 0)

> 📌 **버전 표기:** v9.97.7 (2026-05-06) — `~하다` 동사 chain에서 stem+ending 분해 단계 제거. 사용자 통찰 "불규칙은 외워야 한다" 반영. lemma → final 직접 매핑 + `(하다 → 했습니다, irregular — memorize)` 만 표기. 11~20 3건 적용.

> 🔁 **표준 분리:** 활용 explanation 형식 = 정규(4단계 chain) vs 불규칙(직접 매핑 + 외움 노트) 두 분기. ~하다는 lexical irregular로 후자 적용. 음운 불규칙(ㄷ/ㅂ 등)은 chain 유지 + parenthetical에 변화 룰 명시.

---

## 15-16. 사용자 추가 지시 — ㄷ-불규칙 chain 형식 확산: 들었습니다 (v9.97.5 재적용)

**스크린샷 (11~20 들었습니다)**:
> Before: `ㄷ-irregular: ㄷ batchim changes to ㄹ before a vowel ending → ~었습니다.`
> 사용자 지시: `듣다 - 듣 + 었습니다 - 들었습니다 (받침 ㄷ이 모음을 만나면 부드러운 발음인 ㄹ로 변한다)`

**해석**: §15-13에서 정형화한 ㄷ-불규칙 chain 형식 (걷다 → 걸었습니다) 룰을 듣다 → 들었습니다에도 동일 적용.

**적용 (1건, 11~20)**:
- After: `듣다 (verb) → stem 듣 + 었습니다 (ㄷ-irregular: 받침 ㄷ + vowel → softer ㄹ, so 듣+어 → 들어) → 들었습니다.`

**검증**:
- v9.97.4 멱등성: 0 tildes stripped (~ 없음 ✓)
- v9.97.5 ㄷ-불규칙 표준 형식과 일치

> 📌 **버전 표기:** v9.97.5 재적용 (2026-05-06) — ㄷ-불규칙 chain 룰을 듣다 → 들었습니다 row에 확산. 동일 패턴으로 다른 ㄷ-불규칙 동사(묻다·물어보다 등) 출현 시 즉시 적용 가능.

---

## 15-17. 사용자 추가 지시 — 셀 본문 끝 마침표 일괄 제거 (v9.97.8)

**스크린샷 (21~30 한국어를 row)**:
> 셀: `한국어 (no final consonant) + object marker 를.`
> 사용자 지시: `"."를 지워라.`

**해석**: 표의 각 행은 prose 단락이 아니라 discrete 정보 단위이므로 sentence-ending 마침표는 시각 노이즈만 추가. 영어 prose 단일화(v9.85) + chain 형식(v9.97.x)과 일관된 시각 단순화.

**v9.97.8 룰**:
- 모든 `<td>` 셀 본문 끝 `.` 금지
- regex `\.</td>` → `</td>` (셀 끝만 매치, 셀 안 중간 `.`는 무영향)
- 약어(`e.g.`, `i.e.`) · 십진수(`1.5`) false positive 안전 검증됨

**자동화 — `d:\놀라운 한국어 2026\_strip_trailing_period_in_cells.py`**:
- 정규식 단순: `\.</td>` → `</td>`
- 멱등성: 두 번째 실행 시 매치 0건

**적용 결과**: OK=67, **Total: 1398 periods stripped**
- 멱등성 검증: 67/67 SKIP (재실행 0 changes) ✓

**파일별 분포 상위**:
- 123~141 (58) · 31~40 (37) · 105~114 (35) · 277~283 (35) · 284~291 (35) · 41~50 (32) · 419~435 (32) · 271~276 (30) · 474~488 (30) · 316~324 (31)
- 합계 1398건 / 67파일 평균 21건/파일

**검증** (사용자 캡처 21~30 한국어를 row):
- After: `한국어 (no final consonant) + object marker 를` ✓ 마침표 제거

**부수 효과**: 모든 chain 끝 `.` 자동 제거 — 예: `좋다 (adjective) → stem 좋 (ends in a consonant) + 습니다 → 좋습니다` (이전 `좋습니다.`)

> 📌 **버전 표기:** v9.97.8 (2026-05-06) — 모든 `<td>` 셀 본문 끝 마침표 일괄 제거. 67파일 1398 periods stripped. 표 셀이 prose 단락이 아니므로 sentence-ending 마침표 불필요. 멱등성 보장.

> 🔁 **표준 추가:** 신규 파일 작성·자동 생성 시 모든 `<td>` 셀 본문 마침표 금지. 의문 부호(`?`)·감탄 부호(`!`)는 의미 표시이므로 보존. 셀 안 중간 마침표(영문 약어 등)는 무영향 (regex 끝 매치만).

---

## 15-18. 사용자 추가 지시 — `Group X: verb (lemma)` divider 일괄 삭제 (v9.97.9)

**스크린샷 (3장)**:
> 31~40에서: `── Group A: buy (사다) ──` · `── Group B: meet (만나다) ──` · `── Group C: watch (보다) ──`
> 사용자 지시: `삭제하라`

**해석**: Practice 섹션의 `Group X: verb (lemma)` 형태 divider는 학습 흐름을 분절하기보다 시각 노이즈로 작용. 동사별 그룹 구분은 이미 `<h3>` 문제 헤더와 KOREAN 컬럼으로 충분.

**v9.97.9 적용 (5건, 2 파일)**:

| 파일 | 위치 | 삭제 내용 |
|------|------|----------|
| 31~40 | line 380 | `── Group A: buy (사다) ──` |
| 31~40 | line 548 | `── Group B: meet (만나다) ──` |
| 31~40 | line 712 | `── Group C: watch (보다) ──` |
| 41~50 | line 365 | `── Group A: give (주다) ──` |
| 41~50 | line 575 | `── Group B: call (전화를 하다) ──` |

**부수 정리**: 두 파일에서 더 이상 사용되지 않는 `.group-title` CSS 룰도 제거 (각 1라인).

**보존 (다른 의미 — 다른 단원)**: 79~86 (와/과·이나·~고·~거나·~지만, 5건) · 51~60 (Who·When, 2건) · 61~70 (Where·What, 2건) 의 `group-title` divider는 **의문사·접속어 카테고리 표지**로 의미 기여하므로 보존.

> 📌 **버전 표기:** v9.97.9 (2026-05-06) — `Group X: verb (lemma)` 형태 동사 divider 일괄 삭제. 31~40 3건 + 41~50 2건 = 5건 + CSS 룰 2건 정리. 의문사·접속어 카테고리 divider(79~86 등)는 의미 보존을 위해 유지.

> 🔁 **표준 추가:** Practice 섹션에서 동사별 그룹 divider 금지 — `<h3>` 문제 헤더로 충분. 카테고리·의미 단위 divider는 의미 기여 시 보존 가능.

---

## 15-19. 사용자 추가 지시 — 주었습니다 chain + 줬습니다 contraction 변형 (v9.97.10)

**스크린샷 (41~50 주었습니다)**:
> Before: `Past form of 주다. Stem 주 + past ~었 → 주었 + formal ending ~습니다`
> 사용자 지시: `주다 - 주 + 었습니다 - 주었습니다 (= 줬습니다)`

**해석**: §15-10 4단계 chain 형식 + 사용자 명시 contraction 변형(`= 줬습니다`) 통합. `Past form of X. Stem Y + past ~었 → ... + formal ending ~습니다` verbose 형식을 깔끔한 chain으로.

**v9.97.10 적용 (6건, 2 파일)**:

| 파일 | 위치 | After |
|------|------|-------|
| 41~50 | 5건 (line 400, 441, 483, 524, 565) | `주다 (verb) → stem 주 + 었습니다 → 주었습니다 (= 줬습니다 contracted)` |
| 277~283 | 1건 (line 584) | 동일 형식 (Verb 2 final past 컨텍스트 유지) |

**Contraction 변형 표기 룰**: 
- `주었습니다 / 줬습니다` 같은 ㅜ+었 → 워+있 contraction 변형은 `(= {contracted form} contracted)` 형식으로 chain 끝에 명시
- 다른 contraction 후보: `보았습니다 / 봤습니다`, `오았습니다 / 왔습니다`, `배웠습니다` 등

**유사 패턴 잔여 통계** (`Past form of <strong>` 검색):
- 21 파일 64건 — 다른 동사(사다·만나다·보다·읽다·마시다 등)의 generic `Past form of` 패턴
- 각 동사마다 stem과 contraction이 달라서 일괄 변환 불가
- **본 작업 범위 외**: 사용자 단원별 지시 시 단원별 변환

> 📌 **버전 표기:** v9.97.10 (2026-05-06) — 주었습니다 row 6건을 chain + contraction 변형 형식으로 변환. `(= 줬습니다 contracted)` 표기 정형화. 다른 동사의 `Past form of` 잔여 64건은 단원별 후속 작업.

> 🔁 **표준 추가:** 동사 chain에 contraction 변형 존재 시 `(= {contracted form} contracted)` 형식으로 chain 끝에 명시. 학습자에게 두 형태(공식/회화) 병기로 인식 강화.

---

## 15-20. 사용자 추가 지시 — IO marker `~에` vs `~에게` 대비 룰 통합 (v9.97.11)

**스크린샷 (41~50 회사에)**:
> Before: `회사 (thing/place) + marker 에`
> 사용자 지시: `움직이지 않는 간접목적어: ~에, 움직이는 간접목적어: ~에게, 회사 + 에`

**해석**: 한국어 IO marker는 inanimate(움직이지 않는)와 animate(움직이는) 분리 규칙이 핵심. 기존 `(thing/place)` 라벨은 inanimate 측만 표기하고 대비 규칙 누락. 두 marker가 항상 한 쌍으로 학습되도록 explanation에 양방향 통합.

**v9.97.11 적용 (7건, 41~50)**:

| Row | Before | After |
|-----|--------|-------|
| 회사에 (2건) | `회사 (thing/place) + marker 에` | `회사 + IO marker 에 — used for inanimate IO (places/things); animate IO uses 에게` |
| 꽃에 | `꽃 (thing/place) + marker 에` | `꽃 + IO marker 에 — used for inanimate IO (places/things); animate IO uses 에게` |
| 집에 | `집 (thing/place) + marker 에` | `집 + IO marker 에 — used for inanimate IO (places/things); animate IO uses 에게` |
| 그녀에게 | `그녀 (person/animal) + marker 에게` | `그녀 + IO marker 에게 — used for animate IO (people/animals); inanimate IO uses 에` |
| 고양이에게 | `고양이 (person/animal) + marker 에게` | `고양이 + IO marker 에게 — used for animate IO (people/animals); inanimate IO uses 에` |
| 친구에게 | `친구 (person/animal) + marker 에게` | `친구 + IO marker 에게 — used for animate IO (people/animals); inanimate IO uses 에` |

**룰 정형화 (양방향 대비)**:
- Inanimate IO: `{noun} + IO marker 에 — used for inanimate IO (places/things); animate IO uses 에게`
- Animate IO: `{noun} + IO marker 에게 — used for animate IO (people/animals); inanimate IO uses 에`

**검증**: v9.97.8 멱등성 통과 (셀 끝 마침표 없음, regex SKIP) ✓

> 📌 **버전 표기:** v9.97.11 (2026-05-06) — IO marker `~에` (inanimate) vs `~에게` (animate) 대비 룰을 explanation 셀에 양방향 통합. 41~50 7건 일괄 (inanimate 4 + animate 3). 학습자가 두 marker를 항상 한 쌍으로 인식.

> 🔁 **표준 추가:** 신규 파일 작성 시 IO marker explanation은 항상 양방향 대비 (`X uses A; Y uses B`) 형식으로 작성. 학습자가 단방향 룰만 기억하지 않도록 보장.

---

## 15-21. 사용자 추가 지시 — 의문사 술어 영어 어순 정정 (`is X?` → `X is?`)

**스크린샷 (51~60 누구입니까? row)**:
> Before exp-en: `is who?`
> 사용자 지시: `Who is ?`

**해석**: `is who?` 형식은 부자연 — 의문사를 뒤에 두면 echo question 느낌. 자연스러운 영어 의문문은 의문사 우선. `Who is?` 형식으로 정정 (= "Who is (it)?" with implicit subject).

**적용 (3건, 2 파일)**:

| 파일 | 의문사 | Before | After |
|------|--------|--------|-------|
| 51~60 | 누구 | `is who?` | `Who is?` |
| 61~70 | 어디 | `is where?` | `Where is?` |
| 61~70 | 무엇 | `is what?` | `What is?` |

**확장 룰**: 의문사 + 입니까? 술어의 영어 표기는 `{Wh-word} is?` 형식 (의문사 우선). `is {wh}?` 어순 금지 (echo question 회피).

**검증**: regex `>is [a-z]+\?<` → 0 matches (모든 케이스 정정 ✓)

> 📌 **버전 표기:** 의문사 술어 영어 어순 정정 (2026-05-06) — `is X?` → `X is?`. 51~60 1건 + 61~70 2건 = 3건. 의문사 우선 자연 영어 형식.

---

## 15-22. 사용자 추가 지시 — 의문 동사 chain + 의문사 트리거 룰 (v9.97.12)

**스크린샷 (51~60 갑니까? row)**:
> Before: exp-en `do go`, explanation `가다 → 가 + ㅂ니까?`
> 사용자 지시: `go - Verb - 가다 - 가 +ㅂ니까? (When 이 의문부사이기 때문에 의문형이 옴)`

**해석**: 두 가지 정정:
1. exp-en `do go` → `go` (불필요한 영어 조동사 "do" 제거)
2. Explanation에 의문 어미가 사용된 이유 명시 — 문장에 의문사가 있으므로 의문 어미(`ㅂ니까?`)로 응대

**v9.97.12 룰 정형화 (의문 동사 chain)**:
```
{lemma} (verb) → stem {stem} + ㅂ니까?/습니까? → {final} (interrogative ending — triggered by question word {WH})
```

**적용 (7건, 3 파일)** — 사용자 지시 1건 + 일관성 확장 6건:

| 파일 | Row | exp-en (After) | 트리거 의문사 |
|------|-----|--------------|---------------|
| 51~60 | 갑니까? | `go` | 언제 (when) |
| 61~70 | 공부합니까? | `study` | 어디 (where) |
| 61~70 | 좋아합니까? | `like` | 무엇 (what) |
| 71~78 | 좋아합니까? × 4 | `like` | 어떤 (what kind of) |

**4가지 변경**:
1. `do {verb}` → `{verb}` (영어 조동사 제거)
2. `<code>` 래퍼 제거 → `<strong>` 단일화 (v9.97.2 일관)
3. 4-step chain 적용 (`lemma → stem + ending → final`)
4. 의문 트리거 명시: `(interrogative ending — triggered by question word {WH})`

**검증**: v9.97.8 idempotency 3 파일 모두 SKIP ✓

> 📌 **버전 표기:** v9.97.12 (2026-05-06) — 의문 동사 chain 정형화 + 의문 어미 트리거 룰 통합. 7건 일괄 (51~60 1 + 61~70 2 + 71~78 4). `do {verb}` 영어 조동사 제거.

> 🔁 **표준 추가:** 의문 동사 explanation은 항상 `(interrogative ending — triggered by question word {WH})` parenthetical 통합. 학습자가 ㅂ니까? 어미 사용 이유를 명확히 인식. 영어 조동사 "do" 제거 (한국어는 do-support 없음).

---

## 15-23. 사용자 추가 지시 — Place 마커 `에` 트리거 룰 통합 (51~60 어학원에)

**스크린샷 (51~60 어학원에)**:
> Before: Role `Place (destination)`, explanation `어학원 + direction marker 에 (destination of a motion verb)`
> 사용자 지시: `destination 삭제, 문장에 가다 라는 동사가 있기 때문에 장소 + 에, 어학원 + 에 - 어학원에`

**해석**: §15-20 IO marker 양방향 대비 룰을 Place marker로 확장. 학습자가 동사 종류(motion vs 일반)에 따라 marker 선택 룰을 명확히 인식하도록.

**적용 (2건, 51~60)**:

| Row | Before | After |
|-----|--------|-------|
| 어학원에 (line 671) | Role `Place (destination)`, `어학원 + direction marker 에 (destination of a motion verb)` | Role `Place`, `어학원 + 에 — sentence has motion verb 가다, so the place takes 에 (destination marker); general action verbs use 에서 instead` |
| 어학원에 (line 711) | Role `Place`, 동일 generic explanation | 동일 chain-of-reasoning explanation |

**4가지 변경**:
1. Role `Place (destination)` → `Place` (사용자 명시)
2. Generic `direction marker 에` → `에` (위치 표지)
3. Verb-trigger 명시: `sentence has motion verb 가다, so the place takes 에 (destination marker)`
4. 양방향 대비: `general action verbs use 에서 instead`

**v9.97 헤더 자동 재생성**: Role 변경에 따라 51~60 헤더 1건 갱신 (Place로 통일).

**Place marker 양방향 대비 룰 정형화** (v9.97.13):
```
{NOUN} + 에 — sentence has motion verb {VERB}, so the place takes 에 (destination marker); general action verbs use 에서 instead
{NOUN} + 에서 — for general action verbs (전화하다 etc.); motion verbs use 에 (destination) instead
```

**확장 후순위**: 9 파일 11건의 동일 generic 패턴(`direction marker 에 (destination of...)`) — 사용자 단원별 지시 시 적용. 현재 51~60만 적용.

> 📌 **버전 표기:** v9.97.13 (2026-05-06) — Place marker `에` 트리거 룰을 explanation에 통합. 51~60 2건 적용 + v9.97 헤더 갱신. 양방향 대비(에 vs 에서) 명시.

> 🔁 **표준 추가:** Place marker explanation은 IO marker(§15-20)와 동일한 양방향 대비 형식 — `motion verb → 에 (destination); general verb → 에서 (location)`. 학습자가 marker 선택 트리거를 항상 인식.

---

## 15-24. 사용자 추가 지시 — `.gstep-table tbody td .accent` chip 배경 제거 (v9.97.14)

**스크린샷 (3건)**:
> ㅂ니다·었습니다·ㄹ 것입니다 — 라이트 보라/파스텔 배경의 chip 형태 표시
> 사용자 지시: `배경색 삭제`

**해석**: §v9.92 (`_strip_accent_chip_bg.py`)에서 `.gstep-pattern .accent`의 chip 형태(흰 배경)를 제거한 것과 동일한 룰을 `.gstep-table tbody td .accent`로 확장. chip 박스 시각 노이즈 제거, 색상 + 굵기로만 강조.

**v9.97.14 적용 룰**:
- Before: `.gstep-table tbody td .accent { color: VAR; background: VAR; padding: 1px 7px; border-radius: 4px; font-weight: 700; font-family: inherit; }`
- After: `.gstep-table tbody td .accent { color: VAR; font-weight: 700; font-family: inherit; }`

색상 보존 (4 카테고리 팔레트): sentence `#2563eb` · predicate `#6d28d9` · adverbial `#047857` · other `#b45309`.

**자동화 — `d:\놀라운 한국어 2026\_strip_gstep_table_accent_bg.py`**:
- 정규식: `color: VAR; background: VAR; padding: 1px 7px; border-radius: 4px; font-weight: 700; font-family: inherit;` → `color: VAR; font-weight: 700; font-family: inherit;`
- 색상 group capture로 4 카테고리 모두 동일 처리
- 멱등성: 두 번째 실행 시 매치 0건

**적용 결과**: OK=39, SKIP=28, **Total: 39 chips stripped**
- 39 파일: chip 패턴 보유 → 정리됨
- 28 파일: 이미 stripped 상태 (생성기에서 chip 미사용으로 생성된 신규 파일들 — font-weight: 800)
- 멱등성 검증: 67/67 SKIP ✓

**카테고리 분포**:
- 정리됨 (chip → clean) 39 파일: 1~10, 11~20, 21~30, ..., 237~247 (대부분 v9.95 이전 파일)
- 이미 clean 28 파일: 248~252, 253~259, ..., 489~500 (생성기로 작성된 부사어·기타 카테고리)

> 📌 **버전 표기:** v9.97.14 (2026-05-06) — `.gstep-table tbody td .accent` chip 배경 일괄 제거. 39파일 정리 (28파일 사전 적용분 포함 67/67 표준 도달). v9.92 룰의 `.gstep-table` 영역 확산.

> 🔁 **표준 갱신:** chip 형태 강조 박스(`background + padding + border-radius`) 모든 위치 금지. 모든 `.accent` 클래스는 색상 + font-weight만으로 강조 (시각 단순화).

---

## 15-25. Q별 해설 안 추가 설명 카드 표준 정형화 (v9.97.17)

**사용자 명시 (2026-05-06)**:
> "각 번호 내 해설 안에서 추가 설명할 때는 지금과 같은 형식으로 하라. 그리고 이것을 문서화 하라."

**진화 과정 (4 카드 사례)**:

| Q번호 | 카드 주제 | 팔레트 | 컨텐츠 형식 |
|-------|---------|--------|-----------|
| Q145 | 시간 표현 (calendar 4×3) | Teal 본문 + Amber 헤더 | 표 (table) |
| Q147 | 한국 음식 (10가지) | Rose | 칩 그리드 (2×5) |
| Q161 | 장소 (12가지) | Sky | 칩 그리드 (2×6) |
| Q162 | 날씨/오다 의미 | Cyan | 다중 섹션 (① 동의어 + ② 의미·예시) |

**카드 위치**:
- exp-table `</table>` 바로 다음
- explain-content + explain-box 닫기 직전

**카드 HTML 템플릿**:
```html
<div style="margin-top:14px;background:linear-gradient(135deg,{COLOR_50} 0%,{COLOR_100} 100%);border:1px solid {COLOR_200};border-left:4px solid {COLOR_600};border-radius:10px;padding:14px 18px;box-shadow:0 1px 3px rgba({R},{G},{B},0.05);">
    <div style="font-weight:800;color:{COLOR_800};margin-bottom:10px;font-size:0.95rem;">{ICON} {TITLE_KO} · {TITLE_EN}</div>
    {CONTENT}
</div>
```

**컨텐츠 형식 3종**:

1. **표 (table)** — 데이터가 행/열 그리드일 때
   - `<thead>` 헤더 색은 본문과 다른 톤 (cool/warm 대비, 예: 본문 Teal + 헤더 Amber)
   - 모든 셀 `text-align:center` (헤더-본문 컬럼 정확히 일치)

2. **칩 그리드 (chip grid)** — 어휘 리스트일 때
   - `display:grid;grid-template-columns:repeat(2,1fr);gap:8px 12px;`
   - 각 칩: `padding:8px 12px;background:#fff;border:1px solid {COLOR_200};border-radius:8px;font-size:0.9rem;`
   - 형식: `<strong style="color:{COLOR_800};">한국어</strong> <span style="color:#6b7280;font-size:0.85rem;">— English</span>`

3. **다중 섹션 (multi-section)** — 여러 설명을 한 카드에 묶을 때
   - 각 섹션 헤더: `<div style="font-weight:700;color:{COLOR_700};margin-bottom:6px;font-size:0.92rem;">① {제목}</div>`
   - 섹션별 칩 grid 또는 prose

**4 카테고리 팔레트 표준** (Tailwind 50-200 채도):

| 카테고리 | 50 | 100 | 200 | 600 | 700 | 800 | shadow RGB |
|---------|------|------|------|------|------|------|------------|
| 시간 | `#f0fdfa` | `#ecfeff` | `#a7f3d0` | `#14b8a6` | `#0d9488` | `#0f766e` | `20,184,166` |
| 음식 | `#fff1f2` | `#fef2f2` | `#fecdd3` | `#e11d48` | — | `#9f1239` | `225,29,72` |
| 장소 | `#f0f9ff` | `#e0f2fe` | `#bae6fd` | `#0284c7` | `#0369a1` | `#075985` | `2,132,199` |
| 날씨 | `#ecfeff` | `#cffafe` | `#a5f3fc` | `#0891b2` | `#0e7490` | `#155e75` | `8,145,178` |

추가 카테고리 발생 시 (예시):
- 인사·예절 → Indigo / Soft Purple
- 가족·관계 → Pink / Magenta
- 색깔·자연 → Emerald / Lime
- 숫자·돈 → Amber / Yellow

**텍스트 룰** (기존 표준 일관):
- 모든 prose 영어 (v9.85 Rule A)
- 한국어는 칩의 `<strong>` 또는 inline morpheme으로만
- 영어 보조 텍스트 `color:#6b7280;font-size:0.85rem;` (gray-500)
- 셀/칩 끝 마침표 금지 (v9.97.8)
- 한국어 토큰 모두 `<strong>` (v9.89)
- **격식체 우선 (`~ㅂ니다/~습니다`)**, 해요체(`~요`) 회피 (사용자 2026-05-06)

**아이콘 가이드** (Title 줄 앞):
- 📅 시간 · 🍱 음식 · 📍 장소 · 🌧️ 날씨 · 💡 학습 팁 · ⚠️ 주의

> 📌 **버전 표기:** v9.97.17 (2026-05-06) — Q별 해설 안 추가 설명 카드 표준 정형화. 4 사례(Q145·Q147·Q161·Q162)에서 도출한 디자인 패턴 + 4 카테고리 팔레트 + 3 컨텐츠 형식(표/칩 그리드/다중 섹션). 메모리 `feedback_supplemental_card.md` 영구화.

> 🔁 **표준 추가:** 신규 Q에 추가 설명 카드 작성 시 메모리 룰 참조 — 동일 의미 카테고리는 동일 팔레트 사용. 새 카테고리 발견 시 메모리 룰에 팔레트 추가.




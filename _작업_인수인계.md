# 놀라운 한국어 500 해설집 — 작업 인수인계

_최종 갱신: **2026-08-10** (이전 갱신 2026-06-23)_

이 문서 하나로 **다른 컴퓨터에서 Claude Code로 작업을 이어갈 수 있습니다.**

> 이 문서에서 **★ 표시는 실제로 사고가 났던 자리**입니다. 그 부분만은 반드시 읽으십시오.

---

## 0. 30초 요약

- **편집 대상**: `마스터-템플릿-적용본\놀라운 한국어 500문장 해설 최종\` 의 **HTML 106개**. 그림을 base64로 품은 자족(self-contained) 파일입니다. 다만 **소리는 자족이 아닙니다** — 같은 폴더의 `_소리\` 를 함께 옮겨야 합니다.
- **동기화**: OneDrive **+ git**(저장소 뿌리 = `놀라운 한국어 500 해설집`, 브랜치 `master`, **원격 없음 = 로컬 전용**). 컴 사이 이동은 OneDrive가 합니다. git은 되돌리기용입니다.
- **도구**: `마스터-템플릿-적용본\_도구\` — 검사 4종 + 음원 파이프라인. **무엇을 고치든 반영 뒤 `python check_all.py`.** 자세한 것은 그 폴더의 `README.md`.
- **규칙(메모리)**: `_claude_메모리_백업\2026-08-10 현행 메모리\` (38개, 최신) + 같은 폴더의 6월판 55개(부록·숫자표 규칙은 여기에만 있음).
- **최근 작업(8월)**: 어휘 전수 개편 → 다국어 번역 준비(i18n) → **신경망 음성 3,948개** → 조사 단원 전면 재작성.
- **다음에 할 일**: 아래 **§7 남은 일**. 사용자와 정하십시오. 마음대로 고르지 마십시오.

---

## 1. 다른 컴퓨터에서 시작하는 법

1. **OneDrive 동기화** — 떠나는 컴에서 트레이 "최신 상태" 확인. 새 컴에서 폴더 우클릭 → **"이 장치에 항상 유지"**. `_소리\` 만 **57MB / 3,948개 파일**이라 시간이 걸립니다. 끝까지 내려온 뒤 시작하십시오.
2. **환경** — 확인된 조합입니다.
   ```
   Python 3.12.10
   pip install edge-tts      (7.2.8 사용 중 · 무료 · API 열쇠 필요 없음)
   git  user.name  = 놀라운 한국어
        user.email = kkjune2000@gmail.com
   ```
3. **Claude Code 로그인** — 같은 계정 `kkjune2000@gmail.com`.
4. **시작 멘트 복붙**:

   > 놀라운 한국어 500 해설집 작업을 이어서 한다. 먼저 `_작업_인수인계.md` 와 `_claude_메모리_백업\2026-08-10 현행 메모리\MEMORY.md` 및 그 폴더의 규칙 파일을 읽고 규칙·진행 상황을 파악하라. 편집은 `마스터-템플릿-적용본\놀라운 한국어 500문장 해설 최종\` 안에서만 한다. 무엇을 고치든 반영한 뒤 `마스터-템플릿-적용본\_도구\check_all.py` 를 돌려라.

> ℹ️ 메모리는 원래 그 컴퓨터의 `~/.claude/projects/<프로젝트>/memory/` 에만 있어 새 컴엔 없습니다. 그래서 `_claude_메모리_백업\` 에 사본을 둡니다. 인수인계를 갱신할 때마다 이 사본도 새로 뜨십시오.
>
> ⚠️ 이 프로젝트의 메모리는 **토픽1 작업 폴더 이름으로 저장**되어 있습니다(`d--OneDrive---------2026-----------1---------`). 500문장 작업을 토픽1 폴더에서 해 왔기 때문입니다. 새 컴에서 폴더를 바꿔 열면 메모리가 안 보입니다 — 그때는 위 백업 폴더를 직접 읽히십시오.

---

## 2. 폴더 구조 & 편집 범위

```
놀라운 한국어 500 해설집\                       ← git 뿌리 (원격 없음)
├── 마스터-템플릿-적용본\
│   ├── 놀라운 한국어 500문장 해설 최종\          ← ★ 실제 편집 대상
│   │   ├── 1 시제 … .html            (21) 시제란·현재/과거/미래 분할·시험
│   │   ├── 2 조사 … .html            ( 9) 조사란·은는이가~에서장소·시험
│   │   ├── 3 001~010 … .html         (67) 1~500 본문
│   │   ├── 4 부록 1~8 … .html        ( 8)
│   │   ├── _음성 확인.html            브라우저별 한국어 음성 진단용
│   │   └── _소리\                     ★ 음원 (f\ m\ *.json) — 함께 옮길 것
│   ├── _도구\                        ★ 검사·음원 스크립트 (README.md 먼저)
│   ├── _언어판 시험\                   다국어판 빌더 (lang_build.py, tr_ja.py, ja\)
│   ├── _음성 견본\                     목소리 비교용 mp3 + _들어보기.html
│   ├── _원본 인쇄본\                   옛 A4 인쇄본 (보존, 편집 X)
│   ├── _점검보고 2026-08-08.md
│   ├── _기초문법연습_템플릿_문서.md       부록 4~7 정본 스펙
│   ├── _숫자표_색·스타일_문서.md          부록 1 색 팔레트
│   └── 교재 (놀라운 한국어 500 원본)\      원본 (편집 X)
├── _claude_메모리_백업\
│   ├── 2026-08-10 현행 메모리\        ← 최신 38개
│   └── (6월판 55개)                   부록·숫자표·파일명 규칙은 여기에만
├── _작업_인수인계.md                   ← 이 문서
└── _작업_로그_2026-05-*.md            (역사)
```

- ✅ 편집 **O**: 위 `놀라운 한국어 500문장 해설 최종\` 의 HTML, `_도구\`
- 🚫 편집 **X**: `_원본 인쇄본\`, `교재 …\`, `_backup_*`, OneDrive 밖 orphan 경로
- **파일명 체계**: `1 시제 / 2 조사 / 3 ###~### / 4 부록 N` **접두사 + 공백**. 정렬이 곧 목차입니다.
- **본문 테마색**(하드코딩 금지, 번호대로): 문장구조 1~86 파랑 · 서술어 문법 87~247 보라 · 부사어 문법 248~369 초록 · 기타 문법 370~500 앰버.

---

## 3. 도구 — 고치기 전과 후에 무엇을 돌리는가

```powershell
cd "…\마스터-템플릿-적용본\_도구"
python check_all.py            # 넷을 한 번에. 모두 0건이어야 정상
python show_text.py "2 조사"    # 화면에 보이는 글자만 뽑아 보기
python audio_build.py list     # 음원이 없는 문장 보기 (→ make → wire)
```

자세한 설명·사고 이력은 `_도구\README.md` 에 있습니다. 여기서는 요점만:

| 검사 | 잡는 것 |
|---|---|
| `check_structure.py` | 태그 짝 · 중괄호 · **정답(`data-ans`)이 HEAD와 같은가** · 번호표가 가리키는 mp3가 있는가 |
| `check_script_order.py` | 정의보다 앞선 `<script>` 블록에서 함수를 부르는가 |
| `check_style.py` | `<style>` 중괄호 · 스크립트가 스타일 안에 끼었는가 |
| `check_spec.py` | 폭·여백·viewport·lang·charset |

---

## 4. 작업 방식 — 이 순서를 지키십시오

1. **모의 실행(dry run)** — 스크립트에 `--apply` 를 따로 두고, 먼저 바꿀 개수만 셉니다. **예상과 맞을 때만** 반영합니다.
2. **쓰기 직전 태그 짝 검사** — 깨지면 그 파일은 건너뜁니다. 한 번에 여러 파일을 망가뜨리는 것을 막습니다.
3. **반영**
4. **`check_all.py`** + `show_text.py` 로 **전후 화면 글자 대조**
5. **커밋** — 무엇을 왜 고쳤는지, 사고가 있었으면 그것까지 적습니다.

### 도구를 쓸 때 지킬 것

- 경로를 글자로 박지 말고 **`import _paths`**. 컴마다 드라이브 문자가 다릅니다.
- 출력은 **`_paths.stdout()`**. PowerShell 기본 코드페이지(cp949)에서 한글이 깨집니다. ★ PowerShell의 `ConvertFrom-Json`·콘솔 출력으로 한글을 다루려다 여러 번 깨뜨렸습니다. **UTF-8을 다룰 일은 파이썬으로** 하십시오.
- 여러 자리를 고칠 때는 **뒤에서 앞으로**. 정렬은 반드시 **위치 기준**으로. ★ 한 번은 파일 **이름 문자열**로 정렬해 위치가 뒤엉켜 17개 파일의 태그를 깨뜨렸습니다(git으로 되돌림).
- 편집 구간이 **겹치면 손상**됩니다. 후보를 다 모은 뒤 `(시작 오름차순, 끝 내림차순)` 으로 정렬해 **겹치지 않는 바깥쪽만** 남기십시오.

---

## 5. 내용 규칙

### 문체
- 설명·UI는 **격식체**(-ㅂ니다 / -십시오). 해요체 금지.
- **문법 용어를 설명에 늘어놓지 않습니다.** "양보" → "앞말이 맞지만 그래도". 연결어미 뒤 쉼표 금지. 군더더기 꼬리표 금지.
- 예문은 격식체. "예시" 아니라 **"예문"**.
- ★ **한 문장에 한 가지 생각.** 2026-08-10 사용자 지적: 수준을 올린다며 문장을 압축했더니 **오히려 어려워졌습니다.** 「그 일이 놓인 때를 표시합니다」→「시간을 나타내는 말 뒤에 붙습니다. 그 일을 언제 하는지 알려 줍니다.」 비유 대신 예문을 쓰십시오.
- **용어는 풀지 않습니다.** 학생들은 목적어·간접목적어·서술어를 이미 압니다. 초등학생에게 설명하듯 하지 마십시오. 다만 **받침·관형어** 같은 한국어 고유 용어는 처음 한 번만 짧게 풀어 줍니다.

### 다국어 대비 (i18n)
이 책은 나중에 일본어·중국어 등으로 번역됩니다. 그래서:

- ★ **한국어 문장 안에 영어를 넣지 마십시오.** `He = 그는` 같은 표기는 번역하면 무너집니다.
- ★ **영어를 기준점으로 삼은 설명 금지.** 「영어의 to와 같습니다」는 일본어·몽골어·터키어 독자에게 틀린 말입니다. 한국어 안에서 판별할 기준을 주십시오(사람·동물 ↔ 그 밖 / 하는 곳 ↔ 있는 곳).
- 어쩔 수 없이 독자의 언어를 기준 삼아야 하면 **`data-reauthor="lang-anchor"`** 를 달고 주석으로 "다른 언어판에서는 그 언어 기준으로 다시 쓸 것"이라고 적으십시오. 번역이 아니라 **다시 쓰기** 대상이라는 표시입니다.
- **구조**: `<html lang="ko" translate="no">` 로 페이지를 잠그고, 번역할 자리만 `translate="yes"` 로 엽니다. 영어 문장 **안**의 한국어는 `<span lang="ko" translate="no">` 로 다시 잠급니다.
- **한국어 위 · 영어 아래**가 원칙입니다. 단 **해설 표의 열 순서 `영어 | 역할 | 한국어 | 해설` 은 사용자 결정으로 고정** — 영어를 보고 한국어로 옮기는 연습이기 때문입니다. **뒤집자고 제안하지 마십시오.**
- 언어판은 원본을 고치지 않고 **빌더로 찍어 냅니다**: `_언어판 시험\lang_build.py` + `tr_ja.py`. 일본어판 시험작 1개가 `_언어판 시험\ja\` 에 있습니다.
- ★ 그릇(태그)만 쫓으면 못 닿는 글자가 남습니다. 글자 단위로 감싸십시오.

### 소리
- 여성 `ko-KR-SunHiNeural` · 남성 `ko-KR-HyunsuMultilingualNeural` **고정**(사용자가 들어 보고 고름).
- 브라우저가 보는 것은 **HTML 안의 `AK_SND`**. json만 고치면 아무 일도 일어나지 않습니다.
- ★ `speechSynthesis.cancel()` 직후 바로 `speak()` 하면 **목소리가 바뀝니다.** 60ms `setTimeout` 을 지우지 마십시오.
- ★ **쓰기 칸의 🔊 는 정답을 맞힌 뒤에만 보입니다**(`input.correct + .jm-spk`). 먼저 들으면 쓰기 연습이 안 되기 때문입니다. 그래서 `.jm-spk` 의 기본값이 `display:none` 입니다 — **설명 예문에 🔊 를 새로 달면 안 보입니다.** `blockquote .jm-spk, .example-box .jm-spk{display:inline-block}` 규칙이 그것을 살립니다.
- 성별 단추는 **문장 바로 옆**에 둡니다(따로 모아 두지 않습니다).

---

## 6. ★ 함정 — 실제로 터졌던 것

| 무엇 | 어떻게 터졌는가 | 막는 법 |
|---|---|---|
| **스크립트 선후** | `jmSpk` 정의를 뒤 블록에 두어 `ReferenceError`. **23개 파일의 퀴즈 표가 사라짐** | 도우미 함수는 `<body>` 뒤 **첫 스크립트**에. `check_script_order.py` |
| **CSS 주석 안의 `<body>`** | 정규식 `<body[^>]*>` 가 **CSS 주석 속 글자**에 걸려 `<style>` 한복판에 스크립트 삽입 → CSS가 통째로 죽어 **조사 A~F가 화면 가득 퍼짐** | `</head>` **뒤**의 진짜 `<body>` 를 찾을 것. `check_style.py` |
| **눈으로 본 오판** | CSS 글자가 있으니 같다고 답했으나 **적용이 안 되고 있었음** | 태그 말고 **적용되는가**를 보라 |
| **정렬 기준** | `sorted(reverse=True)` 가 위치가 아니라 **이름 문자열**로 정렬 → 17파일 태그 깨짐 | `key=lambda c: c[1]` 처럼 **위치 기준** |
| **겹치는 편집 구간** | `<em>` 안에 `<small>` 이 있어 두 번 고쳐 내용 중복 (4파일) | 겹치지 않는 **바깥쪽만** 남기기 |
| **정규식이 첫 `</span>`에서 멈춤** | `<p class="en">` 의 끝을 잘못 잡아 **같은 껍데기를 두 번** 씌우고 뒤쪽은 놓침 | **깊이를 세어** 짝을 찾을 것 |
| **한 글자 한국어** | 정규식이 2글자 이상만 잡아 `들·색·이·가·딸` 을 놓침 | 1글자도 잡히는지 확인 |
| **큰따옴표 자료** | 작은따옴표만 훑어 1,339개 낱말을 놓침 | 두 따옴표 다 |
| **PowerShell UTF-8** | `ConvertFrom-Json`·콘솔 출력이 한글을 깨뜨림 | 파이썬 + `_paths.stdout()` |
| **`<title>`** | 제목 안의 한국어 80개 파일이 안 잠김 → 문법 항목 「에」까지 번역될 것 | 다국어 작업 때 처리 (§7-2) |

그 밖에 **조용히 실패하는 곳**(오류 없이 빈칸이 되는 곳): 번역칸 폴백(약어·따옴표), frame 키 누락, dict 열쇠 겹침, 물려받은 조건문. 자세한 것은 메모리 `haeseol-silent-failure.md`.

---

## 7. 진행 상황

### 8월 작업 (최근)

| 시기 | 한 일 |
|---|---|
| 08-08 | 105개 HTML 전수 점검 + 확정 결함 전량 수정. 보고서 `_점검보고 2026-08-08.md` |
| 08-09 | 어휘 500상자 전면 개편(한국어 위·영어 아래, 번역 슬롯 1,524 개방) |
| 08-09 | 다국어 1층 완결 — 화면 영어 전량 슬롯화, 잠긴 영어 0 (`translate="yes"` 현재 **23,867**개) |
| 08-09 | **신경망 음성** — 여성·남성 각 1,974개(합 3,948 · 57MB), 102개 파일 연결 |
| 08-09 | 쓰기 칸 🔊 는 정답 뒤에만 표시 · 활용표 칩에도 소리 · 「퀴즈 2 쓰기」 정리(직접 만들기와 겹쳐서) |
| 08-09 | 폭 규격 수리 (940px 한 파일 + CSS가 죽어 있던 조사 A~F) |
| 08-09~10 | **조사 0·A~G 전면 재작성** — 영어 기준점 제거, 눈높이 상향, 예문 한국어 먼저 + 소리 |
| 08-10 | 위 문장이 어렵다는 지적 → **18곳 쉽게 풀어 씀** |

### 남은 일 (사용자와 정할 것)

1. **다국어 2층** — 자바스크립트 안에 박힌 문자열(경고·안내문)이 아직 한국어·영어 그대로입니다. 언어판 빌더가 3단계로 처리하지만 사전 추출이 필요합니다.
2. **`<title>` 안의 한국어 80개 파일** — 제목은 전부 `<title translate="yes">` 라 번역은 열려 있는데, 그 안의 한국어(「시제란 무엇인가」, 「조사 Ⓕ 에 시간」)가 `lang="ko" translate="no"` 로 감싸여 있지 않습니다. 그대로 번역하면 문법 항목인 **「에」·「은/는」까지 번역돼 버립니다.** 본문에 쓴 것과 같은 방식으로 감싸면 됩니다.
3. **`4 부록 2 부정법.html` 폭** — `.container` 도 `max-width` 도 **없습니다**. 화면 가득 퍼질 것으로 보입니다. (부록 8은 `max-width:1380px` 로 **일부러** 넓은 문법 맵입니다.) 브라우저로 확인 후 결정하십시오.
4. **부록 8 기초문법총정리** — 6월 기록: 옛 A4 "마스터 문법 맵"을 인터랙티브로 만들 것인지 3안 중 사용자 결정 대기.
5. **말하기 PPT** — 500문장 PPTX 전체 생산 보류 중.

> §7의 6월 항목(4·5)은 이번 세션에서 확인하지 않았습니다. 6월 기록 그대로 옮긴 것입니다.

---

## 8. 사용자와 일하는 방식

- 사용자는 **화면 스크린샷 + 짧은 지시**로 일을 줍니다. 브라우저로 렌더 확인하며 진행하십시오.
- ★ **브라우저 캐시** — 사용자가 "안 고쳐졌다"고 할 때, 먼저 디스크 상태를 확인하십시오. 2026-08-10에 이미 지운 문장이 화면에 남아 있었습니다. `Ctrl + Shift + R`.
- ★ **기능·규칙 지시는 작업 중인 파일만이 아니라 이미 만든 파일 전부에** 적용하십시오. "현재형에도 적용하라"는 말을 듣기 전에 하십시오.
- ★ **"~가 없습니다"라고 단정하지 마십시오.** 못 찾은 것일 때가 많습니다. 세어야 할 것은 파일에서 세십시오.
- ★ **확인한 것과 안 한 것을 갈라 적으십시오.** 훑어본 기억을 사실처럼 문서에 적지 마십시오.
- 대량 작업은 멀티에이전트 워크플로가 효율적입니다. 단 **사용자가 명시적으로 동의해야** 씁니다.
- 인수인계를 갱신할 때는 **이 문서 + `_claude_메모리_백업\` 사본** 둘을 함께 갱신하십시오.

# 놀라운 한국어 500 해설집 — 작업 인수인계

_최종 갱신: **2026-08-21** (이전 갱신 2026-08-20)_

> ★★ **작업 폴더 — 컴퓨터마다 드라이브 문자가 다릅니다 (2026-08-21).**
> **데스크탑 — `D:\한국어 클로드\놀라운 한국어 500 해설집 클로드\`**
> **노트북 — `F:\한국어 클로드\놀라운 한국어 500 해설집 클로드\`** (2026-08-21 에 clone 해 만들었습니다)
> 폴더 모양은 같고 드라이브 문자만 다릅니다. 두 대를 잇는 것은 **원격 하나뿐**이니
> **앉을 때 `pull`, 일어설 때 `push`** 하십시오.
> ★2026-08-21 에 노트북이 **154커밋** 뒤처진 채였습니다. 그날 clone 으로 맞췄습니다.
> · ★**노트북 F: 는 exFAT 으로 붙은 SDXC 카드(이동식)** 입니다. 소유권을 기록하지 않는
>   파일 시스템이라 git 이 「dubious ownership」으로 저장소 열기를 **거부합니다.** 새로 잡을 때는
>   `git config --global --add safe.directory 'F:/한국어 클로드/놀라운 한국어 500 해설집 클로드'`.
>   저널이 없어 `.git` 이 상할 수 있는데, 그때는 고치려 들지 말고 **다시 clone** 하는 게 빠릅니다.
>   신원(`user.name`·`user.email`)도 전역에 없으니 새 사본마다 **저장소 지역으로 넣어야** 합니다.

> ★★ **폴더가 OneDrive 밖으로 나왔습니다 (2026-08-20).**
> 데스크탑 옛 자리 `D:\OneDrive\놀라운 한국어 500 해설집\` 은 **`(옛 · 쓰지 않음 2026-08-20)`** 으로
> 이름을 바꿔 두었습니다. **거기서 고치지 마십시오** — 파일이 그대로 남아 있어
> 열면 똑같아 보입니다. 노트북 옛 자리는 `C:\Users\김광준\OneDrive\놀라운 한국어 500 해설집\` 이고
> **`4de35b8`(08-18)에 멈춰 있습니다.** 원격도 안 걸려 있고, 뿌리에 OneDrive 충돌 사본
> (`마스터-템플릿-적용본 (1)`·`정리 (1)`·**`.git (1)`**)이 널려 있습니다. 거기서도 고치지 마십시오.
> · `.git` 은 새 폴더로 **옮겼습니다**(이력·원격 그대로, 저장소는 하나뿐입니다).
> · 새 자리는 **OneDrive 밖**입니다 — 두 동기화가 부딪히지 않습니다(§6 OneDrive 함정).
> · ★**경로를 박은 스크립트 13개를 그때 함께 걷어냈습니다.** 그대로 두었으면 새 폴더에서
>   돌려도 **옛 폴더를 고쳤을 것**입니다(`import _paths` 가 옛 자리에서 딸려 옵니다).

이 문서 하나로 **다른 컴퓨터에서 Claude Code로 작업을 이어갈 수 있습니다.**

> ★★ **2026-08-19 의 갱신 두 번이 이 파일이 아니라 백업 사본에 들어가 있었습니다.**
> `15881be`(12:30)·`0602f40`(20:29)이 고친 것은 `_claude_메모리_백업\2026-08-18 현행 메모리\_작업_인수인계.md` 였고,
> **이 파일에는 그날 낮 12:30 뒤의 기록이 하나도 없었습니다**(그날 커밋 **81개 중 36개**가 12:30 뒤입니다).
> 2026-08-20 에 그 갱신을 이 파일로 옮겨 담고 나머지 하루치를 마저 적은 뒤, **두 파일을 같게 맞췄습니다.**
> ★ **인수인계는 저장소 뿌리의 이 파일이 본입니다.** 백업 폴더에 있던 사본(`_claude_메모리_백업\2026-08-18 현행 메모리\_작업_인수인계.md`)은 **2026-08-21 에 지웠습니다**(사용자 지시). 헷갈릴 자리를 아예 없앴습니다 — 옛 판이 필요하면 git 이력에 있습니다.

> 이 문서에서 **★ 표시는 실제로 사고가 났던 자리**입니다. 그 부분만은 반드시 읽으십시오.

---

## 0. 30초 요약

- **편집 대상**: `마스터-템플릿-적용본\놀라운 한국어 500문장 해설 최종\` 의 **HTML 107개**(2026-08-20 셈. 2026-08-19에 `4 부록 9 요일.html` 이 늘었습니다). 그림을 base64로 품은 자족(self-contained) 파일입니다. 다만 **소리는 자족이 아닙니다** — 같은 폴더의 `_소리\` 를 함께 옮겨야 합니다.
- **동기화**: **git**(저장소 뿌리 = 데스크탑 `D:\한국어 클로드\놀라운 한국어 500 해설집 클로드`, 노트북 `F:\한국어 클로드\놀라운 한국어 500 해설집 클로드`, 브랜치 `master`, **원격 있음** — `github.com/kkjune2000-debug/500-haeseol`. ★**공개(public) 저장소입니다** — 2026-08-21 확인. 이 문서에 08-20까지 「비공개」로 적혀 있었으나 틀린 기록이었습니다. ★2026-08-20에 **올렸습니다**(`8603f42`). 올릴지는 그때그때 사용자와 정하십시오). ★**컴 사이 이동은 이제 git 이 합니다**(2026-08-20 폴더가 OneDrive 밖으로 나왔습니다). 일을 마치면 **push**, 다른 컴에서는 **clone 또는 pull**.
  - ★ **일이 끝나면 커밋하십시오.** `check_structure.py` 의 정답 대조가 **HEAD를 기준**으로 삼기 때문에, 커밋을 미루면 그 그물이 무뎌집니다(2026-08-10에 사흘치가 쌓여 23파일이 걸려 있었습니다 — 값을 대조해 보니 전부 의도한 변경이었습니다).
- **도구**: `마스터-템플릿-적용본\_도구\` — 검사 4종 + 음원 파이프라인. **무엇을 고치든 반영 뒤 `python check_all.py`.** 자세한 것은 그 폴더의 `README.md`.
- **규칙(메모리)**: 세 벌이 있고 **서로 대신하지 못합니다.** `_claude_메모리_백업\2026-08-18 현행 메모리\`(9개, 최근 사고 자리) + `2026-08-10 현행 메모리\`(38개, 8월 대작업) + 같은 폴더의 6월판 55개(**부록·숫자표·파일명 규칙은 여기에만** 있음). 그 폴더의 `_읽어 주세요.md` 참조.
- **최근 작업(8월)**: 어휘 전수 개편 → 다국어 번역 준비(i18n) → **신경망 음성 4,494개**(여성 2,247 · 남성 2,247) → 조사 단원 전면 재작성 → **어순 해설 표를 「문장 사다리」로**(08-19) → **어휘에서 조사·문법 표현 걷어내기**(08-19).
- **다음에 할 일**: 아래 **§7 남은 일**. 사용자와 정하십시오. 마음대로 고르지 마십시오.

---

## 1. 다른 컴퓨터에서 시작하는 법

1. **OneDrive 동기화** — 떠나는 컴에서 트레이 "최신 상태" 확인. 새 컴에서 폴더 우클릭 → **"이 장치에 항상 유지"**. `_소리\` 만 **56MB / 4,494개 파일**(여성 2,247 · 남성 2,247, 2026-08-20 `check_all.py` 셈)이라 시간이 걸립니다. 끝까지 내려온 뒤 시작하십시오.
2. **환경** — 확인된 조합입니다.
   ```
   Python 3.12.10 (3.14.3 도 확인됨)
   pip install edge-tts      (무료 · API 열쇠 필요 없음)
   pip install playwright  +  python -m playwright install chromium
       (audio_build.py 가 쓰기 칸 data-ans 를 렌더로 보는 데 필요.
        없으면 「못 봄」 경고가 뜨고 그물이 일부만 돕니다)
   git  user.name  = 놀라운 한국어
        user.email = kkjune2000@gmail.com
   ```
3. **Claude Code 로그인** — 같은 계정 `kkjune2000@gmail.com`.
4. **시작 멘트 복붙**:

   > 놀라운 한국어 500 해설집 작업을 이어서 한다. 먼저 `_작업_인수인계.md` 를 읽고, 이어서 `_claude_메모리_백업\2026-08-18 현행 메모리\`(9개)와 `2026-08-10 현행 메모리\`(38개)의 규칙 파일을 읽어 규칙·진행 상황을 파악하라. 편집은 `마스터-템플릿-적용본\놀라운 한국어 500문장 해설 최종\` 안에서만 한다. 무엇을 고치든 반영한 뒤 `마스터-템플릿-적용본\_도구\check_all.py`·`check_audit.py`·`check_render.py --width 390` 을 돌려라.

> ℹ️ 메모리는 원래 그 컴퓨터의 `~/.claude/projects/<프로젝트>/memory/` 에만 있어 새 컴엔 없습니다. 그래서 `_claude_메모리_백업\` 에 사본을 둡니다. 인수인계를 갱신할 때마다 이 사본도 새로 뜨십시오.
>
> ⚠️ 이 프로젝트의 메모리는 **토픽1 작업 폴더 이름으로 저장**되어 있습니다(`d--OneDrive---------2026-----------1---------`). 500문장 작업을 토픽1 폴더에서 해 왔기 때문입니다. 새 컴에서 폴더를 바꿔 열면 메모리가 안 보입니다 — 그때는 위 백업 폴더를 직접 읽히십시오.

---

## 2. 폴더 구조 & 편집 범위

```
놀라운 한국어 500 해설집\                       ← git 뿌리 (원격 없음)
├── 마스터-템플릿-적용본\
│   ├── 놀라운 한국어 500문장 해설 최종\          ← ★ 실제 편집 대상
│   │   ├── 1 시제 … .html            (21) 시제란·현재/과거/미래 분할·시험
│   │   ├── 2 조사 … .html            ( 9) 조사란·은는이가~에서장소·시험
│   │   ├── 3 001~010 … .html         (67) 1~500 본문
│   │   ├── 4 부록 1~8 … .html        ( 8)
│   │   ├── _음성 확인.html            브라우저별 한국어 음성 진단용
│   │   ├── _소리\                     ★ 음원 (f\ m\ *.json) — 함께 옮길 것
│   │   └── 애니메이션_삽화_16x9\        ★ git 에 넣지 않습니다 (2026-08-13 사용자 결정)
│   │                                   쪽마다 1.7~2MB PNG · 98개 180MB · HTML 이 참조하지 않음
│   ├── _도구\                        ★ 검사·음원 스크립트 (README.md 먼저)
│   ├── _언어판 시험\                   다국어판 빌더 (lang_build.py, tr_ja.py, ja\)
│   ├── _음성 견본\                     목소리 비교용 mp3 + _들어보기.html
│   ├── _원본 인쇄본\                   옛 A4 인쇄본 (보존, 편집 X)
│   ├── _점검보고 2026-08-08.md
│   ├── _기초문법연습_템플릿_문서.md       부록 4~7 정본 스펙
│   ├── _숫자표_색·스타일_문서.md          부록 1 색 팔레트
│   └── 교재 (놀라운 한국어 500 원본)\      원본 (편집 X)
├── _claude_메모리_백업\
│   ├── 2026-08-18 현행 메모리\        ← 이 컴퓨터 9개 + `_읽어 주세요.md`
│   ├── 2026-08-10 현행 메모리\        ← 38개 (8월 대작업)
│   └── (6월판 55개)                   부록·숫자표·파일명 규칙은 여기에만
├── _작업_인수인계.md                   ← 이 문서
└── _작업_로그_2026-05-*.md            (역사)
```

- ✅ 편집 **O**: 위 `놀라운 한국어 500문장 해설 최종\` 의 HTML, `_도구\`
- 🚫 편집 **X**: `_원본 인쇄본\`, `교재 …\`, `_backup_*`, OneDrive 밖 orphan 경로
- **파일명 체계**: `1 시제 / 2 조사 / 3 ###~### / 4 부록 N` **접두사 + 공백**. 정렬이 곧 목차입니다.
- **본문 테마색**(하드코딩 금지, 번호대로): 문장구조 1~86 파랑 · 서술어 문법 87~247 보라 · 부사어 문법 248~369 초록 · 기타 문법 370~500 앰버.

---

## 3. 도구 — 고치기 전과 후에 무엇을 돌리는가

```powershell
cd "…\마스터-템플릿-적용본\_도구"
python check_all.py            # 넷을 한 번에. 모두 0건이어야 정상
python show_text.py "2 조사"    # 화면에 보이는 글자만 뽑아 보기
python audio_build.py list     # 음원이 없는 문장 보기 (→ make → wire)
```

**가끔 전수로 훑을 때** (대량 편집이 지나간 뒤 · 회귀를 잡는다)

```powershell
python check_audit.py                  # 구조·슬롯·내용·어휘 24검사
python check_i18n.py                   # 표 머리 순서 · 슬롯이 부호를 가르는가
python check_render.py                 # 실제 렌더 (넓은 화면)
python check_render.py --width 390     # ★휴대폰 화면 — 넘침은 여기서만 난다
python check_wordbreak.py              # 낱말이 낱글자로 갈리는가 (390px)
python check_koen_line.py --width 390  # 한국어 문장과 영어가 같은 줄인가 (1280 도 함께)
python check_exp_line.py               # ★해설 칸 안에서 한 줄인가 (소스만 봄)
python check_ko_noslot.py              # ★UI 한국어가 번역 슬롯 밖에 있는가 (소스만 봄)
python check_vocab_match.py            # 어휘 영어가 원문 문장과 어긋나는가 (0이 목표가 아님)
python check_card_expand.py            # 카드가 눌러서 펴지는가 (28파일)
python sim_longtext.py                 # 번역문이 길어지면 견디는가 (흉내)
```

자세한 설명·사고 이력은 `_도구\README.md` 에 있습니다. 여기서는 요점만:

| 검사 | 잡는 것 |
|---|---|
| `check_structure.py` | 태그 짝 · 중괄호 · **정답(`data-ans`)이 HEAD와 같은가** · 번호표가 가리키는 mp3가 있는가 |
| `check_script_order.py` | 정의보다 앞선 `<script>` 블록에서 함수를 부르는가 |
| `check_style.py` | `<style>` 중괄호 · 스크립트가 스타일 안에 끼었는가 |
| `check_spec.py` | 폭·여백·viewport·lang·charset. 바깥 상자 이름이 갈래마다 달라(`.container` 103 · `.page-container` · `.sheet` · `.wrap`) **예외 3개를 폭까지 함께** 스크립트 맨 위 `EXCEPT` 에 적어 두었습니다 |
| `check_audit.py` | 인코딩 4 · id 중복 · 화면 누수 · 없는 id · **문장 번호 1~500** · 음원 열쇠 → mp3 · **슬롯 밖 영어** · en 슬롯 속 안 잠긴 한국어 · 영어 제목 · **쓰기 정답이 문장과 같은가** · 어휘 상자 4검사 |
| `check_i18n.py` | **표 머리 순서**(한국어가 위인가) · **번역 슬롯이 부호 짝을 반으로 가르는가**. 둘 다 2026-08-18에 0으로 만들었습니다 |
| `check_render.py` | 스크립트 오류 · **가로 넘침** · 상자 밖으로 나간 요소 · 깨진 그림 · **글자색=바탕색** · 빈 쪽. Playwright 필요 |
| `check_wordbreak.py` | **낱말이 낱글자로 갈리는가**(390px). Playwright 필요 |
| `check_koen_line.py` | 한국어 문장과 영어가 **같은 줄**인가 (`--width 390` 과 `--width 1280` 둘 다) | Playwright |
| `sim_longtext.py` | 영어 슬롯을 35% 늘려 **번역문이 길어질 때 판이 견디는가**. Playwright 필요 |
| `check_exp_line.py` | **해설 칸 안에서 한국어와 영어가 한 줄인가**(2026-08-19 신설). ★`check_koen_line.py` 로는 못 잡습니다 — 그 그물은 한국어 **문장**(니다./세요.)이 끝난 뒤의 영어를 보는데, 해설 칸은 「~보다 = comparison particle」처럼 **문장이 아닌 조각**이라 통과합니다. 12곳이 그렇게 `koen_line 0` 아래 숨어 있었습니다. ⚠️**지금은 볼 대상이 없습니다** — 그 표를 「문장 사다리」가 걷어내 `exp-en` 마크업이 0개입니다(§5) |
| `check_ko_noslot.py` | **UI 글자가 번역 슬롯 밖에 한국어로 남았는가**(2026-08-19 신설). ★「슬롯 밖 영어」의 **반대는 아무도 안 세고 있었습니다** — 카드 힌트 578곳이 그렇게 숨어 있었고 **사용자가 눈으로** 찾았습니다. 심어 놓고 시험했습니다(일부러 슬롯 밖으로 돌린 5곳을 정확히 잡음) |
| `check_vocab_match.py` | **번호 문장(영어 원문)과 어휘의 영어가 어긋나는가**(2026-08-19 신설). ★**0이 목표가 아닙니다** — 2026-08-20 셈으로 **35곳 / 23파일**이 남아 있고 대부분 판단이 갈리는 자리입니다(문장에 대응 낱말이 **있는데** 다른 말을 쓴 곳만 고칠 것) |
| `check_card_expand.py` | 카드를 눌렀을 때 아래로 펴지는가(28파일). Playwright 필요 |
| `sweep_survey.py` | **구간을 정해 어휘 규칙 여덟 갈래를 전수 조사**합니다(2026-08-20 신설). `--from 100 --to 500` · `--rule R5`. 고치지 않고 **후보만** 냅니다 |
| `pastel_ng_ok.py` | 「안 됨·됨」의 진한 빨강·초록을 파스텔로(2026-08-20 반영 완료, 책 전체 459곳). `--from 1` 이면 번호 없는 파일(시제·조사·부록)까지 |
| `batchim_xo.py` | 받침 칸을 「X · O」 알약 하나로(2026-08-20 반영 완료, 53곳). ★되풀이가 아닌 아래 줄은 남깁니다 |
| `eosun_ladder.py` | 검사기가 아니라 **한 번 쓰는 파이프라인**입니다(2026-08-19 반영 완료). 어순 해설 표 500개를 「문장 사다리」로 바꿨습니다. **사다리를 놓아도 되는 조건과 걸름망 아홉**을 스크립트 머리글에 적어 두었으니 비슷한 일을 하기 전에 읽으십시오 |

★ **검사기는 13개이고 2026-08-19에 전부 컴퓨터 독립이 되었습니다**(`check_card_expand.py` 가 `D:\OneDrive\…` 를 박고 있던 마지막 하나였습니다 — `9bbd923`). `check_*.py` 는 모두 14개이고 그중 `check_all.py` 는 넷을 부르는 모음 실행기입니다.

★ **다만 한 번 쓰고 남은 파이프라인 13개는 아직 `sys.path.insert(0, r"D:\OneDrive\…")` 를 박고 있습니다**(2026-08-20 셈: `eosun_ladder`·`eosun_fix_spacing`·`eosun_make_ko_only`·`eosun_sample`·`hint_english`·`sl_wt_accent`·`vocab_base_form`·`vocab_drop_josa`·`vocab_order_ko`·`vocab_order_ko2`·`wo_hl_added`·`wolab_block`·`womk_align`). 이미 반영이 끝난 것들이라 급하지 않지만, **다른 컴퓨터에서 그중 하나를 다시 돌릴 일이 생기면 첫 줄에서 죽습니다.** 새 스크립트에는 `import _paths` 를 쓰십시오.

★ **`check_audit.py` 의 「슬롯 밖 영어」는 4건이 정상입니다** — 고유명사 `BTS`·`KTX`·`Amazing Korean 1` 과 진단 페이지입니다. 나머지는 모두 0건이어야 합니다.

---

### ★★ 그물을 칠 때 — 2026-08-13에 잣대를 여섯 번 틀렸습니다

검사기가 낸 숫자를 **그대로 보고하지 마십시오.** 반드시 원문을 열어 보고 갈라야 합니다. 그날 실제로 이랬습니다.

| 검사기가 말한 것 | 진실 | 왜 틀렸나 |
|---|---|---|
| 죽은 핸들러 **102파일** | 0건 | `event.stopPropagation()` 은 메서드인데 「없는 함수」로 셌습니다. 점 앞을 봐야 합니다 |
| 문장 번호 **500개 빠짐** | 0건 | `sentence-num` 은 속성이 아니라 **클래스**입니다 |
| 머리 없음 **105파일** | 0건 | `<header class="title-block sentence">` — class 가 하나가 아닙니다 |
| 활용형 **88개 어긋남** | 0건 | 전부 불규칙 동사(걷다→걸었습니다)로 **책이 맞았습니다** |
| 안 보이는 글자 **13파일** | 0건 | 그라데이션은 `backgroundColor` 가 아니라 **`backgroundImage`** 에 있습니다 |
| 기호만 든 슬롯 **2,885곳** | 4곳 | 「세 글자 이상 라틴」 잣대를 써서 `to go`·`or` 가 「글자 없음」이 됐습니다 |
| 미보호 DOM **637곳** | 0건 | 「`getElementById` 뒤에 점이 있다」만으로는 뜻이 없습니다. **그 id 가 그 파일에 있는가**를 봐야 합니다 |

반대로 **걸린 것이 결함이 아닐 때**도 있습니다 — 「슬롯 밖 영어」에는 한국어 원문 속 고유명사(`TOPIK II`)가, 「슬롯 안 한국어」에는 단추 속 UI(`여`·`남`, 번역돼야 맞음)가 걸립니다.

★ **규칙 엔진으로 못 가르면 「파일끼리 서로 맞는가」로 바꾸십시오.** 활용형이 그랬습니다 — 걷다가 불규칙인지 꼴로는 알 수 없지만, 같은 동사가 파일마다 다르게 적혀 있으면 **반드시 한쪽이 틀린 것**이라 결정적입니다. 08-13의 채점 결함이 그렇게 나왔습니다.

---

## 4. 작업 방식 — 이 순서를 지키십시오

1. **모의 실행(dry run)** — 스크립트에 `--apply` 를 따로 두고, 먼저 바꿀 개수만 셉니다. **예상과 맞을 때만** 반영합니다.
2. **쓰기 직전 태그 짝 검사** — 깨지면 그 파일은 건너뜁니다. 한 번에 여러 파일을 망가뜨리는 것을 막습니다.
3. **반영**
4. **`check_all.py`** + `show_text.py` 로 **전후 화면 글자 대조**
5. **커밋** — 무엇을 왜 고쳤는지, 사고가 있었으면 그것까지 적습니다.

### 도구를 쓸 때 지킬 것

- 경로를 글자로 박지 말고 **`import _paths`**. 컴마다 드라이브 문자가 다릅니다.
- 출력은 **`_paths.stdout()`**. PowerShell 기본 코드페이지(cp949)에서 한글이 깨집니다. ★ PowerShell의 `ConvertFrom-Json`·콘솔 출력으로 한글을 다루려다 여러 번 깨뜨렸습니다. **UTF-8을 다룰 일은 파이썬으로** 하십시오.
- 여러 자리를 고칠 때는 **뒤에서 앞으로**. 정렬은 반드시 **위치 기준**으로. ★ 한 번은 파일 **이름 문자열**로 정렬해 위치가 뒤엉켜 17개 파일의 태그를 깨뜨렸습니다(git으로 되돌림).
- 편집 구간이 **겹치면 손상**됩니다. 후보를 다 모은 뒤 `(시작 오름차순, 끝 내림차순)` 으로 정렬해 **겹치지 않는 바깥쪽만** 남기십시오.

---

## 5. 내용 규칙

### 문체
- **설명·서술은 격식체**(-ㅂ니다 / -습니다). 평서문의 해요체(-아/어요, 봤어요·같아요)는 금지입니다.
- ★★ **학생에게 시키는 안내문의 「~세요」는 그대로 씁니다 — 2026-08-11 사용자 결정**(「~세요: 그대로 쓰는 것이 좋다」). 「고르세요」 403 · 「보세요」 150 · 「확인하세요」 140 · 「클릭하세요」 84 … 화면 글자에 **1,232회**입니다. 「~십시오」는 **9곳뿐**이고 그중 5곳은 본문이 아닌 `_음성 확인.html`, 본문 4곳은 설명 속 권유(「두 무리를 함께 외우십시오」)입니다.
  - ★ **일괄로 격식체로 바꾸지 마십시오.** 2026-07-19에 **TOPIK 해설집에서 똑같은 변환을 했다가 사용자 지적으로 23파일을 전부 되돌린 전례**가 있습니다(메모리 `haeseol-gyeoksikche` 의 예외 ④). 점검 도구가 안내문의 「~세요」를 해요체 위반으로 세면 **그것은 오탐입니다.**
- **문법 용어를 설명에 늘어놓지 않습니다.** "양보" → "앞말이 맞지만 그래도". 연결어미 뒤 쉼표 금지. 군더더기 꼬리표 금지.
- 예문은 격식체. **「예시」라는 말은 학생 화면에 쓰지 않습니다** — 2026-08-10 전수 정리: 문장에는 **「예문」**, 문장이 아닌 것(활용형·낱말·발음·미리 푼 견본 행)에는 **「보기」**. 저자용 주석의 「예시」 8곳만 남아 있습니다(정상).
- ★ **예문(완전한 문장)에는 주어를 넣습니다** — 2026-08-10 사용자 결정. 한국어는 주어를 자주 빼지만 초급 학습자에게는 「저는 수미에게 책을 줍니다」처럼 주어가 보여야 영어 대역(I ~)과 짝이 맞습니다. 활용형 칩(먹었습니다)·문답 짝(누가 왔습니까? → 친구가 왔습니다)·어순 시범은 대상이 아닙니다.
- ★ **한 문장에 한 가지 생각.** 2026-08-10 사용자 지적: 수준을 올린다며 문장을 압축했더니 **오히려 어려워졌습니다.** 「그 일이 놓인 때를 표시합니다」→「시간을 나타내는 말 뒤에 붙습니다. 그 일을 언제 하는지 알려 줍니다.」 비유 대신 예문을 쓰십시오.
- **용어는 풀지 않습니다.** 학생들은 목적어·간접목적어·서술어를 이미 압니다. 초등학생에게 설명하듯 하지 마십시오. 다만 **받침·관형어** 같은 한국어 고유 용어는 처음 한 번만 짧게 풀어 줍니다.

### 다국어 대비 (i18n)
이 책은 나중에 일본어·중국어 등으로 번역됩니다. 그래서:

- ★★ **한국어와 영어를 한 줄에 두지 마십시오 — 2026-08-11 사용자 원칙.** 「한국어와 영어가 같은 줄에 있으면 다국어 번역 때 문제가 생긴다. **영어 줄은 위 한국어 줄을 자리 그대로 옮긴 번역**이어야 하고, 한국어를 섞지 않는다. 위 줄이 형태를 이미 보여 주므로 영어만으로 뜻이 통한다.」
  - 보기: 한국어 「와/과, 나/이나, ~고, ~지만, ~거나」 ↔ 영어 「and, or, and, but, or」 (자리끼리 대응).
  - ★ 제가 **아래로 옮기면서 이 잘못을 만들었습니다**(`97dea30`) — 한국어 칸에서 걷어낸 영어를 그대로 아래에 붙이니 「와/과 (and), 나/이나 (or)…」가 되어 한국어가 영어 줄에 섞였습니다. `06d8065` 에서 바로잡았습니다.
- ★ **한국어 문장 안에 영어를 넣지 마십시오.** `He = 그는` 같은 표기는 번역하면 무너집니다.
- ★ **영어를 기준점으로 삼은 설명 금지.** 「영어의 to와 같습니다」는 일본어·몽골어·터키어 독자에게 틀린 말입니다. 한국어 안에서 판별할 기준을 주십시오(사람·동물 ↔ 그 밖 / 하는 곳 ↔ 있는 곳).
- 어쩔 수 없이 독자의 언어를 기준 삼아야 하면 **`data-reauthor="lang-anchor"`** 를 달고 주석으로 "다른 언어판에서는 그 언어 기준으로 다시 쓸 것"이라고 적으십시오. 번역이 아니라 **다시 쓰기** 대상이라는 표시입니다.
- **구조**: `<html lang="ko" translate="no">` 로 페이지를 잠그고, 번역할 자리만 `translate="yes"` 로 엽니다. 영어 문장 **안**의 한국어는 `<span lang="ko" translate="no">` 로 다시 잠급니다.
- ★★ **「한국어 위 · 영어 아래」가 어디에 걸리는지 — 2026-08-13 사용자 결정.** 원칙은 하나지만 적용 범위가 갈립니다. 이 선을 넘나들지 마십시오.

  | 자리 | 적용 | 까닭 |
  |---|---|---|
  | **해설·설명** | **적용** | 설명은 한국어가 원본이고 영어는 그 번역입니다 |
  | **어휘 상자** | **적용** | 표제어(한국어)가 위, 뜻풀이(영어)가 아래 |
  | **500문장** | **적용 안 함** | **영어를 보고 한국어로 바꾸는 연습**이라 영어가 문제(prompt)입니다 |

  - **500문장에 한국어를 얹으면 정답을 미리 보여 주는 것**이 됩니다 — 문장 제목 `1) I am happy.` 바로 아래 쓰기 칸의 답이 「저는 행복합니다.」입니다. 같은 이치로 **플래시카드 앞면**, **관형어의 어순 견본**(`the book that I read`, `translate="no"` 로 잠금)도 그대로 둡니다. **뒤집자고 제안하지 마십시오.**
  - ⚠️ **옛 규칙 「해설 표의 열 순서 `영어 | 역할 | 한국어 | 해설` 은 고정」은 2026-08-19에 없어졌습니다** — 사용자가 36언어 일괄 번역을 앞두고 **표 자체를 없애기로** 정했습니다. 지금 그 자리는 「문장 사다리」입니다(§5 아래 · §7).
  - 2026-08-13 전수 확인: 어휘 **1,525항목** 모두 표제어가 위·뜻풀이가 아래이고, 표제어가 영어인 것 0 · 뜻풀이에 안 잠긴 한국어 0. **영어가 한국어 짝 없이 홀로 있는 설명 덩이도 0곳**입니다.
  - ★ **이 원칙은 기계로 감시하기 어렵습니다.** 표 머리(`Examples`·`Verbs`)·플래시카드·문장 제목 501개는 **영어가 앞에 오는 것이 옳아서**, 범용 잣대로 세면 4,000곳이 헛걸립니다(2026-08-13에 잣대를 네 번 고쳐 보고 접었습니다). 검사기 대신 **아래 본보기를 지키십시오.**

- **설명 상자는 이 꼴 하나로 씁니다** — 제목 짝과 본문 짝이 각각 한국어 먼저입니다.

  ```html
  [한국어 제목]<br><small lang="en" translate="yes">[영어 제목]</small><br><br>
  [한국어 본문]<br><small lang="en" translate="yes">[영어 본문]</small>
  ```

  - ★★ **`<br>` 을 빠뜨리면 한 줄이 됩니다** — 2026-08-13에 **13곳(9파일)** 이 그랬습니다. 본보기는 같은 파일 안에 이미 있었습니다(`3 123~141` 460행은 `<br><small lang="en">` 로 나뉘어 있는데 540행은 `<br>` 이 빠져 「그는 제 친구입니다. (He is my friend.)」가 한 줄이었습니다).
    - 두 꼴이었습니다 — 게임 안내문 8곳(회색 `<span style="color:#94a3b8;">` 앞) · 예문 5곳(`<small lang="en">` 앞).
  - ★★ **`display:flex` 도 두 말을 한 줄로 붙입니다** — `<br>` 로는 못 고칩니다. `1 시제 0` 의 `.sx-ex{display:flex; align-items:baseline}` 이 「저는 밥을 먹습니다. *I eat rice.*」를 나란히 세우고 있었습니다. **`flex-direction:column`** 으로 고쳤습니다.
    - 반대로 `.cr-title{display:flex; flex-direction:column}` 은 **바르게 쌓는 본보기**입니다.
  - ★ **예문도 짝 안에 넣으십시오.** 2026-08-13에 `3 105~114` 의 「표기 안내」 상자에서 **영어 예문이 영어 본문보다 위**에 있고 **같은 예문이 두 번** 나오는 것을 사용자가 화면에서 찾아냈습니다. 게다가 둘의 말이 달라(`to the verb stem` ↔ `to the Verb`) **상자의 요지인 「어간에 붙인다」를 예문이 어기고** 있었습니다.
- ★ **페이지 머리(제목)도 한국어가 위·큰 글자입니다** — 2026-08-10 사용자 결정으로 본문·부록 **73파일**을 뒤집었습니다(그전에는 영어 38.4px 대문 제목 / 한국어 16.3px 곁글씨). 이유: ①한국어 교재이므로 학습 언어가 제목의 주인 ②시제·조사 39파일이 이미 한국어 우선이라 106파일이 갈려 있었음 ③**언어판을 찍으면 영어 자리에 일본어가 들어가는데, 옛 구조라면 일본어판 대문 제목이 「文型 1」이 되고 한국어가 곁글씨로 밀립니다** — 번역으로 안 풀리는 구조 문제.
  - 구조: `<header class="title-block">` 안이 `.title-ko`(위) → `.title-divider` → `.title-en`(아래) 순서이고, **큰 제목이 `<h1 class="title-ko-main">`** 입니다. 태그까지 맞바꾼 까닭은 전역 `h1{background:linear-gradient…}` 규칙 때문입니다 — 순서만 바꾸면 작은 영어가 그라데이션 글자를 물려받아 안 보이게 됩니다.
  - CSS 는 값을 그대로 두고 **선택자만 맞바꿨습니다**(`.title-en .title-main` ↔ `.title-ko .title-ko-main`, 부제·좁은화면·`@media print` 까지 7규칙). 도구: scratchpad `header_flip.py` + `header_verify.py`(글자 다중집합 대조 — 순서가 바뀌므로 순서 대조는 오탐).
  - ★ **본문 67파일 머리에는 문장 범위 뱃지가 있습니다** — `<p class="title-range">문장 11~20</p>`(한국어 칸, 부제 아래) + `<p class="title-range-en" lang="en" translate="yes">Sentences 11–20</p>`(영어 칸 끝). 2026-08-11 사용자 결정. **제목 앞에 번호를 붙이지 마십시오** — 제목이 이미 단원 번호를 달고 있고(「문장구조 2」), 갈래가 넷인데 **갈래마다 1부터 다시 세기 때문에** 통일된 앞 번호가 아예 없습니다. 넣은 까닭: 이 책의 실제 주소는 문장 번호인데(파일명 `3 011~020 …` 의 정렬 키 = 목차) 그전에는 **67파일 중 1파일만** 화면에 범위를 보여 주었습니다.
    - 범위는 파일명에서 읽되 **`sentence-num` 실제 값으로 파일마다 검산합니다**(67/67 일치 확인). 화면에는 앞 0을 떼고 씁니다(`001~010` → 「문장 1~10」). 한국어는 `~`, 영어는 en dash `–`.
    - 뱃지 색은 `--title-sub-color` 를 물려받아 갈래 색(파랑·보라·초록·주황)을 저절로 따라갑니다. `align-self:flex-start` 가 없으면 `.title-ko` 가 flex column 이라 **뱃지가 가로로 꽉 늘어납니다.**
    - 도구: scratchpad `range_badge.py`(모의 실행 기본 · 넣은 5조각을 도로 빼면 원본과 같은지 파일마다 확인) + `range_shot.py`(렌더 실측).
- ★ **절 제목(h2·h3)도 한국어가 위·큰 줄입니다** — 2026-08-11 사용자 지시로 104곳을 뒤집었습니다. 두 본보기만 쓰십시오.
  - `<h2>` : `<h2>핵심 문법<span class="gloss" lang="en" translate="yes">Key Grammar</span></h2>` (`.gloss` 가 `display:block` 이라 저절로 아랫줄)
  - `<h3>` : `<h3>한국어<br><small style="font-weight:600;"><span lang="en" translate="yes">English</span></small></h3>`
  - ★ **첫 절 제목은 「핵심 문법 / Key Grammar」로 통일**(사용자 결정). 그전에는 넷으로 갈려 있었습니다 — 문법 정리 61·기본 문형 2·기본 문장 구조 1·새로운 문법 1. **제목 앞에 번호를 붙이지 마십시오**(갈래마다 1부터 다시 셉니다).
    - ★★ **67곳이 아니라 65/67 이고, 그것이 맞습니다 (2026-08-13 확인).** `3 061~070`·`3 071~078` 에는 문법 절이 **아예 없습니다.** 이 둘은 책에서 유일하게 이어지는 단원 **6-2·6-3** 이고, 문법은 6-1(`051~060`)에서 한 번 가르친 뒤 의문사만 바꿔 연습합니다. **문법 절을 새로 지어 넣지 마십시오.**
  - **BONUS → 「더 배우기 N」**(5곳, 사용자 결정). 뱃지 자간은 라틴 대문자용 `0.12em` 이라 한국어에는 `0.02em` 로 낮췄습니다.
  - ★ **본체 절 제목은 「문장 만들기 / Sentence Building」** — 67/67, 2026-08-11 사용자 결정. 그전 「연습: 한국어 문장을 만들어 보세요!」는 셋이 어긋났습니다 ①**뒤따르는 1~4번이 전부 연습**이라 이 절만의 이름이 될 수 없음 ②다른 제목은 이름꼴인데 이것만 명령문 + 느낌표(게다가 `보세요`는 해요체) ③다른 제목 2~7자인데 19자.
    - **번호를 붙이지 않습니다** — 핵심 문법·문장 만들기는 **배우는 곳**, 1~4번은 **연습하는 곳**으로 층이 갈립니다. (절 번호를 학생 화면 글자에서 부르는 곳은 0곳이라 재번호도 가능했지만 사용자가 무번호를 골랐습니다.)
    - 제목이 **아예 없던 4파일**(051~060 · 061~070 · 071~078 · 079~086)에도 넣어 67/67로 맞췄습니다. 넣는 자리는 **첫 문장 h3 바로 앞** — 그러면 정상 파일과 같은 꼴(`<hr>` → h2 → 부제 → `<hr>` → h3)이 됩니다. 그 4파일에서 📖 보충 설명 절은 문장 **뒤**에 있어 새 제목이 문장 10개를 다 덮습니다.
    - 도구: scratchpad `prac_title.py`(모의 실행 기본 · 태그 짝 검사 · 태그 걷어낸 글자 대조).
  - ★★ **절 제목의 영어는 두 자리에 있습니다** — h2 안 `<span class="gloss">`(핵심 문법 65 · 더 배우기 4) **또는 바로 뒤 `<p class="subtitle">`**(문장 만들기 · 1~4번). `<h2>` 안만 세면 「다섯 절 다 영어가 없다」는 **틀린 답**이 나옵니다 — 2026-08-11에 그렇게 세어 사용자에게 잘못 보고했고 **이미 있는 영어를 335줄 덧붙일 뻔했습니다.** 지금 본문 h2 중 영어가 없는 것은 **0곳**입니다.
    - 이웃 넷의 영어: `1. 따라 읽기 / Read Along` · `2. 카드 보고 말하기 / Look & Speak` · `3. 문장 짝 맞추기 / Sentence Matching` · `4. 쓰기 시험 / Writing Test`. 새 제목의 영어는 여기에 결을 맞춰 골랐습니다(짝 맞추기 = Matching ↔ 만들기 = Building).
  - ★ **번역 슬롯이 낱말·따옴표 가운데를 자르지 않게 하십시오.** `<span lang="en">Possessive particle '</span>의'` 처럼 **따옴표가 반으로 갈린 곳이 여럿 있었습니다.** 영어 안의 한국어는 `<span lang="ko" translate="no">` 로 잠그고 따옴표·물결·물음표는 그 안에 함께 넣습니다.
  - ★★ **제목 검사는 「첫 글자가 어느 말이냐」로 하면 안 됩니다.** `'이다' is a special verb` 는 한국어로 시작해 그 검사를 통과합니다 — 그렇게 6곳이 숨어 있었고 **Playwright 렌더가 잡았습니다.** 반드시 **줄 단위**로 「큰 줄에 영어가 많고 작은 줄에 한국어가 많은가」를 보십시오. 도구: scratchpad `h_lines.py`(두 줄짜리 제목 311개 전수).
  - **문법 링크 카드(`gl-ex`) 5개**도 예문 줄에 영어를 붙였습니다(2026-08-11) — `<span class="gl-ex">한국어<span class="gloss" lang="en" translate="yes">English</span></span>`. 동사 뜻은 **내가 정하지 않고 그 카드가 가리키는 문장 번호의 영어와 대조**했습니다(뛰다↔14) He ran. …). 도구: scratchpad `glex_en.py` — 책의 문장이 기댄 것과 다르면 멈춥니다.
  - 뒤집지 않는 것: **`<h3>1) I am happy.` 꼴 문장 제목 501개** — 영어를 보고 한국어로 옮기는 연습이라 사용자가 고정한 결정입니다.
- ★ **이름표 뒤는 콜론(`:`)입니다 — 제목 밖에도 적용합니다.** 2026-08-11에 제목 68곳에 이어 **패턴 상자 15개(7파일)** 와 **이름표 자리 75곳(47파일 — 부제 21 · 굵은 이름표 18 · 작은 이름표 36)** 을 바꿨습니다.
  - ★★ **문장 속 이음표는 그대로 두십시오.** 화면 글자의 대시는 **732곳**인데 그중 이름표는 90곳뿐입니다. 「구조 2에서는 서술어가 동사입니다 — 누군가가 무엇을 하는지 설명합니다」·「Coffee is delicious. — talking about coffee」 같은 것은 콜론이 들어갈 자리가 아닙니다.
  - **가른 잣대**: 대시 왼쪽이 **22자 이하**이고 **문장 끝(`.` `?` `!` 니다 세요 어요)이 아닐 것**. 대시가 **둘 이상** 든 것도 건너뜁니다 — 콜론이 두 번 되어 어색해집니다(「Add the receiver — the indirect object — with '에게'」).
  - ★ **부제(`p.subtitle`)를 잊지 마십시오** — 대문 제목의 **영어 짝**이라 제목만 바꾸면 한국어는 콜론, 영어는 대시로 어긋납니다. 2026-08-11에 21곳이 그렇게 어긋나 있었습니다.
  - ★★ **대시를 셀 때 속성값·주석을 빼십시오** — 처음에 **4,988곳**으로 셌는데 `title="Listen — male voice"` 같은 속성과 CSS·JS 주석이 대부분이었습니다. `<script>`·`<style>`·`<!-- -->` 를 걷어내고 **텍스트 노드만** 세면 732곳입니다. 도구: scratchpad `emdash2.py`.
- ★ **제목에 대시(`—`)를 쓰지 마십시오 — 이름표 뒤는 콜론(`:`)입니다.** 2026-08-11 사용자 지시로 **68곳(51파일)** 을 바꿨습니다. 대문 제목 26(`시제 1 · 현재형: Ⓐ 받침 없음`) · N단계 6 · 더 배우기 4 · 절 제목 32. **한국어에만 든 것 64곳과 영어에만 든 것 4곳을 함께** 고쳐 두 말이 같은 부호를 쓰게 했습니다.
  - ★ **제목의 영어는 한국어와 같은 것을 말해야 합니다** — 한국어에만 있고 영어에 빠진 뒷부분을 **15곳(12파일)** 채웠습니다(`Multiple Choice` → `Multiple Choice: 에 vs 에서`). 영어 안의 한국어는 `lang="ko" translate="no"` 로 잠급니다.
    - `3 079~086` 제목 하나는 **윗줄까지 영어**였습니다(한국어가 아예 없었음). 「세 가지 뜻: 그리고 · 또는 · 그러나」를 윗줄에 놓았습니다.
  - ★★ **글자 대조는 빈칸을 빼고 하십시오** — `flat()` 이 태그 자리마다 빈칸을 넣기 때문에 `<span>왜</span><span>: two ways</span>` 가 「왜 : two ways」로 보여 **멀쩡한 것을 이상하다고 잡습니다.** 화면에서는 붙어 나옵니다.
  - 도구: scratchpad `dash_fix.py` · `en_fill.py`(`{한국어}` 를 자동으로 `lang="ko"` 로 잠그는 틀).
- ★ **패턴 상자(`gstep-pattern`)도 한국어가 위·영어가 아래입니다** — 2026-08-11 사용자 지시로 **45곳(30파일)** 을 뒤집었습니다. `<div class="gstep-pattern">[영어]<small>[한국어]</small></div>` → `[한국어]<small>[영어]</small>`.
  - **CSS 는 손대지 않습니다** — `.gstep-pattern small{display:block; font-size:.8rem}` 이 이미 아랫줄을 작게 그리므로 **두 덩이를 맞바꾸기만** 하면 됩니다(제목 뒤집기 때와 달리 태그를 바꿀 일이 없습니다).
  - ★ **번호 표지 ①②③ 은 윗줄에 남기십시오.** 6곳(051~060)에서 표지가 큰 줄 맨 앞의 맨 글자라, 그냥 맞바꾸면 **영어와 함께 아랫줄로 내려갑니다.** 표지는 말이 아닙니다.
  - 상자 68곳의 갈래: **A 45곳** 뒤집음 · **B 19곳** 영어뿐이고 한국어 줄이 아예 없음(뒤집을 것이 없어 **한국어를 새로 써야 합니다 — 아직 안 함**) · C·D 13곳은 이미 한국어가 위이거나 짝이 아님(「저 = I → 제 = my」 + 보기들).
  - `<small lang="en" translate="yes">` 가 **한국어를 감싸고 있던 4곳**은 뒤집으면서 그 껍데기가 없어졌습니다(한국어가 영어 슬롯 안에 있던 것이 바로잡힘).
  - **영어 이름표는 한국어로 옮깁니다** — Verb 동사 · Adjective 형용사 · Noun 명사 · Past 과거 · Formal 격식체 · Polite 정중 · **No final consonant 받침 없음** · **With 받침 받침 있음** · Subject 주어 · Object 목적어 · ending 어미 · ㅂ-irregular ㅂ 불규칙. 원래 영어 줄은 **한 글자도 바꾸지 말고** `<small>` 로 내리십시오.
  - ★★ **이 상자를 셀 때 두 번 틀렸습니다. 그물을 이렇게 치십시오.**
    - ① **길이 제한을 걸지 마십시오** — `[\s\S]{0,900}?` + 줄바꿈 조건으로 세어 **80개를 68개로** 셌습니다. 12개가 통째로 안 보였고 그 안에 영어가 위인 상자가 있었습니다.
    - ② **「한국어가 있는가」로 가르지 마십시오** — 윗줄에 「받침」 한 낱말만 있어도 「한국어 있음 = OK」로 새어 나갑니다. **「윗줄에 3글자 이상 영어 낱말이 있는가」**로 보십시오. 이 잣대로 27곳이 더 나왔습니다.
    - ③ 셀 때 **`<small>` 과 `.gloss` 는 먼저 걷어내십시오**(둘 다 이미 아랫줄로 그려지는 영어입니다). 겹친 `<small>` 은 비탐욕 정규식이 안쪽부터 먹어 **오탐**을 만듭니다.
  - 도구: scratchpad `gpat7.py`(갈래 나누기) · `gpat10.py`(윗줄 영어 낱말 그물 — **이것이 바른 잣대**) · `marker.py`(표지 찾기) · `flip_pat.py`·`bfill.py`·`kind1fix.py`(모두 모의 실행 기본).
- ★★ **번역 슬롯 밖에 남은 영어를 찾으십시오 — 화면으로는 절대 안 보입니다.** 2026-08-11에 패턴 상자 **79곳**을 훑어 `translate="yes"` 가 없는 영어 **11곳**을 찾아 고쳤습니다(`2d1dc6d`). 페이지가 `<html translate="no">` 로 잠겨 있으므로 **슬롯 밖 영어는 일본어판에서도 영어 그대로 남습니다.** 한국어 자리는 눈에 띄지만 이것은 영어 독자에게 멀쩡해 보여 세션 셋을 지나도록 살아 있었습니다.
  - 갈래 둘: ①**이미 아랫줄인데 껍데기만 없던 것 5곳**(`<em>The usual spot…</em>`) — 속성만 붙이면 됩니다 ②**한국어와 한 줄이던 것 5곳**(`오늘부터 여름방학입니다. (Summer vacation starts today.)`·`원인(Cause) = …`·`학생이 네 명 = 4 students`) — 괄호를 떼고 **아랫줄로 내립니다**(§5 「괄호 인라인 영어」 규칙과 같은 자리).
  - ★ **영어가 「번역할 말」이 아니라 「견줄 자료」인 곳이 있습니다** — `3 474~488 관형어`의 「the book that I read」는 **영어 어순을 보여 주는 표본**이라 번역되면 상자가 무의미해집니다. `<span lang="en" translate="no">` 로 **영어를 잠갔습니다**. 한국어를 잠그는 것과 같은 이치입니다. 이름표(`영어:`/`한국어:`)만 번역에 엽니다.
  - ★★ **`&nbsp;` 의 「nbsp」가 영어 낱말로 잡힙니다.** 「3글자 이상 라틴 글자」 그물에 걸려 처음에 **24곳**이 나왔는데, 엔티티를 풀고(`html.unescape`) 다시 세니 **0곳**이었습니다. 이 책의 패턴 상자는 사이띄개로 `&nbsp;|&nbsp;` 를 쓰므로 거의 모든 상자가 헛걸립니다. **글자를 세기 전에 엔티티를 먼저 푸십시오.**
  - 그물: `<small>`은 **짝을 세어**(겹침 대비) 걷어내고, `<br>` 로 줄을 가른 다음, 줄마다 `<span|em|small ... lang="en"|translate="yes"|translate="no">…</span>` 를 지우고 남은 글자에 라틴 낱말이 있는지 봅니다. 도구: scratchpad `slot_en.py`(모의 실행 기본).
- ★ **「~해 보세요!」 꼴 지시문을 문장 칸에 끼우지 마십시오** — 2026-08-11 사용자 지시로 **8곳(2파일)** 을 지웠습니다. `<p style="font-weight:700;color:#1e3a8a;">목적어를 추가해 보세요! …</p>` 가 어휘 칩과 쓰기 칸 사이에 끼어 있었습니다(031~040 4곳 · 041~050 4곳).
  - 까닭 넷: ①해요체 + 느낌표 ②**영어 제목이 이미 할 일을 다 말합니다**(`31) I bought.` → `32) I bought a gift.`) ③새 낱말은 **어휘 칩**이 이미 보여 줍니다 ④67파일 중 **2파일에만** 있었습니다.
  - 「완성된 구조 4!」·「구조 5!」도 함께 지웠습니다 — **그 쪽 제목이 이미 「문장구조 4」·「문장구조 5」** 라 새 정보가 아닙니다.
  - 남은 「~세요」는 손대지 않았습니다 — 「카드의 영어를 보고 한국어로 말해 보세요」처럼 **모든 파일에 있는 절 안내문**입니다(`p.ko` 164곳 · 91파일). 이것을 격식체로 바꿀지는 **아직 정하지 않았습니다.**
  - 도구: scratchpad `addstep.py`(「~세요」 전수·감싼 태그별) · `block.py`(묶음 전체 보기) · `step_del.py`(모의 실행 기본).
- ★ **제목 앞에 장식 이모지를 붙이지 마십시오** — 2026-08-11 사용자 지시로 **113곳(59파일)** 을 걷어냈습니다(📝 82 · ✍️ 19 · 📚 3 · 📅 3 · 📌 2 · 📖 2 · 🔊 2). 제목 자리(h1~h4·strong·b)의 **글자가 이모지로 시작하는 곳**만 골랐습니다.
  - **일부러 남긴 82곳** — ①`✅`10·`❌`5 는 **이모지 하나가 곧 내용**입니다(맞음/틀림 칸). 지우면 빈 칸이 됩니다 ②`⚠️`24·`💡`21 은 장식이 아니라 **무엇을 알리는 쪽지인지 가르는 표지** ③`★`14 는 한 파일의 문법 갈래 표지.
  - ★★ **단추의 이모지는 절대 건드리지 마십시오** — 🔊 재생 · 📚 어휘 · 📖 해설 · ✅ 채점 · 💡 정답 · 🔄 초기화. **책 전체 이모지 7,116곳 가운데 대부분이 단추입니다.** 「📝 가 몇 개냐」로 세면 단추까지 걸립니다. 그물을 **「제목 자리 + 글자 맨 앞」** 으로 좁혀야 합니다.
  - 손대기 전 확인 셋: **이모지만 든 제목이 있는가**(빈 껍데기가 될 곳 — 0곳이었습니다) · **그 이모지가 단추인가**(🔊 2곳은 `div.caution` 안 발음 주의 쪽지였습니다) · **번역 슬롯 안에 있는가**(7곳 — 지우면 슬롯도 함께 깨끗해집니다).
  - 도구: scratchpad `emoji_inv.py`(감싼 태그별 전수) · `title_emoji2.py`(제목 자리만) · `pre_check.py`(손대기 전 확인) · `emoji_strip.py`(모의 실행 기본).
- ★ **괄호 인라인 영어 `한국어 (English)` 를 쓰지 마십시오 — `<span class="gloss" lang="en" translate="yes">English</span>` 로 아래 줄에 놓습니다.** 2026-08-10 사용자 결정으로 문장 37곳 + 제목·라벨 117곳을 전환했습니다(47파일). 굵은 태그로 싸여 있으면 gloss 는 그 **바깥**에 둡니다(영어까지 굵어지는 것을 막음).
  - **일부러 남긴 87곳** — ①**영어 문장 속 괄호** 65곳(`니다 (statement) / 니까 (question) right after the stem.` — 옮기면 영어 문장이 깨집니다) ②**한 줄에 영어 괄호가 둘 이상** 7곳(`누구 (Who), 언제 (When)` — 하나만 옮기면 짝이 어긋납니다) ③**문장 중간** 등 12곳(`영어는 천(thousand) 단위로 끊지만` — 블록을 끼우면 한국어 문장이 갈라집니다) ④**부록 2·부록 8** 2파일(특수 판형 보존).
  - 도구: scratchpad `label_apply3.py`(안전 규칙 5개) + `label_verify.py`(HEAD 대조로 글자 보존 확인).
  - **남겨 둔다고 적었던 「짝이 어긋난다」 7곳 가운데 2곳은 풀렸습니다**(2026-08-11) — `누구 (Who), 언제 (When)` 처럼 **영어끼리 모아 gloss 한 줄로 옮기면** 짝이 어긋나지 않습니다. 진짜 남는 것은 짝 자체가 내용인 곳뿐입니다(`와/과 (and), 나/이나 (or), ~고 (and)…` 1곳 — 물결만 슬롯 밖으로 뺐습니다).
- ★ **번역 슬롯이 부호를 반으로 가르지 않게 하십시오** — 2026-08-11에 **따옴표 52곳·괄호 97곳**을 이었습니다(현재 둘 다 0곳). 화면에는 멀쩡히 보여서 눈으로는 못 찾습니다.
  - 나쁜 꼴 `<span lang="en">Present Progressive '~</span>고 있다' (문법 2: 현재진행)` → 일본어판을 찍으면 「現在進行 '~」 뒤에 `고 있다'` 가 남습니다.
  - 바른 꼴 `<span lang="en">Present Progressive</span><span lang="ko" translate="no">'~고 있다'</span> (문법 2: 현재진행)`
  - **규칙 셋** ①따옴표·괄호 기호는 **슬롯 밖** ②영어 안의 한국어는 `<span lang="ko" translate="no">` 로 잠그고 그 안에 따옴표를 **함께** 넣는다 ③심벌(💡⚠️📝)도 슬롯 밖.
  - ★ **기호만 든 슬롯은 아예 만들지 마십시오** — `<span lang="en" translate="yes">&nbsp;|&nbsp;</span>` 같은 구분선·사이띄개는 번역할 것이 없습니다. 2026-08-11에 30곳(14파일)을 걷어냈습니다(현재 0곳). **껍데기만 없애는 것이 안전한 까닭**: 30곳 모두 속성이 `lang="en" translate="yes"` 하나뿐이었고, **CSS 가 `[lang=…]`·`[translate…]` 를 겨냥하는 규칙은 책 전체에 0개**입니다 — 그래서 모양이 바뀔 수 없습니다. 손대기 전에 이 두 가지를 꼭 확인하십시오. 도구: scratchpad `symslot_scan.py`·`symslot_fix.py`.
  - ★★ **그물을 어떻게 치느냐로 답이 달라집니다.** 「홀따옴표가 홀수」만 세면 `let's`·`stem's` 같은 영어 축약형이 섞여 **241곳**으로 부풀고, 「괄호 짝이 안 맞음」만 세면 **666곳**으로 부풉니다. **「짝이 바깥에 있고 그 사이에 한국어가 있는가」로 좁혀야** 진짜만(따옴표 52·괄호 108) 남습니다. 그리고 따옴표를 0곳으로 만든 뒤 **괄호로 그물을 바꾸니 108곳이 새로 나왔습니다** — 한 부호를 다 잡았다고 병이 끝난 것이 아닙니다.
  - 도구: scratchpad `split_quote.py`·`paren_scan.py`(찾기) · `quote_fix.py`·`paren_fix.py`(기계로, 괄호 안이 한국어뿐일 때만 — 화이트리스트라 라틴 글자는 못 들어옴) · `quote_fix2.py`·`paren_fix2.py`(사슬처럼 엮인 것은 낱낱이) · `spot_text.py`(고친 자리만 HEAD 와 렌더 대조 — 문장 짝 맞추기 게임이 문장을 섞으므로 **본문 전체 대조는 오탐입니다**).
- 언어판은 원본을 고치지 않고 **빌더로 찍어 냅니다**: `_언어판 시험\lang_build.py` + `tr_ja.py`. 일본어판 시험작 1개가 `_언어판 시험\ja\` 에 있습니다.
- ★★ **스크립트 안 문자열은 「한국어냐 영어냐」로 절대 못 가릅니다 — 「어디에 놓였느냐」로만 갈립니다.** 2026-08-12에 이것으로 그물을 세 번 고쳐 쳤습니다.
  - `삽니까?` 와 「다시 고르세요.」는 둘 다 한국어지만 하나는 **배우는 대상**이고 하나는 **화면 안내**입니다. `All matched!` 와 `buy` 는 둘 다 영어인데 **둘 다 번역 대상**입니다(독자의 언어).
  - **잠글 자리**: `AK_SND`·`AK_SEQ` 의 열쇠(★한국어 문장입니다 — **바꾸면 음원을 못 찾아 소리가 죽습니다**) · `emPairs` 의 `ko:` 쪽 · `data-ans` · 활용표 배열의 한국어 칸.
  - **번역할 자리**: `innerHTML`·`textContent` 에 대입되거나 `+` 로 이어 붙는 문구 · `return` 으로 나가는 문구 · 배열의 **영어 뜻풀이 칸**(`["사다","buy",…]` 의 buy) · `emPairs` 의 `en:` 쪽.
  - ★ **`data-ans` 로만 「학습 내용」을 걸러 내려 하면 실패합니다** — 그 그물에는 537가지만 걸리고, 활용표·플래시카드의 한국어 1,000여 가지가 「UI」로 새어 나옵니다. 처음에 그렇게 세어 「UI 한국어 1,110가지」라는 **틀린 답**을 얻었습니다.
  - ★ **긴 이어붙이기 사슬의 가운데 토막은 앞 400자 안에 `innerHTML` 이 안 보입니다.** 「안 썼습니다」(67파일)·「↻ 클릭」(28파일)이 그래서 「모름」에 빠졌습니다. **태그를 품고 화면 글자가 있으면 DOM 으로 갈 수밖에 없다**는 규칙을 더해 잡았습니다(단 **배열 칸 판별을 먼저** 해야 활용표가 안 새어 나옵니다).
  - ★★ **삼항의 두 가지를 함께 보십시오.** `has?'받침 O':'받침 X'` 에서 **`:` 만 보면 「받침 X」만 잡히고 「받침 O」는 모름에 남습니다** — 짝이 갈립니다. `?` 뒤도 같은 자리입니다. 그리고 `{"가다":"to go"}` 의 객체 값도 `:` 뒤라 **생김새가 삼항과 똑같으니**, 앞쪽에 `,`·`;`·`{` 없이 `?` 가 있는지로 갈라야 합니다(안 그러면 부록 3 뜻풀이 21개가 잠겨 번역에서 빠집니다).
  - ★ **정규식 리터럴을 건너뛰십시오.** 이 책에는 따옴표를 품은 정규식이 **165곳** 있습니다 — `/[?？!.。,'"“”]/g`(채점기 `wtNormalize`)·`/speakKorean\('([^']*)'/`(음원 그물). 그 따옴표를 문자열 시작으로 읽으면 렉서가 어긋납니다. **다만 이 책에서는 결과가 안 바뀌었습니다**(105파일 전수 대조 결과 2,463가지로 동일 — 같은 `.replace(…)` 안에서 곧 되맞춰지고 헛것은 전부 걸러졌습니다). 고쳐 두었으니 앞으로 다르게 깨지지 않습니다.
  - ★ **화살표만 보고 「번역 금지」 표를 붙이지 마십시오** — 「→ 짝이 되는 영어 문장을 고르세요.」는 통째로 번역 대상입니다. 화살표 **뒤가 조사만으로 끝나는 것**(`받침 O → 은 · 이`)과 **자모가 든 것**(ㅂ니다·ㄹ 것입니다)만 표를 붙여야 합니다. 헐거운 그물로는 70여 줄이 헛걸립니다. 지금 표가 붙은 것은 **47줄**입니다.
  - ★ **겹침이 0인지 반드시 확인하십시오.** 빌더 ③단계는 `js.replace(q+k+q, q+v+q)` 라 **자리를 가리지 않고 전부 바꿉니다.** 한 글자가 번역 자리와 잠글 자리에 함께 나오면 사전 하나로는 못 풉니다. 2026-08-12 기준 **0가지**입니다 — 자료 구조를 고쳤으면 `python js_extract.py --check` 로 다시 세십시오.
  - 도구: `_언어판 시험\js_extract.py` (`--dump 뼈대.py` · `--check`).
- ★ 그릇(태그)만 쫓으면 못 닿는 글자가 남습니다. 글자 단위로 감싸십시오.
- ★★ **글자를 셀 때는 태그를 먼저 다 걷어내십시오.** 이 책은 낱말 하나가 태그로 조각나 있는 곳이 많습니다 — `<strong>운동</strong><strong>을</strong> <strong>하다</strong>` 는 사람 눈에 「운동을 하다」지만 정규식에는 이어진 글자가 아닙니다. 2026-08-11에 이것 때문에 「`→` 로 이은 짝 0개」라고 **두 번 잘못 말했습니다**(부호 주위의 태그만 허용한 두 번째 시도도 못 잡음). 태그를 다 지운 글자에서 세니 4개였습니다.
- **표의 「— N/A —」는 그대로 둡니다 — 2026-08-11 사용자 결정.** `3 123~141 부정`의 견줌표 둘, 형용사 줄의 명령문·청유문 칸 여섯입니다. 한국어(「해당 없음」)를 올리자고 다시 꺼내지 마십시오.
- ★ **부호 규칙**: 같은 뜻의 두 꼴은 **`=`**(운동하다 = 운동을 하다 · 책 안에 22곳), 꼴이 **바뀌는** 것은 **`→`**(전화를 하다 → 전화를 했습니다). 한 표 안에서도 나눠 씁니다. 이름표 뒤는 **`:`**(특별 규칙: … / Special rule: …) — 한국어와 영어가 같은 부호를 쓰게 하십시오.

### 한국어 한 문장 / 영어 한 문장 (2026-08-15 사용자 결정)

- 문법 설명은 **한국어 한 문장 뒤에 그 영어 한 문장**을 붙입니다. 한국어 두세 문장을 이어 쓰고 영어를 아래에 몰아 두지 않습니다. 책 전체 **짝 512개**가 이 꼴입니다.
- ★**여백이 짝을 만듭니다** — `p.ko{margin-bottom:2px}` · `p.en{margin-bottom:16px}`. 거꾸로면 빈칸이 짝 **안**에 생겨 한국어와 그 번역이 따로 놀고 영어와 다음 한국어가 붙어 보입니다. **74파일이 거꾸로였습니다.**
- ★**계산식에는 영어를 붙이지 않습니다** — 「돕 → 도우 + ㄹ → 도울 것입니다」 같은 줄은 언어가 없는 줄이라 `p.ko` 하나로 둡니다(짝 없는 `.ko` 8곳이 그것입니다).
- 나눌 때 지킬 것 셋 — 모두 실제로 걸렸습니다.
  - **되이었을 때 글자가 없어지거나 늘어나면 손대지 말 것.** `<br>` 을 자르면 그 자리 빈칸이 달라지므로 빈칸은 빼고 견줍니다.
  - **문장 끝이 `<strong>` 안이면 자르지 말 것.** 태그가 깨집니다. 닫는 태그를 앞 문장에 붙여 손으로 나눕니다.
  - **`<br>` 도 문장 경계입니다.**
- ★「`「무엇을?」`」처럼 **따옴표 안 물음표**는 문장 끝이 아닙니다. 문장 수를 셀 때 빼야 헛걸림이 없습니다.

### ★ 영어 문법을 잣대로 쓰지 않습니다 (2026-08-17 사용자 원칙)

> 「다양한 언어로 번역을 할 때는 이렇게 영어 문법과 비교하는 것은 전혀 도움이 되지 않는다.
> 영어 문법을 사용하지 않고 쉽게 설명하는 것은 어떤가?」

- **까닭**: 이 책은 여러 언어판으로 나갑니다. 「영어의 to 부정사처럼」이라고 적으면 **영어를 아는 사람에게만** 설명이 됩니다. 스페인어판·일본어판 독자에게는 잣대가 하나 더 늘 뿐입니다.
- **어떻게**: 영어 문법 용어로 견주지 말고 **뜻으로** 풉니다 — 「무엇을 할 때 쓰는가 / 무엇과 무엇이 갈리는가」.
- 영어 **번역**은 그대로 답니다. 금지한 것은 **영어 문법을 설명의 잣대로 삼는 것**입니다.
- 같은 줄기의 결정 둘
  - **문법 용어를 앞세우지 않습니다** — 「동사 활용 / Verb conjugation」 같은 이름표를 지우고, 배우는 이가 실제로 할 일(「받침을 보고 무엇을 붙이는가」)로 적습니다.
  - **한 문장에 여럿을 몰아넣지 말고 표로 가릅니다** — 1·2·3인칭을 한 문장에 이어 쓰면 읽는 이가 셋을 머릿속에서 갈라야 합니다. 줄을 나누면 눈이 갈라 줍니다.

### ★ 꼴을 적는 자리 — 원형과 격식체 (2026-08-17 사용자 결정)

- **공식 상자·표 머리칸·제목**은 **원형**으로 적습니다 — 「동사 + 고 싶다」·「동사 + ~려고 하다」·「(으)려고 하다」.
- **결과 칸(Full form)**은 **격식체**로 둡니다 — 「가려고 합니다」. 원형에서 격식체로 가는 마지막 걸음이 그 칸입니다.
- 한 블록 안에서 둘이 섞이지 않게 하십시오. 2026-08-17에 공식 상자만 원형이고 표는 「합니다」라 꼴이 둘이었습니다.

### ★ 「틀림」과 「덜 자연스러움」을 가릅니다 (2026-08-17 사용자 지적)

- 문법에 어긋나지 않는데 **❌** 를 붙이면 배우는 이는 「쓰면 안 되는 말」로 외웁니다.
- **△** 를 씁니다. 다만 △ 는 이 책에 없던 표라 **그 줄에서 뜻을 바로 말해 줍니다** — 「쓸 수는 있지만 덜 자연스럽습니다」. 기호만 놓고 알아채기를 바라지 마십시오.
- 색은 `#b45309`(흰 바탕 5.02:1). ★ **테두리에 쓰는 호박색 `#c2831f` 를 글자에 그대로 옮겨 쓰지 마십시오** — 3.2:1 로 모자랍니다. 테두리는 3:1 이면 되지만 글자는 4.5 가 필요합니다.
- 책이 쓰는 표는 ✅ 302 · ✓ 110 · ⚠️ 52 · ❌ 51 · ✗ 16 (2026-08-17 셈). **△ 는 소망 쪽 한 곳뿐**이니 늘릴 때는 뜻을 함께 적으십시오.

### ★ 칩·배지 디자인 — 조용하게, 그러나 한눈에 (2026-08-17 사용자 지시)

「너무 촌스럽다. 세련되고 너무 튀지 않지만 고급스럽게, 직관적으로 이해하기 쉽게.」

- 튀던 까닭 셋 — ①채도가 높다 ②색이 저마다 다른 이야기를 한다 ③**알약(999px) + 800 굵기**라 읽을 것이 **누를 단추**처럼 보인다.
- 짜임의 뼈대는 **「빈 것과 채운 것」**입니다. 없음 = 테두리만 있고 속이 빈 칩, 있음 = 같은 색조에 속을 채운 칩. **낱말을 읽기 전에 눈이 먼저 압니다.**
- 색은 **둘까지** — 짝을 이루는 것 하나(회청)와 짝에서 벗어난 것 하나(모래). 넷은 많습니다.
- 알약 → **6px 모서리**, 800 → **700**, 채운 바탕 → **실선 테두리**.
- 갈리는 자리가 한 글자면 **그 한 글자만** 강조하십시오 — 「**으**려고 하다」. 규칙 전체가 그 글자입니다.
- ★ 색을 낮추면 반드시 **밝기차를 쪽에서 재십시오**. 4.5 아래가 0건이어야 합니다(굵어도 14px 안팎이면 3.0 이 아니라 4.5 가 필요합니다).

### 화면 폭 · 표

- ★ **이 책은 휴대폰도 겨냥합니다** — 106파일 전부에 `width=device-width` 가 있고 `@media (max-width:600px)` 규칙이 374개입니다. **넓은 화면만 보고 표 폭을 넓히지 마십시오.**
- ★ **표는 `<div class="tbl-scroll">` 로 감쌉니다** — 2026-08-13에 표 738개(99파일)에 씌웠습니다. 규칙은 `@media (max-width:600px)` 안에만 있어 **넓은 화면에서는 아무 일도 하지 않습니다.**
  - 칸(div)은 감싸지 않고 자신에게 겁니다 — `.cr-result`·`.vstack`·`.rs-cell` 은 grid·table-cell 의 자식이라 상자를 끼우면 배치가 바뀝니다.
  - ★ **이미 있는 부모 상자에 걸지 마십시오** — §6 함정 참조. 세로로 잘립니다.
  - 새 표를 넣을 때 이 상자를 함께 씌우십시오. `check_render.py --width 390` 으로 확인합니다.
- **일부러 넓은 쪽 셋은 넘쳐도 정상입니다** — `부록 2 부정법`(A4 인쇄 낱장) · `부록 8`(넓은 문법 맵) · `_음성 확인`(본문 아님).

### ★★ 교재 밖 예문은 어디까지 되는가 (2026-08-19 사용자 정정)

**자리를 가르십시오 — 가르치는 내용인가, 설명인가.**

| 자리 | 교재 밖 것 |
|---|---|
| 문항의 **정답·예문**, 연습표 **낱말** | ✗ 안 됩니다. 원 교재(「놀라운 한국어 500문장」) 안의 것만 |
| **문법 설명** 상자·「왜 배워야 하나」 같은 자리 | ○ 됩니다 |

★★ **이 문서가 2026-08-18~19에 「교재 밖이면 무엇이든 안 된다」로 넓게 적혀 있었는데
오해였습니다.** 소망 문법 6의 「차를 사고 싶습니다」를 뺀 것은 그것이 **문법 비교표의
예문**, 곧 가르치는 내용 자리인데 **사용자가 가진 파일에 없는 문장**이어서였습니다.
문법을 설명하려고 드는 예는 그 제한을 받지 않습니다.

- **낱말** — 2026-08-15 사용자 원칙: 「**카드에 없는 낱말은 원 교재에도 없다**」. 연습표에서 15개를 뺐고, `check_audit.py` 가 「연습표 낱말이 카드에 없음」을 봅니다.
- 가르치는 내용이면 먼저 **그 문장이 500문항 안에 있는지 세십시오**.
  - 세는 법: `class="sentence-kr">` 안의 글자와 대조합니다. 낱말만 있는지 보면 안 됩니다 — 「사다」·「차」는 둘 다 교재에 있지만 「차를 사고 싶습니다」라는 **문장은 없습니다.**
- **문법 형태를 보여 주는 조각**(`~고 싶다`·`받침 O → 은`)은 예문이 아니라 규칙 표기라 언제나 됩니다.

### ★★ 문장 사다리 — 어순 해설 표를 대신합니다 (2026-08-19 사용자 결정)

**어순 해설 표(4열 `영어 | 역할 | 한국어 | 해설`)는 500문항 전량에서 없어졌습니다.** 36언어 일괄 번역을 앞두고 사용자가 표를 없애기로 정했습니다 — 영어 열 **1,518칸**이 `data-reauthor`(언어마다 **사람이 다시 써야** 하는 자리)였고 줄 차례가 영어 어순이라 SOV 언어판은 줄을 다시 배열해야 했습니다(500표 × 36언어 = 18,000장).

지금 꼴은 **제목(그대로) + 문장 사다리 + 만들기**입니다.

- 한 단 = **한국어 한 줄 / 그 언어 한 줄** → 번역이 **문장 단위**라 기계번역이 듣습니다.
- 이름표(`.wo-add`)는 그 문장의 **어순 전체**이고 이번 단계에 늘어난 자리만 진하게 둡니다(자리가 다섯을 넘으면 늘어난 자리만 — 486은 자리가 아홉입니다).
- 「만들기」(`.wo-mk`)는 제목의 어순과 **같은 차례**로 놓습니다. **한국어뿐이라 번역 비용 0.**
  - ⚠️ **「자리마다 한 줄」이 아닙니다** — 2026-08-20에 그렇게 적었다가 세어 보고 고쳤습니다. 만들기는 **만들 것이 있는 조각**만 보입니다. 그래서 제목 자리 수와 만들기 줄 수가 다른 곳이 100~500 에만 **67곳**이고 전부 정상입니다.
    - 223) 제목 「주어 + 서술어(감탄)」 · 만들기 세 줄(꽃 + 이 / 참 / 예쁘다 → 예쁘 + 네요 → 예쁘네요)
    - 249) 제목 네 자리 · 만들기 세 줄(목적어가 마지막 줄에 함께 듭니다)
    - 273) 제목 다섯 자리 · 만들기 세 줄(활용이 필요한 것만)
  - **「제목 자리 수 ≠ 만들기 줄 수」로 그물을 치지 마십시오.** 67곳이 헛걸립니다.
- 표가 아니므로 가로 스크롤(`.tbl-scroll`)이 사라졌습니다.
- **2026-08-20 파일 셈**: `wo-box` **500개 / 67파일** = 사다리 **151**(2단 131 · 3단 16 · 4단 4) + 한 단 **349**(371 에 한 단을 더한 뒤의 셈입니다). ★12:25 커밋 메시지의 「146 + 354」는 그 시점 값이고, 그날 오후 41~50 의 구멍을 메우며 넷이 늘었습니다 — **숫자는 파일에서 다시 세십시오.**

★★ **사다리를 아무 데나 놓으면 안 됩니다.** 기계로 257곳을 뽑아 눈으로 보니 **3분의 1이 못 쓸 것**이었습니다(「저는 듣습니다 / I listen to.」·「그는 영화배우입니다 / He an actor.」·「시험 범위는 입니다.」·「당신은 갑니까??」). 그래서 두 곳에만 놓습니다.

  ㉠ **교재가 이미 앞 문장을 가진 곳** — 그 문장을 그대로 씁니다(31→32→33→34). 앞 문장의 낱말이 이번 자리로 **빠짐없이** 덮여야 합니다. 안 그러면 이름표가 거짓말을 합니다(123→124는 확장이 아니라 부정문입니다).
  ㉡ **부사어만 빼서 뼈대가 되는 곳** — 부사어를 빼는 것은 두 언어 모두 안전합니다.

  그 밖에는 **한 단으로 두고 이름표를 붙이지 않습니다**(제목이 이미 같은 말을 합니다). 걸름망 아홉은 `_도구/eosun_ladder.py` 머리글에 있습니다.

★ **사슬이 자동으로 안 이어지는 묶음이 있습니다** — 41~50은 앞 문항이 아니라 **그 묶음의 뼈대**(41 「저는 주었습니다」· 46 「저는 전화를 했습니다」)에서 자랍니다. 간접목적어는 부사어가 아니라 부사어 빼기도 안 통합니다. 2026-08-19에 44·45·48·49 를 손으로 메웠고 **그때 새로 쓴 문장은 둘뿐**입니다(「저는 생선을 주었습니다」·「저는 물을 주었습니다」 — 교재 42 와 같은 꼴).

### ★★ 어휘 상자에 무엇을 넣는가 (2026-08-19 사용자 규칙 다발)

하루 동안 사용자가 한 자리씩 짚어 정한 규칙입니다. **어휘는 낱말과 뜻만 담습니다.**

| 넣지 않는 것 | 2026-08-19에 뺀 것 |
|---|---|
| **조사** | 표제어에 붙은 조사 50개(42상자 13파일) + 「~에서·~(으)로·~에·~까지·~부터」 따위 · 뒤늦게 찾은 「나/이나」·「누구를」·「어디에」 21곳 |
| **문법 표현·어미** | 108개 36파일(59가지) — 「~아/어서」·「~(으)면서」·「~자마자」·「~보다 더」·「~ㄴ 적이 있다」… |
| **활용형** | 과거형을 기본형으로 15곳 7파일(「했다 did」 → 「하다 do」). 형용사는 책 규칙대로 **「to be + 형용사」** |
| **문법 꼬리표** | 「(needs no particle)」·「(object)」·「(the modifier form of …)」·「the past of …」 — 남은 것 0 |
| **묶음 풀이** | 「boyfriend (남자 man + 친구 friend)」 같은 부분 쪼개기 3곳 |
| **인칭 대명사** | **101번부터** 71곳 30파일 + 조사가 붙어 살아남은 일곱(그를·저를·저에게·제가) |
| **정답을 미리 보여 주는 것** | 관형형 셋(474~476 「다닌 학교」…) · 500 아드님·따님(문법 설명이 이미 가르칩니다) |

- ★ **어휘의 영어는 「한국어 정답」이 아니라 그 문항의 500문장 **원문(영어)**을 기준으로 씁니다.** 「데리고 가다 = take (someone) along」은 문장에 `along` 이 없어 「take (someone)」이 되었습니다. 검사기 `check_vocab_match.py` 가 이 어긋남을 셉니다 — **0이 목표가 아닙니다**(2026-08-20 셈 35곳/23파일).
- ★ **차례는 한국어 정답의 차례**입니다(2026-08-19 사용자 결정 — 72상자 31파일 + 마무리 81곳).
- ★ **한 표제어가 다른 표제어 안에 들어 있으면 묶음만 남깁니다**(46 「전화·하다」 → 「전화를 하다」, 18개 11상자). 다만 **둘을 다 채울 때만** 뺍니다 — ①같은 상자의 다른 표제어 안에 들어 있다 ②**정답에서 혼자 나오는 자리가 없다.** ②가 결정적이라 겹쳐 보여도 남긴 것이 여섯 있습니다(40 영화 ⊂ 영화관 · 351 한국 ⊂ 한국어 · 357 운전 ⊂ 운전면허증 · 125 못 · 461 다 · 154 잘).
- ★ **상자가 비면 안 됩니다** — 책이 쓰는 표시 **「새 어휘 없음 / No new vocabulary」**(`v-item v-none`)로 바꿉니다. 2026-08-20 파일 셈으로 **34곳**입니다. 어휘 상자는 **500개**, 항목은 **1,174개**입니다.
- ★ **「보다」 13개는 동사(watch, see)라 빼지 않습니다.** 표제어 글자만 보고 걸면 걸립니다 — **뜻풀이까지** 봐야 합니다.
- ★ **뜻풀이 안에 잠긴 한국어 `span` 이 겹쳐 있으면 비탐욕 정규식이 안쪽에서 끊깁니다.** `span` 짝을 세어 닫는 자리를 찾으십시오(어휘를 건드릴 때마다 걸렸습니다).

### ★ 해설 칸에 영어를 넣을 때는 아래 줄에 (2026-08-19 사용자 규칙)

「해설에 영어 해석을 넣을 때에는 반드시 같은 줄에 넣지 말고 **아래 줄**에 위치하도록 한다.」 2026-08-18의 「한국어 한 줄, 영어 한 줄」을 해설 칸에도 그대로 적용한 것입니다. 꼴은 책의 관례 그대로입니다.

```html
<td>한국어<br><small class="en-line" lang="en" translate="yes"><em>영어</em></small></td>
```

- 영어 줄로 옮겨 간 한국어(하다·해 주세요·ㅅ·다)는 **`<span lang="ko" translate="no">` 로 잠급니다**(도구 주석의 「ko-guard」가 이것입니다).
- ★★ **이 자리는 세 시간 뒤 「문장 사다리」가 표를 걷어내면서 「만들기」 줄(`.wo-mk`)이 되었습니다.** 만들기는 **한국어뿐**이라 그날 아래 줄로 내렸던 영어 12곳은 결국 **사라졌고**, 한국어는 그대로 살아 있습니다(존댓말 파일의 「하다 → 하시다 → 하십니다」를 화면 글자로 확인). 2026-08-20 셈으로 마크업에 `<td class="exp-en">` 은 **0개**, `.exp-*` **CSS 선택자 72개만 잔재**입니다.
- ⚠️ 그래서 **`check_exp_line.py` 는 지금 볼 대상이 없어 늘 0을 냅니다** — 「0이니 지켜지고 있다」로 읽지 마십시오. **규칙 자체는 살아 있습니다**(새로 한국어·영어를 한 칸에 담는 자리를 만들면 그때 다시 듣습니다).
- ★ **영어를 아예 걷어도 되는 자리가 있습니다** — 존댓말 파일에서는 옆 칸(역할 Role)이 이미 「목적어 / Object」로 영어를 달고 있어, 이 자리는 **한국어가 어떻게 바뀌는지**만 보이면 됩니다. 그래서 15칸을 「하다 → 하시다 → 하십니다」 꼴 **한국어 사슬**로 바꿨고, 그 뒤 사다리로 옮겨 가면서도 그대로 남았습니다.
- ★ **해설 칸의 설명이 그 줄의 자리와 맞는지 보십시오** — 499 목적어 줄에 서술어 설명(「모시다」)이, 497 목적어 줄에 「→ 뵈다」가 들어 있었습니다.

### ★ 플래시카드 — 뒤집지 않고 아래로 폅니다 (2026-08-18 사용자 결정)

「카드 보고 말하기」의 **문장 카드 600장**은 **영어를 보고 한국어로 말한 뒤 확인**하는 자리입니다. 2026-08-18에 **뒤집기를 버리고 펼치기로** 바꿨습니다.

- **왜** — 앞뒤 면이 `position:absolute; inset:0` 이라 카드에 **높이를 잴 내용이 없어** `height:140px` 가 뼈대였습니다. 그래서 `min-height` 로 못 바꿉니다(카드가 찌그러집니다). 그때 이미 **6장이 넘치고** 있었고 한 장은 글자를 줄여 놓은 땜질이었습니다.
- **어떻게** — 마크업과 `onclick` 은 **한 글자도 안 건드렸습니다.** `.flipped` 가 「뒤집힘」에서 「펼침」이 됐을 뿐이고, 덮어쓰기 CSS 를 **마지막 `<style>` 에** 덧붙였습니다(기본 규칙이 73파일 모두 첫 블록이라 마지막에 붙이면 확실히 덮습니다). 되돌리려면 그 블록만 지우면 됩니다 — 표시 `/* 카드를 뒤집지 않고 아래로 편다 (2026-08-18) */`.
- **덤으로 좋아진 것** — 답을 볼 때 **문제(영어)가 그대로 위에 남아** 견주기 쉽습니다. 뒤집으면 문제가 사라졌습니다.
- 힌트 글자 `↻ 클릭` → **`▼ 눌러서 확인`** 515곳.
- ★ **`fc-*` 카드(시제·조사의 「퀴즈 1」)는 건드리지 않았습니다.** 다른 부품이고 낱말 카드라 210px 안에서 **563장 넘침 0** 입니다. 그쪽 힌트는 여전히 `↻ 클릭` 이고 맞는 말입니다.
- ★ **책은 이미 이 방식을 알고 있었습니다** — `fc-*` 의 `@media print` 안에 똑같은 펼치기 규칙이 있습니다(인쇄할 때는 앞뒤를 쌓아 보여 줍니다).
- ★ **🔊 는 흐름 안에 두어야 합니다.** 절대 위치로 두면 가운데 정렬된 글자 **위로 올라옵니다**(휴대폰에서 겹치는 것을 눈으로 확인). 지금은 글자 아래 가운데입니다.

### 채점

- ★ **같은 뜻의 두 꼴을 가르쳤으면 채점기도 둘 다 받아야 합니다.** 정규화기가 그 일을 합니다:
  `.replace(/봤/g,'보았').replace(/줬/g,'주었').replace(/됐/g,'되었').replace(/냈/g,'내었')`
  - 2026-08-13까지 **`냈` 만 빠져 있어** 전체·종합 시험에서 「내었습니다」가 오답이 됐습니다. 지금은 책 전체 **73곳**이 같은 규칙을 씁니다.
  - 줄임 파일의 `pr80Check` 는 `data-ans` 를 **`/` 로 갈라** 어느 쪽이든 정답으로 셉니다(`봤습니다/보았습니다`). 새 채점기를 만들면 두 방식 중 하나를 반드시 쓰십시오.
  - ★ **관용이 지나치지 않은지 함께 재십시오** — `냈다`·`냈습니당`·`먹었습니다` 는 그대로 오답이어야 합니다.

### 소리
- 여성 `ko-KR-SunHiNeural` · 남성 `ko-KR-HyunsuMultilingualNeural` **고정**(사용자가 들어 보고 고름).
- 브라우저가 보는 것은 **HTML 안의 `AK_SND`**. json만 고치면 아무 일도 일어나지 않습니다.
- ★ `speechSynthesis.cancel()` 직후 바로 `speak()` 하면 **목소리가 바뀝니다.** 60ms `setTimeout` 을 지우지 마십시오.
- ★ **쓰기 칸(직접 만들기)에는 🔊 단추가 없습니다 — 칸(박스)을 눌러 재생합니다**(`jm-box v2`, 2026-08-10 사용자 결정, 시제·조사 23파일). 소리가 나는 칸은 **맞혀서 초록이 된 칸 + 보기(readonly) 칸**뿐이고, 빈칸·틀린 칸은 눌러도 무음입니다(정답 먼저 듣기 방지). 재생 문장은 `data-ans` 에서 옵니다 — **음원 그물도 data-ans 를 봐야 합니다**(audio_build.py 렌더 스캔).
- 설명 예문의 🔊 단추(`.jm-spk`)는 그대로 삽니다. `.jm-spk` 기본값이 `display:none` 인 파일이 있어 — **설명 예문에 🔊 를 새로 달면 안 보일 수 있습니다.** `blockquote .jm-spk, .example-box .jm-spk{display:inline-block}` 규칙이 그것을 살립니다.
- 성별 단추는 **문장 바로 옆**에 둡니다(따로 모아 두지 않습니다).
- ★ **「따라 읽기」에는 전체 듣기 단추가 있습니다** — 2026-08-11 사용자 제안으로 **67파일**에 넣었습니다(`slAll`). 절 머리에 여성·남성 두 단추, 재생 중에는 「■ 멈춤」으로 바뀌고 지금 읽는 문장에 초록 테두리(`.sl-now`)가 듭니다.
  - ★ **이어 재생을 새로 짓지 마십시오** — `1 시제 종합 시험`의 **`akSpeakSeq`** 가 이미 그 방식을 갖고 있습니다(음원은 `onended`, 브라우저 음성은 `onend` 로 잇기 · `AK_SEQ_ID` 로 앞 재생 끊기 · `cancel()` 뒤 **60ms**). 그것을 옮겨 썼습니다.
  - ★ **Audio 요소를 새로 만들지 말고 `AK_AU` 하나를 재사용하고 `src` 만 바꾸십시오** — 휴대폰(iOS)에서 두 번째 문장부터 막히는 것을 피합니다.
  - ★ **재생 중에 문장을 누르면 전체 듣기를 멈춥니다**(capture 단계 click 듣개). 안 그러면 낱개 재생이 `AK_AU` 를 갈아 치워 **전체 듣기가 소리 없이 멈추고 단추만 「멈춤」으로 남습니다.**
  - **시험 방법**: 진짜 소리를 기다리지 말고 **가짜 `Audio`**(20ms 뒤 `onended`)를 끼워 차례·강조·멈춤을 봅니다. 도구: scratchpad `listen_all.py` · `la_test.py`.
  - **시제·조사에는 활용표 전체 듣기**를 따로 넣었습니다(`tblAll`, **48덩이 25파일**) — 그쪽에는 문장 목록이 없고 낱말마다 `onclick` 이 달린 활용표뿐이라, 표 한 덩이를 통째로 읽습니다(가다 → 갑니다 → 갑니까? …).
    - 덩이 이름: `cr-rows` 16 · `rs-rows` 13 · `conj-ir` 10 · `cc-group` 6 · `cp-rows` 2 · `conj-hada` 1. 소리가 **둘 이상** 든 덩이만 골랐습니다.
    - ★ **단추는 표 위가 아니라 아래**에 둡니다 — 표 바로 위에 열 이름 줄(기본형 · 어간+ㅂ니다 · 결과)이 있어 그 사이에 끼우면 표가 갈라집니다.
    - ★ 읽을 것은 **`onclick` 의 `speakKorean('…')` 에서 뽑습니다**(활용표에는 문장 칸이 따로 없습니다).
    - ★ 함수 이름을 `tblAll`·`slStop2` 로 **따로** 두었습니다 — 지금은 파일이 겹치지 않지만 이름이 같으면 나중에 한 파일에 둘 다 들어갈 때 **조용히 부딪힙니다.**
- **연습표·쓰기 시험에도 전체 듣기**가 있습니다(2026-08-14). 「전체 정답」(연습표) 또는 「💡 정답」(쓰기 시험)을 **누른 뒤에만 켜집니다** — 정답을 먼저 듣지 못하게. 「초기화」를 누르면 다시 꺼집니다.
  - ★ **단추 갈래가 셋입니다** — 활용표 `.sl-all-btn` · 연습표 `.pr80-listen` (23파일) · 쓰기 시험 `.pt-listen` (`1 시제 1 현재형 전체 시험`, 160칸). **`slStop2` 의 그물에 셋 다 넣으십시오.**
  - ★ 활용표는 낱말마다 `onclick` 이 있어 거기서 읽을 것을 뽑지만, **연습표·쓰기 시험 칸은 `onclick` 이 없어 `data-ans` 에서 모읍니다.** 그래서 「재생 중 누르면 멈춤」 듣개의 `speakKorean(` 그물에 **안 걸립니다** — 칸(`INPUT` + `.correct`/`.revealed`/`.prex`/readonly)을 따로 봐야 합니다.
  - 쓰기 시험은 160칸이라 지금 읽는 칸이 화면 밖으로 나갑니다 — `scrollIntoView` 로 초록 테두리를 따라 굴려 줍니다.

---

## 6. ★ 함정 — 실제로 터졌던 것

| 무엇 | 어떻게 터졌는가 | 막는 법 |
|---|---|---|
| **`overflow-x:auto` 는 세로도 바꾼다** | 좁은 화면 넘침을 고치려 이미 있는 부모 상자(`.pr80`·`.gstep-body`·`.cr-row`)에 걸었더니 **10파일에서 세로로 잘렸다.** CSS 규칙상 한 축을 `visible` 이 아니게 하면 다른 축이 `auto` 가 되는데, 그 상자들은 높이가 매여 있었다 | **새 상자를 씌운다** — `<div class="tbl-scroll">`. 높이가 내용대로라 잘릴 일이 없다. 그리고 **두 안을 브라우저에서 재 보고** 고른다 |
| **스크립트 선후** | `jmSpk` 정의를 뒤 블록에 두어 `ReferenceError`. **23개 파일의 퀴즈 표가 사라짐** | 도우미 함수는 `<body>` 뒤 **첫 스크립트**에. `check_script_order.py` |
| **CSS 주석 안의 `<body>`** | 정규식 `<body[^>]*>` 가 **CSS 주석 속 글자**에 걸려 `<style>` 한복판에 스크립트 삽입 → CSS가 통째로 죽어 **조사 A~F가 화면 가득 퍼짐** | `</head>` **뒤**의 진짜 `<body>` 를 찾을 것. `check_style.py` |
| **눈으로 본 오판** | CSS 글자가 있으니 같다고 답했으나 **적용이 안 되고 있었음** | 태그 말고 **적용되는가**를 보라 |
| **정렬 기준** | `sorted(reverse=True)` 가 위치가 아니라 **이름 문자열**로 정렬 → 17파일 태그 깨짐 | `key=lambda c: c[1]` 처럼 **위치 기준** |
| **겹치는 편집 구간** | `<em>` 안에 `<small>` 이 있어 두 번 고쳐 내용 중복 (4파일) | 겹치지 않는 **바깥쪽만** 남기기 |
| **정규식이 첫 `</span>`에서 멈춤** | `<p class="en">` 의 끝을 잘못 잡아 **같은 껍데기를 두 번** 씌우고 뒤쪽은 놓침 | **깊이를 세어** 짝을 찾을 것 |
| **한 글자 한국어** | 정규식이 2글자 이상만 잡아 `들·색·이·가·딸` 을 놓침 | 1글자도 잡히는지 확인 |
| **큰따옴표 자료** | 작은따옴표만 훑어 1,339개 낱말을 놓침 | 두 따옴표 다 |
| **PowerShell UTF-8** | `ConvertFrom-Json`·콘솔 출력이 한글을 깨뜨림 | 파이썬 + `_paths.stdout()` |
| **`<title>`** | 제목 안의 한국어 81개 파일이 안 잠김 → 문법 항목 「에」까지 번역될 것 | 다국어 작업 때 처리 (§7-2) |
| **검사기가 이름 하나만 앎** | `check_spec.py` 가 `.container` 만 찾아 부록 2·부록 8·`_음성 확인` 셋을 "화면 가득 퍼집니다"로 알렸고 **이 문서가 그 오보를 옮겨 적었습니다**(§7-3). 셋 다 `margin:0 auto` 로 멀쩡했습니다 | 검사기가 **못 찾은 것**을 **없는 것**으로 말하게 두지 말 것. 예외는 **값까지 함께** 적어 그 값이 바뀌면 잡히게 |
| **제목의 영어를 못 봄** | `<h2>` 안만 세어 「다섯 절 제목에 영어가 없다」고 **사용자에게 보고**했으나, 영어는 바로 뒤 `<p class="subtitle">` 에 처음부터 다 있었음. 하마터면 335줄을 덧붙일 뻔 | 한 자리만 보고 「없다」고 하지 말 것. 제목의 영어는 **h2 안 `.gloss` 또는 뒤 `p.subtitle`** 두 자리 |
| **`.gitignore`의 `*.py`** | `마스터-템플릿-적용본\.gitignore` 가 `*.py` 를 막아 **`_도구` 스크립트 8개 + 언어판 빌더 2개가 git에 하나도 안 들어가 있었음.** "도구는 저장소 안에" 라는 규칙이 반만 지켜지고 있었다 | 새 도구를 넣으면 **`git status` 에 보이는지** 확인 |
| **OneDrive 중복 사본** | `… 때문에 (2).html` 이 6월 1일자로 남아 목차에 두 번 보였음(어휘 0·번역 슬롯 0·소리 없음) | 파일명에 ` (2)` 가 붙은 것이 있는지 이따금 볼 것 |
| **한 갈래만 보는 그물을 물려받음** | 연습표 전체 듣기를 넣을 때 `slStop2`·「누르면 멈춤」 듣개를 **활용표(`.sl-all-btn`) 것 그대로** 썼다. 그 그물에 `.pr80-listen` 이 없어 **23파일에서 ㉠ 끝까지 다 들으면 단추가 「■ 멈춤」에 멈춰 있고 ㉡ 재생 중에 칸을 누르면 소리는 끊기는데 단추만 남았다.** 초기화 말고는 되돌릴 길이 없었다. 2026-08-14 브라우저에서 확인 | 있는 셈본을 옮겨 쓸 때 **그 안의 선택자가 새 갈래를 아는지** 볼 것. §5 「소리」에 이미 같은 규칙이 적혀 있었는데(312줄) **연습표에는 안 지켜졌다** — 규칙을 적는 것과 새 갈래에 적용하는 것은 다른 일이다 |
| **글자로 갈래를 가름** | 검사기가 `"pr80-listen" in s` 로 파일을 갈랐는데, 쓰기 시험 파일의 `slStop2` **안에도** `.pr80-listen` 이라는 글자가 있어 연습표로 잘못 갈렸다. 없는 단추를 15초 기다리다 죽었다 | 갈래는 **마크업**으로 가를 것 — `class="pr80-btn pr80-listen"` 처럼 |
| **스타일시트가 한 줄이다** | 색을 바꾸려고 **줄 단위**로 고쳤더니 103파일이 망가졌다. 이 책은 `<style>` 을 한 줄에 몰아 두어, 그 줄에 목표 클래스가 하나만 있어도 **줄 전체의 색**(본문 글자색·테마 색까지)이 바뀐다 | CSS 는 **규칙(`선택자{선언}`) 단위**로 고칠 것. 고친 뒤 「건드리면 안 되는 값」이 그대로인지 세어 볼 것(`--theme-primary` 등) |
| **변환기를 두 번 돌림** | 그라데이션 가지가 **선언 전체**에서 색을 훑어, 이미 바뀐 `color` 값까지 옅게 만들었다. 단계 번호 214곳이 바탕과 글자가 같아졌다(밝기차 1.0) | 바꾸는 자리를 **좁힐 것**(그라데이션 *안*만). 이미 옅으면 건너뛰게 해 **두 번 돌려도 같은 결과**가 되게 |
| **낱말이 파일에 두 번 나오면 카드가 있다고 셈** | 「연습표 낱말이 카드에 있는가」를 글자 수로 셌는데, `AK_SND`(음원 열쇠)에도 낱말이 들어 있어 카드가 없어도 두 번이 됐다 | 자료의 **생김새**로 가를 것 — 연습표 `["덥다","hot","더웠습니다",BI]`(따옴표 셋) vs 카드 `[…여섯]` |
| **검사기 한 판이 페이지 하나를 백 쪽 내내 돌려 쓴다** | 2026-08-17 에 손대지도 않은 **103쪽**이 「404」로 걸렸다. 새 페이지로 열면 0건인데 검사기에서만 났다. 맨 처음 글꼴 요청이 한 번 실패하면 그 실패가 **캐시에 앉아 나머지 전부로 번진다** — 그래서 「걸린 쪽 다시 보기」로도 안 지워졌다 | `check_render.py` 가 **「무엇을 못 받았다」(Failed to load resource)를 안 셉니다.** 그 말에는 주소가 없고, 밖에서 받는 것은 글꼴뿐이며, **안에 있는 파일이 없어진 것은 `badImg`(그림)와 `check_all`(소리)이 따로 잡습니다.** 밖의 요청이 실패하면 셈에 넣지 않고 알려만 줍니다 |
| **인라인 스타일이 물려받기를 이긴다고 믿음** | `<span style="color:#5a6d87;"><strong>저는</strong></span>` 의 회색이 안 먹고 있었다. 인라인이라도 **물려받은 색**은, 안쪽 요소를 **직접 겨눈 규칙**(`blockquote strong{color:…}`)에 진다 | 인라인이 이기는 것은 **같은 요소**일 때뿐. 색을 **그 글자를 담은 태그 자신**에게 걸 것 |
| **`word-break:keep-all` 로 다 막힌다고 믿음** | 제목이 「비교: ~」 / 「고 싶다」로 끊겼다. `keep-all` 은 **낱말 안**을 지킬 뿐이고, 브라우저는 `~`·`/` 앞뒤를 서로 다른 덩이로 본다 | **형태 이름은 `white-space:nowrap` 으로 통째로 묶을 것** — `~고 싶다`, `~았/었으면 좋겠다` |
| **좁은 화면에서 표를 「고쳤는데」 나빠짐** | 짜부라진 표에 `min-width:500px` 을 못 박았더니 낱말은 살았지만 **셋째 칸이 화면 밖으로 밀려났다** | 세 갈래를 **재서** 고를 것 — ①그냥 두기(글자 짜부라짐) ②min-width 못 박기(크게 넘침) ③**`keep-all` 만 걸기**(표가 「낱말을 안 끊고 들어가는 가장 좁은 폭」을 스스로 찾음). 2026-08-17 두 쪽에서 ③이 이겼다(208→260px, 높이 1023→731px) |
| **조건절의 주어와 주절의 주어를 뭉갬** | 「차가 있었으면 좋겠습니다」의 주어를 **차**라고 적었다. 차가는 **있다**의 주어이고 **좋겠다**의 주어는 바라는 사람(생략된 저)이다. 이 잘못 때문에 비교표의 「인칭 제약」 줄까지 거꾸로 적혔다 | 안은문장은 **절마다 주어를 따로 셀 것**. 그리고 **같은 쪽 안에서 낱말이 어긋나면 표를 믿을 것** — 그 쪽의 어순 상자 일곱 개가 이미 차·돈·부모님을 「주어」라 적고 있었다 |
| **OneDrive 가 두 컴퓨터를 부딪힘** | 2026-08-18에 열어 보니 **`_소리\f`·`m` 이 텅 비어 음원 4,494개가 없었고**(소리 단추 4,944개가 전부 죽을 상태), `.git (1)`·`_claude_메모리_백업 (1)`·`tmp (1)`·`정리 (1)`·`-DESKTOP-…` 사본이 생겨 있었다. ★**`정리` 폴더는 OneDrive 가 이름을 `정리 (1)` 로 바꿔 버려 원본이 사라진 것**이라, 잔재로 보이지만 유일한 사본이었다 | 잃은 것은 없었다 — **HTML 은 HEAD 와 한 글자도 다르지 않았고 음원도 git 에 온전했다.** `git restore` 로 되살릴 것. ★지우기 전에 **하나하나 대조할 것** — 「모든 줄이 다르다」고 나온 두 파일은 줄끝(CRLF/LF) 차이였다(크기 차이가 줄 수와 정확히 같았다) |
| **그물 잣대 하나로 564곳** | 「번역 슬롯이 부호를 가르는가」를 **「슬롯 안에서 짝이 안 맞으면」**으로 세어 564곳이 나왔다. 대부분 문장 번호 **`489)`** 였다 | **모자란 짝이 슬롯 바로 밖에 있는가**로 좁힐 것 → 진짜 34곳 |
| **오탐이라 단정했다가 틀림** | 「Korean order: …」 11곳을 「영어 문장이라 오탐」이라고 **사용자에게 보고**했다. 세어 보니 같은 제목 행이 **489개는 한국어 먼저 · 11개만 영어 먼저**로 진짜 결함이었다 | 「오탐이다」도 **세어 보고** 말할 것. 「걸렸다≠결함이다」의 반대 방향도 똑같이 위험하다 |
| **세로쓰기를 줄바꿈으로 봄** | 「낱말이 갈린다」 검사가 부록 2 의 「동사」·「형용사」를 잡았다. `writing-mode: vertical-rl` 이라 글자마다 top 이 다를 뿐이었다 | **폭을 바꿔 보고 개수가 그대로면 폭 문제가 아니다**(390·794·900·1200에서 3곳 고정). 그물에 세로쓰기 거르기를 넣었다 |
| **그물이 아니라 시험이 틀림** | `js_net_test.py` 를 처음 돌리니 14개가 다 실패했다. 추출기가 아니라 **내 시험**이 틀렸다 — `literals()` 는 **여는 따옴표 자리**를 넘기는데 한 칸 뒤를 넘겼다 | 규약은 **소스에서 확인**할 것. 시험이 전부 실패하면 시험을 먼저 의심 |

| **소스로 「같은 줄」을 셈** | 「한국어…`<span lang=en>`영어」로 찾아 **84줄 / 77파일**을 헤아렸다. 그 span 이 `display:block` 이면 이미 줄이 갈려 있어 대부분 헛것이었다(짝 맞추기 안내문 72곳 포함). 거꾸로 문장 끝(`니다.`)으로만 찾으니 15파일에서 inline 인 `.cr-en` 을 1줄만 잡았다. 실제로 재니 **1280px 5곳** | **줄이 갈렸는지는 소스로 못 안다 — 그려서 잴 것**(`check_koen_line.py`) |

| **줄 갈래를 `top` 반올림으로 가름** | 같은 줄인데도 한국어와 영어가 따로 세어졌다 | **한글과 영문은 글꼴이 달라 같은 줄에서도 `top` 이 어긋난다.** 세로 **겹침**으로 묶을 것 |

| **접힌 글자마디를 줄마다 통째로 셈** | `getClientRects()` 는 줄마다 사각형을 주지만 **글은 갈라 주지 않는다.** 넉 줄로 접힌 문단이 넉 줄 모두 「한국어+영어」로 보였다(390px 76곳 · 1280px 6곳으로 갈려 들켰다) | 여러 줄에 걸친 마디는 **글자 하나씩** 재어 줄을 가를 것 |

| **좁은 화면만 재고 끝냄** | 390px 에서 0곳이라 「없다」고 볼 뻔했다. 좁으면 줄이 저절로 접혀 한국어와 영어가 갈린다 — 1280px 에서 5곳이었다 | 줄 배치 검사는 **좁은 폭·넓은 폭 둘 다** 돌릴 것 |

| **글자로 맞춘 치환이 0곳** | 인용문 셋을 바꾸려는데 세 개 다 안 걸렸다. 원문 `<blockquote>` 안에 **줄바꿈과 들여쓰기**가 섞여 있었다 | 여러 줄짜리 덩이는 글자로 맞추지 말고 **`<blockquote>…</blockquote>` 통째로 잡아** 닻 문구로 고를 것 |

| **heredoc 이 `\b`·`\1` 을 먹었다** | 셸 heredoc(`<<'PY'`, 따옴표로 막았는데도)으로 스크립트를 고치자 파일에 **chr(8)·chr(1)** 이 박혔다. 정규식이 조용히 아무것도 안 맞춰, 역할 칸의 영어가 안 걷힌 채 이름표가 「주어 Subject + …」로 나왔다. **스크립트는 멀쩡히 돌았다** | 역참조·`\b` 가 든 치환은 **셸에 넣지 말고 Write 로 파일에 쓸 것.** 고친 뒤 **제어문자 0** 을 세어 확인할 것(`ord(c)<9 or 11<=ord(c)<=31`) |

| **기계로 뽑은 문장을 안 읽고 믿음** | 어순 표를 사다리로 바꾸며 뼈대 문장을 기계로 뽑았다. 257곳을 찍어 보니 「I listen to.」·「He an actor.」·「시험 범위는 입니다.」·「당신은 갑니까??」처럼 **못 쓸 것이 3분의 1** 이었다 | 문장을 **생성**했으면 전수로 찍어 눈으로 볼 것. 걸름망은 한 번에 안 나온다 — **아홉 번에 걸쳐** 하나씩 붙였다 |

| **이 책은 태그마다 줄바꿈되어 저장된다** | 취미 카드를 바꾸려고 붙여 쓴 정규식이 **하나도 안 맞았다**(오류 없이 0곳). 이어서 `[\s\S]*?</div>` 를 썼더니 카드 묶음의 **첫 `</div>`** 에서 끊겼다 | 마크업을 글자로 맞출 때 **줄바꿈·들여쓰기를 넣어 맞출 것.** div 가 겹겹인 구간은 **짝을 세어** 끝을 찾을 것 |

| **CSS 축약이 앞선 선언을 되돌린다** | 71파일에 왼쪽 강조선을 넣으며 `border-left:4px` 를 선언 **중간**에 넣었더니, 뒤에 오는 `border:1px solid` 축약이 그것을 되돌려 화면에서는 1px 회색이었다. **CSS 글자는 멀쩡해 보였다** | 덧붙이는 선언은 **맨 뒤**에. 그리고 **재어서만 드러난다** — 브라우저로 실제 값(4px·안쪽 14px)을 전수로 잴 것 |

| **클래스 이름만 빌려 쓰고 규칙이 있는지 안 봄** | 여섯 문항에 `gram-link` 꼴로 요일 쪽 링크를 달았는데 **그 CSS 는 두 파일에만** 있었다. 나머지 다섯에서는 꾸밈 없는 링크가 되고 안의 `.gloss` 가 한 줄을 차지해 **네모 칸처럼** 보였다(사용자 지적으로 링크 6개를 도로 지움) | 다른 파일의 클래스를 빌려 쓸 때 **그 파일에 그 규칙이 있는지** 먼저 셀 것 |

| **줄끝이 바뀐다 — 읽을 때도, 쓸 때도** | `io.open(p, encoding="utf-8")` 로 읽고 그대로 쓰면 이 책의 **CRLF 가 LF 로 바뀝니다**(2026-08-20에 37파일). ★그리고 `newline=""` 로 열어도 **새로 만드는 조각에 `"
"` 을 넣으면 그 파일만 줄끝이 섞입니다** — 같은 날 어휘 상자를 다시 쓰며 18파일이 그렇게 됐습니다. **git 은 내용이 같아 안 보여 줍니다**(autocrlf) | 읽고 쓸 때 **둘 다 `newline=""`** + **새 조각의 줄바꿈도 파일에서 가져올 것** (`nl = "
" if "
" in s else "
"`). 고친 뒤 `open(f,'rb').read().count(b'
')` 로 셀 것 |

| **빈 태그를 짝으로 셈** | `<br>` 은 닫는 태그가 없는데 「`<br` 수 − `</br>` 수」로 검산해 아래 줄을 뗄 때마다 「짝이 어긋남」이 났다 | `br`·`img`·`hr` 은 짝 검산에서 빼고 **줄어든 수가 뜻대로인지**를 따로 셀 것 |

| **원본 글자로 세면 0이 된다** | 문장 안에 `<em>` 을 끼워 넣고 「그 문장이 그대로 있는가」를 **원본 글자**로 셌더니 0이 나왔다 | 문장 수는 **화면 글자**로 셀 것. 태그를 끼우는 편집은 원본 대조가 통하지 않는다 |

| **그물을 친 자리가 그날 안에 없어짐** | 09:33에 `check_exp_line.py` 를 만들었는데 12:25에 「문장 사다리」가 **그 표를 통째로 걷어냈다.** 검사기는 지금도 「0곳」이라고 말하지만 **볼 대상이 0개**다 | 검사기가 0을 내면 **대상 수도 함께 셀 것**(「0 / 0파일」인지 「0 / 500곳」인지). §5 참조 |

그 밖에 **조용히 실패하는 곳**(오류 없이 빈칸이 되는 곳): 번역칸 폴백(약어·따옴표), frame 키 누락, dict 열쇠 겹침, 물려받은 조건문. 자세한 것은 메모리 `haeseol-silent-failure.md`.

---

## 7. 진행 상황

### 8월 작업 (최근)

| 시기 | 한 일 |
|---|---|
| 08-08 | 105개 HTML 전수 점검 + 확정 결함 전량 수정. 보고서 `_점검보고 2026-08-08.md` |
| 08-09 | 어휘 500상자 전면 개편(한국어 위·영어 아래, 번역 슬롯 1,524 개방) |
| 08-09 | 다국어 1층 완결 — 화면 영어 전량 슬롯화, 잠긴 영어 0 (`translate="yes"` 현재 **23,867**개) |
| 08-09 | **신경망 음성** — 여성·남성 각 1,974개(합 3,948 · 57MB), 102개 파일 연결 |
| 08-09 | 쓰기 칸 🔊 는 정답 뒤에만 표시 · 활용표 칩에도 소리 · 「퀴즈 2 쓰기」 정리(직접 만들기와 겹쳐서) |
| 08-09 | 폭 규격 수리 (940px 한 파일 + CSS가 죽어 있던 조사 A~F) |
| 08-09~10 | **조사 0·A~G 전면 재작성** — 영어 기준점 제거, 눈높이 상향, 예문 한국어 먼저 + 소리 |
| 08-10 | 위 문장이 어렵다는 지적 → **18곳 쉽게 풀어 씀** |
| 08-10 | ★**사흘치(08-08~10)가 통째로 미커밋이던 것을 커밋** `692d68b`. 메모리에 적힌 커밋 `6710ea0`·`883b261`·`01f6f3f` 는 **이 저장소에 없습니다**(기록이 틀렸던 것). |
| 08-10 | 검사기 `check_spec.py` 예외표 · `_도구` 파이썬 10개 git 추적 시작 · 6월자 중복 파일 삭제 → `74125ff` |
| 08-11 | 쓰기 시험 개편·채점 관용 · gstep 번호 위로 · 머리 문장 범위 뱃지 67파일 · **절 제목 한국어 위 104곳** · 갈라진 따옴표 52·괄호 97 이음 · 기호만 든 슬롯 30곳 제거 (`b5a2135`…`c73b5cd`) |
| 08-11 | 본체 절 제목 「연습: 한국어 문장을 만들어 보세요!」 → **「문장 만들기 / Sentence Building」** 67/67 (`77adc06`) |
| 08-11 | 제목 앞 장식 이모지 113곳 제거 — 단추와 뜻을 가진 표지는 남김 (`ded4383`) |
| 08-11 | 문장 늘리기 지시문 8곳 삭제 — 「목적어를 추가해 보세요!」 (`b17da68`) |
| 08-11 | 문체 규칙 정정 — 안내문의 「~세요」는 그대로 (`2346061`) |
| 08-11 | 패턴 상자 45곳 한국어 위·영어 아래 (`b759c64`) |
| 08-11 | 부정 파일 `3 123~141` 표 다섯 개 손질 — 짧은/긴 부정·안 vs 못·~지 말다 (`9af4e98`·`50e050d`) |
| 08-11 | 패턴 상자 79곳 전수 — 번역 슬롯 밖 영어 11곳 (`2d1dc6d`) |
| 08-12 | **다국어 2층 사전 추출** — `js_extract.py` + 뼈대 932줄. 리터럴 12,164자리를 자리(문맥)로 갈라 냄 |
| 08-13 | **전수 검수** — 8층 31검사. 확정 결함 8갈래를 전부 고침 (`a2dbbfb`·`811ccc9`·`950dbdf`·`ba964e5`) |
| 08-14 | 시제 표 정렬 · 파스텔 결과칩 · 연습표 전체 듣기 23파일 · 「현재형 만들기」 이름 · 단추 갈래별 파스텔 · 밥→사과 |
| 08-14 | **「퀴즈 2 · 쓰기」에 빠져 있던 것을 채움** — 칸 눌러 듣기 · 여성·남성 · 전체 듣기(160칸). 즉시 채점은 원래 되고 있었고 소리만 없었다 |
| 08-14 | **전체 듣기 결함 둘 (23파일)** — 다 듣고 나면·재생 중 칸을 누르면 단추가 「■ 멈춤」에 멈춰 있던 것. §6 함정표 참조 |
| 08-14 | 「퀴즈 2 · 쓰기」 제목 넷에 무엇을 쓰는지 넣음 — ★과거형 파일은 **현재형도 씁니다**(4칸). 파일 이름만 보고 달면 틀립니다 |
| 08-14 | **남은 일 셋을 끝냄** — 반대말 표에 🔊 · 쓰기 시험 채점 안내 71파일 · `<title>` 속 한국어 80개 잠금(+빌더 ④단계, 검사 하나) |
| 08-15 | 영어 부제 군더더기 15곳 · 바닥글 「격식체」 21파일 · 활용표 전체 듣기 오른쪽 11덩이 |
| 08-15 | **파스텔 전면 적용** — 「진한 바탕 + 흰 글자」 979곳 → **0곳**. 활용표 칩 160곳 + 단추·번호·알림 띠 795곳 + 요약 상자 108곳. 표지는 옅은 원 안에 진한 글자로(동그라미 글자 🅐 → 보통 글자 A) |
| 08-15 | **연습표에서 원 교재에 없는 낱말 15개를 뺌** — ★원 교재는 「놀라운 한국어 500문장」이다. **카드에 없는 낱말은 원 교재에도 없다**(사용자). 3계열 67파일을 훑어 확인했고, `check_audit.py` 에 「연습표 낱말이 카드에 없음」을 넣었다 |
| 08-15 | 문법 설명을 **「한국어 한 문장 / 영어 한 문장」으로 전면 적용**(2차까지) · 상자 영어 다시 쓰기 10차 · 전체 듣기 단추 **162곳 93파일** 오른쪽으로 · 쓰기 시험 다 맞히면 전체 듣기 67파일 · 숫자 표 전체 듣기(새 음원 81개) · **시제 80낱말 밖 38개**를 연습표·카드에서 뺌 |
| 08-16 | 청유에 **「더 배우기: ~ㄹ/을래요?」** · 반대말 막대를 표 아래로 · 부록 4~7 쓰기 시험에 전체 듣기(`drl*`) |
| 08-16 | ★**설명 상자 전면 통일** — 395상자 35벌 → **상자 넷 + 이름표**(`.gb`/`.gb-key`/`.gb-ex`/`.gb-warn`). 327곳 변환. 뜻을 가진 색은 지킴(주의=호박, 안·못·말다 밑줄) |
| 08-16 | ★**표 머리줄 색 하나로 103파일** · **표 테두리 색 하나로 105파일**(CSS 633 + 인라인 1,103 선언 → `#dfe1ec`). 3px 이상 강조선 21곳과 상태 규칙은 남김 |
| 08-16 | 짝맞추기 — 칸 사이 벌림 · 양쪽 높이 같게(`subgrid`) · 색을 카드 수준으로 낮추고 갈래는 24색으로 늘림 |
| 08-16 | 늘 떠 있던 「남성 음성이 없다」 안내 — TTS 가 아니라 **CSS 가 빠진 것**이었다(106쪽에서 `speechSynthesis.speak` 를 가로채 확인, 새는 곳 0) |
| 08-16 | ★**한국어 위 · 영어 아래 전수** — 나란히 놓인 영어 **747곳**을 갈래로 나눠 **396곳**을 아래로 내리고 **351곳은 일부러 두었다**(어휘 카드·배치를 지는 상자 13개) |
| 08-17 | **부정 쪽 전면 손질** — 안/못 표에서 long form 을 빼고 짝끼리 붙임 · ~하다 표 머리줄을 공식으로 · 긴 부정은 형용사에도 · **~하다 형용사는 보통 형용사와 같이**(사용자 판단, ㉯안) · ①② 줄 다시 씀 · 머리줄 「긍정 · 부정」 |
| 08-17 | ★**영어 문법을 잣대로 삼은 설명을 뜻으로 푸는 설명으로**(사용자 원칙). 아래 §5 참조 |
| 08-17 | **공식 상자를 원형으로** — 희망 「동사 + 고 싶다」 · 계획 「동사 + ~려고 하다」 · 공감 「동사·형용사 + ~겠습니다」 |
| 08-17 | 희망 — 부정 두 꼴을 칸에 넣고 설명 상자를 뺌 · **인칭 표**(1·2·3인칭 한 줄씩) |
| 08-17 | 소망 — 제목·설명·이름표·빈 액자를 걷어내고 결과 칸에 영어를 담. **틀리지 않은 문장의 ❌ 를 △ 로**. 「누가 바라는가」 블록은 넣었다가 사용자 지시로 통째로 지움(되살릴 곳 `49c24da`) |
| 08-17 | 계획 — 「동사 활용」 글상자를 **규칙 표**로 · 붙이는 말을 원형(하다)으로 · **칩 재디자인**(빈 것과 채운 것) |
| 08-17 | ★`check_render.py` 두 번 손질 — **다시 보기**(걸린 쪽만 한 번 더) + **「무엇을 못 받았다」는 안 셈**. §6 함정 참조 |
| 08-18 | ★★**음원 4,494개가 디스크에서 사라져 있던 것을 git 에서 되살림.** OneDrive 가 두 컴퓨터를 부딪혀 `_소리\f`·`m` 이 비었고 `.git (1)`·`정리 (1)` 따위 충돌 사본이 생겨 있었습니다. 아래 §6 함정 참조 |
| 08-18 | 한쪽 말만 있던 설명 상자 넷에 짝을 채움 (`7af6aa9`) — 간접화법은 설명이 **영어뿐**이었습니다 |
| 08-18 | 설명 글의 해라체 12곳을 격식체로 (`6c8932c`). 부록 3 의 7곳은 **가르치는 문어체**라 그대로 |
| 08-18 | 셈을 적은 한국어 부제 넷에 짝을 채움 (`4e70d74`) — `p.subtitle` 은 본디 **영어 슬롯**이었습니다 |
| 08-18 | ★**낱말이 낱글자로 갈리던 2,530곳 → 0곳** (`acce170`, 106파일). `body{word-break:keep-all}` + gstep 표의 비상 끊기 막기 |
| 08-18 | ★**번역하면 기능이 죽을 문자열 14가지를 사전에서 빼냄** (`4b556af`). `querySelector('table.num-table')` 따위 |
| 08-18 | ★**표 머리 69곳을 한국어 위·영어 아래로** (`20f382b`·`fcbfd34`) — 3,344개 중 영어 먼저 0 |
| 08-18 | ★**번역 슬롯이 부호를 가르던 34곳** (`cfa786c`) — 24,750개 중 0 |
| 08-18 | ★**플래시카드를 뒤집기 → 아래로 펼치기** 73파일·600장 (사용자 결정). 번역문 35% 늘림 시험 **26개 넘침 → 0** |
| 08-18 | **쪽마다 훑어 고치기 — 계획·결정·는데·허락 금지 의무 면제·부탁·위해서** (사용자 스크린샷 지시 11건, `55976a9`…`7945e83`). 되풀이뿐인 머리칸 영어 6곳 삭제 · 「어간 + …」 넷에 Stem · **머리칸이 영어뿐이던 30곳에 한글** · 「어조 → 말투」 · 패턴 이름을 **기본형**으로 · 표 첫 칸을 **세 줄**(제목/영어/문법, `/`·`( )` 없앰) · 공식 상자 셋째 줄 삭제 2곳 |
| 08-18 | ★**「한국어 한 줄, 영어 한 줄, 한 줄에 한국어 두 문장이 없게」**(사용자 지시). 인용문 **10상자** 6파일 (`5e54353`·`0c09fa3`) — 부록 1은 문장 **중간** 괄호라 사용자 지시로 뺌 |
| 08-18 | ★위 지시를 **책 전체로** 넓힘 (`aa9dfac`). 소스로 세면 84줄이 나오는데 대부분 헛것이었고, 브라우저에 그려 재니 **390px 0곳 · 1280px 5곳**. 다섯을 고치고 검사기 `check_koen_line.py` 를 만듦. §3·§6 참조 |
| 08-19 | **쪽마다 훑어 고치기 — 변화1·변화2 · 공감 · 정중표현(237~247) · 존댓말(489~500) · 관형어(474~488)** (사용자 스크린샷 지시 40여 건, `d4414f0`…`8b2e899`). 되풀이하는 1단계 설명 상자 삭제 · 정중표현의 관용 여섯을 통째로 어휘에 · 관형어의 「왜 배워야 하나」를 이유 둘의 표로 · 시제 표에서 알약·그러데이션을 걷고 파스텔로(마크업 3,848자 → 1,790자) · 보기 아랫줄을 규칙 설명이 아니라 **그 한국어의 실제 해석**으로 |
| 08-19 | ★**어휘의 영어는 「한국어 정답」이 아니라 그 문항의 500문장 원문 기준**(사용자 원칙). 인칭 대명사를 **101번부터** 어휘에서 뺌 71곳 30파일. 검사기 **`check_vocab_match.py` 신설** — **0이 목표가 아닙니다**(2026-08-20 셈 35곳/23파일) |
| 08-19 | **존댓말 파일 정리** — 정답을 미리 보여 주던 어휘 9개·해설 칸 3개 · 해설 칸 15개를 「하다 → 하시다 → 하십니다」 **한국어 사슬**로(이 파일 해설 칸의 영어 0곳) · **목적어 줄에 든 서술어 설명**을 바로잡음(497·499) · 500은 아들·딸만 남김(아드님·따님은 문법 설명이 이미 가르침) |
| 08-19 | **해설 칸의 영어를 아래 줄로 12곳 7파일** + 검사기 `check_exp_line.py` 신설 (`70b773c`). ⚠️세 시간 뒤 사다리가 그 표를 걷어내 **지금 이 검사기는 볼 대상이 0개**입니다(§5·§6) |
| 08-19 | **어휘를 낱말 카드로 67파일** (`74cbac6`). ★**글은 한 글자도 안 건드리고 CSS 네 줄**만 바꿨습니다 — `.v-item` 규칙이 67파일에서 한 가지로 똑같았습니다. 재고 넣었습니다: 390px 어휘 상자 높이 +39% · 상자 밖으로 나간 카드 0 |
| 08-19 | ★★**어순 해설 표 500개 → 「문장 사다리」** (`4127254` 시안 → `d5cd693` → `11984af` 전량). **36언어 일괄 번역을 앞두고 사용자가 표를 없애기로 결정.** 규칙과 걸름망 아홉은 §5 와 `_도구/eosun_ladder.py` 머리글에 |
| 08-19 | **만들기 줄 다듬기** — 이름표의 영어를 아래 줄로 67파일 · 영어 주석 24곳 삭제(한국어만) · 왼쪽 맞춤 · 뭉갠 띄어쓰기 20곳 · 관형어 이름 하나로 9곳 |
| 08-19 | **따라 읽기·쓰기 시험을 「문장구조 2」 꼴로 — 왼쪽 4px 강조선 71파일** (`2d6bee5`). ★색은 통일하지 않고 **그 장의 색**을 씁니다(파랑 36 · 호박 17 · 초록 9 · 청록 3 · 보라 3 · 분홍 1 · 남색 1). 73파일 전수로 모든 줄을 재어 어긋남 0 |
| 08-19 | ★**어휘에서 조사·문법 표현·활용형을 걷어냄** — 조사 50개(42상자 13파일) · 기본형 15곳 7파일 · **문법 표현 108개 36파일**(59가지) · 묶음만 남기기 18개 11상자 · 부분 풀이 3곳. 빈 상자는 「새 어휘 없음」으로(2026-08-20 셈 **34곳**). 자세한 규칙은 §5 |
| 08-19 | **041~050** — 「에게」와 「에」를 **바탕색으로 가름**(색은 `2 조사 C` 의 이름표에서 가져옴) · 사다리 구멍 넷을 뼈대에서 메움 · 되풀이하는 「주의」 상자 둘 삭제 · 46~50 이름표에 목적어를 살림 |
| 08-19 | **087~096** — 「이었습니다」 칸을 다섯 줄에서 세 줄로 · 「제 = 저의」 보기를 한 줄에 하나씩 · **취미 21개를 「카드 보고 말하기」 꼴로**(무지개 팔레트·2열·뒷면 소리, 21낱말 모두 구워 둔 mp3 가 있음) · 93번 해설에 **요일 카드 7장** |
| 08-19 | **새 쪽 `4 부록 9 요일.html`** (`7ba411a`) — 요일 일곱 카드 + 「~요일에」·「오늘·내일」. ★교재에 나오는 요일은 월·수·토·일 **넷뿐**이라 화·목·금 셋은 교재 밖입니다(쪽이 성립하지 않아 일곱을 다 넣었습니다). ⚠️**링크 6개는 모양이 깨져 도로 지웠고, 지금 이 쪽을 가리키는 링크가 0개**입니다(아래 남은 일) |
| 08-19 | ★**카드 힌트를 영어로 — 「▼ 눌러서 확인」 → 「▼ Tap to check」 578곳 101파일**(2026-08-20 파일 셈으로 확인). 「대상이 처음 한국어를 배우는 학생들이다」. ★★**이 책에서 번역 슬롯이 없던 UI 글자는 이것 하나뿐**이었고 `check_audit` 은 영어만 세어 0을 냈습니다 — **사용자가 눈으로** 찾았습니다. 검사기 `check_ko_noslot.py` 신설. ※JS 문자열이 바뀌었으므로 **`js_사전_뼈대` 재생성이 필요합니다** |
| 08-19 | **105~114** — 「💡 표기 안내」의 괄호 보기 두 줄 삭제 · 받침 표를 **X · O 만**으로 · 「존댓말 polite」 → **「격식체 formal」**(칸의 꼴이 「갈 수 있습니다」라 격식체가 맞음) · 진한 빨강·초록을 책이 이미 쓰는 파스텔로 |
| 08-19 | 카드 목록 속에 잘못 들어간 「설명」 상자 **11곳**을 카드로 되돌림 (`e594950`) · `check_card_expand` 의 박힌 경로를 풀어 **검사기 13개 전부 컴퓨터 독립** (`9bbd923`) |
| 08-20 | **인수인계 정비** — 08-19 하루치를 채우고, 그날의 갱신 두 번이 **백업 사본에만** 들어가 있던 것을 합침 (`d91accc`·`71c9527`). 문서 맨 위 알림 참조 |
| 08-20 | ★**100~500 규칙 전수 조사** (`_도구/sweep_survey.py`) — 어휘 여덟 갈래(조사·문법 표현·활용형·꼬리표·정답 미리보기·차례·겹친 표제어·빈 상자)가 **모두 깨끗**했습니다. 걸린 13곳은 전부 인수인계가 「남긴다」고 적어 둔 오탐(「보다」 동사 9 · 겹침 넷). 08-19 의 쓸기가 책 전체에 닿았다는 뜻입니다 |
| 08-20 | ★**「안 됨 · 됨」 색을 파스텔로 — 책 전체 459곳 79파일** (`7a16b43`·`2b2d5dd`). `#c62020`→`#9f1239` · `#107435`→`#166534`. 어제 105~114 에서 정한 것을 넓혔습니다. 쓰임을 먼저 갈라 보니 **한 가지 뜻**이었습니다(없음/있음 알약 · ✅/❌ · X/O · 취소선). **배경으로 쓰인 곳 0** — 전부 글자색이라 바탕과 같아지는 사고가 날 수 없습니다 |
| 08-20 | ★**받침 칸을 「X · O」 알약 하나로 — 53곳 10파일** (`2b2d5dd`, `_도구/batchim_xo.py`). 아래의 「없음 / none」·「있음 / yes」 줄을 뗐습니다(X·O 는 어느 언어에서나 같습니다). ★**되풀이가 아닌 아래 줄은 남겼습니다** — 237~247 의 「모음 끝 / 자음 끝」 두 칸. 책에 세 가지 꼴이 섞여 있던 것을 한 꼴로 모았습니다 |
| 08-20 | **개조식 설명 셋을 문장으로** (`683f1d5`) · ★★**태그 결함 — `<small>` 안에 `<small>`**: 영어 줄이 0.8×0.8=**64%** 로 작아지던 곳 여덟(362~365 하나 + 책 전체 일곱, `2d3dd01`). **짝 검산으로는 안 잡힙니다**(연 수와 닫은 수가 같습니다) |
| 08-20 | **종별사 표 알약·그러데이션 걷기 — 시안** (`0981bc6`). 13개 → `.accent` 글자. 표 마크업 6,054자 → 4,104자 |
| 08-20 | ★**어휘를 원문 문장에 맞춤 — 24자리**(`8644a82`) + **문법 꼬리표 10곳**(`8603f42`). check_vocab_match **35 → 11**(남은 것은 오탐). 묶음 표제어는 모두 정답 문장의 말입니다 |
| 08-20 | **원격에 올림** — `71c9527..8603f42` (`github.com/kkjune2000-debug/500-haeseol`, 비공개). 사용자 결정 |
| 08-20 | **370~383 동그라미 바탕을 한 색으로** · ★**죽은 CSS 731개 걷기**(72파일, `44eb3ce`). 「3 051~060」은 `exp-table` 을 아직 써서 손대지 않았습니다 |
| 08-20 | ★**표 알약·그러데이션 전량 걷기** (`a52754f`) — 여섯 파일 28개. 책 전체 41개 → **0개**. 어제 관형어 표가 가른 기준대로 **형태는 `.accent` · 이름은 `<strong>`** 으로 갈랐습니다. 함정 둘: 글자 크기가 두 가지(0.9·0.92rem) · 알약 속 `<strong>` |
| 08-20 | ★**영어 문장만 있던 설명 상자 일곱에 한국어 짝을 채움** (`5834c63`, `_도구/gb_ko_pair.py`) — 353·362·367·452·457·458·459. ★362 의 「Korean takes the subject particle …」은 **영어를 잣대로 삼은 설명**이라 한국어 안에서 가릴 수 있는 까닭으로 다시 썼습니다. ★「규칙 동사입니다. ㅂ이 바뀌지 않습니다」를 **한 문장으로** 묶었습니다(한 줄에 한국어 한 문장) |
| 08-20 | **371 사다리 구멍** (`5431440`) — 뼈대가 바로 앞 문항 370 에 있었습니다. 같은 파일 372 가 이미 그 꼴이라 짝을 맞췄습니다. ★100~500 을 전수로 훑어 나온 **유일한** 사다리 구멍입니다(한 단 276곳 중) |

### 2026-08-13 전수 검수 — 무엇을 보았고 무엇이 나왔나

08-08 전수 점검 뒤로 대량 편집이 여러 번 지나갔으므로(어휘 개편·조사 재작성·쓰기 시험 개편·제목 104곳·머리 73파일·이모지 113곳·대시 158곳·슬롯 이음) **그 편집들의 회귀**를 겨냥해 8층을 훑었습니다.

**고친 것 여덟**

| | 무엇 | 얼마나 | 커밋 |
|---|---|---|---|
| ① | 번역 슬롯 **밖**에 있던 영어 — 패턴 상자의 영어 줄이 통째로 슬롯 밖이었습니다 | 31자리 15파일 | `a2dbbfb` |
| ② | 죽은 슬롯 — 비교급 표의 `&gt;`·`&lt;` 는 수학 기호라 번역할 것이 없습니다 | 2곳 | `a2dbbfb` |
| ③ | `lang="en"` 인데 영어가 한 글자도 없던 `<small>` | 2곳 | `a2dbbfb` |
| ④ | 제목 앞 장식 이모지 잔존 — 🥢🎓🎮📋🎵📋🔗🔢 | 8곳 | `811ccc9` |
| ⑤ | `※ 주의:` 하나만 나머지 24곳(`⚠️ 주의:`)과 달랐음 | 1곳 | `811ccc9` |
| ⑥ | **채점기가 「내었습니다」를 오답으로 치던 것** | 70파일 72곳 | `950dbdf` |
| ⑦ | **휴대폰 화면에서 표가 쪽 밖으로 나가던 것** | 99파일 표 738개 | `ba964e5` |

★ ①이 08-11 청소(`2d1dc6d`)에서 살아남은 까닭 — 그때 그물이 `<small>` 을 걷어낸 뒤 검사했는데 **병이 바로 그 `<small>` 안에 있었습니다.** 걷어내는 순간 검사 대상이 사라진 것입니다.

**결함이 아니라고 판정한 것 셋 — 다시 「고치자」고 꺼내지 마십시오**

- **「핵심 문법」이 없는 본문 2파일**(`061~070`·`071~078`) — 책에서 유일하게 이어지는 단원 **6-1·6-2·6-3** 이고, 문법을 6-1에서 한 번 가르친 뒤 6-2·6-3은 의문사만 바꿔 연습합니다. §5의 「67곳 통일」은 실제로 **65/67**이며 그것이 맞습니다.
- **판형이 둘** — 시제·조사·부록 **32파일**은 `<h1 class="main-title">` + `<p class="subtitle">`, 나머지 74파일은 `<header class="title-block">`. 다만 32파일 **모두** 한국어 제목 + 영어 부제 + 슬롯을 갖췄습니다. 규칙은 지켜졌고 마크업만 두 갈래입니다.
- **슬롯 밖 영어 4건** — `BTS`·`KTX`·`Amazing Korean 1`·진단 페이지. 고유명사는 번역 대상이 아닙니다.

**결함 0으로 확인한 것** (검사 24종)

인코딩 4 · 태그 짝 · id 중복 · 스크립트 괄호 · 정답 1,101개 · 없는 id · 죽은 핸들러 · 화면 누수 · **문장 번호 1~500 빠짐 0·겹침 0** · **음원 열쇠 4,857개 → 없는 mp3 0** · en 슬롯 속 안 잠긴 한국어 0 · 머리 영어가 위 0 · 제목의 대시 0 · **따라 읽기 500문장** · **쓰기 시험 1,000문항 정답이 문장과 전부 일치** · 어휘 상자 500·항목 1,507 구조 0건 · **의문형 1,365개가 평서형과 전부 일치** · 넓은 화면 렌더 7검사 0건.

### ★★ 보류 — 플랫폼 저작 규격 (2026-08-14 사용자 결정)

`OneDrive\문서\카카오톡 받은 파일\AMK_CONTENT_AUTHORING_SPEC_2026-08-12.md` 로
플랫폼 쪽 저작·납품 규격이 왔습니다. **보류하기로 했습니다. 이 규격으로 변환을
시작하지 마십시오.** 재개는 사용자 지시로만 합니다.

무엇인지만 적어 둡니다(2026-08-14에 읽고 실측한 것 — 다시 재지 마십시오):

- 단원 단위로 **설명 문서 / 문제 문서 / 담당자 JS / 정답 파일** 4종을 납품하는 규격입니다. 정답·해설을 문제 문서에서 **분리**하고, 납품 HTML은 변환 없이 그대로 실립니다.
- **단원 수는 정확히 맞습니다** — 규격 105(시제 21·조사 9·문장 67·부록 8) = 지금 책 105파일(`_음성 확인` 제외).
- **지금 책은 어느 파일도 그대로 낼 수 없습니다.** 규격이 금지하는 것을 실측하면 `translate` 속성 **26,191**곳 · `on*` **8,867**곳 · 문서 안 정답 **1,101**곳 · 인라인 `<script>` **419**곳 · 음원 참조 **392**곳 · 외부 폰트 **105**곳.
- 절을 갈라 보면 **설명 82 · 문제 389**, 활동은 `writing` 166 · `matching` 147 · `flashcard` 74 · `read_along` 67 · `choice` 1.
- ★ **시작할 수 없는 곳 넷** — ①단원 목록(manifest) 없음(임의 ID 금지·반려) ②브리지 URL·integrity 없음 ③짝맞추기 규격 미전달(활동 147개가 대기) ④듣기 규격 미전달(음원 4,324개가 대기).
- ★ **규격에 답이 없는 것** — `translate` 속성을 써도 되는가. 8장은 `lang` 만 정하고 언급이 없는데, 이 책은 다국어판 때문에 26,191곳에 슬롯을 열어 두었습니다. 다만 7-3이 정답 파일에서는 `class`·`lang` 외 속성을 금지합니다.
- ★ **규격과 어긋난 것 둘** — 규격이 설명 전용으로 지정한 `basic_2_particle_01`(조사 0)은 소리 단추 14개가 있어 그것을 빼야 맞고, 규격에 없는 `4 부록 2 부정법`은 상호작용이 0이라 오히려 설명 전용입니다.
- 5-10이 **TTS 단추는 허용**하고 5-7·3-2가 **구운 음원은 막습니다.** 이 책은 둘을 함께 쓰므로(음원을 못 찾으면 TTS로 떨어짐) 소리를 어떻게 할지가 계획을 크게 가릅니다.

---

### 남은 일 (사용자와 정할 것)

1. **다국어 2층** — 자바스크립트 안에 박힌 문자열이 아직 한국어·영어 그대로입니다.
   - **사전 추출은 끝났습니다 (2026-08-12).** 도구 `_언어판 시험\js_extract.py`, 뼈대 `_언어판 시험\js_사전_뼈대.py`(932줄, 번역어는 빈칸). 105파일에서 리터럴 **12,164자리 · 서로 다른 글자 2,463가지**를 훑었습니다(`_음성 확인.html` 은 언어판을 안 찍으므로 뺐습니다).
   - 가른 결과: **번역할 것 932 · 잠글 것 1,402 · 모름 0 · 조각 129.**
   - **검산 셋 통과**: ①빌더가 `q+k+q` 로 **932가지를 모두 찾아냅니다**(못 찾으면 오류 없이 조용히 지나갑니다) ②번역 열쇠가 잠글 목록에도 있는 것 0 ③**음원 맵 글자가 사전에 샌 것 0**.
   - ★★ **번역어 채우기는 「놀라운 한국어」 완전 검수가 끝난 뒤에 합니다 — 2026-08-12 사용자 결정.** 번역한 뒤에 고치는 것보다 **미리 고치는 것이 낫기 때문입니다.** 먼저 하자고 다시 꺼내지 마십시오.
     - ★ **그래서 `js_사전_뼈대.py` 는 2026-08-12자 스냅숏입니다.** 열쇠가 **소스에 적힌 그대로**여야 빌더가 찾으므로, 검수하며 스크립트 문자열을 한 글자라도 고치면 **그 줄은 조용히 죽습니다**(오류가 안 납니다). **검수가 끝나면 `python js_extract.py --dump` 를 다시 돌려 뼈대를 새로 내십시오.** 채운 번역어가 있다면 옛 뼈대와 열쇠를 대조해 옮겨야 합니다.
   - 채울 때: 공통 틀 **36가지**만 채워도 105파일의 UI가 다 덮입니다(나머지 896가지는 그 파일의 영어 문장·뜻풀이라 양이 큽니다). 지금 `tr_ja.JS` 27개 중 24개가 이 뼈대 안에 있습니다 — 손으로 적은 그 사전은 `3 011~020` 한 파일용이었습니다.
   - ★ **뼈대에 「★한국어 표지 그대로」 표가 붙은 47줄**이 있습니다 — 「어간에 받침이 없습니다 → 어간 + ‑ㅂ니다 .」·「받침 O → 은 · 이」처럼 **번역할 문구 안에 배우는 한국어가 든 자리**입니다. 자모(ㅂ니다·ㄹ 것입니다·ㄷ 불규칙)와 화살표 뒤 조사는 **옮기지 말고 그대로 두십시오.**
2. ~~**`<title>` 안의 한국어**~~ — **끝났습니다 (2026-08-14).**
   - 셈이 틀려 있었습니다: 한국어가 든 제목은 **81이 아니라 80**입니다(106개 중, 나머지 26은 영어뿐). 「옛 기록 80은 하나 적었다」던 이 문서의 정정이 도리어 틀린 것이었습니다 — **파일에서 다시 세십시오.**
   - **원인**: 바탕이 `<html lang="ko" translate="no">` 라 제목은 본래 잠겨 있는데, 제목마다 **`translate="yes"` 가 붙어 일부러 열려 있었습니다.** 그래서 문법 항목인 「에」·「은는」·「ㄹ받침」이 새 나갔습니다.
   - **방법**: `<title>` 안에는 자식 태그를 못 넣어 부분만 잠글 수 없습니다. 그래서 **한국어가 들면 통째로 잠급니다**(`translate="no"`, 80파일). 영어뿐인 제목 26개는 잠글 까닭이 없어 그대로 열어 두었습니다.
   - 언어판은 **`lang_build.py` ④단계**가 제목을 통째로 갈아 끼웁니다 — `tr.TITLE` 을 먼저 보고 없으면 `tr.TEXT` 로 되물립니다. 표에 없으면 그대로 두고 **「제목 빠짐」으로 알립니다**(조용히 지나가지 않습니다).
   - `check_audit.py` 에 **「제목 속 한국어가 안 잠김」**을 넣었습니다. ★층 2의 `Walk` 는 `<title>` 을 건너뛰므로(`SKIP`) 따로 봅니다 — 그래서 여태 아무 검사에도 안 걸렸습니다.
   - ★**빌더가 `UI` 표가 비면 터지던 것**도 함께 고쳤습니다(`"|".join` 이 빈 갈래가 되어 `KeyError: ''`). 표를 갓 만들어 UI 가 아직 빈 언어판에서 터집니다.
3. ~~**`4 부록 2 부정법.html` 폭**~~ — **끝났습니다 (2026-08-10).** 폭 문제가 아니었습니다. 이 파일은 `.page-container{width:210mm; margin:0 auto}` 인 **A4 인쇄 낱장**(회색 책상 위의 흰 종이 + `@media print`)이라 화면 가득 퍼지지 않습니다. 검사기가 `.container` 라는 이름만 찾아 "폭=-"로 알린 것을 이 문서가 그대로 옮겨 적은 것이었습니다. 검사기에 예외표를 두어 바로잡았습니다(§3).
   - ★ **부록 2는 인쇄 낱장 그대로 둡니다 — 2026-08-10 사용자 결정. 마스터 템플릿으로 옮기자고 다시 제안하지 마십시오.**★ 이 파일만 `.container` 800px 이 아니고 배경도 `#555` 이며 소리가 없는 것(16KB)은 **알고 있는 상태**입니다. 결함이 아닙니다.
4. **부록 8 기초문법총정리** — 6월 기록: 옛 A4 "마스터 문법 맵"을 인터랙티브로 만들 것인지 3안 중 사용자 결정 대기. **→ 미룸 (아래).**
5. **말하기 PPT** — 500문장 PPTX 전체 생산 보류 중.
6. **답을 기다리는 것 셋** — 2026-08-10~11 세션에서 꺼냈고 아직 결정을 못 들었습니다. 숫자는 **2026-08-11에 파일에서 직접 셌습니다.**
   - ~~**쓰기 시험 채점 안내**~~ — **끝났습니다 (2026-08-14).** 71파일의 「쓰기 시험」 안내 문장 뒤에 이어 붙였습니다: 「띄어쓰기와 마침표·물음표는 채점에 넣지 않습니다.」 / 「Spacing, periods and question marks are not graded.」
     - ★**「문장부호는 채점하지 않습니다」라고 쓰면 안 됩니다** — `wtNormalize` 가 지우는 것은 `[?？!.。]` 뿐이라 **쉼표 24곳**(「예, 저는 …」)과 **큰따옴표 8곳**(간접화법)은 그대로 대조합니다. 지우는 것만 적으십시오.
     - ★**문단을 새로 만들지 마십시오** — `ko`·`en`·`ko`·`en` 이 되어 한국어–영어 짝이 눈에 안 잡힙니다. 있던 문장 뒤에 이어 붙여 문단은 둘로 둡니다.
     - 채점기 갈래는 여섯이고 규칙이 서로 다르지만(120자리) **실제 결함은 0**입니다 — 물음표를 안 지우는 `prJNorm` 6파일은 그 파일 정답에 `?` 가 0개, 줄임말을 안 푸는 곳도 정답에 `보았·주었·되었·내었` 이 0개입니다. **띄어쓰기는 120자리가 모두 지웁니다.**
   - ~~**제목 속 이모지**~~ — **끝났습니다 (2026-08-11, `ded4383`).** 아래 §5 참조.
   - ~~**반대말 표에 🔊**~~ — **끝났습니다 (2026-08-14).** `3 001~010 문장구조 주어 형용사.html` 한 파일. 바로 아래 「반대말 카드」에는 🔊 가 있는데 **표에만 없었습니다.** 낱말 20개에 낱개 🔊(`.speak-btn.ant-spk`)를 달았습니다 — 카드와 같이 고른 목소리(`AK_SEX`)를 따릅니다. 여/남 쌍은 단추가 40개가 되어 표가 무거워집니다.
   - ~~**패턴 상자 B갈래 19곳**~~ — **끝났습니다 (2026-08-11).** 21곳(`58d95c8`) + 16곳(`0184c77`)을 채웠습니다.
   - ~~**머리 영어 부제에 한국어가 섞인 13파일**~~ — **끝났습니다 (2026-08-11, `1fc7f5f`).** 이 문서가 「손대지 않았습니다」로 남겨 두고 있었으나 그날 15:21에 다 했습니다. 부록 4~7 은 한국어 부제가 아예 없어 새로 지었고(그 파일이 실제로 연습하는 형태에서 뽑음), 지금 106파일 전체에서 머리 영어 칸에 한국어가 든 것은 **0곳**입니다.
   - ~~**패턴 상자에 남은 12곳**~~ — **끝났습니다 (2026-08-11, `2d1dc6d`).** 세어 보니 진짜 병은 「뒤집기」가 아니라 **번역 슬롯 밖 영어 11곳**이었습니다. §5 참조.
     - `3 232~236 확인` 1곳은 **겹친 `<small>` 때문에 생긴 오탐**이었습니다(이미 한국어가 위).

7. ~~**답을 못 들은 것 (2026-08-17 세션에서 꺼냄)**~~ — **넷 중 셋이 끝났습니다 (2026-08-18).**
   - ~~**`.gb` 상자의 낱말 끊김**~~ — **끝났습니다.** 물어보니 사용자가 **「책 전체」**를 골랐습니다. `.gb` 만이 아니라 106파일에 `body{word-break:keep-all}` 을 걸어 **2,530곳 → 0곳**(`acce170`). §5 「화면 폭·표」 참조.
   - ~~**셈을 적은 한국어 부제 넷**~~ — **끝났습니다** (`4e70d74`). 087~096 셋은 셈을 떼고 영어를 달았고, 부록 3은 셈을 두고 영어만 달았습니다(셈을 떼면 「단어 × …」가 되어 문장이 깨집니다). **셈은 넷 다 맞았습니다** — 전공 8·취미 8·요일 7·`STYLE_M` 21.
   - ~~**영어가 없는 상자 셋**~~ — **끝났습니다** (`7af6aa9`). 세어 보니 **넷**이었습니다(`3 441~444 ㅡ불규칙` 589행의 `.explain-note` 는 상자 전체에 `lang="en"` 이 0개).
   - ~~**소망 쪽 문법 6의 예**~~ — ★★**하지 않기로 끝났습니다 (2026-08-18 사용자 결정: 「교재에 없기 때문에 삭제하라」).** 다시 꺼내지 마십시오.
     - 제안이었던 것: 문법 6의 예를 「한국에 가고 싶습니다」에서 「**차를 사고 싶습니다**」로 바꾸면 문법 7의 「차가 있었으면 좋겠습니다」와 **한 낱말(차)로 견주는** 대비가 표 안에서 살아납니다.
     - **기각한 까닭**: 「차를 사고 싶습니다」는 **원 교재에 없는 문장**입니다(책 전체 「사고 싶」 0건). 낱말 「사다」·「차」는 교재에 있지만 **문장이 없습니다.**
     - 지금 표의 두 예문은 **둘 다 원 교재 문장**입니다 — 「저는 한국에 가고 싶습니다」 = **문항 143**, 「차가 있었으면 좋겠습니다」 = **문항 150**. 손댈 것이 없습니다.
     - 이 대비는 커밋 **`7cf0eeb`**(2026-08-17 「문법 6·7 비교의 설명 상자를 지움」)에서 지워진 상자에 실제로 있었습니다. 그 상자도 되살리지 않습니다.
     - ★ **여기서 나온 원칙은 §5 「원 교재에 없는 것을 지어 넣지 않습니다」로 옮겨 적었습니다.**

8. **다국어 준비에서 남은 것**
   - ~~**플래시카드 `height:140px` 73파일**~~ — ★**끝났습니다 (2026-08-18 사용자 결정).** 높이를 늘리는 대신 **뒤집기를 버리고 아래로 펼치게** 바꿨습니다. §5 「플래시카드」 참조. 번역문 35% 늘림 시험에서 **26개 → 0개.**
   - **사전 뼈대 재생성** — §7-1 참조. **검수가 끝난 직후**에 해야 합니다. ★2026-08-19에 **JS 문자열 33곳이 바뀌었습니다**(카드 힌트 `fc-flip-hint` 28 · `flashcard-hint` 5). 뼈대의 열쇠는 **소스에 적힌 그대로**여야 빌더가 찾으므로 **그 33줄은 지금 조용히 죽어 있습니다.**

9. ~~**요일 쪽을 어떻게 할지**~~ — **끝났습니다 (2026-08-20 사용자 결정: 「링크 없이 그대로 둔다」).**
   - 새 쪽 `4 부록 9 요일.html` 은 그대로 있는데, 붙였던 링크 6개(58·79·92·93·159·301)는 **모양이 깨져 도로 지웠습니다**(§6 함정 — `gram-link` CSS 가 두 파일에만 있었습니다). **2026-08-20 셈으로 이 쪽을 가리키는 링크는 0개**입니다.
   - 사용자가 **㉮ 그대로 둠**을 골랐습니다. 쪽은 남아 있고 가리키는 링크는 0개입니다 — 목차나 폴더에서 직접 엽니다.
   - ★**링크를 다시 달자고 먼저 꺼내지 마십시오.** 정해진 일입니다.

10. ~~**어휘와 원문이 어긋나는 35곳**~~ — **끝났습니다 (2026-08-20 사용자 결정 「전부·묶음으로」, `8644a82`·`8603f42`).**
    - **24자리를 고쳤습니다** — 뜻풀이만 바꾼 곳 12(참 = so · 받다 = get · 타다 = get on …) + **묶음으로 바꾼 곳 12자리**(비가 오다 = rain · 눈이 오다 = snow · 노래를 부르다 = sing · 교통사고가 나다 · 담배를 끊다 · 잠이 안 오다 · 돈이 없다 · 맛있게 먹다 · 한 다발 …).
    - ★묶음 표제어는 모두 **정답 문장에 있는 말**입니다(교재 밖 낱말 0). 부분이 정답에서 혼자 나오는 자리가 없을 때만 묶었습니다(08-19 조건).
    - **35곳 → 11곳.** 남은 열하나는 검사기 오탐이라 손대지 않았습니다 — 59 당신(원문 your) · 137(do not ↔ Don't 축약) · 211 병원 · 234 11시 · 383 어머니 · 449·450 너무 · 494 죽다 · 495 있다 · 500 아들·딸. **494·495 는 그 쪽이 가르치는 것이 돌아가시다·계시다**이고 어휘는 바탕말입니다.
    - 함께 나온 **문법 꼬리표 10곳도 뗐습니다**(`8603f42`) — 「안 = not (short form)」·「돕다 = help (ㅂ irregular)」 따위. 08-19 규칙(어휘는 뜻만)의 남은 자리였습니다. 도구 `_도구/vocab_drop_tail.py`.
    - 도구 `_도구/vocab_source_fix.py`. ★그 도구가 두 번 위험했습니다 — `v-item v-none`(「새 어휘 없음」)을 못 봐서 지울 뻔했고, 「뜻풀이에 한국어 0」 검산이 **잠근 한국어**까지 걸어 헛경보를 냈습니다.

10-옛. 옛 설명: — `check_vocab_match.py` 가 내는 목록입니다(2026-08-20 셈 35곳/23파일). **0이 목표가 아닙니다.** 「죽다 = die」(494 passed away)처럼 **판단이 갈리는 자리**라 사용자와 함께 볼 것입니다.

11. ~~**`.exp-*` 죽은 CSS**~~ — **끝났습니다 (2026-08-20, `44eb3ce`).** 72파일 **731개 규칙**을 지웠습니다. ★「3 051~060」은 `<table class="exp-table">`(무슨 + 명사 표)를 아직 써서 **한 규칙도 지우지 않았습니다** — 이름이 같다고 다 죽은 것으로 보면 표가 무너집니다. 잣대는 **파일 안에서** 셉니다. 도구 `_도구/css_dead_exp.py`. 옛 설명: — 어순 해설 표를 걷어낸 뒤 남은 잔재입니다(마크업은 0개). 화면에 아무 일도 하지 않으니 급하지 않지만, 스타일을 정리할 일이 생기면 함께 걷으십시오.

12. ~~**영어 문장만 있고 한국어 짝이 없는 설명 상자 일곱**~~ — **끝났습니다 (2026-08-20 사용자 결정, `5834c63`).**
    - `3 352~355` 「Negative of 자다 (안 자다) = a deliberate refusal to sleep…」 · `3 362~365` 「Korean takes the subject particle 이/가 (not 을/를)…」 · `3 366~369` 「승리/패배 are Sino-Korean compounds…」
    - `3 449~453` 452) · `3 454~459` 457)·458)·459) — 「입다 is REGULAR! ㅂ does NOT change!」 꼴 넷. 한국어는 활용 보기(입다 → 입었습니다)뿐이고 **설명 문장은 영어뿐**입니다.
    - 채운 말: 353 「안 자다는 일부러 자지 않는 것입니다」·「잠이 안 오다는 자고 싶은데 잠이 오지 않는 것입니다」 · 362 「있다·없다·많다·적다 앞에는 을/를이 아니라 이/가를 씁니다」·「가진 것이나 양은 하는 일이 아니라 상태이기 때문입니다」 · 367 「승리·패배는 한자어라서 …」·「말할 때는 이기다·지다를 씁니다」 · 452·457·458·459 「~는 ~이 바뀌지 않는 규칙 동사입니다」.
    - ★**362 는 영어를 잣대로 삼고 있었습니다** — 「Korean takes the subject particle …」. 한국어 안에서 가릴 수 있는 까닭으로 다시 썼습니다(§5).
    - ★**한 줄에 한국어 한 문장**만 두었습니다 — 「규칙 동사입니다. ㅂ이 바뀌지 않습니다」를 한 문장으로 묶었습니다.
    - ★낱말은 그 쪽이 이미 쓰는 것만 — 「규칙 동사」는 그 파일 핵심 문법 표의 말입니다. 새 용어를 만들지 않았습니다.
    - 파이프라인 `_도구/gb_ko_pair.py`. 영어 문장만 있는 상자는 **8곳 → 5곳**이 되었고, 남은 다섯은 오탐 둘(「볼까요?」로 끝나는 문장 · 상자 속 규칙 표)과 아래 §7-14 입니다.

13. ~~**알약·그러데이션이 짙은 표 여덟**~~ — **끝났습니다 (2026-08-20 사용자 지시, `0981bc6`·`a52754f`).**
    - 시안: 419~435 종별사 표의 그러데이션 알약 **13개** → `<span class="accent">명</span>`. 표 마크업 **6,054자 → 4,104자**. 같은 표 「예문」 칸이 이미 `.accent` 로 종별사를 짚고 있어 한 표 안에서 같은 것이 같은 모양이 됩니다. 파이프라인 `_도구/pill_strip_419.py`.
    - 나머지 여섯 파일 **28개**도 걷었습니다. 책 전체에 같은 꼴 알약은 **0개**입니다(모두 41개).
    - ★★**알약이라고 다 같은 것을 가리키지 않습니다** — 어제 관형어 표가 가른 기준대로 갈랐습니다.
      **형태·어미·조사·자모 → `.accent`**(436~440 어미 넷 + 자모 ㄴ·ㅂ·ㅅ · 489~500 조사 넷) · **이름·분류·명사 → `<strong>`**(408~413 의미 갈래 여섯 · 414~418 시간 명사 넷 · 462~473 문장 종류 다섯 · 370~383 범위 갈래 둘).
    - ★두 번 놓칠 뻔했습니다 — 글자 크기가 **0.9rem · 0.92rem** 두 가지였고, 알약 **속에 `<strong>` 이 든 것**(자모 셋)이 있었습니다. 그물을 넓히고 「남은 알약 0」을 세어 확인했습니다.
    - 파이프라인 `_도구/pill_strip.py` — 두 번 돌려도 되게 이미 걷은 파일은 건너뜁니다.
    - ★**남겨 둔 것 하나**: 370~383 의 「관계」 표는 알약이 아니라 **42×42 동그라미 안의 `=` · `&gt;` · `&lt;`** 라 글자로 바꾸면 그림이 사라집니다. 손대려면 따로 정하십시오. 책에서 표 안 그러데이션이 셋 이상인 표는 **이제 이 하나뿐**입니다.
    - 어제처럼 **한 표를 시안으로 만들어 보이고** 정합니다(관형어 표는 3,848자 → 1,790자였습니다).

14. ~~**개조식으로 적힌 설명 셋**~~ — **끝났습니다 (2026-08-20 사용자 지시, `683f1d5`).** 문장으로 고쳤습니다.
    - 「~부터만 쓰면 시작을 말하고 끝은 말하지 않습니다」 · 「~까지만 쓰면 끝만 말합니다」 · 「모음을 고르는 방법은 ~아/어서와 같습니다」·「강조할 때는 아무리와 함께 씁니다」.
    - ★「~까지」 줄은 **한국어 문장 안에 영어(until / up to)를 품고** 있었습니다 — 함께 없앴습니다. 「룰」도 화면 글자에서 0곳이 되었습니다.
    - `3 300~306` 「~부터 = ~부터 시작해서 (끝점 없이 출발만 명시).」 · 「~까지 = until / up to (끝점만 명시).」
    - `3 362~365` 「~아/어서와 같은 모음 조화 **룰**: ㅏ/ㅗ → 아도 …」 — 「룰」은 이 책이 쓰는 말이 아닙니다(책은 「규칙」).
    - 문장으로 고쳐 쓰면 되지만 **한국어를 새로 쓰는 일**이라 사용자 확인 뒤에 하십시오.

> ★ **부록 일은 뒤로 미룹니다 — 2026-08-10 사용자 결정.** 부록 2(§7-3)는 그대로 두기로 끝났고, 부록 8(§7-4)도 지금 하지 않습니다. **부록부터 하자고 먼저 꺼내지 마십시오.**
>
> **2026-08-20 기준 살아 있는 것은 §7-1 다국어 2층 · §7-8 사전 뼈대 재생성 · §7-13 도 끝났습니다(370~383 동그라미는 모양을 두고 바탕만 한 색으로), 그리고 아래의 「쪽마다 훑어 고치기」입니다.** §7-2·§7-6·§7-7·플래시카드는 모두 끝났습니다(소망 예문은 「하지 않기로」 끝난 것입니다). §7-1 은 **완전 검수가 끝나야** 손댑니다(§7-1 의 ★★).
>
> **지금 하고 있는 일은 「쪽마다 훑어 고치기」입니다.** 사용자가 화면을 보며 스크린샷 + 짧은 지시로 한 자리씩 짚어 줍니다.
> - 08-16~17 서술어 문법 **부정(123~141) · 희망(142~149) · 소망(150~156) · 계획(157~163) · 결정(164~168)**
> - 08-18 **는데(366~369) · 허락 금지 의무 면제(169~180) · 부탁(181~189) · 위해서(332~337)**
> - 08-19 **변화 · 공감(218~222) · 정중표현(237~247) · 존댓말(489~500) · 관형어(474~488) · 041~050 · 087~096 · 105~114** (마지막으로 보신 쪽이 **105~114 능력 가능**입니다)
>
> - 08-20 **규칙으로 가를 수 있는 것은 100~500 전수로** — 색 · 받침 X·O · 사다리 구멍. 쪽을 눈으로 본 것은 아닙니다(그것은 아래 ★ 그대로입니다).
>
> ★ **차례대로 가지 않습니다** — 사용자가 그날 보는 쪽을 짚어 줍니다. 이 문서가 08-18에 적어 둔 「이어서 하면 190~198 경험부터」는 **아직 안 했습니다**(08-19에 190~198 을 건드린 것은 해설 칸 규칙 쓸기 한 곳뿐입니다). **다음 쪽은 사용자에게 물어서 정하십시오.**
>
> ★ **이 훑기는 끝나지 않았습니다.** 지금까지 사용자가 짚어 준 자리만 고쳤고, 아직 화면으로 보지 않은 쪽이 많습니다. 「다 고쳤다」고 적지 마십시오 — **검사기가 0이라는 것과 쪽을 다 보았다는 것은 다릅니다.**
>
> ★ **한 자리를 짚어 주시면 같은 꼴을 전수로 찾아 함께 고칩니다** — 08-19에 되풀이해 그렇게 했습니다(45만 말씀하셨으나 48도 같은 되풀이 · 46만 말씀하셨으나 같은 꼴 17곳을 목록으로 알림 · 474만 짚으셨으나 475·476도 같은 꼴). **다만 판단이 갈리는 것은 고치지 말고 목록으로 알릴 것.**
>
> §7의 6월 항목(4·5)은 이번 세션에서 확인하지 않았습니다. 6월 기록 그대로 옮긴 것입니다.

---

## 8. 사용자와 일하는 방식

- 사용자는 **화면 스크린샷 + 짧은 지시**로 일을 줍니다. 브라우저로 렌더 확인하며 진행하십시오.
- ★ **브라우저 캐시** — 사용자가 "안 고쳐졌다"고 할 때, 먼저 디스크 상태를 확인하십시오. 2026-08-10에 이미 지운 문장이 화면에 남아 있었습니다. `Ctrl + Shift + R`.
- ★ **기능·규칙 지시는 작업 중인 파일만이 아니라 이미 만든 파일 전부에** 적용하십시오. "현재형에도 적용하라"는 말을 듣기 전에 하십시오.
- ★ **"~가 없습니다"라고 단정하지 마십시오.** 못 찾은 것일 때가 많습니다. 세어야 할 것은 파일에서 세십시오.
- ★ **확인한 것과 안 한 것을 갈라 적으십시오.** 훑어본 기억을 사실처럼 문서에 적지 마십시오.
- 대량 작업은 멀티에이전트 워크플로가 효율적입니다. 단 **사용자가 명시적으로 동의해야** 씁니다.
- 인수인계를 갱신할 때는 **이 문서 + `_claude_메모리_백업\` 사본** 둘을 함께 갱신하십시오.

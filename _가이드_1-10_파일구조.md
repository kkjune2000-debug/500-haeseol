# `놀라운 한국어 500 해설집` — 학습 인터페이스 패턴 가이드

이 문서는 `1~10 문장구조 주어 형용사.html`을 기준으로 정립된 학습 인터페이스 패턴을 정리한 것이다. 이후 같은 형식의 파일(`11~20`, `21~30`, … `79~86`)에 동일한 패턴을 일관되게 적용하기 위한 종합 레퍼런스다.

> **2026-05-05 (v9.77) 갱신**: 1~500 시리즈 전체에 4 카테고리 디자인 시스템 적용 완료. 본 가이드의 §1·§4-4·§6·§7·§10·§12·부록은 v9.74-v9.77 신규 표준을 반영함. 단원별 12종 색상은 4 카테고리(Sapphire/Violet/Emerald/Amber)로 통합되었고, h1 디자인은 `<header class="title-block {category}">` 구조로 교체됨. 상세 변경 이력은 §12, 세션 요약은 `_작업_로그_2026-05-05.md` Part 46.

---

## 목차

1. [페이지 전체 구성](#1-페이지-전체-구성)
2. [인터랙티브 요소](#2-인터랙티브-요소)
3. [문제 블록 템플릿](#3-문제-블록-템플릿)
4. [CSS 클래스 시스템](#4-css-클래스-시스템)
5. [JavaScript 함수](#5-javascript-함수)
6. [색상 테마 (단원별)](#6-색상-테마-단원별)
7. [Grammar Overview · gstep 카드](#7-grammar-overview--gstep-카드)
8. [Antonym Flashcards (BONUS)](#8-antonym-flashcards-bonus)
9. [다른 파일 적용 체크리스트](#9-다른-파일-적용-체크리스트)
10. [자동화 스크립트](#10-자동화-스크립트)
11. [디자인 결정](#11-디자인-결정)
12. [변경 이력](#12-변경-이력)

---

## 1. 페이지 전체 구성

상단부터 하단 순서:

| 섹션 | 역할 |
|---|---|
| **`<header class="title-block {category}">` (v9.74+)** | 단원 제목 블록 — 3-tier 위계 (LARGE 카테고리 라벨 + 부제목 + 구분선 + 한국어). 4 카테고리 그라데이션 팔레트 자동 적용. 자세한 구조는 §1-1 참조 |
| **Grammar Overview · 문법 정리** *(필요 시)* | `gstep` 카드 + `qword-card`로 단원 핵심 문법 시각화 (§7 참조 — v9.76 refined 디자인) |
| **The Basic Pattern** *(1-10만)* | 영문/한글 설명 + `.pattern` 블록 (주어부 + 서술부 시각화) |
| **Practice: Make Korean sentences!** | 8~10개 문제 블록 (§3 참조) |
| **BONUS** *(1-10만)* | 형용사 반대말 표(`.antonym-table`) + Antonym 플래시카드(§8 참조) |
| **1. Read Along** | `.sentence-list` — 영어/한글 + 🔊 발음 버튼 |
| **2. Look & Speak** | `.flashcard-grid` — 클릭 시 뒤집히는 카드 (앞: 영어, 뒤: 한글 + 🔊) |
| **3. Writing Test** | `.wt-toolbar` (점수·초기화·채점·정답) + `.writing-practice` (문항 입력) |
| `<footer>` | 단원명 표시 |
| `<script>` | `speakKorean()` 음성 합성 + `wtCheck/Score/Reset/Reveal/Grade` + `swtReveal/swtSpeak` |

**삭제 대상** (모든 파일 공통):
- `<h2>What is new in Structure N?</h2>` 류 인트로 (정보는 해설로 통합됨)
- `<h2>New grammar: ...</h2>` 류 부제 섹션
- `<h2>Word order ...</h2>`, `<h2>How sentences grow ...</h2>` 보조 시각화
- `<h2>Complete Summary</h2>` 전체 요약 표
- `<h2>Key Rules to Remember</h2>` 핵심 규칙
- 문항 사이의 `<div class="note-box">` (모두 해설 박스로 이동)

### 1-1. Title Block (v9.74+, 모든 67개 파일 적용 완료)

```html
<header class="title-block predicate">  <!-- {category} = sentence | predicate | adverbial | other -->
    <div class="title-en">
        <h1 class="title-main">Predicate Grammar 5</h1>          <!-- LARGE 2.4rem 그라데이션 텍스트 -->
        <p class="title-sub">Negation (Don't, Can't)</p>          <!-- 1.2rem 단색 부제 -->
    </div>
    <div class="title-divider"></div>                              <!-- 3px 카테고리 그라데이션 띠 -->
    <div class="title-ko">
        <p class="title-ko-main">서술어 문법 5: 부정</p>             <!-- 1.02rem 한국어 메인 -->
        <p class="title-ko-sub">안 (don't), 못 (can't), ~지 않다</p>  <!-- 0.9rem 한국어 부설명 -->
    </div>
</header>
```

**3-tier 위계**:
1. **Tier 1** (LARGE, 2.4rem): 단원 카테고리 라벨 (예: `Predicate Grammar 5`) — 그라데이션 텍스트
2. **Tier 2** (sub, 1.2rem): 단원 부제목 (예: `Negation (Don't, Can't)`) — 단색
3. **Tier 3** (구분선 아래): 한국어 정보 (메인 + 부설명) — 회색조

**Legacy `h1` / `.h1-line2` / `h1 + .subtitle` CSS 룰은 보존**되며, `.title-en .title-main` (specificity 0,2,0)이 `h1` (0,0,1)을 안전 override.

**자동화**: `_apply_title_block.py` (1~500 일괄 적용 완료, 멱등성 보장).

---

## 2. 인터랙티브 요소

| 동작 | 트리거 | 결과 |
|---|---|---|
| 🔊 한국어 발음 듣기 | `.speak-btn` / `.sentence-listen` / `.flashcard-listen` 클릭 | `speakKorean(text)` 호출, ko-KR 음성 합성 |
| 📚 단어 보기 | `.vocab-toggle` 클릭 | 다음 형제 `.vocab-box`에 `.shown` 토글 |
| ✍️ Writing Test 실시간 채점 | `.single-wt` 안의 `<textarea>` 입력 | `wtCheck()` 호출, 정답이면 초록·틀리면 빨강 강조 |
| 💡 정답 (개별 문항) | `.single-wt-reveal` 클릭 | `swtReveal(btn)` — 해당 입력칸만 정답 채움 |
| 🔊 정답 발음 듣기 | `.single-wt-speak` 클릭 | `swtSpeak(btn)` — `data-ans` 값을 `speakKorean`으로 발음 |
| Show Answer | `.answer-toggle` 클릭 | 다음 형제 `.answer-content`의 display 토글 |
| 📖 해설 | `.explain-toggle` 클릭 | 다음 형제 `.explain-content`에 `.shown` 토글 |
| 카드 뒤집기 | `.flashcard` 클릭 | `.flipped` 클래스 토글 (3D 회전) |
| 🔄 / ✅ / 💡 (하단 종합 Writing Test) | `.wt-btn` 클릭 | `wtReset()` / `wtGrade()` / `wtReveal()` |

---

## 3. 문제 블록 템플릿

각 번호 1개당 HTML 구조 — 위에서 아래 순서:

```html
<!-- Sentence N -->
<h3>N) English question.</h3>

<button class="vocab-toggle" onclick="this.nextElementSibling.classList.toggle('shown')">📚 New Vocabulary 보기</button>
<div class="vocab-box">
    <div class="vocab-title">New Vocabulary</div>
    word_en = 단어<br>
    ...
</div>

<div class="single-wt">
    <div class="single-wt-header">
        <span class="single-wt-label">✍️ Writing Test</span>
        <div class="single-wt-buttons">
            <button class="single-wt-speak" onclick="swtSpeak(this)" title="Listen">🔊</button>
            <button class="single-wt-reveal" onclick="swtReveal(this)">💡 정답</button>
        </div>
    </div>
    <textarea class="writing-input" data-ans="한국어 정답." oninput="wtCheck(this)" placeholder="Write in Korean..."></textarea>
</div>

<div class="answer-box">
    <button class="answer-toggle" onclick="this.nextElementSibling.style.display=this.nextElementSibling.style.display==='block'?'none':'block'">Show Answer</button>
    <div class="answer-content">
        <div class="ans-kr">한국어 정답. <button class="speak-btn" onclick="speakKorean('한국어 정답.')" title="Listen">🔊</button></div>
    </div>
</div>

<div class="explain-box">
    <button class="explain-toggle" onclick="this.nextElementSibling.classList.toggle('shown')">📖 해설</button>
    <div class="explain-content">
        <table class="exp-table">
            <thead><tr><th style="width:22%">English</th><th style="width:26%">한국어</th><th>해설</th></tr></thead>
            <tbody>
                <tr><td class="exp-role">Subject</td><td class="exp-form"><strong>저는</strong></td><td>'저' (받침 X) + 화제 조사 '는'.</td></tr>
                <tr><td class="exp-role">Time</td><td class="exp-form"><strong>어제</strong></td><td>'어제'는 시간 명사이지만 '에'를 붙이지 않는다 (예외: 어제, 오늘, 내일, ...)</td></tr>
                <tr><td class="exp-role">Place</td><td class="exp-form"><strong>백화점에서</strong></td><td>'백화점' + 장소 조사 '에서' (행동이 일어나는 곳).</td></tr>
                <tr><td class="exp-role">Object</td><td class="exp-form"><strong>선물을</strong></td><td>'선물' (받침 O) + 목적격 조사 '을'.</td></tr>
                <tr><td class="exp-role">Verb</td><td class="exp-form"><strong>샀습니다</strong></td><td>어간이 받침으로 끝남 → 격식체 '~습니다'.</td></tr>
            </tbody>
        </table>
        <p class="explain-tip">💡 핵심 팁 1줄.</p>
        <div class="explain-note">⚠️ Note: 추가 보조 설명 (옵션, 기존 note-box 내용을 여기로).</div>
    </div>
</div>
```

### 해설 표 (3-컬럼 exp-table)
- **Col 1 — English**: 역할 (Subject / Time / Place / Object / Verb / Predicate / Indirect Object / From / Adj 1 (middle) / Clause 2 (final) 등)
- **Col 2 — 한국어**: 최종 한국어 형태 (저는, 어제, 백화점에서, 샀습니다)
- **Col 3 — 해설**: 한국어 프로즈 설명

### 해설 작성 가이드 (모두 한국어 프로즈, 마침표 끝)
- 주어: `'명사' (받침 X/O) + 화제 조사 '는/은'.` 또는 `+ 주격 조사 '이/가'.`
- 시간(예외): `'어제/오늘/내일'은 시간 명사이지만 '에'를 붙이지 않는다 (예외: ...)`
- 시간(일반): `시간 명사 '~' + 시간 조사 '에'.`
- 장소(행동): `'명사' + 장소 조사 '에서' (행동이 일어나는 곳).`
- 장소(이동): `'명사' + 방향 조사 '에' (이동 동사의 도착지).`
- 간접목적어(생물): `'명사' (사람·동물) + 조사 '에게'.`
- 간접목적어(무생물): `'명사' (사물·장소) + 조사 '에'.`
- 목적어: `'명사' (받침 X/O) + 목적격 조사 '를/을'.`
- 동사 격식체: `어간이 받침/모음으로 끝남 → 격식체 '~습니다/~ㅂ니다'.`
- 의문사: `의문사 '누가/언제/어디/무엇'. ~ 자리에 그대로 사용.`
- 불규칙: `ㄷ/ㅂ/르 불규칙 — (변화 규칙 1줄).`

### English ↔ 한국어 역할 매핑 (특수 케이스)

**보조동사(auxiliary) 처리 규칙** — 영어가 `do/does/did + 본동사`처럼 보조동사와 본동사로 분리되어 의문·시제·진행을 표현하는 경우, 보조동사를 별도의 Subject 행에 두지 않고 **본동사와 합쳐 하나의 Verb 행으로 매핑**한다. 주어 행에는 항상 한국어 주어(저는/당신은/그는…)와 의미가 일치하는 영어 명사·대명사만 들어간다.

예시: "How long did you live in Korea?" / "당신은 얼마 동안 한국에서 살았습니까?"

| English | Role | Korean | 비고 |
|---|---|---|---|
| ✅ How long | Duration | 얼마 동안 | |
| ✅ **you** | Subject | **당신은** | "you"가 주어, "did"는 동사의 일부 |
| ✅ **Did ~ live?** | Verb | **살았습니까?** | "Did"+"live"를 합쳐 의문 동사로 |
| ✅ in Korea | Place | 한국에서 | |

**잘못된 매핑 (피할 것):**

| English | Role | Korean |
|---|---|---|
| ❌ did | Subject | 당신은 |
| ❌ you live | Verb | 살았습니까? |

**같은 원칙이 적용되는 영어 패턴:**
- `did + verb` (과거 의문) → 한 Verb 행 (예: Did ~ live? = 살았습니까?)
- `do/does + verb` (현재 의문) → 한 Verb 행 (예: Do ~ go? = 갑니까?)
- `will + verb` (미래) → 한 Verb 행 (예: will go = 갈 것입니다)
- `is/are + ~ing` (진행) → 한 Verb 행 (예: is going = 가고 있습니다)
- `have + p.p.` (완료) → 한 Verb 행 (예: have gone = 갔습니다)

> 💡 영어 보조동사는 한국어 어미(시제·의문)에 흡수되므로, 영어 표현을 분해할 때 보조동사를 따로 떼어 다른 역할로 배정하지 않는다.

---

## 4. CSS 클래스 시스템

### 4-1. 공통 핵심 클래스 (모든 단원 동일)

```css
/* 단어 토글 */
.vocab-toggle { background:<theme>; color:#fff; border:none; padding:8px 18px; border-radius:6px; cursor:pointer; font-size:0.85rem; font-weight:700; margin:12px 0 0 0; transition:background 0.2s; }
.vocab-toggle:hover { background:<theme-darker>; }
.vocab-toggle + .vocab-box { display:none; margin-top:10px; }
.vocab-toggle + .vocab-box.shown { display:block; }

/* 개별 Writing Test (모든 단원 초록 고정) */
.single-wt { background:#f0fdf4; border-radius:10px; padding:14px 18px; margin:12px 0 20px 0; border-left:4px solid #22c55e; }
.single-wt-header { display:flex; align-items:center; justify-content:space-between; gap:10px; margin-bottom:8px; flex-wrap:wrap; }
.single-wt-label { font-size:0.78rem; font-weight:800; color:#166534; text-transform:uppercase; letter-spacing:0.12em; }
.single-wt-buttons { display:flex; gap:8px; align-items:center; flex-wrap:wrap; }
.single-wt-speak { background:#fff; color:#166534; border:1px solid #86efac; padding:6px 10px; border-radius:6px; cursor:pointer; font-size:0.95rem; transition:background 0.2s, transform 0.2s; }
.single-wt-speak:hover { background:#f0fdf4; transform:scale(1.08); }
.single-wt-reveal { background:#16a34a; color:#fff; border:none; padding:6px 14px; border-radius:6px; cursor:pointer; font-size:0.8rem; font-weight:700; transition:background 0.2s; }
.single-wt-reveal:hover { background:#15803d; }
.single-wt textarea.writing-input { background:#fff; min-height:42px; }

/* 해설 (모든 단원 보라 고정) */
.explain-box { margin: -4px 0 18px 0; }
.explain-toggle { background:#7c3aed; color:#fff; border:none; padding:8px 18px; border-radius:6px; cursor:pointer; font-size:0.85rem; font-weight:700; transition:background 0.2s; }
.explain-toggle:hover { background:#6d28d9; }
.explain-content { display:none; margin-top:10px; padding:14px 18px; background:#faf5ff; border-radius:8px; border-left:4px solid #7c3aed; font-size:0.95rem; line-height:1.7; color:#1e293b; }
.explain-content.shown { display:block; }
.explain-content p { margin: 4px 0; }
.explain-content strong { color:#6d28d9; }
.explain-content code { background:transparent; padding:0; font-family:inherit; color:#000; font-size:inherit; font-weight:700; }
.explain-tip { margin-top: 10px !important; padding-top: 8px; border-top: 1px dashed #c4b5fd; color: #6d28d9; }
.explain-note { margin-top: 10px; padding: 10px 14px; background: #fef3c7; border-left: 4px solid #f59e0b; border-radius: 6px; font-size: 0.92rem; line-height: 1.65; color: #78350f; }
.explain-note strong { color: #92400e; }

/* 해설 표 3-컬럼 */
.exp-table { width: 100%; border-collapse: collapse; margin: 6px 0 12px 0; font-size: 0.93rem; table-layout: fixed; }
.exp-table thead th { background: #f5f3ff; color: #6d28d9; font-weight: 800; padding: 8px 10px; text-align: left; border-bottom: 2px solid #c4b5fd; font-size: 0.72rem; letter-spacing: 0.08em; text-transform: uppercase; }
.exp-table tbody td { padding: 7px 10px; border-bottom: 1px solid #ede9fe; vertical-align: top; line-height: 1.6; word-break: keep-all; overflow-wrap: anywhere; }
.exp-table tbody tr:last-child td { border-bottom: none; }
.exp-table tbody tr:nth-child(even) td { background: #faf5ff; }
.exp-table .exp-role { font-weight: 700; color: #6d28d9; width: 22%; word-break: keep-all; }
.exp-table .exp-form { font-weight: 700; color: #1e293b; width: 26%; }

/* 인쇄 모드 */
@media print {
    .vocab-toggle + .vocab-box { display:block !important; }
    .vocab-toggle { display:none; }
    .explain-content { display:block !important; }
    .explain-toggle { display:none; }
}
```

### 4-2. Writing Test 입력 상태
- `.writing-input.correct` — 입력값 = 정답 (초록 보더 + 연한 초록 배경)
- `.writing-input.wrong` — 정답 prefix 아닐 때 (빨간 보더 + 연한 빨강 배경)
- `.writing-input.revealed` — 정답 자동 채움 (단원 테마색 강조)

### 4-3. 플래시카드 (모든 단원 동일)
- `.flashcard { perspective: 1200px; height: 190px; cursor: pointer; }` — **카드 높이 190px**
- `.flashcard-front .flashcard-text { font-size: 1.4rem; font-weight: 800; }`
- `.flashcard-back .flashcard-text { font-size: 1.55rem; font-weight: 900; }`
- `.flashcard-text-en` — 영어 자막 (1-10 antonym 카드용)

### 4-4. Grammar Overview gstep 카드 (단원별 색상 적용)
- `.gstep` — 카드 컨테이너 (단원 테마 보더)
- `.gstep-num` — 좌측 원형 번호 (단원 테마 그라데이션)
- `.gstep-body` — 본문 영역
- `.gstep-pattern` — 문장 구조 박스 (단원 테마)
- `.gstep-pattern .accent` — 강조 텍스트
- `.gstep-table` — 비교 표 (단원 테마)

### 4-5. 의문사 카드 (qword-card, 51-60에서만 사용)
- `.qword-card` — 의문사별 사용법 카드
- `.qword-head` — 카드 헤더
- `.qword-table` — 역할별 형태 표

---

## 5. JavaScript 함수

### 음성 합성 (전역)
```javascript
function speakKorean(text) {
    if (!('speechSynthesis' in window)) return;
    window.speechSynthesis.cancel();
    const u = new SpeechSynthesisUtterance(text);
    u.lang = 'ko-KR'; u.rate = 0.85; u.pitch = 1.1;
    const preferred = ['Yuna','SunHi','Heami','Google 한국의'];
    const voices = window.speechSynthesis.getVoices();
    const koVoices = voices.filter(v => v.lang.startsWith('ko'));
    let picked = null;
    for (const name of preferred) { picked = koVoices.find(v => v.name.includes(name)); if (picked) break; }
    if (!picked && koVoices.length > 0) picked = koVoices[0];
    if (picked) u.voice = picked;
    window.speechSynthesis.speak(u);
}
```

### 개별 문항 (Writing Test 안)
```javascript
function swtReveal(btn) {
    const inp = btn.closest('.single-wt').querySelector('.writing-input');
    inp.value = inp.dataset.ans;
    inp.classList.remove('correct','wrong');
    inp.classList.add('revealed');
}
function swtSpeak(btn) {
    const inp = btn.closest('.single-wt').querySelector('.writing-input');
    if (inp && inp.dataset.ans) speakKorean(inp.dataset.ans);
}
```

### 하단 종합 Writing Test (`.writing-practice` 범위)
- `wtNormalize(s)` — 공백·구두점 제거
- `wtCheck(inp)` — 입력 시마다 채점·강조
- `wtScore()` — `.correct` 개수 카운트
- `wtReset()` — 모든 입력 초기화
- `wtReveal()` — 모든 입력에 정답 자동 채움
- `wtGrade()` — 일괄 채점 + 점수·퍼센트 표시

> ⚠️ `swtReveal/swtSpeak` (개별)과 `wtReveal/wtGrade` (전체)는 다른 함수. 개별 `.single-wt`는 `.writing-practice` 바깥이라 종합 점수에 영향을 주지 않음.

---

## 6. 색상 테마 (v9.74+ 4 카테고리 시스템, 1~500 적용 완료)

**v9.74 이전**까지는 단원별로 12종의 별도 색상 테마(Blue/Green/Amber/Purple/Pink/Teal/Indigo 등)가 사용되었으나, **2026-05-05 (v9.74-v9.77)**에 4 카테고리 통합 디자인 시스템으로 정형화되어 모든 67개 파일에 적용됨. title-block(§1-1)과 refined gstep(§7)이 동일한 카테고리 hue를 공유.

### 6-1. 카테고리 매핑 (파일명 키워드 기반 자동 결정)

| 카테고리 | 클래스 | 그라데이션 (deep → mid → light) | bg_start | 적용 파일 수 |
|----------|--------|-----------------------------|----------|------------:|
| 문장구조 (Sentence Structure) | `.sentence` | `#1e3a8a → #2563eb → #3b82f6` (Sapphire) | `#eff6ff` | 9 |
| 서술어 문법 (Predicate Grammar) | `.predicate` | `#4c1d95 → #6d28d9 → #8b5cf6` (Violet) | `#faf5ff` | 21 |
| 부사어 문법 (Adverbial Grammar) | `.adverbial` | `#064e3b → #047857 → #10b981` (Emerald) | `#ecfdf5` | 18 |
| 기타 문법 (Other Grammar) | `.other` | `#7c2d12 → #b45309 → #f59e0b` (Amber) | `#fffbeb` | 19 |

→ predicate violet은 기존 exp-table 헤더 색(`#6d28d9`)과 일치하여 한국어 학습 영역의 시각 통일성 보장.

### 6-2. 카테고리 적용 범위

| 적용 컴포넌트 | 사용 변수 |
|--------------|-----------|
| `.title-block.{category}` (§1-1) | 4 변수 (`--title-gradient`, `--title-bg-start`, `--title-sub-color`, `--title-ko-main-color`) |
| `.gstep` 카드 시스템 (§7, refined) | 11 색 토큰 (deep/mid/light/bg_start/border/border2/bg_mid/bg_mid2/text_str/text_sm/shadow) |

### 6-3. 카테고리 무관 고정 색상

| 컴포넌트 | 색상 | 의미 |
|---------|------|------|
| `.single-wt` 보더 / `.single-wt-reveal` | 초록 (#22c55e / #16a34a) | "정답·완료" |
| `.explain-toggle` / `.explain-content` | 보라 (#7c3aed / #faf5ff) | "해설" |
| `.explain-note` (글로벌) / `.gstep .explain-note` | 노랑 (#fef3c7 / #fef9c3) | "주의·예외" |
| `.exp-table` 헤더 | 보라계 (#6d28d9) | 한국어 학습 영역 |
| Writing Test 채점 | 정답 초록 / 오답 빨강 / 정답 자동 채움 단원 테마색 | 학습 피드백 |

> 💡 단어/Writing/해설 버튼은 **목적별 고정 색상** (모든 카테고리 동일). 카테고리 색조는 title-block + gstep 카드만 적용.

### 6-4. 단원별 12종 색상 (v9.74 이전, 참고용 — 현재 비사용)

`<details>` <summary>구버전 색상 테마 (펼치기)</summary>

| 단원 | 테마 | Primary | Dark | Light bg |
|---|---|---|---|---|
| **1~10** 형용사 | Blue | `#3b82f6` | `#1e40af` | `#eff6ff` |
| **11~20** 동사 | Green | `#10b981` | `#065f46` | `#f0fdf4` |
| **21~30** 주어+목적어+동사 | Amber | `#f59e0b` | `#92400e` | `#fffbeb` |
| **31~40** 시간+장소+목적어 | Purple | `#8b5cf6` | `#5b21b6` | `#f5f3ff` |
| **41~50** 간접/직접목적어 | Pink | `#ec4899` | `#9d174d` | `#fdf2f8` |
| **51~78** 의문사 시리즈 | Teal | `#14b8a6` | `#0f766e` | `#f0fdfa` |
| **79~86** 접속어 | Indigo | `#6366f1` | `#4338ca` | `#eef2ff` |

</details>

---

## 7. Grammar Overview · gstep 카드 (v9.78, 1~500 전 67개 파일 100% 적용 완료)

모든 단원의 Grammar Overview 섹션은 **v9.76 refined gstep 카드 시스템**으로 시각화. **2026-05-05 v9.78 시점에 1~500 전 67개 파일에 100% 적용 완료** — 카테고리별 그라데이션 + 좌측 액센트 바 + 그라데이션 뱃지 + chip 스타일 accent + refined 콜아웃.

### 7-1. 적용 분포 (67개 파일, 카테고리별)

| 카테고리 | 클래스 | 파일 수 | gstep 카드 수 |
|----------|--------|--------:|--------------:|
| sentence | `.sentence` | 9 | 1~5 per file |
| predicate | `.predicate` | 21 | 1~5 per file |
| adverbial | `.adverbial` | 18 | 1~5 per file |
| other | `.other` | 19 | 보통 1 (단일 래핑 카드, v9.78) |
| **합계** | | **67** | |

→ 적용 경로 2가지:
- **기존 gstep HTML 보유 (31개 파일)**: `_apply_refined_gstep.py`로 CSS swap만 (HTML 무변경)
- **gstep HTML 미보유 (35개 파일, v9.78)**: `_wrap_gstep_to_all.py`로 Grammar Overview 콘텐츠를 단일 gstep 카드로 자동 래핑 + CSS 삽입

### 7-2. gstep 카드 HTML 구조 (v9.76 — HTML 무변경, 기존 구조 그대로)

```html
<div class="gstep">
    <div class="gstep-num">1</div>                                  <!-- 60px 그라데이션 원 -->
    <div class="gstep-body">
        <p class="en"><strong>Concept</strong> in English...</p>     <!-- 1.02rem · 진한색 · strong=카테고리 deep 색 -->
        <p class="ko"><strong>개념</strong>은 한국어로 ...</p>         <!-- 0.94rem · 회색조 · strong=카테고리 mid 색 -->
        <div class="gstep-pattern">                                  <!-- 그라데이션 박스 -->
            동사 어간 + <span class="accent">고 있다</span>            <!-- 흰 chip + 그림자 -->
            <small>Verb stem + 고 있다 → "be V-ing"</small>
        </div>
        <blockquote>                                                 <!-- 흰 카드 + 카테고리 좌측 보더 -->
            I <strong>am going</strong> → 저는 <strong>가고 있습니다</strong>.
        </blockquote>
        <table class="gstep-table">                                  <!-- 둥근 카드 셀 -->
            <thead><tr><th>Pattern</th><th>Form</th></tr></thead>
            <tbody>
                <tr><td><strong>'~고 있다'</strong></td>
                    <td>verb stem + <span class="accent">고 있습니다</span></td></tr>
            </tbody>
        </table>
        <div class="explain-note">                                   <!-- 노란 그라데이션 콜아웃 -->
            <strong>⚠️ Edge case:</strong> ...
        </div>
    </div>
</div>
```

### 7-3. v9.76 refined 디자인 7개 요소

| 요소 | 디자인 |
|------|--------|
| `.gstep` 카드 | 그라데이션 bg(`{bg_start} → #fff`) + 좌측 4px 액센트 바(카테고리 그라데이션, `::before`) + hover lift(`translateY(-1px)`) + 라운드 18px |
| `.gstep-num` 뱃지 | 60px 그라데이션 원 + 이중 그림자(외부 + 내부 inset) + 1.5rem 폰트 |
| `.gstep-body p.en` | 1.02rem · `#1e293b` · `<strong>` = 카테고리 deep 색 |
| `.gstep-body p.ko` | 0.94rem · `#475569` · 회색조 · `<strong>` = 카테고리 text_str 색 |
| `.gstep-pattern` | 그라데이션 박스(`{bg_mid} → {bg_start}`) + `.accent` 흰 chip + 그림자 |
| `.gstep blockquote` | 흰 카드 + `{light}` 좌측 보더 + `<strong>` mid 색 — 글로벌 blockquote override |
| `.gstep-table` | 둥근 카드 셀 + thead 그라데이션 + uppercase 캡션 + accent chip · 짝수 row tint |
| `.gstep .explain-note` | 노란 그라데이션 콜아웃(`#fef9c3 → #fefce8`) + 깊은 라운드 — 카테고리 무관 고정 노랑 |

**HTML 무변경**: 기존 `<div class="gstep">` 구조 그대로 reused, CSS만 swap. `_apply_refined_gstep.py` 자동화 스크립트 사용.

### qword-card (의문사 카드, 51-60 전용)
```html
<div class="qword-card">
    <div class="qword-head">1) <strong>누구</strong> · who</div>
    <table class="qword-table">
        <thead><tr><th>Role · 역할</th><th>Form</th></tr></thead>
        <tbody>
            <tr><td>Subject (주어)</td><td>누구 + 가 = <strong>누가</strong> (축약)</td></tr>
            ...
        </tbody>
    </table>
    <div class="gstep-pattern">주어(<span class="accent">누가</span>) + 시간 + 장소 + 목적어(<span class="accent">누구를</span>) + 서술어(<span class="accent">누구입니까?</span>)<small>Subject(누가) + Time + Place + Object(누구를) + Predicate(누구입니까?)</small></div>
</div>
```

---

## 8. Antonym Flashcards (BONUS)

**1-10 파일 전용**. BONUS 섹션의 형용사 반대말 표(`.antonym-table`) 다음에 10쌍 플래시카드 추가.

### 구조
```html
<div class="antonym-flashcards">
    <div class="antonym-flashcards-title">🃏 Antonym Flashcards · 반대말 카드 (탭하여 뒤집기)</div>
    <div class="flashcard-grid">
        <div class="flashcard" onclick="this.classList.toggle('flipped')">
            <div class="flashcard-inner">
                <div class="flashcard-front">
                    <div class="flashcard-num">01 · 형용사</div>
                    <div class="flashcard-text">크다</div>
                    <div class="flashcard-text-en">big / large</div>
                    <button class="flashcard-listen" onclick="event.stopPropagation(); speakKorean('크다')" title="Listen">🔊</button>
                    <div class="flashcard-hint">Tap to flip</div>
                </div>
                <div class="flashcard-back">
                    <div class="flashcard-num">01 · 반대말</div>
                    <div class="flashcard-text">작다</div>
                    <div class="flashcard-text-en">small</div>
                    <button class="flashcard-listen" onclick="event.stopPropagation(); speakKorean('작다')" title="Listen">🔊</button>
                </div>
            </div>
        </div>
        ...
    </div>
</div>
```

### 양면 발음 버튼 위치
- 앞면 (흰 배경): 🔊 버튼은 좌측 하단 (`left:10px`, 파란 배경 + 진한 파란 글씨)
- 뒷면 (그라데이션 배경): 🔊 버튼은 우측 하단 (기본 위치, 흰색)
- `.flashcard-front .flashcard-listen { background:#eff6ff; color:#1e40af; left:10px; right:auto; }`

---

## 9. 다른 파일 적용 체크리스트

기존 파일에 패턴을 옮길 때 **반드시** 수행할 항목:

### A. CSS 추가 (`</style>` 직전)
1. `.vocab-toggle`, `.vocab-toggle + .vocab-box[, .shown]`
2. `.single-wt`, `.single-wt-header`, `.single-wt-buttons`, `.single-wt-label`, `.single-wt-speak[:hover]`, `.single-wt-reveal[:hover]`, `.single-wt textarea.writing-input`
3. `.explain-box`, `.explain-toggle[:hover]`, `.explain-content[, .shown, p, strong, code]`, `.explain-tip`, `.explain-note[, strong]`
4. `.exp-table`, `.exp-table thead th`, `.exp-table tbody td`, `.exp-table .exp-role`, `.exp-table .exp-form`
5. `@media print` 분기 — vocab/explain 토글 펼침
6. (옵션) Grammar Overview 사용 시 `.gstep[-num,-body,-pattern,-table]` 추가

### B. JS 추가 (`<script>` 안, `wtCheck` 정의 부근)
- `swtReveal(btn)` 함수
- `swtSpeak(btn)` 함수

### C. 각 문제 블록 변환 (h3 직후 순서대로)
1. `<button class="vocab-toggle">📚 New Vocabulary 보기</button>` 삽입
2. `<div class="single-wt">` 삽입 (해당 문항 정답을 `data-ans`에 입력 + 🔊 + 💡 정답 버튼)
3. `<div class="answer-box">` 그대로 유지
4. `<div class="explain-box">` 삽입 (3-컬럼 `exp-table` + `explain-tip` + 옵션 `explain-note`)

### D. 일괄 정리
1. **placeholder 통일** — `placeholder="Write in Korean..."` (한국어 표시 금지)
2. **해설은 한국어 프로즈** — §3 해설 작성 가이드 따름
3. **중복 섹션 제거**:
   - 인트로 류 (`What is new`, `New grammar:`, `Word order`, `How sentences grow`, `About '...'`)
   - `Complete Summary`, `Key Rules to Remember` (Practice 이후)
4. **note-box 이동** — 문항 사이 모든 `.note-box`를 해당 `.explain-content` 안의 `.explain-note`로 이동
5. **중복 `<hr>` 정리** — 인접한 `<hr>` 두 개는 하나로 합침
6. **h1 줄바꿈** — 길면 `<h1>Title:<br><span class="h1-line2">long part</span></h1>`

### E. 단원 핵심 문법 보존 (필요 시)
- 인트로 섹션을 단순 삭제하지 않고 **`gstep` 카드의 "Grammar Overview · 문법 정리"** 1개로 재구성

---

## 10. 자동화 스크립트

`d:\놀라운 한국어 2026\` 루트에 변환 스크립트가 있다. 새 파일에 일괄 적용 시 사용:

### 10-1. 신규 v9.74-v9.78 스크립트 (1~500 전체 대상, 멱등성·dry-run·상대 경로)

| 스크립트 | 역할 | 대상 |
|---|---|---|
| **`_apply_title_block.py`** (v9.75) | 5종 h1+subtitle 패턴 통합 파서 → `<header class="title-block {category}">` 11라인 블록 치환 | 67개 파일 (1~500 전체) |
| **`_apply_refined_gstep.py`** (v9.77) | Legacy gstep CSS 블록 정규식 swap → 4 카테고리 팔레트(11개 색 토큰) refined CSS 32라인 | 31개 파일 (기존 gstep HTML 보유) |
| **`_wrap_gstep_to_all.py`** (v9.78) | Grammar Overview 콘텐츠 자동 래핑 → 단일 `<div class="gstep">` 카드 + refined CSS 삽입 | 35개 파일 (gstep HTML 미보유) |
| **`_apply_universal_5_standards.py`** (v9.79) | 5종 보편 표준 자동 적용 (§4-2/§4-3 + Subgrid + `--em-rows` + 모바일 + HTML 평탄화 + Role parens 삭제) | 12개 서술어 문법 파일 (exp-table 보유) |
| **`_bold_quoted_text.py`** (v9.84) | 본문 텍스트의 `"X"` → `<strong>X</strong>` (따옴표 제거 + 굵게, HTML 속성·CSS·JS·`<title>` 안전 skip, idempotent) | 67개 전체 파일 |
| **`_strip_quotes_around_bold.py`** (v9.84) | `"<strong>X</strong>"` → `<strong>X</strong>` 따옴표만 제거 (v9.83 → v9.84 마이그레이션 도구) | 일회성 마이그레이션 |
| **`_strip_korean_translation.py`** (v9.85) | exp-table explanation td의 `<br>` + Korean translation 라인 제거 (영어 단일화, idempotent) | exp-table 보유 ~20개 파일 |
| **`_apply_v9_87_rules.py`** (v9.88) | exp-table 헤더 영어화 (`Korean structure:` → `English order: ... — Korean: KO1 + KO2 + ...`) + tilde 제거 + 주어 생략 표기 통일 | 29개 파일 (문장구조 9 + 서술어 문법 20) |
| **`_bold_korean_in_explanations.py`** (v9.89) | exp-table 해설 컬럼 안의 모든 한국어 토큰(음절·자모·어휘·형태소·구) 자동 `<strong>` (placeholder substitution + idempotent) | 67개 파일 (전 카테고리) |

두 스크립트 공통:
- `python {script}` 또는 `python {script} --dry-run` 또는 `python {script} --only PATTERN`
- 파일명 키워드(`문장구조`/`서술어 문법`/`부사어 문법`/`기타 문법`)로 카테고리 자동 추론
- 멱등성: 이미 적용된 파일은 SKIP
- 상대 경로(`Path(__file__).parent / "놀라운 한국어 500 해설집"`) — 다른 머신 이식 가능
- UTF-8 안전 I/O (콘솔 mojibake와 무관)

### 10-2. 구버전 v8 (1~86 양국어 표준) 스크립트

| 스크립트 | 역할 |
|---|---|
| `_bulk_update_documentation.py` | colgroup·헤더·CSS·explain-tip 일괄 갱신 |
| `_bilingual_structure_header.py` | 한국어 어순 헤더 양국어 변환 (60+ 치환 규칙) |
| `_apply_remaining_standards.py` | gstep 헤더·예시 일괄 정리 |

### 10-3. 구버전 v5-v7 (해설 박스·플래시카드) 스크립트

| 스크립트 | 역할 |
|---|---|
| `_transform_explain_to_table.py` | 해설의 `<p><strong>Role</strong>: Y</p>` → 2-col `exp-table` 변환 |
| `_transform_to_3col_table.py` | 2-col → 3-col (English/한국어/해설) + 한국어 프로즈 자동 생성 |
| `_update_code_style.py` | `.explain-content code` 스타일을 흰 배경 → 투명, 모노스페이스 → 본문 폰트, 검은색 굵게로 일괄 변경 |
| `_fix_table_overlap.py` | 표 컬럼 너비 조정 (16%/28% → 22%/26%, `nowrap` 제거 + `word-break: keep-all`) |
| `_add_swt_speak_button.py` | Writing Test 헤더에 🔊 버튼 + `swtSpeak` JS + CSS 일괄 추가 |
| `_move_notes_to_explain.py` | 문항 사이의 `.note-box`를 해당 `.explain-content`의 `.explain-note`로 이동 |
| `_update_flashcard_font.py` | 플래시카드 폰트 크기·굵기 증가 + 카드 높이 +20px |

> ⚠️ **주의**: v5-v7 / v8 스크립트는 `FOLDER` 변수가 다른 머신의 절대 경로(`C:\Users\김광준\...`)로 하드코드되어 있음 — 실행 시 경로 수정 필요. v9.74+ 신규 스크립트는 상대 경로 사용으로 이 문제 해결됨.

---

## 11. 디자인 결정

| 결정 | 이유 |
|---|---|
| **placeholder 영어** | 한국어 학습자는 한국어 안내문을 읽기 어렵다 → 모든 입력 안내 영어 |
| **해설 한국어 프로즈** | 학생들이 자기 모국어가 아닌 한국어로 된 분석을 보면서 자연스럽게 노출. 단, 본문 용어(받침, 어간, 은/는, 이/가, 해요체 등)는 그대로 학습 대상 |
| **Writing Test 위치** | 단어 → Writing Test → Show Answer → 해설 순. 시도 → 답 확인 → 문법 이해의 흐름 |
| **단어 박스 기본 숨김** | 모르는 단어 노출로 답을 추측하게 하는 것 방지. 필요 시 펼침 |
| **개별 vs 종합 Writing Test 분리** | `.single-wt`는 `.writing-practice` 바깥이라 두 시스템 독립 동작 |
| **인쇄 시 자동 펼침** | 종이 학습지로 출력 시 모든 답·해설·단어가 보이도록 — 채점된 답안지/교사용 자료 겸용 |
| **해설 표 3-컬럼** | 한 줄 텍스트보다 표가 한눈에 비교됨. 영어(역할) → 한국어(형태) → 해설(이유) 흐름 |
| **note-box 해설 안으로 이동** | 본문에 노란 박스가 끼어들면 산만함. 해설 박스 안에 모아 토글 시 함께 펼침 |
| **인라인 `<code>` 검은 굵게** | 보라 본문(#1e293b/#6d28d9) 사이에서 검은색 굵은 글씨로 시각적 구별. 흰 배경 + 모노스페이스는 산만 |
| **플래시카드 키움** | 1.2rem/1.3rem → 1.4rem/1.55rem, 굵기 700→800/900. 한국어 정답이 명확하게 강조됨 |
| **Complete Summary / Key Rules 삭제** | 모든 핵심이 해설 박스에 통합됨. Read Along·Look & Speak·Writing Test가 이미 시각적/음성 요약 |
| **인트로 문법 섹션 삭제** | 동일 사유. 단원 핵심은 `gstep` 카드 1개로 재구성 |
| **h1 길면 줄바꿈** | 영어 단어가 임의 위치에서 잘리는 것 방지 (예: "Indirect Obj | + Direct Obj") |

---

## 12. 변경 이력

### v1: 1-10 패턴 정립 (원본)
- vocab → answer 단순 구조
- BONUS 섹션 (형용사 반대말 표)

### v2: Writing Test + 해설 도입
- `.single-wt` 추가 (각 문항 입력칸)
- `.explain-box` 추가 (📖 해설 토글)
- 단어를 `.vocab-toggle` 뒤로 숨김
- placeholder를 영어로 통일

### v3: 11-86 일괄 적용 (`_가이드_1-10_파일구조.md` 작성)
- 11-20, 21-30, 31-40, 41-50 패턴 적용
- 51-60, 61-70, 71-78 의문사 시리즈 적용
- 79-86 접속어 적용
- 단원별 색상 테마 정립

### v4: 인트로/요약 섹션 정리
- "Complete Summary", "Key Rules to Remember" 일괄 삭제
- "What is new", "New grammar" 류 인트로 삭제
- 31-40, 41-50, 51-60, 79-86: Grammar Overview gstep 카드로 재구성
- h1 길면 줄바꿈 규칙 (`<br><span class="h1-line2">`)

### v5: 해설 박스 시각화 강화
- 2-컬럼 `exp-table` 도입 (자동 변환 스크립트로 일괄)
- 3-컬럼으로 확장 (English / 한국어 / 해설) + 한국어 프로즈 자동 생성
- 인라인 `<code>` 흰 배경 + 모노스페이스 → 투명 + 본문 폰트 + 검은 굵게
- 표 컬럼 너비 조정 (긴 역할명 겹침 방지)

### v6: 발음 + 노트 통합
- ✍️ Writing Test 헤더에 🔊 버튼 추가 (`swtSpeak`)
- `.note-box` 모두 `.explain-note`로 이동 (해설 안으로 통합)

### v7: 플래시카드 강화
- 카드 높이 170 → 190px
- Front 폰트 1.2rem/700 → 1.4rem/800
- Back 폰트 1.3rem/700 → 1.55rem/900
- 1-10 BONUS에 Antonym Flashcards 10쌍 추가 (양면 🔊 버튼)

### v8 (2026-05-02·05-04): 양국어 표준 — 87~212 일괄 확산
- exp-table colgroup 20/12/18/50 표준
- 헤더 stack `English<br>영어` 양국어 표기
- 어순 헤더 양국어: `Korean structure: ...<br>한국어 어순: ...`
- `.exp-en`/`.exp-role` 에 `overflow-wrap: normal; hyphens: none` (Korean 토큰 분절 방지)
- §4-2 (exp-table 너비 미세조정) + §4-3 (4컬럼 가운데 정렬) + §4-24 (noun+하다 통합) 도입
- em-game CSS Subgrid 행 정렬 + `--em-rows` 변수화
- Predicate Grammar / Adverbial Grammar / Other Grammar 영문 라벨 일괄 적용

### v9.73-v9.74 (2026-05-05 시범): 5종 표준 + 제목 블록 시범 (123~141)
- 보편 표준 5종(정렬·Subgrid·Role 단순화) 단일 파일 검증
- `<header class="title-block {category}">` 신규 디자인 시범 — 4 카테고리 팔레트 정의

### v9.75 (2026-05-05): 제목 블록 1~500 전 단원 일괄 적용 (67개 파일)
- 자동화 스크립트 `_apply_title_block.py` (5종 h1 패턴 통합 파서, 멱등성, 상대 경로)
- 카테고리: sentence(9) + predicate(21) + adverbial(18) + other(19) = 67
- Legacy `h1`/`.h1-line2`/`h1 + .subtitle` CSS 룰 보존 (비파괴)
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 42, 43

### v9.76 (2026-05-05 시범): refined gstep 디자인 (97~104)
- 7개 요소 개편 (`.gstep` 카드 + 좌측 액센트 바 + 60px 그라데이션 뱃지 + chip accent + refined 콜아웃)
- predicate violet 팔레트 시범 — exp-table 보라(`#6d28d9`)와 일치
- HTML 무변경 — CSS-only swap (자동화 친화)

### v9.77 (2026-05-05): refined gstep 31개 파일 일괄 적용
- 자동화 스크립트 `_apply_refined_gstep.py` (4 카테고리 팔레트 매핑, 11개 색 토큰)
- 적용: sentence(4) + predicate(16) + adverbial(11) = 31 (기존 gstep 사용 파일 100%)
- 미적용 36개(평문 또는 condensed 구조)는 v9.78에서 처리
- title-block과 카테고리 hue 통일 → 페이지 진입 즉시 카테고리·번호·핵심 패턴 시각 식별
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 44, 45, 46

### v9.78 (2026-05-05): 비-gstep 35개 파일 자동 래핑 — 1~500 100% 달성
- 자동화 스크립트 `_wrap_gstep_to_all.py` (Grammar Overview 콘텐츠를 단일 gstep 카드로 래핑 + CSS 삽입)
- 적용: sentence(5) + predicate(5) + adverbial(8) + other(18) = 36 (= 35 신규 + 1 사전 테스트 1~10)
- 1~500 전 67개 파일이 모두 `class="gstep"` HTML + refined CSS 보유
- 본문 콘텐츠 의미 변화 0건 (래핑은 텍스트 보존, 컨테이너만 추가)
- v9.79+ 후순위: 일부 복잡 섹션(예: 489~500 3 하위 섹션) multi-step 분할
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 47

### v9.79 (2026-05-05): 5종 보편 표준 — 서술어 문법 시리즈 exp-table 보유 15/15 도달
- 자동화 스크립트 `_apply_universal_5_standards.py` (5개 표준 독립 멱등 검출, 부분 적용 파일도 누락만 보강)
- 5종 표준: §4-2/§4-3 (exp-table 정렬·너비) + em-game CSS Subgrid + `--em-rows` + 모바일 + HTML 평탄화 + exp-role 괄호 부설명 삭제
- 적용 분포: 신규 12개(87~96·105~114 부분 + 142~212 9개 5종 전체) + 이미 완료 3개(97~104, 115~122, 123~141)
- SKIP 5개(213~217 ~ 237~247): exp-table 부재 → 별도 작업 필요
- 부사어·문장구조·기타 카테고리에 확산 시 색 변수화(predicate violet `#6d28d9` 하드코드) 필요
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 48

### v9.83 (2026-05-05): `"X"` 따옴표 텍스트 강조 룰 + 1~500 전 67개 파일 일괄 적용
- 자동화 스크립트 `_bold_quoted_text.py` (placeholder substitution 방식 + 멱등성 정규식)
- 룰: 본문 텍스트의 `"X"` → `"<strong>X</strong>"` (학습 핵심 키워드 즉시 식별)
- 안전 skip: HTML 속성값·`<style>` CSS·`<script>` JS·`<title>` 헤드 (모두 처리 안 함)
- 멱등성: `(?<!</strong>)"([^"<>]+)"(?!<strong>)` lookbehind/lookahead로 이미 강조된 항목 재처리 방지
- 적용 결과: 67개 파일 × 평균 ~34건 = **2295 신규 `<strong>`**
- v9.79 완전체 표준의 시각 강조 layer — 신규 파일 마지막 단계로 실행 권장
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 52

### v9.84 (2026-05-05): 강조 룰 무인용화 — `"<strong>X</strong>"` → `<strong>X</strong>`
- 자동화 스크립트 `_strip_quotes_around_bold.py` (1회성 마이그레이션 + idempotent)
- v9.83의 따옴표 wrap을 제거 — 굵은 글씨만으로 강조 충분 (시각 노이즈 감소)
- `_bold_quoted_text.py`도 v9.84로 동기화: `"X"` → `<strong>X</strong>` (따옴표 없이 직접 생성)
- 적용 결과: 67개 파일 2373개 따옴표 제거
- 향후 신규 파일에 `_bold_quoted_text.py` 한 번 실행만으로 v9.84 표준 도달
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 53

### v9.85 (2026-05-05): exp-table Explanation 컬럼 영어 단일화 — KR translation 제거
- **룰**: explanation 컬럼은 영어 prose 단일 라인 (한국어 형태소 inline 가능, 한국어 prose 번역 금지)
- 자동화 스크립트 `_strip_korean_translation.py` (exp-table explanation td의 `<br>` + KR translation 라인 자동 제거, idempotent)
- 적용 결과: exp-table 보유 ~20개 파일에서 92개 KR translation 라인 제거
- 디자인 정당화: ① 외국인 학습자 영어 우선 원칙 강화 ② 시각 단순화 (2-line → 1-line) ③ 유지 비용 절감 ④ 한국어 형태소는 영어 prose 안에 inline 보존
- 신규 파일 작성 시 처음부터 영어 단일 라인으로 작성 (한국어 번역 라인 추가 금지)
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 54

### v9.86 (2026-05-06): English order 헤더 정형화 + "~" placeholder 제거 + 주어 생략 1차 명료화
- exp-table colspan 헤더 형식: `English order: ... (상황)\n영어 어순: ... (상황) — Korean: {Korean order example}`
- 행은 영어 어순으로 나열 (Korean의 자연 어순과 다를 때 헤더의 보조 표시로 한국어 순서 보존)
- "~" placeholder (예: `Have ~ ever been?`) → 단순화 (예: `Have ever been` — 주어는 별도 row에 명시)
- Q229·Q231(2026-05-05) + Q190~Q198(2026-05-06) 정형화
- 자세한 내용: `_작업_로그_2026-05-05.md` Part 53 / `_작업_로그_2026-05-06.md` Part 1

### v9.89 (2026-05-06): 해설 한국어 토큰 일괄 굵게 룰 (Rule B) — 1~500 67개 파일 적용
- **룰 A (기존 v9.85)**: exp-table 해설 컬럼은 영어 prose, Korean translation 라인 금지
- **룰 B (신규 v9.89)**: 해설 영어 prose 안의 모든 한국어 토큰(음절·자모·어휘·형태소·구·시제·의문 어미) `<strong>` 처리
- 자동화 스크립트 `_bold_korean_in_explanations.py` (placeholder substitution + idempotent regex)
- 한국어 토큰 정규식: `~?[가-힯ㄱ-ㆎ]+(?:\s+[가-힯ㄱ-ㆎ]+)*[~?]?`
- 적용 결과: 38 파일 OK + 29 SKIP, **778개 한국어 토큰 굵게**
- 멱등성: 재실행 0 changes
- 신규 파일 워크플로: bold_quoted → strip_korean → apply_v9_87 → bold_korean_in_explanations
- 자세한 내용: `_작업_로그_2026-05-06.md` Part 4

### v9.88 (2026-05-06): 문장구조 + 서술어 문법 29개 파일에 v9.86-v9.87 자동화 일괄 적용
- 자동화 스크립트 `_apply_v9_87_rules.py` (헤더 재구축 + tilde 제거 + 주어 생략 통일, 멱등성 보장)
- 적용: 문장구조 9개 + 서술어 문법 20개(exp-table 보유) = 29개 파일, ~252개 헤더 변경
- 룰 4 (형성 공식 한국어 형태소 굵게)는 자동화 deferred — 단원별 수작업 (Part 2에서 190~198 적용한 패턴)
- 자세한 내용: `_작업_로그_2026-05-06.md` Part 3

### v9.87 (2026-05-06): 형성 공식 한국어 형태소 굵게 룰 + 주어 생략 표기 통일
- **룰 1 — 형성 공식 한국어 형태소 굵게**: exp-table explanation cell의 동사 활용 chain (`X + Y → Z → W` 형태)에 등장하는 모든 한국어 형태소를 `<strong>` 처리
  - 사전형 (`먹다`, `가다`), 보조동사 (`어 보다`, `고 싶다`), 결합형 (`먹어 보다`), 활용형 (`먹어 보세요`), 어미·조사 (`~지 못했다`, `ㄴ 적이`, `습니까?`) 모두 굵게
  - 영어 prose는 자연스럽게 흐르되 한국어 형태소가 시각 anchor 역할
- **룰 2 — 주어 생략 표기 통일**:
  - 이전 (모호): `Subject I omitted.` 또는 `Subject you omitted.`
  - 신규 (명료): `Korean omits the subject **{KOREAN}** — context implies the {speaker/listener}.`
  - {KOREAN}: 한국어 주어 굵게 (저는·당신은 등)
  - {speaker/listener}: 1인칭 → speaker, 2인칭 → listener
- 적용: 190~198 9개 row 일괄
- 자세한 내용: `_작업_로그_2026-05-06.md` Part 2

---

## 부록 A: v1-v7 적용 완료 파일 (1~86, 9개 파일)

원래 v1~v7 패턴(vocab→answer→Writing Test→해설→플래시카드)이 정립된 9개 파일. 단원별 색상은 v9.74부터 4 카테고리 시스템(`.sentence`)으로 통합됨.

| 파일 | 단원 | 문항 수 | 구버전 색상 → v9.74+ |
|---|---|---|---|
| `1~10 문장구조 주어 형용사.html` | 형용사 | 10 + Antonym 10쌍 | Blue → Sapphire |
| `11~20 문장구조 주어 동사.html` | 동사 (과거) | 10 | Green → Sapphire |
| `21~30 문장구조 주어 목적어 동사.html` | S+O+V | 10 | Amber → Sapphire |
| `31~40 문장구조 시간 장소 목적어 동사.html` | S+T+P+O+V | 10 | Purple → Sapphire |
| `41~50 문장구조 간접목적어 직접목적어.html` | S+IO+DO+V | 10 | Pink → Sapphire |
| `51~60 문장구조 의문사 누구 언제.html` | 의문사 1 | 10 | Teal → Sapphire |
| `61~70 문장구조 의문사 어디 무엇.html` | 의문사 2 | 10 | Teal → Sapphire |
| `71~78 문장구조 의문사 어떤.html` | 의문사 3 | 8 | Teal → Sapphire |
| `79~86 문장구조 접속 과와 이나나.html` | 접속어 | 8 | Indigo → Sapphire |
| **합계** | **9개 파일** | **86 문항** | |

## 부록 B: v9.74-v9.78 적용 완료 (1~500, 67개 파일 100%)

**title-block 적용**: 67/67 (모든 1~500 단원).
**refined gstep 적용**: 67/67 (1~500 전체).

| 카테고리 | 클래스 | 파일 수 | title-block | refined gstep CSS | gstep HTML |
|----------|--------|--------:|:-----------:|:-----------------:|:----------:|
| 문장구조 | `.sentence` | 9 | ✓ (9) | ✓ (9) | ✓ (9) |
| 서술어 문법 | `.predicate` | 21 | ✓ (21) | ✓ (21) | ✓ (21) |
| 부사어 문법 | `.adverbial` | 18 | ✓ (18) | ✓ (18) | ✓ (18) |
| 기타 문법 | `.other` | 19 | ✓ (19) | ✓ (19) | ✓ (19) |
| **합계** | | **67** | **67** | **67** | **67** |

→ **1~500 시리즈 100% v9.78 표준 적용 완료**. 신규 파일 추가 시 3개 자동화 스크립트(`_apply_title_block.py`, `_apply_refined_gstep.py`, `_wrap_gstep_to_all.py`) 재실행으로 즉시 표준 적용.

**v9.79+ 후순위**: 일부 복잡 섹션(예: 489~500 3 하위 섹션이 단일 gstep 카드 안에 있음)을 의미 분해하여 multi-step gstep 카드로 분할.

자세한 적용 내역은 `_작업_로그_2026-05-05.md` Part 43 (title-block) + Part 45 (gstep CSS swap) + Part 46 (세션 종합) + Part 47 (gstep wrap 100% 달성) 참조.
